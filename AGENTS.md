# AGENTS.md — Avesmaps project brief for AI agents

> Authoritative, tool-agnostic onboarding for any AI coding agent (Claude Code,
> Cursor, Copilot, Codex, …). Keep this file current; it is the single source of
> truth for "how this project is built". `CLAUDE.md` is a thin stub that imports
> this file.

## 1. Project purpose & status

Avesmaps is a **non-commercial fan project** for *Aventurien*, the world of the
German pen-and-paper RPG *Das Schwarze Auge* (DSA / "The Dark Eye"). It is an
interactive tile map **and an in-browser route planner**.

- **Live:** https://avesmaps.de (full app, with PHP+MySQL backend).
- **Frontend:** vanilla JavaScript, **no build step**, no bundler. `index.html`
  hand-includes ~117 `<script>`/`<link>` tags. Leaflet **1.9.4** with
  `L.CRS.Simple`, image bounds `0..1024`, zoom `0..5` (a marker tier exists up to 6).
- **Backend:** PHP 8 (strict types) + MySQL via PDO, hosted on **STRATO shared
  hosting**. Optional in the sense that the static map renders without it, but
  the live site relies on it for features, search, territories, routing, reviews
  and the editor.
- **Routing:** Dijkstra over a weighted graph built from GeoJSON paths
  (min-heap priority queue); runs both client-side and via `POST /api/route/`.

> **Note:** `llms.txt`, `site-summary.md` and `README.md` once claimed "no backend /
> no database / no external API". That was **false** and was **corrected on
> 2026-06-29** — all three now describe the PHP+MySQL backend (see §4).

## 2. Domain glossary (English ↔ Aventurien/DSA)

These German terms are **content, not translatable** — they are data, join keys,
or in-world vocabulary. Keep them as-is in code.

| German / in-world | Meaning |
|---|---|
| *Herrschaftsgebiet* | political / dominion territory (the core entity) |
| *Reich*, *Grafschaft*, *Baronie* | realm / county / barony (territory ranks) |
| *Umstrittene Gebiete* | disputed/contested territories (rendered as diagonal hatching) |
| *Weg / Pfad / Straße / Reichsstraße / Gebirgspass / Wüstenpfad / Flussweg / Seeweg* | path subtypes (stable keys in `PATH_SUBTYPE_KEYS`) |
| *Querfeldein* | synthetic cross-country route type |
| *Kreuzung* | crossing node (route graph), name prefix `Kreuzung-` |
| *Kraftlinien (powerlines), Regionen, Aggregat* | map display modes |
| *Wappen* | coat of arms |
| *Literatur* | the published DSA works tied to a place (code: `game-literature` / `GameLiterature`). *Abenteuer* is **one kind** of it, not the category. 💣 Not to be confused with *Quellen* — see the next row |
| *Quellen* | the **citations** an entry carries (`sources` + `feature_sources`, `publication-*.php`, `avesmapsPublication*`, `publisher`). The opposite direction from *Literatur*: a work vs. the reference to a work. The two words must never share an autocomplete prefix |
| *Gültigkeit / valid_to_bf*, **BF** | in-world validity; **BF = "Bosparans Fall"** calendar year (e.g. `1049 BF`) |
| *Derived / abgeleitete Außengrenze* | computed outer boundary of a territory aggregate |
| *WikiSync* | importer that crawls *Wiki Aventurica* into staging tables |
| *Schraffur* | hatching (contested-territory rendering) |
| *Albenhus / Zwerch* | known data-anomaly territories (display-inheritance bug, see §10) |

Settlement type slugs (stable keys): `metropole`, `grossstadt`, `stadt`,
`kleinstadt`, `dorf`, `gebaeude`. Their **visible labels** are German UI strings
(see §8).

## 3. Architecture map

Build-free, multi-`<script>` app. Load order in `index.html` is a **contract**
(later files can shadow earlier globals — verify order before assuming which
definition wins). Frontend clusters under `js/`:

| Dir | Responsibility |
|---|---|
| `js/app/` | bootstrap, `api-client.js`, runtime state, shared utils, share-link |
| `js/map-features/` | ~50 modules: layers, markers, labels, regions, political layer, canvas overlays |
| `js/territory/` | political-territory editor (embedded host), wiki-tree engine, derived geometry |
| `js/routing/` | route engine, graph, plan/render, waypoints (+ inline glue in `index.html`) |
| `js/review/` | review panels, WikiSync UI |
| `js/ui/` | spotlight search, popups, controls |
| `js/community/` | location reviews/ratings |
| `js/pages/` | scripts for standalone `html/*.html` pages |
| `js/third-party/` | Leaflet 1.9.4, jQuery 3.6.0, jQuery-UI, leaflet.textpath, polygon-clipping, polylabel |

Rendering: Leaflet SVG paths for vectors; **Canvas overlays** for markers,
labels, boundaries, contested hatching (performance). SQL is the source of truth;
the map hydrates from `GET /api/app/map-features.php`.

## 4. API contract

Backend lives under `api/`, tiered by audience:

| Zone | Purpose | Auth |
|---|---|---|
| `api/route/`, `api/locations/` | **stable** public developer API | public |
| `api/app/` | app-facing read endpoints (map-features, search, coat, territory-detail, …) | mostly public |
| `api/edit/{map,political,reports,wiki}/` | authenticated editor/review writes | capability-gated |
| `api/import/location-reports/` | server-to-server import | import token |
| `api/diagnostics/` | read-only diagnostics | mixed |
| `api/_internal/{routing,wiki,political}/` | private PHP libraries (no direct public surface) | — |

**Stable contract = `POST /api/route/` and `GET /api/locations/`.** They define
the canonical envelope every endpoint is being unified toward:

```
success: { "ok": true,  ... }
error:   { "ok": false, "error": { "code": "<machine_code>", "message": "<human>" } }
```

Auth/CORS/JSON live in `api/_internal/bootstrap.php`
(`avesmapsLoadApiConfig`, `avesmapsApplyCorsPolicy`, `avesmapsJsonResponse`,
`avesmapsReadJsonRequest`, `avesmapsCreatePdo`) and `api/_internal/auth.php`
(`avesmapsRequireUserWithCapability`). Protected dirs (`_internal`, `_schema`,
`diagnostics`) must be `.htaccess`-denied in deployment. **Canonical reference:
`api/README.md`.**

> The contract is **not yet uniform** — most endpoints still return a flat
> `error: "string"` and several leak exception text. This is the subject of the
> refactoring milestone M3 (see `docs/refactoring-masterplan.md`).

## 5. Data model

Schema of record currently lives **mostly as inline `CREATE TABLE IF NOT EXISTS`
DDL in PHP** (self-healing pattern), partially mirrored in `sql/`. Key tables:

- `map_features` — locations, crossings, paths, rivers, regions, labels (+ `map_revision`, `map_audit_log`, `map_feature_locks`, `editor_presence`).
- `political_territory` + `political_territory_wiki` + `political_territory_geometry` (+ `_geometry_audit_log`, `_derived_geometry`, `_claim`) — territory hierarchy via `parent_id`, GeoJSON Polygon/MultiPolygon, BF-year timeline (`valid_from_bf`/`valid_to_bf`, `9999` = open/never-dissolved sentinel).
- `location_reports` / `map_reports`, `map_reviews`, `map_share_links`.
- `changelog_entry` — the milestones behind „Änderungsverlauf" in the notices dialog (see §11).
- `sources` + `feature_sources` — multi-source system (shipped): shared source catalog (`url`/`url_hash`=SHA256, `label`, `source_type`, `is_official`) linked to elements (`entity_type` ∈ settlement|region|path|territory|citymap|lore|powerline|ecosystem — 🚩 die zwei letzten fehlten hier bis zum 26.08.2026; ✅ **`ecosystem` ist seit dem 03.09.2026 abends die Ablage der Landschaftsquellen** (Schritt 5 des Quellen-Umbaus, Eintrag in §11): die Fläche trägt sie, die gebundene Beschriftung liest sie unter `ecosystem:<region_public_id>`; freie Beschriftungen bleiben `region` + `map_features.public_id`. Vom 26.08. bis zum 03.09. lagen sie an der Beschriftung — der Flächenkasten war leer (0 von 30), und die Karte las nur die Liste des Schilds), `entity_public_id` — VARCHAR(190), widened 2026-07-22 because a lore key is a wiki-article slug, not a short public id, `status`). Public read `GET /api/app/feature-sources.php`; editor write `POST /api/edit/map/feature-sources.php` (capability `edit`, dedup by `url_hash`, atomic `other_source` takeover). Wiki-publication bulk lookup + provenance **shipped**: `feature_sources` gains `origin` (wiki_publication|manual|community), `reference_kind`, `pages`, `note` (+ `status='suppressed'` tombstone); a resumable `publication_sources` dump-sync phase parses `{{Infobox Produkt}}` + `==Publikationen==` into `wiki_publication_catalog`/`wiki_entity_publication` staging, and an **owner-triggered `sync_publications` action** reconciles them override-safely into the wiki layer (writes/deletes ONLY `origin='wiki_publication'`, manual/suppressed untouched, idempotent by `wiki_key`). Sources travel in the `map-features` payload (shared `source_catalog` + per-entity refs, rendered synchronously — no lazy per-popup fetch). See §11.
- WikiSync staging: `wiki_sync_runs/pages/cases`, `wiki_*_staging/queue`, `political_territory_wiki_test`, `wiki_territory_model`, `wiki_redirect_alias`.

> 💣 **Sources live in ONE place. Never build a second source system.**
> A new entity that needs sources joins `sources` + `feature_sources` by adding its
> name to the `entity_type` whitelist — today `settlement | region | path | territory
> | citymap | lore`, in `api/edit/map/feature-sources.php` and
> `api/app/feature-sources.php`. That is a two-line change, and it is how `citymap`
> and `lore` joined.
>
> This is written down because it was ignored once: the Lore feature (Natur & Waren,
> 2026-07-21) shipped its own `lore_source` table. The cost was not theoretical — a
> publication title is stored once in the shared catalogue but was duplicated into
> every one of ~35 000 lore rows, the editor had no add/remove/autocomplete and no
> `note`/`status` provenance, and the same wiki publication data flowed through two
> unrelated reconcilers. **Undone on 2026-07-22** (spec:
> `docs/superpowers/specs/2026-07-22-lore-quellen-vereinheitlichung-design.md`): it
> cost a schema widening, a data migration and a re-test of the whole lore sync —
> against the two lines it would have cost up front.
>
> The tell: if you are about to write `CREATE TABLE <feature>_source`, stop — the
> answer is one more `entity_type`.

**`wiki_key` derivation is a fixed table, never a locale.** `avesmapsPoliticalSlug()`
and the WikiSync match key fold non-ASCII through `avesmapsFoldToAscii()`
(`api/_internal/text/ascii-fold.php`) — **not** `iconv//TRANSLIT`, which is
libc-dependent and keyed the same name differently on the dev machine and on
STRATO. The table reproduces the **server's** form: umlauts fold to `'?'` and
lose their base letter (`Fürstentum Kosch` → `wiki:f-rstentum-kosch`), verified
against 1384/1384 live rows. Making it "nicer" rewrites every umlaut-bearing key
and silently breaks every join using one — that is a data migration across ~10
tables, not an edit to the table. Guarded by `tools/wikidump/test-ascii-fold.php`
(self-test panel) and `tools/wikidump/verify-live-key-parity.php` (against prod).

**Coordinate convention:** GeoJSON stores `[x, y]`; Leaflet `L.CRS.Simple` uses
`[lat, lng] = [y, x]`. Always swap consciously.

## 6. Build / deploy flow

- **No build.** Source files are served directly.
- GitHub Action `.github/workflows/deploy-avesmaps-strato.yml` mirrors an
  **allowlist** of items to STRATO over SFTP. **It does not delete** — files
  removed from the repo persist on the server (see §10 server↔repo drift).
- An asset-version stamping step rewrites `?v=` hashes for every asset reachable
  from `index.html` / `html/*.html`, following the CSS `@import` chain. **The
  sources themselves stay unstamped** (§7).

## 7. Asset-versioning gotcha (read before debugging "my change didn't show up")

Two independent cache-busting mechanisms:

1. **Automatic** content-hash `?v=` for everything reachable from `index.html` or
   an `html/*.html` page — linked directly *or* through the CSS `@import` chain
   (`styles.css` → `base/tokens.css` → …, at any depth). Handled by the deploy
   stamping step; do nothing. **Never write a `?v=` by hand anywhere** — the deploy
   overwrites it and a hand-written tag can only go stale. The deploy verifies the
   chain and refuses to upload if a hash disagrees.
2. **Manual** `const ASSET_VERSION` in
   `js/territory/territory-editor-inline-host.js` — governs the **dynamically
   loaded editor HTML/CSS/JS**. **Bump it on every change to editor assets**, or
   the browser serves stale editor code. `inline-host.js` itself is loaded by
   `index.html` *without* `?v=`, so after editing it you need one hard reload.
3. **Manual, and the exception to "never by hand": every `.php` page that links an
   asset.** The stamper walks `index.html` and `html/*.html` only, so it never reaches
   a `.php` page and can neither overwrite nor verify those tags. **Bump them by hand
   whenever the linked file changes**, otherwise editors keep the cached version.
   Rule 1's ban applies to everything the stamper *does* reach — which is everything else.
   🔴 **Bis zum 31.08.2026 stand hier „the ONE exception", und genannt war nur
   `edit/index.php` → `css/pages/edit.css`.** Eine Zahl liest sich wie eine vollständige
   Liste, und niemand zählt nach: `edit/svg-export.php` trägt **fünf** solche Tags (vier
   Skripte unter einem gemeinsamen Stempel plus die CSS). Wer die alte Fassung las, hielt
   seine Seite für gestempelt und lieferte Editoren die gecachte alte Datei — die Datei
   liegt live, der Browser holt sie nie. ⭐ Die Gegenprobe kostet nichts:
   `grep -n '?v=' <die .php-Seite, die deine Datei lädt>`.

Diagnosis when a deployed change doesn't appear: compare `fetch(url+'?cb='+Date.now())`
(server-fresh) vs `fetch(url)` (as the app loads). See
`docs/asset-caching-and-versioning.md`.

## 8. Language policy

Primary UI language is **German** and stays German. An i18n layer
(`?lang=en` overlay backed by a string table) is planned (milestone M8) — German
is the default, English is opt-in. Therefore:

- **Do not** translate user-facing German UI strings inline (that would change the
  default UX). Extract them into the i18n table instead.
- 💣 **Code comments, docs and commit messages are written in GERMAN** — like the
  code you are standing in. The `error.code` machine values stay English and are
  never translated; internal API error *messages* may be either.
  🔴 This line said the opposite until 2026-08-15 ("write them in English going
  forward"). It was dead law and had been for months: every spec under
  `docs/superpowers/`, every recent commit subject and nearly every new source file
  is German, while a handful of older libraries are English. A reviewer following
  the old rule flagged brand-new, correctly-written German comments as a deviation
  — which is what a rule nobody follows costs. Owner's ruling on that review:
  German, and fix the rule. **Match the file you are editing**; do not translate
  an existing English file into German just because of this line.
- Never translate domain content (§2), `<option value>` slugs, `queryParam`
  toggle names, `PATH_SUBTYPE_KEYS`, or the `BF` calendar suffix.

## 9. Dev conventions

- **OS:** Windows + PowerShell. Watch the CRLF edit trap (prefer single-line
  edits on CRLF files). `.gitattributes` now sets `text=auto` + binary markers.
  💣 **Und die zweite CRLF-Falle sitzt in den TESTS: hier ist CRLF, im Tor ist LF.** Ein
  Quelltext-Test, der `\r\n` sucht, ist auf diesem Rechner gruen und in der CI rot -- die
  Arbeitskopie traegt CRLF, `actions/checkout` legt LF hin. Am 25.08.2026 kostete das **zwei**
  Deploys: `rumpf.slice(0, rumpf.indexOf("\r\n}") + 3 || rumpf.indexOf("\n}") + 2)` sollte auf LF
  zurueckfallen und tut es nie -- `+` bindet staerker als `||`, und **`-1 + 3` ist 2, also
  truthy**. Der Test schnitt seinen Prueftext auf zwei Zeichen zusammen und behauptete, die
  Verdrahtung fehle. ⭐ Also: in Quelltext-Tests **zeilenendenneutral** suchen (`\n\}`, oder den
  Text vorher `.replace(/\r\n/g, "\n")`), und einen Rueckfall **nie** ueber `||` auf ein
  `indexOf`-Ergebnis bauen -- `-1` ueberlebt jede Addition ab +1 als gueltig aussehende Zahl.
  ⚠️ Gegenprobe vor dem Push, wenn ein Test Quelltext liest: denselben Baum als LF spiegeln
  (`find js css -type f -exec sed -i 's/\r$//' {} +` auf einer KOPIE) und den Test dort fahren.
- **Git:** small, verified commits **directly to `master`**; push triggers a
  ~1–2 min auto-deploy. Verify the remote SHA after pushing. Conventional-commit
  prefixes (`feat/fix/chore/docs/perf/refactor`, plus the repo's custom `ui:`). 💣 **A scope word names ONE feature.** `verlauf:` belongs to the paths' wiki-course sync (`path-verlauf.php`, `verlauf_cases`) and **never** to the „Neuigkeiten" window — that one is `neuigkeiten:` or `changelog:`. It was miscommitted twice on 2026-08-08; `git log --grep` and `git grep` cannot tell the two apart afterwards.
- **Shared working tree — never `git add -A`:** multiple agent sessions share
  this one checkout and `.git`, and other sessions may have **live uncommitted
  work** in the tree right now. Never `git add -A`, `git add .`, or `git commit
  -a` — that sweeps another session's half-finished files into your commit under
  your message and mangles their history. Always `git status` first, identify
  changes that aren't yours, and stage **only the files you yourself touched**,
  by explicit path. Leave foreign modified/untracked files alone (that session
  will commit them). If a push is rejected, `fetch` + `rebase origin/master`
  (autostash) + retry — never force-push.
- **STRATO caution:** never loop expensive endpoints (e.g. the political layer) —
  it saturated PHP workers once and looked like a DB outage. Probe with a single
  request.
- **Editor-visible change → name it in the commit subject; do NOT edit the
  handbook in passing.** `html/editor-handbuch.html` is owned by a nightly routine
  (`avesmaps-handbuch-pflege`, 00:00 local — see §11), not by whoever happens to
  touch an editor surface. Your only obligation: a commit subject that states the
  user-visible effect ("rename the X button", "new Y tab", "Z now runs globally").
  The routine reads `git log`, judges whether an editor following the handbook
  would now find something different, and rewrites the affected section. Editing
  the handbook yourself competes with that routine over the same file and its
  `Stand:` date — leave it alone unless the owner asks you directly. This replaced
  the old "same commit" rule on 2026-07-22: it depended on every one of many
  parallel sessions remembering, and a wrong handbook is worse than none (it went
  from written to materially wrong in **13 days**, 2026-07-07 → 07-20, back when
  it was nobody's job).
- 💣 **Sichtbare Änderungen an der Oberfläche gehen EINZELN live, und der Owner sieht jede.**
  Ein Commit, ein Push, sein Blick — dann der nächste. Kein Bündel.
  Am 10.08.2026 gingen neun Mobil-Commits am Stück auf die Live-Seite; vier Regressionen waren
  darin, gemeldet vom Telefon des Owners („irgendwie is mobil jetzt alles kaputt"), Rückbau war
  die einzige schnelle Antwort. Der Auftrag hatte „Aufgabe für Aufgabe, Halt nach jeder"
  gelautet; ein späteres „zieh alles durch" meint das Tempo, **nicht** den Wegfall des Blicks.
  ⚠️ Gilt für alles, was ein Besucher sieht — Layout, Bedienelemente, Kartenbedienung. Nicht
  für Tests, Doku und reine Innenumbauten.
- 💣 **Abnahme heißt ABLAUF, nicht Maß.** Eine Prüfseite, die Rechtecke misst, ist kein Beleg,
  dass etwas funktioniert — sie belegt nur, dass eine Zahl stimmt. Vor „fertig" werden die
  echten Handgriffe ausgeführt und benannt: das Ding anklicken, das Panel aufziehen, den
  Suchtreffer wählen, die Infobox öffnen. Genau diese drei Handgriffe waren am 10.08. kaputt,
  während sämtliche Maßtabellen grün waren. ⚠️ Was ein Emulator nicht beantworten kann
  (Bildschirmtastatur, echtes Touch-Verhalten), wird als offene Frage gemeldet, nicht als
  bestanden.
- 💣 **Der eigene Entwurf ist die Abnahmeliste.** Jede Zeile mit 💣 / ⚠️ / 🔴 in Spec und Plan
  wird vor „fertig" einzeln abgehakt — erfüllt, oder ausdrücklich verworfen mit Begründung.
  Zwei der vier Regressionen vom 10.08. standen wörtlich als Warnung im eigenen Entwurf
  (`--avesmaps-corner-stack` an die Steuerhöhe binden; die versprochene Animation) und wurden
  nicht gebaut. Es fehlte kein Wissen, sondern das Abhaken.
  ⭐ Dafür gibt es zwei Sub-Agenten: **`usability-konsistenz`** (vor jedem Commit: Entwurf gegen
  Diff, gekoppelte Werte, gewinnt die Regel überhaupt?) und **`usability-design`** (vor dem
  Push: Mockup gegen gebauten Zustand, Designsprache, Rangfolge in hell UND dunkel). Sie
  ersetzen das Abhaken nicht, sie fangen das, was man selbst überliest.
- 💣 **Vor dem Push läuft das GANZE Testfeld, nicht die eigenen Tests.** Der Deploy ist
  ein Tor: ein einziger roter Test lädt **nichts** hoch. Am 12.08.2026 brach eine
  erweiterte Selektorliste in `legal-dialog.css` einen Test in
  `js/app/__tests__/touch-scale.test.js`, der wörtlich einen Block nur für die Suchkachel
  erwartete — an der Regel war nichts falsch, und fünf Deploys hintereinander fielen aus.
  Wer nur seine eigenen Tests laufen lässt, sieht so etwas nie: die Datei, die bricht,
  gehört jemand anderem. Der Lauf -- ⚠️ **das enge Muster, der Workflow faehrt 21 Dateien mehr,
  siehe unten**:
  `for t in $(find js tools -path '*__tests__*' -name '*.test.js'); do node "$t"; done`
  PHP analog -- 💣 **aber mit den Erweiterungen, sonst luegt der Lauf**:
  `for t in $(find api tools -path '*__tests__*' -name '*-test.php'); do php -d zend.assertions=1 -d assert.exception=1 -d extension=php_mbstring.dll -d extension=php_pdo_sqlite.dll -d extension=php_gd.dll "$t"; done`
  ⚠️ Ohne `mbstring`/`pdo_sqlite`/`gd` melden **45 Tests** rot, die alle nur die Erweiterung
  vermissen und es in ihrer eigenen Fehlermeldung auch sagen -- am 15.08.2026 gemessen. Wer das
  nicht weiss, haelt das Testfeld fuer kaputt oder sich selbst fuer den Verursacher. Vorbestehend
  rot bleibt genau einer: `linkcheck/link-url-test.php` (echter DNS-Abruf), kein Regressionssignal.
  💣 **Und dieser Lauf ist NICHT das ganze Feld -- der Workflow faehrt mehr, als dieses Muster
  findet.** Unter `tools/` liegen weitere PHP-Tests, die weder in einem `__tests__`-
  Verzeichnis stehen noch auf `-test.php` enden (sie heissen `test-*.php`). Am 15.08.2026 kostete
  genau diese Luecke **zwei** Deploys: ein Commit gab der geteilten Fixture `mini-dump.xml` eine
  24. Seite und zog nur den eigenen Test nach; `test-dump-reader.php` schreibt dieselbe Zahl an
  sieben Stellen fest und fiel um. Beide Sitzungen fuhren vorher ein gruenes Feld -- das Muster
  oben sah die Datei nie.
  🔴 **Und es ist NICHT nur `tools/wikidump/`** -- hier stand genau das, und es war um **neun
  Dateien** zu eng: `tools/paths/` (7) und `tools/routing/` (2) tragen dieselbe Namensform, und
  das Deploy-Tor faehrt sie mit. Gemessen 25.08.2026: Tor 288 Dateien, diese Zeile 278. Wer die
  alte Fassung fuhr, sah neun Dateien nie und konnte das Tor blind stellen. Also zusaetzlich --
  **ohne** Verzeichnis im Muster:
  `for t in $(find tools -name 'test-*.php' -not -path '*__tests__*'); do php -d extension=php_mbstring.dll -d extension=php_pdo_sqlite.dll "$t" >/dev/null || echo "ROT: $t"; done`
  ⭐ **Am sichersten ist ohnehin das Muster des Workflows selbst** (`.github/workflows/
  deploy-avesmaps-strato.yml`), denn nur das entscheidet ueber den Deploy:
  `find api tools \( -path '*__tests__*' -name '*.php' \) -o \( -name 'test-*.php' -not -path '*__tests__*' \)`
  💣 **Und dieselbe Luecke steht auf der JS-Seite** -- die Zeile oben ist das ENGE Muster, das Tor
  faehrt mehr: **21 `test-*.mjs`** unter `tools/{paths,routing,wikidump}`. Gemessen 25.08.2026:
  enges Muster 275 Dateien, Tor 296. Also auch hier das Muster des Workflows nehmen:
  `find js tools \( -path '*__tests__*' -name '*.test.js' \) -o \( -name 'test-*.mjs' -not -path '*__tests__*' \)`
  🔴 **Hier stand bis zum 25.08.2026 abends, diese 21 fahre „KEIN Lauf" und 7 davon seien rot.**
  Beides ist falsch und in dieser Reihenfolge gefaehrlich: das Tor faehrt sie (sein eigenes
  JS-Muster, `deploy-avesmaps-strato.yml`), und sie sind **alle gruen** -- einzeln nachgemessen,
  aus dem Wurzelverzeichnis wie aus `tools/paths/`. Der Satz haette den naechsten Leser davon
  abgehalten, das richtige Muster zu fahren („erst die roten heilen"), obwohl daran nichts zu
  heilen war.
  ⚠️ Was bleibt: **5 `.js`** unter `tools/svg-export/__tests__/`, die nicht auf `.test.js` enden,
  fahren tatsaechlich in keinem Lauf (1 rot, `abzug-pruefen.js`). 🔴 **Das ist ABSICHT, keine
  Luecke** -- die Endpunkt-Ablaeufe starten einen echten Server, und der Name haelt sie bewusst aus
  dem Tor heraus, damit ein Portproblem den Deploy nicht anhaelt (§11, SVG-Abzug). Wer das Muster
  dorthin zieht, hebt diesen Entscheid auf und stellt das Tor zu.
  ⭐ **Und der ganze Lauf geht PARALLEL -- 12 s statt rund 4 Minuten** (gemessen 26.08.2026 ueber
  beide Workflow-Muster: 306 JS- und 308 PHP-Dateien, null rot). Das ist kein Komfort, sondern die
  Bedingung, unter der man das Tor vor **jedem** Push wirklich faehrt: bei vier Minuten zieht
  `origin` zwischen Rebase und Push davon, und der Push wird abgelehnt -- an diesem Tag zweimal,
  woraufhin die Versuchung gross ist, den Lauf beim naechsten Mal zu ueberspringen.
  `… -print0 | xargs -0 -P 8 -I{} sh -c 'node "{}" >/dev/null 2>&1 || echo "ROT: {}"' > roteliste`
  💣 **KEIN `2>&1` auf die Ergebnisdatei.** `xargs` schreibt Warnungen nach stderr; landen sie
  darin, liest ein `[ -s roteliste ]`-Test die Warnung als roten Test und bricht den Push ab,
  obwohl kein Test rot ist -- am 26.08.2026 einmal passiert und als echter Befund missdeutet.
  💣 `-n 1` und `-I{}` schliessen sich aus; genau das war die Warnung.
  💣 **UND DIE KLAMMER UM DIE BEIDEN GRUPPEN IST TRAGEND -- ohne sie faehrt der Lauf 21 von 312
  Dateien und meldet „null rot".** `find js tools \( A \) -o \( B \) -print0` bindet `-print0` NUR
  an die zweite Gruppe: `find` liest das als `\( A \) -o \( \( B \) -print0 \)`, also wird nur
  ausgegeben, was auf B passt -- die 21 `test-*.mjs`. Die 291 Dateien aus A laufen NIE.
  ⚠️ Und es sieht wie ein gruenes Tor aus: die Ergebnisdatei ist leer, der Lauf dauert eine
  Sekunde, nichts warnt. Am 26.08.2026 dreimal hintereinander so gefahren und geglaubt; aufgefallen
  ist es erst, als ein Test, der nachweislich rot war, im Lauf nicht auftauchte. Richtig ist die
  zusaetzliche Klammer um BEIDE Gruppen:
  `find js tools \( \( -path '*__tests__*' -name '*.test.js' \) -o \( -name 'test-*.mjs' -not -path '*__tests__*' \) \) -print0`
  ⭐ Die Gegenprobe kostet nichts und beantwortet genau die Frage, die man sich sonst nicht stellt:
  `… -print0 | tr -dc '\0' | wc -c` muss die Zahl aus `.github/workflows/deploy-avesmaps-strato.yml`
  ergeben (26.08.2026: 312 JS, 310 PHP). Eine Zahl, die viel zu klein ist, ist der einzige
  Unterschied zwischen diesem Fehler und einem gruenen Feld.
  ⚠️ Bei einem **unerwarteten** Roten seriell nachfahren, bevor man ihn glaubt: nur der serielle
  Lauf schliesst aus, dass zwei Tests sich eine Fixture oder einen Port teilen.
  ⚠️ Und bei einem **unerwarteten GRUENEN** ebenso: erst die Dateizahl nachzaehlen, dann glauben.
  ⚠️ **Und danach: der Fehlschlag vergiftet den `?v=`-Stempel.** Der nächste grüne Lauf
  hält die nie hochgeladenen Dateien für aktuell — live standen zwei davon auf HTTP 404
  und sechs in alter Fassung, während `index.html` schon die neue anforderte. Nur eine
  **Inhaltsänderung** heilt das. Diagnose: `fetch(url+'?cb='+Date.now())` gegen
  `fetch(url)` (siehe `docs/asset-caching-and-versioning.md`).
  💣 **Und ein ABGEBROCHENER Lauf tut dasselbe — den holt man sich beim Einzeln-live-Gehen
  selbst.** Ein zweiter Push, während der erste noch läuft, bricht dessen Lauf ab; ein
  abgebrochener Lauf lädt **nichts** hoch. Der nächste rechnet seine geänderten Dateien ab
  `github.event.before` — also ab dem abgebrochenen Commit —, und dessen eigene Dateien lädt
  damit **nie jemand**. Gemessen am 22.08.2026: von zehn Dateien eines Umbaus stand genau die
  eine nicht live, die nur ein einziger, abgebrochener Commit angefasst hatte; die anderen neun
  waren später noch einmal berührt worden und kamen dadurch mit. ⚠️ Es fällt nicht auf: der
  Stempel in `index.html` ist der neue, die Datei dahinter die alte — die Funktion fehlt
  einfach. Wer mehrere Schritte hintereinander live schickt, **wartet den Lauf ab** oder prüft
  danach jede Datei einzeln gegen die Live-Seite. Geheilt wird auch hier nur durch eine
  Inhaltsänderung.
- 💣 **Ein SQLite-Test kann eine MySQL-Regression ERZWINGEN.** Am 16.08.2026 baute eine Sitzung
  `DELETE g FROM political_territory_geometry g LEFT JOIN …` in
  `DELETE … WHERE id IN (SELECT g.id FROM political_territory_geometry g …)` um, damit die
  SQLite-Fixture den Sammelknopf fahren konnte. MySQL lehnt genau das mit **Error 1093** ab
  („You can't specify target table … for update in FROM clause"): der Knopf wäre live beim ersten
  Klick abgebrochen, und der Test wäre **grün geblieben**, weil SQLite die Einschränkung nicht
  kennt — der Test hatte die Regression nicht verhindert, sondern erzwungen. ⭐ Das Haus-Idiom
  dagegen steht längst da: `avesmapsPoliticalPruneGeometryAuditLog` (`territories-audit.php`)
  wickelt die Subquery **doppelt** in eine Ableitungstabelle
  (`… IN (SELECT id FROM (SELECT id FROM …) x)`) und erzwingt so die Materialisierung; das läuft
  auf beiden. ⚠️ Die Lehre ist grösser als 1093: wer die Produktionsform verbiegt, damit ein Test
  läuft, hat den Test gegen die Produktion gedreht. Geht beides nicht, gilt **MySQL** — und ein
  Kommentar an der Stelle sagt warum, sonst „vereinfacht" der nächste Leser sie zurück.
- **Secrets:** `api/config.local.php` is gitignored and must never be committed.
  No production dumps, reports, audit logs, tokens or credentials in the repo.
- **Legal:** DSA assets follow the Ulisses fan guidelines (see `NOTICE.md`).

## 10. Known fragilities

- **Server↔repo drift:** the prod webroot has PHP files not in the repo (deploy
  never deletes). "Not in the repo" ≠ "safe to delete". A 161-orphan surgical
  cleanup ran 2026-06-14 (deleted the pre-reorg flat `js/`, old icons/css/images,
  M1/M4 leftovers, and stale dirs `/map /politics /test /js/pages /css/legacy`)
  via the deploy's "Retire orphaned remote files" step — **never `mirror --delete`**
  (its dry-run would also delete live files). **Still load-bearing on the server &
  protected:** `tiles/` (base map; tile files use NEGATIVE y, `map_x_-y`),
  `uploads/`, `admin/phpMyAdmin`, `api/wiki-sync.php` (frontend fallback),
  `api/app/.user.ini`, `config.local.php`. Root shims `api/{auth,bootstrap}.php`
  are kept (clean-deploy safety; the `political-territory-lib`/`wiki-sync-lib` lib
  shims were removed in M3 step 4). Directory URLs return 404 on this server
  (listing off) — not a sign of deletion.
- **`territories-endpoint.php` runs DDL + metadata probes before its cache read**
  on every political-layer request; the derived layer has an N+1 over the full
  territory table. Both are perf hotspots (milestone M6).
  ✅ **Der DDL-Teil ist seit 03.09.2026 erledigt.** Der Endpunkt fuhr 13 DDL-Statements je
  Nicht-Cache-Treffer (`avesmapsPoliticalEnsureTables` + `…EnsureDerivedGeometryTables`), alle
  idempotent und alle auf dem heissen Pfad. Beide gehen jetzt durch **`avesmapsSchemaEnsureOnce`**
  (`api/_internal/schema-ensure-once.php`): eine Marker-Datei je Ensure im Temp-Verzeichnis, wie der
  Dateicache der Ebene daneben, Frist eine Stunde. 🔴 **Der Schluessel traegt die MTIME DER
  DEFINIERENDEN DATEI** — ein Deploy, der eine Spalte ergaenzt, aendert die Datei, damit den
  Schluessel, und der Ensure laeuft sofort wieder; ohne das stuende ein neuer `ALTER TABLE` bis zu
  eine Stunde aus und jede Anfrage liefe in „Unknown column". ⚠️ Faellt OFFEN aus (Temp nicht
  schreibbar → der Ensure laeuft wie bisher jedes Mal, langsamer, nie kaputt), und nach einem
  werfenden Ensure entsteht KEINE Marke. 💣 **Nie innerhalb einer Transaktion rufen** — DDL
  committet in MySQL implizit; die gebundenen Aufrufer rufen ihn alle VOR `beginTransaction()`,
  nachgemessen in `avesmapsAcquireMapFeatureLock`. ⚠️ Gebunden sind nur die TAKT-Pfade (Sperr-Wecker,
  Meldungsliste, Aenderungsverlauf, politischer Endpunkt); die Schreibwege behalten den Roh-Ensure,
  sie laufen je Handlung. Test: `api/_internal/__tests__/schema-ensure-once-test.php`.
  🔴 **Und der N+1 des Derived-Layers ist unberuehrt** — die Zeile darueber bleibt gueltig.
  ✅ **Der Zoom-Teil ist seit 26.08.2026 erledigt** (Perf-Paket 1). Die Ebene wurde bei JEDEM
  Zoomschritt komplett neu geholt — der Zoom ist ein Anfrageparameter, und der Client erzwang
  `cache: "no-store"` plus `_=Date.now()`-Cachebrecher; die Antwort trug serverseitig GAR KEINE
  Cache-Kopfzeile. Gemessen 280–330 ms je Zoomschritt in der Standardansicht, rund 1 s in der
  politischen. Jetzt drei Riegel:
  💣 **Die Antwort verlaesst den Endpunkt an DREI Stellen** — Schnellpfad vor dem PDO, zweiter
  Cache-Treffer dahinter, frischer Aufbau. Alle drei gehen durch `avesmapsPoliticalSendLayerCacheHeaders`;
  eine Regel, die einen von drei Erzeugern bindet, ist keine Regel.
  💣 **`max-age` ist die RESTLAUFZEIT der Servercachedatei, nie ihre volle Frist** — sonst addieren
  sich Server- und Browserfrist (600 s statt 300). ⭐ Und `max-age` ist hier zwingend statt `no-cache`:
  der `ETag` ueberlebt die 200 nicht (siehe unten), ein Browser koennte also NIE revalidieren.
  💣 **ZWEI Client-Speicher mit ZWEI Invalidierungswegen:** der 5-s-Zusagenspeicher in
  `api-client.js` und der geparste Ergebnisspeicher im Loader, je (Zoom, Jahr, Bearbeiten-Modus),
  300 s. Geleert wird aus dem Loader (`immediate`) UND aus dem api-client (nach einem Schreibvorgang) —
  **beide leeren beide.** 🔴 Zwischengespeichert wird NUR im Ansichtsmodus, und der Riegel faellt im
  Zweifel geschlossen aus (`IS_EDIT_MODE` undefiniert = Bearbeiten). Live gemessen: 5 Zoomschritte,
  2 Abrufe. Tests: `ebenen-cache-kopfzeilen-test.php`, `ebenen-zwischenspeicher.test.js`.
  🪤 Und der Kommentar „Default-Start ist Zoom 0 -> spart den ~10MB-Startblock“ im Loader war TOT:
  der Startzoom ist `AVESMAPS_DEFAULT_MAP_ZOOM = 3` (am Telefon 2), die Bedingung greift beim Start
  also nie. Wer dort eine Startersparnis vermutet, sucht an der falschen Stelle.
- **Albenhus/Zwerch** display-inheritance anomaly: a save writes resolved displays
  globally onto all ancestors.
- **Schema is in code, not `sql/`:** ~14 tables exist only as inline PHP DDL;
  `sql/` is a partial, partly-stale snapshot. `map_feature_relations` and
  `map_proposals` are defined but unused (dead schema).
- 💣 **A silent MySQL truncation is indistinguishable from "nothing was ever
  saved".** `app_setting.setting_value` was `VARCHAR(255)` — sized for kill-switch
  flags (`"0"`/`"1"`) and fine for three months. The first key holding a real value,
  `travel_values`, is **1414 characters**: outside strict mode MySQL cut it, the
  reader's `json_decode` returned `NULL`, and every reader fell back to its constant.
  No error, no exception, no warning — and the Tempowerte window's „Speichern"
  therefore did nothing at all from 2026-08-14 until it was measured the same day.
  Widened to `MEDIUMTEXT`; existing installs are pulled up by
  `avesmapsAppSettingEnsureWideValue()` (⚠️ **call it only from writers of large
  values** — its `information_schema` probe on the kill-switch path is exactly the
  load §10 already lists). **The rule this leaves behind: a writer whose value
  matters must READ IT BACK before treating the write as done** — a marker may only
  attest that something *is there*, never that a write *was issued*
  (`avesmapsTravelValuesStoredMatches`).
- 💣 **`css/pages/political-territory-editor-inline.css` is a BUILD PRODUCT** —
  `tools/scope_editor_css.js` generates it from `political-territory-editor.css`,
  `-layout.css` and `political-territory-wiki-tree.css`. A rule written into it by
  hand works immediately and dies, silently, at the next `node tools/scope_editor_css.js`.
  That happened three times (`e5cb8f1b`, `127a7c78`, `5fa1f323`); the regeneration in
  `0bc22ffc` took all **14** rules with it — the Hierarchie-Level table, the
  transparency row and the WikiSync button — and nobody noticed for two weeks, until
  the colour picker started sticking out of its panel. **Edit the source, then
  regenerate, then bump `ASSET_VERSION`** (§7). Guarded by
  `tools/__tests__/scope-editor-css.test.js` (generated == generator output; no markup
  class without a rule). ⚠️ `css/components/political-territory-editor-columns.css`
  loads *after* the generated sheet and blanket-sets `.manual-data-section table {
  table-layout: auto }` — an equal-specificity `table-layout: fixed` in the editor
  sheet is inert, so address such a table through its section.
- 💣 **Ein `ETag` ueberlebt die 200 NICHT, die 304 schon — und das ist ein Fangschluss.** Gemessen
  am 25.08.2026 an `GET /api/locations/` (Meldung #96), mit und ohne gzip: die **200** kommt ohne
  `ETag`, ohne `Content-Length`, chunked und mit einem `Vary: User-Agent,Accept-Encoding,X-Forwarded-For`,
  das der Code nicht setzt; die **304** traegt ihren `ETag` unveraendert. Vor STRATOs PHP sitzt also
  etwas, das Antworten MIT Rumpf anfasst. Der Riegel selbst ist heil — `If-None-Match` kommt an, die
  304 wird korrekt beantwortet, ein falscher Tag liefert weiter 200 —, aber **erfahren** konnte ein
  Client den Tag nie: die einzige Antwort, die ihn traegt, bekommt man erst, wenn man ihn schon hat.
  ⭐ Abhilfe ist ein zweiter Kopf unter eigenem Namen (`X-Avesmaps-ETag`), weil `X-`-Koepfe die 200
  nachweislich ueberleben (`X-Robots-Tag`, `X-Powered-By` standen in derselben Messung da) — dasselbe
  Mittel wie `X-Avesmaps-SHA256` beim SVG-Export. Der echte `ETag` bleibt daneben stehen.
  ⚠️ **Und CORS ist die zweite Haelfte:** ohne `Access-Control-Expose-Headers` (jetzt in
  `avesmapsApplyCorsPolicy`) liest ein fremder Browser-Client **keinen** von beiden — `headers.get()`
  gibt `null`, so treu der Server auch sendet. Das erklaert die offene Frage aus
  [[etag-kommt-live-nur-manchmal-an]] zur Haelfte: bei `map-features.php` schwankte es, weil dort mal
  ein 304 und mal ein 200 gemessen wurde. 🔧 Wer die Zwischenschicht ist, ist weiter unbekannt.
  ✅ **Seit 26.08.2026 tragen ihn auch `GET /api/app/map-features.php` und die politische Ebene**
  (`territories-endpoint.php`). Bei map-features war das keine Kleinigkeit: der 304-Pfad existierte
  dort seit Monaten, war aber fuer echte Browser **unerreichbar** — jeder Wiederbesuch zahlte die
  vollen ~3 MB und die vollen 2,1–2,5 s. Live gegengemessen: die 200 traegt `X-Avesmaps-ETag` und
  weiterhin KEINEN `ETag`; denselben Wert als `If-None-Match` zurueck ergibt **304, null Bytes**.
  ✅ **Und seit 27.08.2026 ist die zweite Haelfte da: der Wiederbesuch zahlt 0 Bytes statt 3 MB.**
  Der Browser-HTTP-Cache revalidiert NICHT ueber `X-`-Koepfe — also legt der Client Nutzlast und Tag
  selbst ab (`js/app/kartendaten-speicher.js`, IndexedDB) und setzt `If-None-Match` von Hand. Live
  gegengemessen: kalt EINE Anfrage → 200, ~3 MB; warm EINE Anfrage → **304, null Bytes** (86,9 ms),
  Karte baut aus dem Speicher in **101 ms** (24,5 ms IndexedDB + 76,5 ms `JSON.parse`), 12.149
  Merkmale, dieselbe Revision.
  💣 **ZUERST mussten die zwei `header()`-Zeilen hinter den Aufbau** — sie gingen davor hinaus und
  reisten damit auch auf einer 500 mit; ein Client, der den Rumpf darunter ablegt, bekaeme spaeter
  „deine Kopie ist aktuell“ fuer eine Fehlerseite, und das heilt nicht von selbst. Solange niemand
  etwas ablegte, war die Falle nur gestellt — mit diesem Umbau waere sie ausgeloest gewesen. Jetzt
  `avesmapsMapFeaturesSendCacheHeaders`, zweimal gerufen: auf der 304 und im EINEN Rumpf-Ausgang
  (Vorrats-Treffer wie frischer Aufbau) NACH dem Aufbau — dasselbe Muster wie in
  `api/locations/index.php`, festgenagelt von `etag-shared-test.php`.
  💣 **DER KNIFFLIGE TEIL IST DER VORABRUF, und er wird im KOPF geloest, nicht im Abruf.**
  `index.html` meldet die Anfrage per `<link rel="preload" as="fetch">` an, und ein Vorabruf passt
  nur bei GLEICHEN Kopfzeilen — deshalb schickt der `fetch` in `routing.js` keine eigenen (der
  `Accept`-Kopf wurde dafuer entfernt). Ein `If-None-Match` verfehlt ihn also, und dann reisen die
  3 MB **ZWEIMAL**, schlechter als der Zustand davor. Darum haelt der Tag zusaetzlich in
  `localStorage` (synchron lesbar), und das Vorabruf-Skript im Kopf entscheidet daraus: Tag da →
  **gar kein Vorabruf** (die Anfrage wird ohnehin bedingt und winzig), kein Tag → Vorabruf wie
  bisher. Im Netz-Protokoll nachgezaehlt: in BEIDEN Faellen genau EINE Anfrage.
  🔴 **Der Schluessel `avesmaps.kartendaten.etag` steht deshalb ZWEIMAL** (Modul und Kopf von
  `index.html`) — der Kopf entscheidet, bevor irgendein Skript geladen ist. Zeichengleich zu halten
  ist Pflicht; `kartendaten-speicher.test.js` haelt beide gegeneinander.
  💣 **Abgelegt wird die ZEICHENKETTE, nicht das geparste Objekt** — gemessen, nicht geraten
  (20,3 MB / 12.149 Merkmale): Lesen 30,5+77,1=107,6 ms gegen 83,8 ms, Ablegen 37,1 ms gegen
  67,1 ms. Das Objekt liest sich 24 ms schneller — Rauschen gegen die rund 2 s, die der Umbau spart
  —, aber die Zeichenkette ist UNVERAENDERLICH und laesst sich gefahrlos in den Leerlauf schieben,
  waehrend das geparste Objekt noch im selben Zug um `avesmapsSource` erweitert und durch die ganze
  Hydrierung gereicht wird.
  💣 **Der Tag in `localStorage` kommt ZULETZT** — erst wenn die Nutzlast wirklich liegt (`oncomplete`,
  nicht `onsuccess` des `put`: ein volles Kontingent schlaegt als `onabort` zu, nachdem `put` laengst
  erfolgreich aussah). Sonst unterbliebe der Vorabruf fuer eine Kopie, die es gar nicht gibt — der
  schlechteste aller Faelle. ⚠️ Und beim Lesen wird der Tag gegengeprueft: `localStorage` und
  IndexedDB sind zwei Speicher und koennen auseinanderlaufen; ohne den Vergleich haengte eine 304
  fuer Tag A die Nutzlast von Tag B in die Karte.
  ⚠️ **Alles faellt offen aus** (kein IndexedDB, volles Kontingent, halber Eintrag, fehlende Datei →
  normaler Vollabruf), und der Rueckfall bei leerem Speicher fasst GENAU EINMAL nach — eine 304 ohne
  mitgeschickten Tag wirft, statt im Kreis zu laufen.
  🔴 **Nur die oeffentliche Fassung.** Im Bearbeiten-Modus wird weder gelesen noch abgelegt: dort holt
  der Live-Abgleich staendig Deltas, und ein zurueckgehaltener Editor-Stand ist genau die Stoerung
  („meine Aenderung kommt nicht an“), die dieses Projekt schon mehrfach bezahlt hat.
  🪤 **Und die Messfalle, die zweimal in die Irre fuehrte:** liest man die Kopfzeilen aus einer
  Antwort, die der Browser aus seinem EIGENEN Cache beantwortet hat, steht `ETag` sehr wohl da — er
  stammt dann aus einer frueheren 304, und die 200 sieht faelschlich heil aus. Nur `cache:
  "no-store"` misst, was wirklich ueber die Leitung kam. Ebenso wertlos ist dort
  `performance.getEntriesByType("resource")`: der Puffer haelt 250 Eintraege, diese Seite laedt rund
  255, und die warme Anfrage fiel deshalb komplett heraus („0 Anfragen“, obwohl eine lief). Gezaehlt
  wird im Netz-Protokoll.
  Tests: `js/app/__tests__/kartendaten-speicher.test.js` (der Speicher mit gefaelschtem IndexedDB
  wirklich gefahren, samt der Vorabruf-Weiche aus `index.html`),
  `js/routing/__tests__/kartendaten-bedingter-abruf.test.js` (`loadRouteDataFromApi` ausgefuehrt:
  kalt, warm, leerer Speicher, Kreis-Riegel, Bearbeiten-Modus, ohne Speicher).
- 💣 **`segments[].cost_units` der Routing-API ist KEINE Stunde**, und `route.cost` schon gar nicht.
  🔴 **UND ES SIND ZWEI UMRECHNUNGEN, NICHT EINE.** `cost_units` entsteht als `distance_units /
  Tempo`; die Strecke steht in KARTENEINHEITEN (mal drei, `AVESMAPS_TERRAIN_MEILEN_PER_MAPUNIT`) und
  jedes Tempo unserer Tabelle ist um `AVESMAPS_TRAVEL_TIME_SCALE` (1,19) UEBERHOEHT — es ist als
  `Tagesleistung × mean_G × 1,19 / Reisetag` gebaut. Echte Stunde = `cost_units × 3 × 1,19`.
  Auf der Landroute Gareth → Perricum stehen deshalb drei verschiedene Zahlen fuer eine Reise:
  Summe der `cost_units` 21,004, `cost` 31,506 (das kalendergewichtete Dijkstra-Gewicht) und
  74,985 Stunden Reisezeit. Seit dem 25.08.2026 liefert die Antwort `duration.travel_hours` /
  `travel_days`; **daraus** liest man Zeit, nie aus `cost`. Es ist dieselbe Einheitenfalle, die am
  30.07.2026 einen falschen Infobox-Text oeffentlich gemacht hat.
  🪤 **Und die zweite Umrechnung fehlte bis zum 26.08.2026 — wegen einer GEGENPROBE, die niemand
  nachgemessen hat** (Meldung #101). Hier stand „63,0 Stunden im Reiseplan der Karte, der sie
  unabhaengig aus der Geometrie rechnet“, Faktor also exakt 3,000. Die Karte zeigt fuer diese Reise
  **73,4**. Der Satz war die einzige Stelle, an der die Umrechnung ueberhaupt geprueft wurde, und
  er hat die fehlende Haelfte als geprueft ausgewiesen — in Code-Kommentar, Test und `api/README.md`
  gleichzeitig, weil alle drei von derselben Messung abgeschrieben waren. ⭐ Eine Gegenprobe gegen
  eine ANDERE Flaeche (hier: die Karte) ist nur dann ein Beleg, wenn man sie wirklich abliest.
  ✅ **Der Rest der Luecke war ein EIGENER Befund und ist seit dem 26.08.2026 geschlossen:** 74,985
  (Server) gegen 73,4 (Karte). Die Karte trug `SPEED_TABLE` als feste Zahl im Browser (js/config.js),
  der Server legte zusaetzlich die im Fenster „Tempowerte“ gespeicherten Werte darueber — live
  gemessen Reisegruppe zu Fuss auf der Reichsstrasse **5,07** gegen **5,18**, Flusssegler **5,95**
  gegen **6,00**. Owner: „die tempowerte sollen auch fuer die karte gelten“. Siehe den Eintrag
  „Die Tempowerte gelten auch fuer die Karte“ in §11.
- Several edit endpoints leak `getMessage()` to clients (info disclosure,
  milestone M1).

## 11. Documentation index

Authoritative docs (being translated to English in M8):

- **`docs/design-language.md` — the warm/aventurian design language + token rules (see §12). Read before any CSS/UI work.**
- **Die Listenzeile — es gibt ZWEI, und das ist die Obergrenze.** `.wikisync-itemlist .tree-item` (`css/components/region-sync.css`) traegt alle acht Listen des WikiSync-Panels, `.avm-row` (`css/components/editor-page.css`) alle sechs Listen der Editorfenster. Entwurf **`docs/superpowers/specs/2026-08-14-wikisync-listen-vereinheitlichung-design.md`**, Mockup `docs/wikisync-listen-mockup.html`, live 14.08.2026. Davor waren es **sieben** Rezepturen: zweimal dieselbe Panel-Zeile in zwei Dateien, eine dritte als ID-Regel, die beide ueberstimmte, plus vier inline abgeschriebene in den Editoren — eine davon mit dem Kommentar „Referenz .se-row“ ueber sich. 💣 Beim Abschreiben faellt die Skala: `.se-row-type`/`.se-row-l2` standen auf `font-size:10px`, unter der 11px-Untergrenze aus §12, und der Fehler wanderte von einer Abschrift in die naechste. 💣 Eine ID-Regel auf einer Listenzeile ueberstimmt die geteilte lautlos (1,1,0 gegen 0,2,0) — die Aenderung wirkt dann bei zwei von acht Listen einfach nicht. 🔴 **Der Statuskreis haengt an `.has-map-status`** und kommt nur, wo eine Liste die Klasse ausdruecklich setzt — nie aus den Daten abgeleitet (Kraftlinien setzt gar keinen `.tree-map-status`-Marker und traegt trotzdem einen Kreis). **Seit 18.08.2026 tragen ihn ALLE Objektarten in BEIDEN Oberflaechen** — Panel *und* Editorfenster; hier stand bis dahin „gehoert NUR den fuenf Karten-Subjekten, sein Fehlen bei Literatur und Karten ist Information“, und der Owner hat das an diesem Tag umgedreht. 🔴 **Und seit 19.08.2026 auch die VORKOMMEN** — die Zeile hier nahm sie am Vortag noch ausdruecklich aus („nur die Vorkommen nicht“); sie sind jetzt die Ausnahme von der Ausnahme, mit einer eigenen, GEGENLAEUFIGEN Regel. 💣 **Die FORM ist geteilt** (`css/components/map-status-circle.css`, drei Wirte; Groesse als Token `--map-status-circle-size`, weil der Territorienbaum sie mit eigenem Markup `.cov` danebenzeichnet) — **die REGEL nicht: es sind FUENF, jede begruendet.** Wiki-Zuweisung (Ort) · `every` ueber eine Namensgruppe aus Segmenten (Weg, Kraftlinie — teilweise ist HALB) · `some` ueber zugeordnete ORTE, **halb schlaegt voll** (Literatur, Karte: leer = liegt nirgends, halb = mindestens ein Ort unaufgeloest) · **zwei unabhaengige BITS** (Region: linke Haelfte = mindestens ein zugewiesenes Label, rechte = mindestens eine zugewiesene Flaeche, voll = beides) · **EIN Fundort genuegt** (Vorkommen: leer = gar keine Ortsangabe, halb = Vorkommen genannt, aber KEINES auf der Karte „… z.b. schiff“, voll = mindestens eines verortet). 🔴 **Und ein Vorkommen ist beim Vorkommen nicht nur eine Ortszeile: eine LEBENSRAUM-REGEL mit Verbreitung zaehlt genauso** (Owner 19.08.2026: „regeln (sofern vorhanden und mit verbreitung) sind gueltige vorkommen“) — der Kasten traegt zwei Knoepfe, „+ Ort“ und „+ Regel“. Sie aendert nur, WAS zaehlt, nie die drei Stufen: ein Eintrag ohne Ortszeile, aber mit Regel, ist mindestens HALB und nie mehr leer. ⚠️ Ein Regeltreffer ist per Konstruktion auf der Karte — gematcht wird gegen `ecosystem_region` (aktiv, kein Klimaband), also gegen dieselbe Familie, gegen die eine Ortszeile geprueft wird; ein Schluessel wird dafuer NICHT nachgeschlagen (561 der 929 Flaechen haben gar keinen). Gezaehlt wird die REGEL, nicht die Zahl ihrer Treffer — eine Regel ueber 119 Waelder ist EIN Vorkommen. Die UND/ODER-Kette wird nicht nachgebaut: `avesmapsLoreReadRuleCountsByEntry` ist der dritte AUFRUFER von `avesmapsLoreRuleChainMatchesSubject`. ⚠️ Der Rechenstand (`ecosystem_assignment_stamp`) gilt nur fuer „trifft etwas“, nicht fuer „ist da“ — waehrend „Zugehoerigkeit rechnen“ laeuft, faellt ein Eintrag von voll auf halb, nie auf leer. 🚩 Live 19.08.2026 hat **genau EIN** Eintrag eine Regel („Alprute“, 119 Waelder, ohne jede Ortszeile) — er wandert von leer auf voll. 🔴 **Die letzten beiden sind GEGENLAEUFIG, und das ist gewollt:** bei einem Werk ist jeder unaufgeloeste Ort offene Arbeit, bei einer Ware genuegt EIN Fundort, damit sie auf der Karte vorkommt. Wer sie vereinheitlicht, dreht mindestens eine Aussage um. Ein Bauer je Objektart in **`js/ui/listen-statuskreis.js`**, geladen von `index.html` UND den fuenf Editorseiten — die zwei Listen einer Objektart liegen in verschiedenen Dokumenten und koennen sich sonst nichts teilen. 💣 **Gemessen wird immer das ZUWEISUNGSfeld, nie das danebenstehende `wiki_url`** (`properties.wiki_settlement` beim Ort, `properties.wiki_path` beim Weg): der Lesepfad raet `wiki_url` bei Leere per Namen nach — 99 Phantome bei den Orten, 12 bei den Wegen, live gemessen. Bei der Region gilt dasselbe fuer den **Namensgleichstand**: `avesmapsWikiRegionMatch` zaehlt ein gleichnamiges Label als Treffer (195 von 540), deshalb reist `label.assigned` mit. ⚠️ Die Modifier `--children-only`/`--own-only` heissen bei der Region rein VISUELL links/rechts (sie stammen aus den Territorien) — kein zweites Vokabular dafuer erfinden. 🪤 **Ein Kreis, der nur leer sein kann, luegt:** die vier Diagnoselisten in `review-path-sync.js` trugen `has-map-status` mit einem Marker ohne Modifier (86 Zeilen „Flussrichtung unbekannt“, alle leer, alle auf der Karte) und 3744 map-only-Zeilen ein festes `--all`; beides am 18.08.2026 beseitigt. ⚠️ `.avm-row__name` ellipsiert — der Marker gehoert NEBEN den Namen in `.avm-row__l1`, nie als `::after` hinein (Kartentitel: Median 42 Zeichen, p90 60). Die Bilanzzeile unter der Suche hat EINEN Erzeuger (`js/review/review-list-balance.js`) und traegt nur, was der Filter bewegt. 💣 **Beim Vorkommen kommt die Zahl vom SERVER** (`place_mapped_count`, `avesmapsLoreReadPlaceKeysOnMap` in `api/_internal/app/lore.php`) — aus zwei Gruenden: die im Payload mitgeschickte Ortsliste `places` ist auf **6 Titel gekappt**, waehrend „voll“ schon ein einziger verorteter Ort ausloest (der kann der siebte sein), und was der Browser geladen hat, haengt an Zoom und Ansicht (die politische Ebene liefert bei Zoom 3 gerade 174 von rund 800 Gebieten) — im Client gerechnet flackerte der Kreis mit dem Kartenausschnitt, dieselbe Begruendung wie bei `derived_source_geometry_public_ids` (§10). Verglichen werden **VIER Familien** im selben Schluesselraum: `properties.wiki_settlement.wiki_key` (Ort) · `properties.wiki_region.wiki_key` (Landschaftslabel) · `ecosystem_region.wiki_region_key` (Landschaftsflaeche) · `political_territory.wiki_key` (Herrschaftsgebiet, mit `wiki:`/`name:`-Praefix). ⚠️ **Kein Namensvergleich und kein Abschneiden von Klammerzusaetzen** — der Spotlight-Loeser kennt beides, weil er einen ANFLUGPUNKT sucht; hier steht die Frage „ist das zugewiesen?“. Preis gemessen: 3 von 5104 Eintraegen („Aventurien (Kontinent)“, „Nostria (Siedlung)“). ⚠️ **Wege sind bewusst nicht dabei** — „Der Grosse Fluss“ und „Szinto“ liegen als `Flussweg` auf der Karte, sind aber weder Ort noch Region. ⚠️ Die zwei JSON-Familien lesen `properties_json` aller Orte und Labels (rund 6,7 MB, einmal je Katalogseite) und sind gegen SQLite nicht pruefbar; ein Fehlschlag wird deshalb **protokolliert**, nie geschluckt — sonst sieht ein SQL-Fehler exakt aus wie „nichts liegt auf der Karte“ (die HY093-Falle von „Was ist hier?“). Am Livebestand 18.08.2026: **2612 voll, 793 halb, 1699 leer**; mit den Regeln (19.08.) **2613 / 793 / 1698**. 🔴 **Und der Kreis geht MIT, wenn im Kasten „Vorkommen“ etwas dazukommt oder verschwindet** (Owner 19.08.2026). ⭐ Ohne zweiten Abruf und ohne Nachrechnen im Browser: `avesmapsLoreReadEntryDetail` liefert dieselben vier Zahlen, die jede Schreibaktion ohnehin zurueckbringt, und `avesmapsLoreRefreshRowStatus` baut die Zeile mit DEMSELBEN Bauer neu — in BEIDEN Listen. 💣 Der Trichter ist `renderLoreDetail`, wo alle vier Schreibwege muenden (add_place/remove_place/set_field direkt, save_rule/delete_rule ueber `openLoreDetail`); haengte er an den Aufrufstellen, waere er beim naechsten Knopf vergessen. ⚠️ Neu gebaut wird die GANZE Zeile, nicht nur die Kreisklasse — ein Ort aendert auch die Meta-Zeile, und ein voller Kreis neben „ohne Ortsangabe“ liest sich wie ein Fehler. Gewacht von `js/review/__tests__/wikisync-list-form.test.js`, `…/wikisync-balance-line.test.js`, `…/lore-dialog-layout.test.js`, `js/pages/__tests__/editor-row-single-source.test.js`, `api/_internal/app/__tests__/lore-orte-auf-der-karte-test.php`, `…/lore-regel-als-vorkommen-test.php` und — fuer den Kreis samt seiner Verdrahtung in allen zwoelf Listen — `js/ui/__tests__/listen-statuskreis.test.js`.
- **Das Sammelmenü im Menüband — es gibt EINES.** `js/ui/ribbon-menu.js` klappt es auf und zu, in allen drei Editoren, die eins haben: **„Wappen & Bilder ▾"** (Orte), **„Wappen ▾"** (Territorien), **„Rechnen ▾"** (Regionen). Die Optik steht in `css/components/editor-page.css` (`.rb-menu*`), das Verhalten nur in der einen Datei — abhängigkeitsfrei wie `js/ui/filter-menu.js` daneben, weil die Editoren eigenständige iframe-Dokumente sind und das Rüstzeug des Hauptfensters nicht laden. Vorher standen dieselben 16 Zeilen **zweimal** inline (Orte und Territorien, unterschieden nur durch das Präfix `se…`/`btn…`); der dritte Editor hätte die dritte Kopie bekommen — genau der Weg, den dieser Abschnitt für die Listenzeilen (sieben Rezepturen) und die Wiki-Zuweisung (sechs Fassungen) schon zweimal beschreibt. Eingesammelt am 24.08.2026, **vor** der dritten Kopie. 💣 **Der Zustand ist das `hidden` des Panels und sonst nichts** — kein Modulzustand daneben, der auseinanderlaufen kann; an genau dem sind das Anzeige-Menü der Karte und die Ansichts-Kacheln schon gescheitert. 🔴 **Die teuerste Falle ist nicht „es klappt nicht auf", sondern die DOPPELANMELDUNG:** am 23.08.2026 blieb beim Umbau der alte Klapp-Block stehen und der neue kam dazu — zwei registrierte Klick-Handler, der erste öffnet, der zweite schließt im selben Klick. Für den Benutzer passiert nichts, und jede einzelne Zeile sieht richtig aus. `avesmapsRibbonMenuAttach` gibt bei einem zweiten Aufruf auf demselben Knopf die vorhandene Steuerung zurück. 💣 **Der Selektor nennt BEIDE Kachelformen** (`.rb-menu > .btn2, .rb-menu > .avm-tile`): Orte und Territorien bauen aus `.btn2`, Regionen aus `.avm-tile`. Fehlt die zweite, schrumpft der Sammelknopf auf seine Textbreite und lässt den Rest der Gitterspalte leer — die Lücke vom 23.08.2026, eine Kachelform weiter. ⚠️ Dass diese zwei Klassen Zeile für Zeile dieselbe Kachel sind, in **derselben Datei** unter zwei Namen, ist die Listenzeilen-Doppelung eine Etage höher — noch nicht zusammengelegt. 🔴 **Was hinein darf:** nie die Haupthandlung der Seite (`🚨 Syncen` bleibt gefüllt und außen) · nichts unter drei Einträgen (ein Menü für zwei Zeilen ist ein Klick für nichts) · **ein Zustand nur, wenn der Knopf ihn in seiner Unterzeile trägt** — „Wappen an · Bilder aus" beim Notaus, „Kurven rechnet …" bei den Läufen. Ein Notaus, dessen Stand man aufklappen muss, ist keiner; und drei der vier Regionen-Läufe dauern zu lange, um ihre Rückmeldung zuzuklappen. Tests: `js/ui/__tests__/ribbon-menu.test.js` (das Bauteil), `js/pages/__tests__/wappen-menue-verdrahtung.test.js` und `…/rechnen-menue-verdrahtung.test.js` (dass die drei Editoren es benutzen, statt eine eigene Fassung anzulegen).
- **Der Hintergrundklick — ein Bauteil, und er gilt nur, wo der Hintergrund ein SCHLEIER ist.** `js/ui/dialog-hintergrund-schliessen.js` schliesst ein Fenster, wenn man daneben klickt; verdrahtet in `js/app/bootstrap.js` als EINE Tabelle von sieben Paaren (Ort · Weg · Kraftlinie · Herrschaftsgebiet · „Karteneintrag melden“ · WikiSync-Fall · Dump-Zugangsdaten). Owner 02.09.2026: „ansonsten ist das das normale verhalten von fenstern“. Live 02.09.2026. 🔴 **Die Regel, die den Umfang bestimmt:** ein Hintergrundklick schliesst nur ein Fenster mit vollem Schleier, nie eines, dessen Hintergrund die ARBEITSFLAECHE ist. Die vier Fenster, die an der Karte arbeiten („Flaeche vereinfachen“, „Grenze aus Territorien“, „Flaeche bearbeiten“, der Eigenschaften-Dialog — Owner 28.07.2026) und der VERKLEINERTE Konflikte-Dialog tragen `pointer-events: none` an ihrer Huelle und reichen Zeiger durch; sie bekommen den Klick ohnehin nie. Die Regel setzt sich also von selbst durch — solange niemand am `document` lauscht. 💣 **UND GENAU DAS IST DIE TRAGENDE ZEILE: der Zuhoerer haengt je Fenster an dessen Overlay, nie am `document`.** Am Dokument delegiert — wie es `changelog-dialog.js` fuer Escape tut — braeche der Ablauf „Neue Position vorschlagen“ (`startChangePositionPick`, `review-locations.js`): der blendet sein Overlay auf `hidden` und laesst den Melder EINEN Punkt auf der Karte waehlen. Dieser Kartenklick waere dort ein Hintergrundklick, und Schliessen heisst hier `resetForm: true` — der halb geschriebene Vorschlag waere weg, im Moment der Positionswahl, und es saehe wie ein verschluckter Klick aus. Im Browser gegengemessen: die verworfene Variante schlaegt bei genau diesem Klick zu, die gebaute laesst Text und uebernommene Position stehen. 💣 **Geschlossen wird nur, wenn DRUCK UND LOSLASSEN auf dem Hintergrund lagen** — und das ist der einzige Unterschied zu den rund 25 aelteren Abschriften im Haus, die alle nur `event.target === overlay` pruefen. `click` feuert am naechsten gemeinsamen VORFAHREN: wer in einem Textfeld markiert und dabei ueber den Fensterrand hinauszieht, druckt im Fenster und laesst auf dem Hintergrund los — der Vorfahre IST das Overlay, und ein ausgefuelltes Formular ist weg. Bei einem Anzeigefenster faellt das nie auf, weil dort nichts zu verlieren ist; deshalb kommen die 25 damit durch. ⚠️ Sie werden hier NICHT umgeschrieben (das waere unbestellter Umbau) — wer eine anfasst, holt sie hierher. 🪤 **Zwei Werkzeugfallen beim Abnehmen:** `computer{action:left_click_drag}` der Browser-Pane erzeugt nur `pointerdown`/`mousedown` und KEIN `mouseup`/`click` — die Ziehgeste ist damit nicht messbar, und ein „0 mal geschlossen“ kommt vom Werkzeug, nicht von der Sache; und ein Dateipfad in einem HTML-Kommentar von `index.html` ist fuer jeden Ladereihenfolge-Test, der mit `html.indexOf(…)` misst, ein frueheres `<script>`-Tag — `zoomstufe-anzeige.test.js` ist beim Bau genau daran umgefallen. ⚠️ **Die Zahl, die hier zuerst stand („zwei Tests“), war zu klein:** `efd669eab` hat noch am selben Tag **acht** Tests die Kommentare strippen lassen und **29** Pfade in Kommentaren gemessen. Der Verzicht auf den Pfad im Kommentar bleibt trotzdem die Regel — der naechste Test bringt sein `indexOf` ungeschuetzt mit. Tests: `js/ui/__tests__/dialog-hintergrund-schliessen.test.js` (das Bauteil wirklich gefahren) und `…/dialog-hintergrund-verdrahtung.test.js` (der Verdrahtungsblock wird ausgeschnitten und ausgefuehrt, damit kein Fenster einen fremden Schliesser bekommt) — zusammen gegen 14 Mutationen gefahren, alle gefangen.
- **`html/editor-handbuch.html` — the handbook human editors actually read.** Not a doc *about* the code; it is product surface, reachable from the edit shell's top bar and the editor panel's status line. Five layers: Erste Schritte / Karten-Features / Aufgaben / Verstehen / Nachschlagen. **Maintained by a nightly scheduled task, not by feature sessions (see §9)**: `avesmaps-handbuch-pflege` runs at 00:00 local and does two things — (A) read every commit since its last run and rewrite the sections an editor would now find wrong, (B) re-verify 3–4 of the 33 anchored `<h3 id=…>` sections against the live code on rotation, so the whole book is checked roughly every 8–10 days. It commits only when something actually changed, and only `html/editor-handbuch.html`. Plan and gap inventory in `docs/superpowers/plans/2026-07-20-editor-handbuch-aufwertung.md`.
- **`docs/kartenflaechen-und-zoomblenden.md` — welche Zeichenflaeche auf welcher Technik liegt, wie scharf sie zeichnet und was beim Zoomschritt blendet.** 🔴 **Die eine Regel: was Geometrie ist, steht; was Schrift ist, blendet** (Owner 24.08.2026). 💣 „Blenden“ hat ZWEI Bedeutungen, und ihre Verwechslung hat an einem Tag vier Runden gekostet: der *weiche Bandrand* (ein Element beginnt schwach, wenn seine Zoomstufe kommt — Ortsmarkierungen ab 1,33 px, Grenzlinien ueber `BOUNDARY_WEAK_ALPHA_BY_ZOOM`) gegen die *Zeitanimation am Zoomschritt*. Gemeint war die zweite. 💣 **Mit EINER Canvas ist „gleichzeitig“ unmoeglich** — sie verliert ihr altes Bild beim Zeichnen des neuen; die Ueberblendung braucht zwei Flaechen mit Rollentausch (Canvas) bzw. einen Pane-Klon (DOM, und der skaliert gratis mit, weil die Label-Panes keine eigene Transform tragen). 💣 **Neu zeichnen statt blenden geht nicht**: ein `redraw()` blockiert gemessen 52–99 ms. 💣 **`transition` ist EINE Eigenschaft** — eine im Einblenden gesetzte Transform-Transition ueberlebt den Zoom und laesst danach jeden PAN nachziehen (live gemeldet, `e85b31d1`); sie gehoert ausschliesslich in den `zoomanim`-Handler. 💣 **Die Blende startet zwei `requestAnimationFrame` NACH dem Redraw**: am `zoomend` blockiert der Hauptthread 215 ms (Standard) bis 836 ms (Politisch), ein dort gestarteter Uebergang verstreicht ungezeichnet und sieht aus wie ein Sprung. 🔴 **Die Schaerfe ist EINE Regel mit fuenf Lesern** — `avesmapsCanvasDpr()` (`js/app/runtime-state.js`, neben `avesmapsIsPhoneViewport`); vorher stand sie viermal verteilt und war messbar auseinandergelaufen (auf einem 3x-Telefon zeichneten Wegenamen und Marker mit 3x, die uebrigen Beschriftungen mit 2x). Jede Flaeche behaelt ihren EIGENEN Deckel fuer Zeigergeraete (Grenzen 1, Wegenamen `Infinity`, Labels 2) — geteilt ist nur die Telefon-Regel. ⚠️ Ihr Grund ist der SPEICHER, nicht die Schaerfe (Label-Bilder sind bei 3x 2,25-mal so gross); die verworfene Variante „voll ueberall“ bleibt als Option `avesmapsPhoneCanvasMaxDpr = Infinity` fuer die geplanten globalen Einstellungen. Stellschrauben, offene Punkte und drei **Messfallen** (`getComputedStyle` ohne Frames · `@import` im CSSOM · ein Canvas ist EIN Knoten) stehen im Dokument.
- `docs/asset-caching-and-versioning.md` — **the deploy/cache gotcha** (see §7).
- `docs/future-map-architecture.md` — north-star architecture & full data model.
- `docs/territories.md` — political-territory data model + WikiSync.
- `docs/refactoring-map.md` — frontend responsibility-cluster map & load order.
- `docs/repository-data-policy.md` — what may/may not enter the repo.
- `docs/database-backup.md` — **the full-database backup** (edit shell top bar → „💾 Datenbank-Backup", `edit/backup.php`, **admin only**). Chunked resumable dump → one gzip-packed `.sql` that restores with `gunzip -c … | mysql` or a phpMyAdmin import; lib `api/_internal/backup/db-dump.php`, endpoint `api/edit/admin/database-backup.php`, state `db_backup_run`, files in HTTP-denied gitignored `uploads/db-backups/` (3 newest kept). 💣 **The `.gz` is built the pigz way — one gzip MEMBER, not one member per chunk.** A deflate stream cannot be resumed across requests, so the naive writer appends a member per flush; `gunzip`/zlib read that whole, but PHP's `gzdecode()` and 7-Zip's GUI read only the FIRST member and hand back a silently truncated dump. Hence: fixed 10-byte header once, then per flush a FRESH raw-deflate context with `ZLIB_SYNC_FLUSH`, then a final empty block + CRC/ISIZE, with the CRC carried across steps via `crc32_combine`. The unit test's `gzdecode()`-returns-everything assert is what guards it. ⚠️ Hot backup (no transaction spans the run) and the file carries password hashes — never in the repo.
- **Das Fenster „Neuigkeiten"** — die Projekt-Meilensteine, erzählt für Leser, die weder Commits noch Code kennen. Live 2026-08-03; **`docs/neuigkeiten-fenster.md`**, Mockup `docs/changelog-mockup.html`. 🔴 Die **Beschriftung** heißt seit 09.08.2026 „Neuigkeiten", der **Code** weiter `changelog` (Tabelle `changelog_entry`, i18n-Schlüssel `changelog.*`, Endpunkt, Dateinamen) — der Deploy löscht nie (§10), eine umgetaufte Adresse liesse eine gecachte `index.html` ins Leere greifen. Aus demselben Grund ist **`verlauf:` kein Commit-Scope dafür**: das Wort gehört dem Wiki-Kurs-Sync der Wege (`path-verlauf.php`, `verlauf_cases`). 💣 Vier Fallen, alle im Dokument begründet: die Ecke ist EIN Bund (`#map-corner-actions`, `--avesmaps-corner-stack` an `:root`) · eine leere Tabelle fällt auf `avesmapsChangelogSeed()` zurück, und ein Seed sieht genau wie ein gepflegter Verlauf aus (so blieb er fünf Tage unbemerkt stehen) · `overflow-anchor: none` am Scroll-Kasten ist tragend · die pflegende Routine hat keine Session, nur ein Token mit **eigenem** Schlüssel (`changelog.app_token`, nie der von Discord) und nur `list`/`save`, nie `delete`.
- **Tastaturbefehle** (`js/app/keyboard-shortcuts.js`, live 2026-08-05) — die Karte lässt sich mit der Tastatur bedienen: Suche, Reiseziel, Verschieben/Zoomen, die sechs Ansichten, die sechs Ortsklassen, schnellste/kürzeste Route, Etappe vor/zurück. 💣 **Die Liste `SHORTCUTS` in dieser Datei ist die EINZIGE Quelle** — sie ist zugleich die Belegung *und* der Bauplan der Tabelle unter **Hinweise → „Bedienung" → „Bedienhilfen"** (`#legal-shortcuts`, Stil in `css/components/legal-dialog.css`). Wer eine Taste woanders verdrahtet oder die Erklärung von Hand in `index.html` schreibt, baut die Divergenz ein, die das verhindern soll. 💣 **Kein Strg, kein Alt, kein Meta** (Owner-Entscheid 2026-08-05): der erste Entwurf legte sieben Befehle auf Strg, und die gehören dem Browser (Strg+R lädt neu, Strg+P druckt, Strg+F sucht im Text, Strg+L/K sind die Adresszeile und lassen sich nicht überall abfangen). Dieselben Buchstaben ohne Strg — `matchShortcut` gibt bei jedem dieser Modifier sofort auf. 💣 **W A S D schieben und liegen VOR den Ansichten — diese vier Buchstaben sind für eine Ansicht unerreichbar.** `matchShortcut` nimmt die erste Zeile mit der Taste, und das Schieben steht oben. Daran ist „Standard" auf `S` gescheitert (Owner 2026-08-05, danach zurückgenommen: „behalte WASD"); die Ansicht sitzt seither auf `N`. Wer einen neuen Buchstaben vergibt, prüft ihn zuerst gegen W A S D. Die Ansichten heißen `O` Original, `P` Politisch, `K` Kraftlinien, `N` Standard, `L` Landschaften, `I` Nur Karte — „Landschaften" ist seit 2026-08-05 dabei (ansehen darf die Ebene seit 2026-08-04 jeder, nur das Bearbeiten hängt an einer Fähigkeit). ⚠️ **Leaflets eigene Tastatursteuerung ist abgeschaltet** (`map.keyboard.disable()`): sie wirkte nur nach einem Klick auf die Karte, und ihre Pfeiltasten hätten sich mit den hiesigen zu doppelten Sprüngen addiert. ⚠️ Der Riegel ist EINE Regel für alle: nichts wirkt beim Tippen, bei offenem Fenster (`[role=dialog]` sichtbar) oder bei laufendem Werkzeug — erkannt an den Klassen, die die Werkzeuge ohnehin an den Kartencontainer hängen (`TOOL_CLASSES`), also ohne zweiten Zustand. Test: `js/app/__tests__/keyboard-shortcuts.test.js`.
- **Der Kartenfächer** (bis 26.08.2026 „Die Ansichts-Kachel in der Kartenecke") (Entwurf `docs/superpowers/specs/2026-08-11-ansichts-kacheln-design.md`, Mockup `docs/layer-kacheln-mockup.html`, live 2026-08-12) — die sechs Derographie-Ansichten stehen als Kachel unter dem Suchknopf im Bund `#map-corner-actions`; zugeklappt zeigt sie die eingestellte Ansicht samt Namen, aufgeklappt faltet sie sich zu allen sechs auf (am Zeiger eine Reihe, am Telefon 2×3). 🔴 **Damit ist die Zeile „Derographie" aus dem Routenplaner verschwunden** — wer sie sucht: sie ist nur AUSGEBLENDET, das `<select>` steht weiter im DOM, denn es IST der Zustand (`getSelectedMapLayerMode` liest es, der geteilte Link `?mapLayerMode=…` kommt dort an). `?layerPanelActive=0` ist der Notausgang und holt die Auswahlbox zurück. 💣 **Vier Kopplungen, jede einmal danebengegangen und vom Owner gesehen, nicht vom Werkzeug:** die aktive Ansicht ist immer die LETZTE Rasterzelle und Kachel wie Raster tragen dieselbe Polsterung an derselben Ecke — nur so bleibt sie beim Aufklappen auf ihrem Fleck, ohne dass ein Versatz gerechnet wird · das Raster steht IM FLUSS, nicht schwebend (schwebend verdeckte es die Zoom-Knöpfe; im Fluss wächst der Bund und der Zoom liest dessen gemessene Höhe) · **an der Hülle werden `opacity` und `transform` NICHT animiert**, Blende und Bewegung sitzen an den Zellen und die aktive ist von beidem ausgenommen (sonst wackelt bzw. flackert der Knopf) · die sechs Ansichten stehen nur in den `<option>`, nie ein zweites Mal im Picker. 🔴 **Seit 15.08.2026 rollt die Hülle beim Aufklappen auf** (Owner: „C aber auch mit mouse over"; Entwurf `docs/layer-kacheln-animation-mockup.html`) — 150 ms auf, 110 ms zu, dazu die Zellen gestaffelt von der Kachel nach außen (20 ms Abstand; die erste Fassung lief mit 260/160/40 und war dem Owner am selben Tag zu langsam). Die Zeile davor stand bis dahin als „an der Hülle wird NICHTS animiert" da und war damit die **einzige** nicht animierte Fläche im Bild: das Aufklappen las sich trotz Blende als Schnitt. 💣 Erlaubt ist genau `clip-path`, weil es weder verschiebt noch blendet und die aktive Zelle im nie beschnittenen rechten Ende liegt; `opacity`/`transform` bleiben verboten. 💣 Der Startwert ist `--map-layer-tile` (= `--map-layer-cell` + 12), nie eine abgeschriebene 78 · gestaffelt wird mit `:nth-last-child`, weil die aktive Ansicht die letzte Zelle ist und ihre Stelle mit jeder Auswahl wechselt · am Telefon (2×3) rollt **nichts** auf, dort trägt die Staffelung allein · `BLENDE_ZU_MS` im JS und die Zuklapp-Dauer im CSS sind ein gekoppelter Wert in zwei Dateien. 💣 **Der Zustand ist eine Variable, nicht `is-open` und nicht `hidden`** — an der Klasse gelesen kam das Zuklappen beim Verlassen nicht zustande (sie wird erst im nächsten Bild gesetzt); derselbe Fehler wie beim Anzeige-Menü nebenan. 🔴 **Ein Klick HÄLT das Menü offen, bis woanders hingeklickt wird** (Owner 15.08.2026: „mouse over wie bisher, aber wenn ich draufklicke bleibts offen"); das Überfahren bleibt flüchtig. 💣 Der Riegel (`festgehalten`) sitzt an der **aktiven Zelle**, nicht an der Kachel: sobald das Überfahren geöffnet hat, ist die Kachel `hidden`, und der Klick, den der Benutzer für einen Kachelklick hält, trifft die aktive Zelle auf deren Fleck — die führte vorher direkt ins Zuklappen, es sah also aus, als ginge das Menü vom Anklicken zu. Zurückgesetzt wird nur in `schliesse()`, damit Auswahl, `Esc` und Kartenklick alle drei greifen. Bauteile: `js/ui/map-layer-picker.js`, `css/components/map-layer-picker.css`, Bilder `icons/layer-tiles/` samt Aufnahme-Werkzeug `tools/layer-tiles/`. Test: `js/ui/__tests__/map-layer-picker.test.js`. 🪤 **Und der Befund, der wie ein Code-Fehler aussah:** das Überfahren war vom 12.–15.08.2026 im Repo, aber nicht auf dem Server — ein Deploy-Fehlschlag hatte den `?v=`-Stempel vergiftet (§9). Wer meldet „Funktion X tut nichts", vergleicht **zuerst** das Ausgelieferte mit dem Repo, bevor er den Code liest. 🔴 **SEIT 26.08.2026 ZWEISTUFIG: „Der Kartenfächer" kreuzt fünf Ansichten mit drei Untergründen** (Entwurf `docs/superpowers/specs/2026-08-26-ansicht-untergrund-kreuzen-design.md`, bedienbares Mockup `docs/ansicht-untergrund-mockup.html`). Überfahren öffnet die Ansichten; Verweilen auf einer fächert **darüber** ihre Untergründe auf — als EINE Kachelbreite genau über dieser Ansicht, die sich dann teilt. 🔴 **„Original" ist keine Ansicht mehr, sondern ein UNTERGRUND** (Old · Original · Modern); sie war nie etwas anderes als eine nackte Karte auf der alten Basis — ihre Zeile in `FRONTEND_LAYER_MODE_DEFAULTS` ist Zeichen für Zeichen die von „Nur Karte". Alte Links `?mapLayerMode=original` werden **übersetzt**, nicht verworfen. 🔴 **Eine Auswahl schließt das Menü**; es gibt genau zwei (Untergrund im Untermenü — wählt BEIDES; zweiter Klick auf dieselbe Ansicht — wählt sie allein). Der erste Klick auf eine Ansicht ist keine Auswahl, sondern das Aufklappen. ⭐ **Daraus fällt das Telefon-Verhalten ab**: ohne Überfahren öffnet der erste Tipp und der zweite wählt — kein zweiter Bedienweg. 💣 **Die Zeiten gelten BEIDEN Stufen** (`SCHWEBE_AUF_MS`/`SCHWEBE_ZU_MS`); sie standen vorher dreimal einzeln da. 💣 **Zwischen den Stufen liegen 6px Lücke** — sie braucht ZWEI Riegel: eine unsichtbare Brücke im CSS und den Nachlauf im JS. 💣 **Der Offen-Zustand der zweiten Stufe steht in einer Variablen**, nicht in `is-open` (die Klasse kommt erst im nächsten Bild). 🔴 **Die Ansichten sind VEKTOREN über dem gewählten Untergrund**, keine Aufnahmen: eine Aufnahme trägt ihren Untergrund eingebrannt mit, also bräuchte jede Kreuzung ein eigenes Bild. 3 Kacheln + 5 Vektoren decken alle 15 ab — und die Bedingung „alle Ansichten am selben Kartenausschnitt" entfällt, weil ein Vektor keinen Ort hat. Die Farben sind die echten (Kraftlinien als DREIFACHER Strang aus `css/features/powerlines.css`), die Straßen aus der Vorlagekachel nachgezeichnet — **diese Koordinaten gehören zur Vorlagekachel, nicht zur Ansicht**. 💣 **Was die Ansicht mit dem Untergrund macht, gehört auf das BILD, nicht auf die Zelle** (an der Zelle entsättigte der Filter den Vektor gleich mit). 💣 **„Old" sieht nur der Editor** — `tiles/old` darf deshalb NICHT gelöscht werden. 💣 **Der stille Rückfall, der es zweimal fast verhindert hätte:** der `change`-Handler des `#mapStyleSelect` trug `if (!IS_EDIT_MODE) { this.value = "stylized"; return; }` und setzte jede Wahl eines Besuchers zurück — das Menü reagierte, die Karte nicht, von außen nicht von einem kaputten Klick zu unterscheiden. 🔴 **Fünf Kacheln nebeneinander, Grenze gerechnet**: 366px Kasten + 2×12px Kartenrand = 390px; darunter drei Spalten. Die Spaltenzahl ist die tatsächliche Anzahl (`--map-layer-spalten`), keine feste 6 — und als CSS-Variable, nicht als Inline-Style, der die Media Query schlüge.
- **Das Inhaltsverzeichnis der Hinweise** (Entwurf `docs/superpowers/specs/2026-08-05-hinweise-inhaltsverzeichnis-design.md`, live 2026-08-05) — die acht Abschnitte des Fensters „Hinweise" (`#legal-dialog` in `index.html`) sind `<details>`: **zugeklappt sind sie sein Inhaltsverzeichnis** (Überschrift plus eine Zeile, was drinsteckt), aufgeklappt sein Inhalt. Aus 4.937 px Scrollstrecke wurden 803. 💣 **Nativ, und nichts anderes:** nur `<details>/<summary>` lässt Strg+F Text in einem ZUgeklappten Abschnitt finden und klappt ihn selbst auf — ein selbstgebautes Auf- und Zuklappen mit `display:none`/`hidden` nimmt der Seitensuche den Text weg, und dieses Fenster trägt Impressum und Datenschutzerklärung. Fokus, Enter/Leertaste und `aria-expanded` kommen ebenfalls vom Element; hier gehört kein JS hin. ⚠️ Die Trennlinie sitzt an `.legal-section` (dem Abschnitt), nicht mehr an der Überschrift: im `<details>` ist das `<summary>` immer erstes Kind, die alte Ausnahme `.legal-dialog__group:first-child` hätte alle acht Linien gelöscht. ⚠️ Kein vollbreiter Hover-Streifen mit negativen Seitenrändern — der Scroll-Kasten hat `overflow-y: auto` und hängt sich sonst eine waagerechte Bildlaufleiste an. Der Absatz „Betreiber/Impressum" steht seit 2026-08-05 in **„Kontakt und Impressum"**, nicht mehr in „Projekt und rechtlicher Status".
- `docs/map-features-rest-architecture.md` — structure of the map-features layer.
- `docs/quellen-system-design.md`, `docs/quellen-system-2-editor-design.md` — multi-source system (infobox display + editor management).
- `docs/wiki-publikations-quellen-design.md` — Wiki publication-source bulk lookup (**shipped**; sources travel in the map payload; wiki/manual/community provenance; manual/suppressed overrides preserved). Implementation instruction: `docs/wiki-publikations-quellen-instruction.md`.
- **Die Quellenliste — sie klappt, sie steht in Spalten, und ihre Seitenangaben werden EINMAL gekürzt.** Live 24.08.2026 (Owner: „wenn mehr wie 5 quellen dranstehen, ist der rest zusammengeklappt … überall mit den quellenlisten“ und „ein tabellarisches layout … lange seitenzahlangaben mit ... abkürzen (oder 1. seite und dann mit ff.)“). Drei Dinge, zwei Oberflächen: der **Quellen-Editor** (`js/review/review-feature-sources.js`, Zeilen `.fs-row`) und die **Infobox**, die jeder Besucher sieht (`js/ui/feature-source-markup.js`, Zeile „Quelle(n):“ plus Publikationstabelle). 🔴 **Ab der sechsten Quelle klappt der Rest in ein natives `<details>/<summary>`** („15 weitere Quellen“) — nativ und nichts anderes, weil nur so Strg+F den Text einer ZUgeklappten Quelle findet und sie selbst aufklappt; dieselbe Begründung wie beim Fenster „Hinweise“. Gemessen: 20 Quellen, 814 px auf 380 px. Die Infobox klappte schon (Publikationsreiter). 💣 **Kein echtes `<table>`:** ein `<details>` darf zwischen `<tr>` nicht stehen, der Parser wirft es aus der Tabelle. Das Raster liegt deshalb **je Zeile** mit derselben Vorlage — nur so stehen die Spalten auch über die Grenze des Klappkastens hinweg untereinander, was `display: contents` nicht leistet. `.fs-row` und `.fs-col-heads` tragen dieselbe `grid-template-columns`, und beide müssen wirklich `display: grid` sein — eine Vorlage an einem Flex-Container ist inert und lässt das Raster lautlos verschwinden (die einzige Mutation, die den Test zunächst überlebt hat). 💣 **Ein fehlender Wert bekommt eine LEERE Zelle, nicht gar keine** — fällt eine weg, rutscht alles rechts davon eine Spalte nach links. ⚠️ Die **Eingabezeile** (`.fs-row--add`) ist vom Raster ausgenommen und bleibt eine Flex-Zeile: sie trägt acht Bedienelemente statt vier Datenspalten und verschob die Seitenspalte um 113 px (771 gegen 658, gemessen). 💣 **DIE TRAGENDE REGEL: `featureSourceShortenPages` gibt es GENAU EINMAL**, in `js/ui/feature-source-markup.js`, und **beide** Oberflächen rufen sie — die Infobox an zwei Stellen (Zeile 1 und Publikationstabelle), der Editor in seinem Zeilenbauer. Eine Regel, die einen von zwei Erzeugern bindet, ist keine Regel. **Darum lädt jede der fünf Seiten mit dem Quellen-Editor `feature-source-markup.js` DAVOR** (`index.html` plus `html/{citymap,landschaften,wiki-sync-powerline,wiki-sync-settlement}-editor.html`) — dieselbe Reihenfolgezusage wie bei `source-autocomplete.js`. 💣 **ZWEI Ladewege, und der zweite ist beim Umzug fast durchgerutscht:** im Browser teilen sich beide Skripte den globalen Raum, unter **Node** `require`t man den Zeilenbauer allein und es gibt dort keinen. Zwei **fremde** Tests taten genau das und fielen mit `ReferenceError` um — gefunden hat das der Lauf über das **ganze** Testfeld, nicht die eigenen Tests. Der Weiterreicher heißt deshalb `featureSourcePagesShorten`; 🪤 **ein gleichnamiger ruft SICH SELBST auf** (zwei `function` desselben Namens im globalen Raum sind gültiges JS, die später geladene gewinnt). ⚠️ Er schlägt bei **jedem Aufruf** nach, nicht einmal beim Laden — sonst hält er eine Fassung fest, und ein Test, der die geteilte ersetzt, misst weiter die alte. 🔴 **Kein stiller Rückfall:** fehlt die Datei, wird laut geworfen; eine Ersatzfassung wäre genau die zweite Wahrheit, die der Umzug beseitigt. 🔴 **Gekürzt wird ab VIER Einzelseiten** auf „S. 16 ff.“, die volle Angabe steht im `title`, und `[title]` trägt in beiden Oberflächen gestrichelte Unterlinie plus Hilfezeiger — ohne dieses Zeichen liest sich „S. 16 ff.“ wie die vollständige Angabe. ⚠️ **DREI bleiben stehen** („S. 8, 15, 80“ liest man in einem Blick) und ein **BEREICH** wird nie gekürzt: „16-122“ ist schon kurz, und `ff.` machte aus einem bekannten Ende ein offenes. Am Livebestand gemessen: **54.571** Seitenangaben, **17,2 %** über drei Einträgen, die längste 31 Einträge / 120 Zeichen; an Bjaldorn (52 Quellen, 17 davon lang) wird die Publikationstabelle 1440 statt 1616 px hoch. Tests: `js/review/__tests__/quellenliste-klappen.test.js` und `…/quellen-kuerzung-eine-quelle.test.js` (letzterer prüft zur **Laufzeit** per Spion, dass alle drei Flächen die geteilte Fassung wirklich aufrufen, **und** beide Ladewege — „die Datei ist eingebunden“ ist erfüllt, auch wenn niemand sie ruft). 🔴 **Und seit dem 03.09.2026 abends gilt dasselbe für den NAMEN VORN** (Owner, mit Bild: „die editoren sagen, dass sie verwirrt sind, dass das was anderes steht“ — Editorzeile „Apfeldorn“, Infobox „AlberniaWiki“, dieselbe Quelle): `featureSourceVornName` in `feature-source-markup.js` entscheidet für Infobox UND Editorzeile, ob der Korpusname (Belegstelle) oder der Titel vorn steht; der Editor reicht per Weiterreicher durch (`featureSourceNameVorn`, laut werfend ohne die Datei) und legt den Seitentitel in den Tooltip des Links („Baronie Hirschfurten — Garetien-Wiki“, Owner: „du kannst den Titel in den Tooltip des links verlagern“), nur wenn er vorn nicht steht; die Spaltenüberschrift heißt „Quelle“. Keine CSS-Änderung, das Raster bleibt. ⚠️ Der Inline-Host lädt beide Dateien dynamisch — `ASSET_VERSION` gebumpt. Test: `js/review/__tests__/quellenzeile-name-vorn.test.js` (hält beide Erzeuger zur Laufzeit gegeneinander; 8 Mutationen gefangen). 🪤 **Und der Client-Zwischenspeicher muss den Korpusschlüssel MITTRAGEN** (`syncFeatureSourcesToClientCache`, seit 03.09.2026 spät): die Nutzlast trägt `corpus` als SCHLÜSSEL, die Editor-Liste als OBJEKT (`corpus.corpus_key`), und der Schreiber übersetzte nicht — nach jedem Öffnen des Quellen-Editors, kein Speichern nötig, zeigte die Infobox den Titel („Apfeldorn“) statt des Korpus („AlberniaWiki“), bis die Seite neu lud (Owner: „wenn ich die seite aktualisiere stimmts“). Dieselbe Klasse wie Lizenz und Namensnennung davor, deren Kommentar im Schreiber genau davor warnt; wer dem Katalogeintrag ein Feld gibt, gibt es dem Zwischenspeicher mit. Ein in der Sitzung angelegter Korpus wird dabei ins Wörterbuch `__sourceCorpora` eingetragen, ein bekannter überschreibt einen Platzhalter. Test: `js/review/__tests__/quellen-korpus-im-client-cache.test.js` (Sync, der Leser aus `popups.js` und der Zeilenbauer der Infobox ausgeführt).
- **Der Quellenkasten hat EINE Zeilenform, und die Wiki-Zeile ist keine Ausnahme mehr.** Live 03.09.2026 (Owner am Bild: „Wir wollen das an das format mit dem (i) anpassen“). Sie war die einzige Zeile mit eigener Bauform — ganze Pille, kein ⓘ, Lizenz als zweite Zeile darunter — und keiner der drei Unterschiede sagte etwas aus. Jetzt: `Wiki Aventurica ↗  ⓘ [INOFFIZIELL | Wiki-Artikel]`. 🔴 **Rechts steht die ART, nie noch einmal der Name** (Owner-Entscheid „nimm B, wiki-artikel“): „Wiki Aventurica | Wiki Aventurica“ wäre derselbe Name zweimal in einer Zeile. Links wer, rechts was — dieselbe Lesart wie bei jeder Katalogzeile. 🔴 **Offiziell ist IMMER eine runde Pille**, ohne Bezeichner (Owner 03.09.2026). Das ist keine Ausnahme, sondern die Regel der Halbpille selbst: sie unterstreicht, dass hier noch etwas OFFEN ist — wer geschrieben hat, was nicht im Kanon steht. „Offiziell“ trennt nichts auf. 💣 **Hinter dem ⓘ stehen Kanon · Verknüpfung · Lizenz · Adresse, und das ⓘ ist deshalb IMMER da**: es trägt seit diesem Tag den Lizenzhinweis der Wiki-Texte (CC BY-SA 3.0). Wer den Knopf je wieder an eine Bedingung hängt — so war es bis zum 02.09. —, nimmt die Lizenzangabe mit. Sie bleibt ein echter Link auf DERSELBEN Seite, einen Klick entfernt (dieselbe Begründung wie bei den Katalogzeilen, Entwurf §4.4). 🔴 **Der Kanon-Satz ist der GEMEINSAME**, auch am Wiki-Artikel („Offiziell — bei Ulisses erschienen.“). Eine eigene Fassung war vorgeschlagen — der ARTIKEL ist nicht bei Ulisses erschienen, er beschreibt, was dort erschienen ist — und der Owner hat am 03.09.2026 den gemeinsamen gewählt: die Pille bedeutet überall dasselbe, also erklärt sie auch überall dasselbe. Wer ihn später aufteilt, teilt die Aussage der Pille mit. ⚠️ **Ohne bekannten Namensraum gibt es KEINE Pille und KEINE Kanon-Zeile** — der Kanon der Wiki-Zeile kommt aus dem Namensraum ihres Artikels, und den kennt nicht jeder Aufrufer (`wikiOfficial` darf fehlen). 🔴 **Zwischen „Quelle(n)“ und „Publikationen“ liegt ein Trenner** (`.fs-src-trenner`, Owner 03.09.2026 mit Bild) — vollbreit, und **die −8px sind das Seitenpolster von `.fs-src--box` (6px 8px 6px, Owner 03.09.2026; vorher 9px 11px 10px und −11px — dazu seither `margin-top: 10px` als Luft zur Tabelle darüber)**, keine freie Zahl; wer eines ändert und das andere vergisst, bekommt eine Linie, die sichtbar vor dem Rand endet. Sie ist als **VERTRAG** im Mockup hinterlegt, und der Test liest das Polster gegen. ⚠️ Er kommt **nur, wenn oben wirklich etwas steht**: ein Objekt ohne Wiki-Artikel und ohne eigene Quelle bekäme sonst eine Linie über der ersten Zeile — ein Trenner, der nichts trennt, sagt „hier fehlt etwas“. ⭐ **Der ganze Umbau kostete GENAU EINE neue CSS-Regel** (eben diese): Halbpille (`.fs-kanon--split`) und ⓘ (`.fs-src-info`) standen seit dem 01./02.09. da, die Wiki-Zeile benutzte sie nur nicht. 🪟 **Messfalle beim Prüfen, zweimal am selben Tag:** die Quellenzeile endet an der TAFEL, nicht am `</li>` — die Tafel steht INNERHALB des `<li>`, ein Ausschnitt bis dorthin misst das Gegenteil dessen, was gefragt ist. Bauteile: `js/ui/feature-source-markup.js` (ein Bauer, alle acht Oberflächen), Verdrahtung in `js/ui/popups.js`, Mockup `docs/quellen-wikizeile-mockup.html`. Tests: `js/ui/__tests__/{feature-source-markup,kanon-halbpille,quellenzeile-info}.test.js` (zusammen gegen 13 Mutationen gefahren, alle gefangen).
- **Herrschaftsgebiete hängen am EINEN Quellen-Bauteil — und Quellen stehen überall UNTEN.** Live 03.09.2026 (Schritt 2 des Quellen-Umbaus; Entwurf `docs/superpowers/specs/2026-09-03-quellen-herrschaftsgebiete-design.md`, Mockup `docs/quellen-herrschaftsgebiete-mockup.html`). Der Territoriumseditor (Sektion „Quellen", reines Anschluss-Modul `js/territory/territory-quellen-anschluss.js`, gerufen aus `renderInfoBox` — dem Trichter jedes Knotenwechsels) und der Kartendialog „Herrschaftsgebiet bearbeiten" montieren `mountFeatureSourceEditor` auf `territory` + `political_territory.public_id` — auch für eigene Knoten (`eigener-knoten:…` ist eine Zeile wie jede andere, nur ohne Artikel); ein abgeleiteter Gruppenknoten ohne `row` zeigt die Sektion nicht. Die Leseseite gab es längst: 879 Gebiete mit 6.633 Verweisen in der Nutzlast, die Infobox zeichnete sie seit Monaten — eintragen konnte man nichts. 🔴 **Owner-Regel (03.09.2026): „generell können quellen immer unten/als letztes in den listen auftauchen"** — im Editor die letzte Sektion der Wiki-Daten-Spalte (nach den Konfliktparteien; Spaltenweiche in `territory-editor-panel-columns.js`, stabile Sortierung nach Dokumentreihenfolge), im Dialog nach dem letzten Feld, „nicht zwischen die felder reinpfrimeln". Wer eine weitere Montagestelle baut, setzt den Kasten ans Ende. 💣 **Das gescopte Bauprodukt `css/pages/political-territory-editor-inline.css` stellt JEDE Editorregel unter `#political-territory-editor-host`:** `button`, `select`, `a`, `p` kommen damit auf (1,0,1), die Bedienhöhen-Regeln (`button:not(…):not(…)`, `select, input[type=…]`) auf **(1,2,1)** — und schlagen jede Klasse eines eingebauten geteilten Bauteils (0,1,0). Im Browser gemessen: ✕ und ✎ des Quellenkastens wurden 32-px-Vollknöpfe. Deshalb ist `css/features/feature-sources.css` seither die **vierte Quelle** von `tools/scope_editor_css.js` (Modulregeln mechanisch unter der Host-ID, als letzte), und die drei Bedienhöhen-Regeln nehmen `.fs-editor *` aus — als VERTRAG im Mockup gebunden. ⚠️ Kein `:where()` um den Bedienhöhen-Block: das ließe die eigenen Klassenregeln des Editors (`.breadcrumb button`) gegen die Bedienhöhe gewinnen — eine sichtbare Änderung an Stellen, um die es nicht ging. ⭐ Wer ein weiteres geteiltes Bauteil in den Territoriumseditor montiert, prüft ZUERST, ob dessen Blatt im Bauprodukt steht. 💣 „Andere Quelle" ist dort samt `display_style.otherSource` ohne Migration gefallen (Owner): das Feld war seit dem 07.07.2026 für keinen Knoten erreichbar — Sektion `hidden`, Feld bei jedem `wiki_key` ausgeblendet, und den hat seit dem 05.06.2026 jeder Knoten. 🪤 Der erste Grep zählte in `assignment.php` **vier** Stellen, es waren **fünf** — ein `head -20` hatte die Liste gekappt; den Schreibpfad fand der Test, nicht der Autor. ⚠️ Der Kartendialog ist für Gebiete nur der RÜCKFALL (Kontextmenü → Inline-Host zuerst); angeschlossen, weil bestellt und weil es einen Mount-Aufruf kostet. 🪤 **Und die Spalte ist SCHMAL: 356px Inhalt, 324 im Messkasten des Bauteils** — die schmale Rastervariante der Quellenzeile (ab 670px) braucht 366px feste Spalten, das ✕ stand 42px außerhalb (Owner-Bild 03.09.2026). Seither eine **dritte Container-Stufe unter 420px** (Flex-Umbruch: Titel oben, Marken teilen sich die zweite Zeile, Typ-Pille wird vor der Seitenangabe gekürzt, ✎/✕ fest 20px rechts), und die Symbolknöpfe setzen `padding: 0` selbst, weil eine Eigenschaft, die die Modulregel nicht nennt, auch mit Host-ID nicht gegen `button { padding }` gewinnt. Test: `js/review/__tests__/quellenzeile-schmaler-kasten.test.js`. 🔴 **„Neue Quelle einfügen" ist eine FALTE** (Owner 03.09.2026: „alle boxen unter einem klapptext verschwinden … immer mit klappe zu"): natives `<details class="fs-add-fold">` um Hinweistext und Eingabeformular, dieselbe Rezeptur wie „n weitere Quellen" (`.fs-more` — eine Regel, zwei Namen; der zweite, weil `quellenliste-klappen.test.js` `.fs-more` als DEN Klappkasten der Liste zählt), **immer zu** — beim Öffnen des Kastens wie nach jedem Eintrag, und ohne Modulzustand: der Bauer setzt nie `open`, und das Bauteil zeichnet nach jedem Schreibvorgang aus der Serverantwort neu. 💣 **Die Meldezeile (`[data-fs-note]`) steht deshalb AUSSERHALB der Falte** — stünde „Hinzugefügt: „X"." darin, verschwände sie genau im Moment des Eintrags (die Meldung vom 03.09.2026 früh). Sie liegt im geteilten Bauer und gilt damit an ALLEN Montagestellen — Territoriumseditor, Ortseditor, Sync-Monitor, Kartendialoge — ohne eine Zeile beim Wirt. Test: `js/review/__tests__/quellen-neue-quelle-falte.test.js`. 🔴 **Und der Sync-Monitor („Wiki-Daten und Eigene Overrides", `html/wiki-sync-monitor.html`) ist die ZEHNTE Montagestelle** (Owner 03.09.2026: „bei Wiki-Daten und Eigene Override fehlts noch"): Gruppe „Quellen" in der Bauform des Ortseditors (`.dt-grp`), als LETZTES Stück des Panels nach dem Bindungs-Kasten, montiert in `renderDetail` auf `n.public_id` (= `political_territory.public_id` aus dem `model_tree`); ein Knoten ohne Zeile bekommt keinen Kasten, der Bearbeiten-Modus des Panels kehrt vorher zurück. 💣 `renderDetail` läuft bei JEDEM Klick im Baum und ersetzt das Panel per `innerHTML` — der vorige Host wird davor per `__fsDetachAutocomplete` gelöst, sonst überlebt die Vorschlagsliste am Dokument als Waise; und der Getter liest den Knoten bei jeder Anfrage aus `selectedKey`/`BYKEY`. Die Seite lädt Bauteil und Blatt selbst, wie die vier anderen Editorseiten. Test: `js/review/__tests__/quellen-im-sync-monitor.test.js`. Tests: `js/territory/__tests__/{territory-quellen-anschluss,quellen-im-territoriumseditor}.test.js`, `js/review/__tests__/quellen-im-herrschaftsgebiet-dialog.test.js` (führt `populateRegionEditForm` gegen eine Dokument-Attrappe aus), `api/_internal/political/__tests__/display-style-ohne-andere-quelle-test.php`.
- **Wege hängen am EINEN Quellen-Bauteil — und die Quelle hängt am ABSCHNITT, die Gruppe ist ein VERTEILER.** Live 03.09.2026 (Schritt 3 des Quellen-Umbaus; Entwurf `docs/superpowers/specs/2026-09-03-quellen-wege-design.md`, Mockup `docs/quellen-wege-mockup.html`). Der Kartendialog „Weg bearbeiten" (Kasten „Quellen" ganz unten, `mountPathEditFeatureSources` in `review-paths.js`, aus BEIDEN Befüllern) und der Wege-Editor (Abschnitt `wpFeatureSources`, Weg-Ebene `wpGroupFeatureSources`, beide als letzter Block vor der Speicherleiste) montieren `mountFeatureSourceEditor` auf `path` + `map_features.public_id` des Abschnitts. „Andere Quelle" ist an beiden Stellen gefallen; `update_path_details` und die Weg-Ebene (`AVESMAPS_PATH_GROUP_FIELDS`) fassen `other_source` nicht mehr an — die 314 gespeicherten Altquellen bleiben unberührt und werden über den `os:`-Erzeuger in `api/app/map-features.php` weiter angezeigt, bis Schritt 4 sie in den Katalog holt. 🔴 **Die Quelle hängt am Abschnitt, nie am Gruppenschlüssel** (`wiki:<key>`, sonst `name:<Wegart>:<Name>` — EINE Rechnung: `wpGroupKeyOf` im Modell, `avesmapsWegGruppenSchluessel` auf der Karte): der Schlüssel wandert beim Umbenennen mit, eine Ablage darunter würde verwaisen; die Gruppe ist nur der Verteiler. 🔴 **`opts.gruppe = { publicIds: () => string[], fest }`**, bei JEDER Anfrage gelesen. Am Abschnitt (nicht fest) heißt der dritte Rahmen der Eingabezeile „An diesem Weg" und trägt die Wahl „alle N Abschnitte dieses Weges" (Vorgabe) / „nur dieser Abschnitt"; verteilt wird NUR `add`/`add_existing` — ✕ und ✎ gelten dem Abschnitt, denn die Wahl steht in einer zugeklappten Falte, und ein Löschen an einer unsichtbaren Auswahl ist eine Falle. Auf der Weg-Ebene (fest) heißt er „An allen N Abschnitten dieses Weges", ohne Wahl, und JEDE Anfrage trägt die Liste — **auch das ✎-Formular sagt es dort** (Befund des Prüfagenten: es versprach „Nur an diesem Objekt" und hätte wortlos 56 Seitenangaben überschrieben; `renderFeatureSourceEditPanel` bekommt dieselbe `wegGruppe` wie die Eingabezeile). Ein einteiliger Weg sieht nichts davon. 💣 **Der Verteiler ist der SERVER, nicht der Client:** `entity_public_ids` im Rumpf (nur `path`, Deckel 250 wie das Sammel-Speichern, Anker vorn, Dubletten raus — `avesmapsFeatureSourceDistributionIds`, ein Fehler statt eines stillen Weglassens bei fremder Objektart) lässt list/add/add_existing/update/remove je Kennung laufen; die Antwort ist `avesmapsListFeatureSourcesForEditMany`: je Katalogzeile EINE Zeile, `segments`/`segments_of`, Seiten und Abdeckung von der Zeile des ANKERS, `by_entity` je Kennung, `revision` null (ein Sperrtoken gehört EINEM Objekt). 🔴 **Der Kartenspeicher wird je Kennung nachgezogen** (`syncFeatureSourcesToClientCache(…, by_entity)`), nie die Vereinigung an alle — sonst stünde eine Quelle an 12 von 56 Abschnitten nach dem nächsten Klick an allen 56. 🔴 **Die Marke „12 von 56 Abschnitten" nur bei einer TEILMENGE** — an allen ist der Normalfall (2.347 von 2.511 Wegquellen, live gemessen) — und sie steht IM Titel-Feld hinter dem Link, keine achte Rasterspalte, die alle übrigen Listen mittragen müssten. 🪤 **Und dort war sie zuerst UNSICHTBAR:** der Titel-Link ellipsiert am Text (`nowrap`/`hidden`), und die Marke hinter dem Text lag bei jedem längeren Titel hinter den drei Punkten (im Browser gemessen: Marke bei 305px, Zelle endet bei 211px) — der Mockup zeigte sie nur, weil sein Dialog unter der Rasterstufe liegt. Ein Flex-Kasten mit ellipsiertem Titel-Span liess vom Namen 51px übrig („Geogr…“). Jetzt darf der Link MIT Marke umbrechen (`.fs-row__link.fs-row__link--marke`, `white-space: normal`) — 💣 **doppelte Klasse, nicht Reihenfolge:** `.fs-row__link` setzt `nowrap` an DREI Stellen des Blatts (Grundregel, Rasterstufe, Container-Stufen), und mit (0,1,0) gewann die spätere wieder; gemessen, nicht geraten. 💣 Der Zeilenbauer der Editorliste ist herausgelöst (`avesmapsFeatureSourceEditorRows`): Einzel- und Sammelliste bauen mit DEMSELBEN, der Test hält beide zeichengleich. ⚠️ Jeder Aufbau bekommt einen eigenen Radionamen — Abschnitt und Weg-Ebene, zwei Kartendialoge dürfen sich die Wahl nicht teilen. 💣 **Im Wege-Editor kommt die Gruppe des Abschnitts aus ALLEN Wegen (`state.ways`)**, nicht aus der gefilterten Liste — ein Filter „Quelle: keine" darf „alle N Abschnitte" nicht verkleinern; die Weg-Ebene nimmt die ANGEZEIGTE Gruppe (`findGroup`), dieselbe, für die „Speichern für N Abschnitte" gilt. ⚠️ Der Filter „Quelle" des Wege-Editors zählt seither Katalogquellen (`source_count` aus `paths-editor.php`, eine gruppierte Abfrage). ⚠️ Das Bauteil wird bei jedem Neuzeichnen des Detailbereichs neu montiert und holt seine Liste erneut — eine Anfrage je Häkchen auf der Weg-Ebene; gewollt einfach, nicht gemessen teuer. 🪤 **Beim Bau, zweimal:** Arrays aus einem `vm`-Kontext tragen einen FREMDEN Array-Prototyp — `deepStrictEqual` gegen ein Host-Array fällt bei zeichengleichem Inhalt („actual [a,b,c], expected [a,b,c]"); `[...x]` vor dem Vergleich. 🪤 `avesmapsFeatureSourceLink` ist ein MySQL-Upsert (`ON DUPLICATE KEY UPDATE`) und läuft nicht auf SQLite — die Bibliothekstests setzen Verknüpfungen per SQL und prüfen den Endpunkt-Aufruf am Quelltext (Tokenizer, kommentarfrei). ✅ **Der Ablauf lief am 03.09.2026 in der Sitzung des Owners gegen die Live-Datenbank** (Schattenbachpass, 7 Abschnitte): Abschnitt → „alle 7“ eintragen → Nachbarabschnitt zeigt sie → dort ✕ nimmt sie nur dort → Weg-Ebene zeigt „6 von 7 Abschnitten“ → dort ✕ nimmt sie überall; Kartendialog zeigt den Kasten unten mit „alle 7 Abschnitte dieses Weges“. Zähler der Katalogzeile danach wieder 1319. 🪤 **Und dabei ist ein ALTER Fehler aufgeflogen, der nicht zu diesem Schritt gehört:** `add` einer BEKANNTEN Adresse schreibt `is_official` aus dem Häkchen des Formulars in die Katalogzeile — auch bei einer wiki-gepflegten Quelle, bei der das ✎ genau das mit `wiki_owned_field` verweigert. Live ausgelöst an „Geographia Aventurica“ (1.319 Objekte, von „ja“ auf „nein“), über denselben Weg sofort zurückgesetzt. Dieselbe Klasse wie #105 bei der Art (`source_type_chosen`); das Häkchen braucht dieselbe Regel: nur eine AUSDRÜCKLICHE Wahl schreibt in den Katalog. ✅ Der Riegel steht seit dem 03.09.2026 abends (`avesmapsSourceOfficialWriteAllowed`, `is_official_chosen` — siehe den Eintrag zur Art einer Quelle). ✅ Schritt 4 (Altquellen) ist am 03.09.2026 abends gelaufen — eigener Eintrag darunter. 🔧 **Offen:** Schritt 5 (Landschaften: `ecosystem` + `region_public_id`); und der Wege-Editor schickt je Klick auf einen Abschnitt ZWEI `list`-Anfragen (das Detail wird zweimal gezeichnet) — billig, aber unnötig. Tests: `js/review/__tests__/{quellen-verteiler,quellen-im-wegedialog}.test.js`, `js/pages/__tests__/quellen-im-wege-editor.test.js`, `api/_internal/app/__tests__/quellen-verteiler-test.php` (zusammen gegen 17 Mutationen gefahren).
- **Die Altquellen sind im Katalog — `properties.other_source` („Andere Quelle") hat keinen Schreiber und keinen Erzeuger mehr.** Live 03.09.2026 abends (Schritt 4 des Quellen-Umbaus). Gemessen davor: **314** Altquellen (168 Orte, 30 Beschriftungen, 116 Wege), 102 verschiedene Adressen, 51 davon schon im Katalog; sie hingen am `os:`-Erzeuger der Kartennutzlast (`avesmapsMapFeaturesMergeLegacyOtherSources`) und wanderten nur einzeln, wenn ein Editor die Quellenliste eines Objekts öffnete. 🔴 **Der Sammel-Takeover** (`avesmapsFeatureSourcesTakeoverAll`, Endpunkt-Aktion `takeover_other_sources`, NUR Admin, ohne `entity_public_id`) hat den **Trockenlauf als Vorgabe** — er zählt je Objektart, Adressen und bekannte Adressen und zeigt eine Stichprobe, schreibt aber in keine Tabelle; scharf nur mit `apply: true`, gedeckelt (Vorgabe 400), je Zeile eine eigene Transaktion, Fehler gemeldet statt geschluckt. Gefahren in der Sitzung des Owners: Trockenlauf 314 = Messung, dann drei scharfe Läufe à 120 (0 Fehler), danach `remaining 0`, Nutzlast ohne `os:`-Zeilen, Katalog 1.438 Zeilen. 💣 **Der Takeover ruft je Zeile DEN Einzel-Takeover** (`avesmapsFeatureSourcesTakeoverOtherSource`) — keine zweite Fassung der Regeln; und der Einzel-Takeover fragt seither den „offiziell"-Riegel mit „nicht gewählt": 51 der 102 Adressen standen schon im Katalog, und der Upsert hätte jeder davon „offiziell = nein" aufgedrückt (dieselbe Falle wie am Vormittag an Geographia Aventurica). 🔴 **Danach fielen die letzten Schreiber und der Erzeuger:** der Beschriftungsdialog trug als einziger noch „Andere Quelle" (der Kasten „Quellen" — das EINE Bauteil auf `region` + `map_features.public_id` — stand dort längst darunter), `review-other-source.js` ist aus Repo und `index.html`, die drei Server-Schreiber (Ort, Beschriftung, Region) samt `avesmapsReadOptionalOtherSource` sind weg, ebenso die `other_source`-Zeile der Feature-Antwort und der `os:`-Erzeuger in `api/app/map-features.php`. ⚠️ Der Einzel-Takeover und die Sammel-Aktion BLEIBEN als Sicherheitsnetz — schriebe je wieder etwas das Feld, würde es beim nächsten Öffnen der Quellenliste eingesammelt. 🪤 **Zwei Tests hielten den Erzeuger fest** (`kanon-etikett-test.php` über die Reihenfolge, `korpus-in-der-karte-test.php` führte ihn per `eval` aus) — beide nachgezogen; wer den Erzeuger je zurückbaut, findet dort die Begründung, warum nicht. 🔧 **Offen:** die LESER im Browser (`readFeatureOtherSource` in `js/app/utils.js`, `map-features-labels.js`, `map-features-region-feature-normalization.js`, `map-features-location-editing.js`) sind tot — sie lesen ein Feld, das es nicht mehr gibt — und warten auf den Ausbau; und die Sammel-Aktion kann fallen, sobald niemand sie mehr braucht. Tests: `quellen-altquellen-takeover-test.php` (21), `altquellen-erzeuger-weg-test.php` (13).
- **Die Landschaft trägt die Quellen, die Beschriftung zeigt sie — `ecosystem` + `region_public_id`, freie Beschriftungen bleiben `region`, nie beides.** Live 03.09.2026 abends (Schritt 5 des Quellen-Umbaus; Entwurf `docs/superpowers/specs/2026-09-03-quellen-landschaften-design.md`, Mockup `docs/quellen-landschaften-mockup.html`; Owner: beide Fragen ja). Gemessen davor: 1.011 Beschriftungen (782 gebunden, 229 frei), **487** gebundene mit Quellen → **477** Flächen (6.976 → 6.811 Verweise, 165 Dubletten), 189 freie mit Quellen bleiben, 646 Flächen ohne Beschriftung konnten GAR KEINE Quelle tragen. 🔴 **EINE Weiche je Seite:** `avesmapsLabelQuellenSchluessel(label)` (`js/map-features/label-quellen-schluessel.js`, rein; Datenbox, Kanon-Etikett und Beschriftungsdialog fragen sie) und `avesmapsEcosystemLabelSourceTarget` (`ecosystem-label-link.php`). Der Beschriftungsdialog einer GEBUNDENEN Beschriftung hat keinen eigenen Kasten mehr — eine Zeile zeigt zur Fläche, deren Kasten (Flächendialog, `mountEcosystemAreaSources`; Landschaften-Editor) auf `ecosystem` + `public_id` montiert; ein zweiter Kasten mit derselben Liste läse sich wie zwei Ablagen. 🔴 **Wird eine Beschriftung gebunden, wandern ihre Quellen zur Fläche** (`avesmapsFeatureSourcesMoveLabelToRegion`: Dubletten per DELETE über eine Ableitungstabelle, dann glattes UPDATE — kein Upsert, MySQL/SQLite-Regel; alle Zustände, auch Grabsteine), gerufen von BEIDEN Zeiger-Schreibern (`update_label` bei geänderter Bindung innerhalb der Transaktion, `avesmapsEcosystemAdoptLabelPointer`); beim Lösen bleiben sie dort (Owner). 💣 **Der Wiki-Publikationsabgleich zielt für `region`-Artikel auf die Fläche, wenn die Beschriftung gebunden ist** — sonst holte der nächste „Syncen" alle 6.811 Verweise an die Schilder zurück. Die Staging-Seite (`wiki_entity_publication`, `region`) bleibt; nur das ZIEL wechselt: `avesmapsPublicationMapLabelTargets` (rein; mehrere Beschriftungen einer Fläche mit demselben Artikel fallen auf EIN Ziel zusammen, die Zeile behält die größte `id` für den Cursor), und Reconcile, Plan-Vorschau und Plan-Übernahme führen den Ziel-Typ GETRENNT vom Staging-Typ (`$targetType`) — 🪤 mit dem Ziel-Typ am Staging-Zugriff liefe „gewünscht" leer und der Abgleich löschte alles. 🔴 `AVESMAPS_MAP_FEATURES_SOURCE_ENTITY_TYPES` trägt `ecosystem`; ohne das reisten die Verweise nie zum Browser (die Falle der leeren Flächenkästen, andersherum). Der Umzug selbst ist eine Übernahme-Vorschau: `takeover_label_sources` (Admin, Trockenlauf-Vorgabe, gedeckelt, EIN Revisions-Bump am Ende; die Bindung kommt aus `avesmapsEcosystemReadLabelRegionMap`, demselben Leser beider Richtungen, der die Nutzlast füllt). Gefahren in der Owner-Sitzung: Trockenlauf 489 → 479 (6.978 → 6.813, 165 Dubletten), scharf in drei Läufen. 💣 **UND DABEI EINE FALLE, DIE SQLITE NICHT KENNT: DDL IN EINER TRANSAKTION COMMITTET IN MYSQL IMPLIZIT.** Der Umzug rief `avesmapsEnsureFeatureSourceTables` (ein `CREATE TABLE IF NOT EXISTS`) INNERHALB der Transaktion je Beschriftung — MySQL beendet die Transaktion bei jedem DDL, auch auf eine bestehende Tabelle; DELETE und UPDATE liefen danach im Autocommit, `commit()` warf „There is no active transaction", und der Lauf meldete 489 Fehlschläge, obwohl ALLES umgezogen war — nur die Revision blieb ungebumpt und die Nutzlast alt. ⭐ Die Regel: die `Ensure…`-Helfer des Hauses laufen VOR einer Transaktion, nie darin; `quellen-zur-flaeche-test.php` hält es am Quelltext fest. Der Fehlerbericht war hier die Lüge in die sichere Richtung — wer ihn geglaubt hätte, hätte den Lauf wiederholt und nichts kaputtgemacht. 🪤 **Zwei fremde Tests pinnten die alte Ablage** (`editor-quellen-eine-quelle` den Mount des Landschaften-Editors auf das Schild, `kanon-halbpille` das feste `"region"` am Kanon-Etikett) — nachgezogen; und ein Klammer-Zähler, der am ersten `{` nach dem Funktionskopf beginnt, endet bei `options = {}` nach zwei Zeichen. 🪤 **Und die Regression, die drei gruene Signale ueberlebt hat (03.09.2026, 13:38–15:44):** `quellenSchluessel` stand nur in der Datenbox-Funktion, das Ansichts-Popup (`buildRegionLabelViewPopupHtml`) las den Namen — ReferenceError beim Vorbauen der Popups, `prepareLabelData` brach ab, Besucher sahen zwei Stunden lang KEINE Beschriftungen. Der Quelltext-Test fand die Zeile per Regex (gruen), 14 Mutationen bissen an Zeichenketten (gruen), die Owner-Abnahme lief im Bearbeiten-Modus mit EIGENEM Popup-Bauer (gruen). Gefunden hat es `read_console_messages` auf der Live-Seite bei einer ganz anderen Pruefung. Hotfix `34aeeba23`: EIN Helfer `regionLabelQuellenSchluessel` fuer beide Bauer, und der Test FUEHRT das Ansichts-Popup aus (Attrappen ohne Proxy — ein Proxy, der jeden Bezeichner beantwortet, verschluckt genau diesen Fehler). ⭐ Zwei Regeln daraus: ein Regex kennt keinen Geltungsbereich, also wird ein Bauer AUSGEFUEHRT, nicht gelesen; und nach jedem Push, der die Karte beruehrt, wird die Live-Seite als BESUCHER geladen (kein `edit=1`) und die Konsole gelesen. Tests: `label-quellen-schluessel.test.js`, `quellen-zur-flaeche-test.php`, `quellen-label-takeover-test.php`, `quellen-landschaft-nutzlast-test.php`, `publication-ziel-landschaft-test.php` (14 Mutationen gefangen).
- **Das Meldeformular der Community hängt am EINEN Quellen-System — der Melder pastet einen Link, die Redaktion sieht, was der Server daraus macht.** Live 03.09.2026 (Paket 1 `2249589be` — die Besucherseite; Paket 2 — das Backend; Entwurf `docs/superpowers/specs/2026-09-03-quellen-meldeformular-design.md`, Mockup `docs/quellen-meldeformular-mockup.html` mit drei Vertragsblöcken). Owner: „sowohl änderungen als auch neue vorschläge müssen das mit dem link machen. ich will allerdings nicht, dass externe nutzer was am korpus machen - die sollen einfach den link pasten. erst wir im backend sollen sehen, ob der korpus passt oder ein neuer erkannt wurde." 🔴 **Der Melder sieht KEINEN Korpus, KEINE Art und KEIN „offiziell"** — nur den Link (Pflicht beim neuen Ort und beim Kartenvorschlag, optional beim Änderungswunsch), die Seite, und unter einer Falte Titel · Art der Erwähnung · Lizenz · Namensnennung (`#report-source-ref`, `#report-source-mehr`; die Regel `avesmapsMeldungQuelleAusEingabe` in `js/review/meldung-quellen.js` ist rein und gilt Ortsmeldung wie Kartenvorschlag). Die Vorschlagsliste läuft für Besucher ohne Marken (`ohneMarken`) — Art und Kanon sind Redaktionswissen. 💣 „Kennen wir schon" wird dem Melder NICHT gesagt (Owner: „verwirrt nur"). 🔴 **Der Server nimmt vom Melder nie Art oder „offiziell"** (`avesmapsNormalizeReportSources`, `api/_internal/app/report-sources.php`); auch die Karten-Annahme in `api/edit/reports/locations.php` reicht beides nicht mehr durch — sie kamen bis dahin aus einem Auswahlfeld, das der Melder nie angefasst hatte (dieselbe Klasse wie #105 bei der Art). 🔴 **Die Vorbelegung ist die Auskunft von `inspect_url` OHNE Abruf der fremden Seite** (`avesmapsReportSourceVorbelegung`): bekannt (Katalogzeile zur Adresse) · katalog (Treffer aus der Vorschlagsliste) · neu (Korpus bekannt — oder unbekannter Wirt, dann „ein neuer Korpus, wenn du ihn anlegst") · ohne_link (Altform). Kein Katalog-Vollscan je Karte: die Reichweite je Korpus kommt aus EINER Zählung je Liste (`avesmapsReportSourcesMitVorbelegung`, `$vorrat`). 🔴 **Die Review-Karte zeigt das GANZE Formular, schreibgeschützt** (Owner: „da wollen wir natürlich alle felder und das ganz normale formular sehen"): Falte `details.review-report__quellen` („n Quellen aus der Meldung"), die erst beim Öffnen `mountFeatureSourceMeldungVorschau` montiert — dasselbe Bauteil mit leerem Store, alle Felder `disabled`, keine Vorschlagsliste. Die ZAHL steht nur an der Falte und „gemeldet von" wandert in die Meta-Zeile — dieselbe Zahl zweimal auf einer Karte las sich wie zwei Angaben (Mockup §2). Die Falte zeigt AUCH die Altform ohne Link (die Zeile darüber sagt „nicht verknüpfbar", Titel und Seite sind trotzdem Information; ohne Falte sähe „1 Quelle" wie ein kaputtes Bauteil aus) — gefiltert wird erst die Warteschlange des Annahme-Dialogs, wo ein Speichern ins Leere liefe. ⚠️ Schreibgeschützt heißt `disabled`, nie weggelassen — ein fehlendes Feld läse sich als „hat der Melder nicht angegeben". 💣 Der 45-s-Poll ersetzt die Liste; offene Falten bleiben offen (`offeneQuellenFalten`), sonst klappt die Karte dem Editor beim Lesen zu. 🔴 **Im Annahme-Dialog ist die Meldung eine WARTESCHLANGE durch die normale Eingabezeile** (`opts.meldung`, `activeReviewReportSourceQueue`): über dem Formular „Aus der Meldung: Quelle 1 von n · …", vorbelegt nach der Rangfolge **Katalog > Korpus > Melder** (`fuelleAusMeldung`; Angebote des Melders füllen nur LEERES und tragen den Marker „vom Melder"; widerspricht ein Angebot einem gefüllten Wert, steht es DANEBEN — „Melder: CC BY-SA 4.0" —, damit der Editor den Korpus korrigieren kann, wenn der Melder recht hat; still verworfen läse sich das Formular, als hätte der Melder nichts gesagt — Befund des Prüfagenten), „Abbrechen" heißt „Überspringen", Speichern ist der normale Weg und rückt weiter (`meldungWeiter`); fehlt der Titel, holt ihn Speichern einmal über `inspect_url`. 💣 **Damit hat das „immer zu" der Falte „Neue Quelle einfügen" GENAU EINE Ausnahme** — `meldung.offen`, aus der Warteschlange abgeleitet, kein zweiter Merker; `quellen-neue-quelle-falte.test.js` zählt die Stelle und hält sie an `offen` gebunden. 🔴 **Korpuswerte füllen nur eine NEUE Katalogzeile** (`avesmapsFeatureSourceKorpusVorgaben`, gerufen in `avesmapsAddFeatureSource` nur bei `$bestehendeZeile === null`): Art, Lizenz, Nennung und „offiziell" kommen vom Korpus, wo nichts gesagt wurde; eine ausdrückliche Wahl (`source_type_chosen`, `is_official_chosen`) schlägt den Korpus, eine bestehende Zeile bleibt unberührt — der Riegel von Geographia Aventurica, eine Stufe früher. 🪤 **Die Warteschlange reist MIT dem Öffnen-Aufruf** (`meldungQuellen` → `populateLocationEditForm`, gesetzt VOR dem Mount), nie über den Modulzustand danach: die erste Fassung setzte `activeReviewReportSourceQueue` in beiden Öffnern NACH `openLocationEditDialog()` — der Kasten war da schon montiert und hatte `opts.meldung` synchron gelesen; die gemeldete Quelle kam nie an, der Annahme-Dialog zeigte den Kasten leer und die Falte zu (Owner am selben Abend: „irgendwie sieht das anders aus“). Vorher hatte dieselbe Reihenfolge nur funktioniert, weil die alte Vorschlagsgruppe im `.then` NACH der Serverliste gelesen wurde — eine Zusage aus dem Timing —, und der Regex-Test „beide Öffner füllen die Warteschlange“ war grün, während der Kasten leer blieb. Seither führt `meldung-warteschlange-erreicht-den-mount.test.js` beide Öffner gegen die echten Dialogfunktionen aus (4 Mutationen gefangen). 💣 **Gefallen sind die stille Vorschlagsgruppe unter der Quellenliste und das stille Verknüpfen nach `create_point`** — beides lief am Editor vorbei, der es sehen sollte; der Anlege-Puffer (`createPendingFeatureSourceStore`) bleibt der Weg beim neuen Ort. 🪤 Beim Bau, beide erst im GANZEN Feld gefunden: ein Mockup-Kommentar, der das großgeschriebene Wort „Vertrag" mit Doppelpunkt nennt, IST für `tools/mockup-vertrag` eine Marke ohne Ende; und `feature-sources.css` ist Quelle des gescopten Bauprodukts — jede neue Regel dort heißt `node tools/scope_editor_css.js` plus `ASSET_VERSION`. Tests: `js/review/__tests__/{meldung-quellen,meldung-im-quellenkasten}.test.js`, `api/_internal/app/__tests__/{meldung-quellen-eingang,meldung-vorbelegung}-test.php`, `js/map-features/__tests__/citymaps-suggest-form.test.js` (zusammen gegen 26 Mutationen gefahren, alle gefangen).
- **Die Art einer Quelle — leer heißt „keine Aussage", und nur eine ausdrückliche Wahl korrigiert den Katalog.** Live 29.08.2026, Meldung #105 (Nottel: „Die Auswahl des Typs einer Quellenangabe wird auf ‚Regionalspielhilfe' gestellt, unabhängig von der Wahl des Benutzers") und die zweite Hälfte von #104. **Zwei Ursachen, beide still.** 💣 **Die Eingabezeile hatte keinen leeren Eintrag** — also stand die ERSTE Art der Liste vorausgewählt da, und die erste ist `regionalspielhilfe`. Wer die Auswahl nie anfasste, legte eine Behauptung an, die er nie getroffen hat; genau so kam „Briefspiel Rommilyser Mark" (Quelle 1322115, live gemessen) als Regionalspielhilfe in den Katalog. 💣 **Und der Upsert ließ `source_type` einer BEKANNTEN Adresse unberührt** — kein Fehler, keine Meldung, gültige id zurück: jeder Korrekturversuch war ein Klick ins Leere und von außen nicht davon zu unterscheiden, dass das Formular die Wahl ignoriert. 🔴 **Die Regel jetzt: `''` ist keine Aussage** (`avesmapsNormalizeSourceType`, Geschwister von `avesmapsNormalizeSourceLicense`; ein unbekannter Wert fällt auf `''`, **nicht** auf `sonstiges` — `sonstiges` wäre eine Aussage und dürfte damit überschreiben). Beim ANLEGEN wird `''` zu `sonstiges`, eine bestehende Zeile bleibt unberührt. 🔴 **Die Erlaubnis zum Umtypen hängt am AUFRUFER, nicht am Wert** (`avesmapsSourceRetypeAllowed`, Vorgabe **nein**, per Reflection festgenagelt): sie setzt genau EINER — die Eingabezeile des Quellen-Editors. Die angenommene Gemeinschaftsmeldung (`api/edit/reports/locations.php`), der Wiki-Abgleich und der Import verknüpfen nur. `sources` ist ein KATALOG; dürfte jeder Erzeuger mitschreiben, würde die Vorauswahl eines fremden Formulars gepflegte Angaben plattmachen — dieselbe Klasse Fehler, nur in die andere Richtung. 💣 **`source_type_chosen` ist ein EIGENER Schlüssel, kein Rückschluss aus dem Wert.** Ein alter, zwischengespeicherter Client schickt die vorausgewählte erste Art mit, ohne dass jemand sie gewählt hat; wer aus „Wert vorhanden" auf „gewählt" schlösse, ließe ausgerechnet den fremde Katalogzeilen umschreiben. 💣 **BEIDE Türen**: `add` (Adresse eingetippt) und `add_existing` (Treffer aus der Vorschlagsliste) — eine Regel, die einen von zwei Erzeugern bindet, ist keine Regel. 🔴 **Und seit dem 03.09.2026 gilt dieselbe Regel dem Haken „offiziell"** (`is_official_chosen` im Rumpf, `avesmapsSourceOfficialWriteAllowed` in der Bibliothek): eine bestehende Katalogzeile bekommt ihn nur bei ausdrücklicher Wahl, eine wiki-gepflegte nie — das ✎ verweigerte das längst mit `wiki_owned_field`, der Eintrage-Weg nicht. Aufgeflogen beim Abnahmelauf von Schritt 3 des Quellen-Umbaus: „Geographia Aventurica" (1.319 Objekte) kippte auf „nein", weil niemand den Haken angefasst hatte. 💣 Die Vorbelegung des Hakens aus dem Korpus (`haken.checked = true`, ohne Ereignis) ist KEINE Wahl — die Wahl entsteht am `change`-Ereignis und steht am Element (`data-fs-chosen`), nicht im Modulzustand, weil die Eingabezeile nach jedem Schreibvorgang neu gebaut wird. ⚠️ Der Upsert selbst behält seine Vorgabe „schreiben" (`setOfficial = true`): Wiki-Abgleich und Importe pflegen den Wert seit jeher; nur der Editor reicht die Antwort des Riegels herein. Tests: `quellen-offiziell-riegel-test.php`, `quellen-offiziell-riegel.test.js` (8 Mutationen gefangen). ⚠️ **Die Korrektur wird BENANNT** (`retyped` in der Antwort → Meldung in der Eingabezeile): eine stille Änderung an einer katalogweit zitierten Zeile wäre dieselbe Falle wie die stille Nicht-Änderung davor. 🔧 **Offen:** eine bestehende Quelle lässt sich weiterhin nur über das erneute Eintragen korrigieren — Art JETZT, Lizenz/Namensnennung weiterhin nur füllend (die Einbahnregel von 0c00f191e), und es gibt keinen Protokolleintrag dafür (anders als beim Zusammenlegen, `source_merge_log`). Tests: `js/review/__tests__/quellen-art-korrigieren.test.js`, `api/_internal/app/__tests__/quellen-art-korrigieren-test.php` (zusammen gegen 9 Mutationen gefahren, alle gefangen).
- **Literatur** — DSA-Werke ↔ ihre geordneten Orte (Siedlung/Territorium/Region/Weg), als Infopanel-Abschnitte plus spoilergesteuerte Questroute. Entwurf **`docs/superpowers/specs/2026-08-06-literatur-design.md`**; Phasen in `docs/abenteuer-feature-design.md`, `-instruction.md`, `-editor-ui-spec.md`, `-editor-p3-autocomplete.md`. P1–P4 live (Katalog, Aggregation, Editor, Wiki-Sync + Ribbon + Cover). 💣 **Drei** Rollen, nicht zwei: `start` („beginnt hier") und `covers` („beschreibt") sind spoilerfrei, **`play` IST der Spoiler** — jede Stelle, an der „alles, was nicht start ist, ist Spoiler" stand, war damit lautlos falsch. Normalisiert an EINER Stelle je Seite (`avesmapsGameLiteratureNormalizeRole`), Rückfall immer Richtung `play`. 🔴 **Umbenannt 2026-08-07 (Abenteuer → Literatur) — die TABELLEN aber nicht** (`adventure`, `adventure_place`, `wiki_adventure_*`), ebenso wenig irgendein String, der als WERT in einer Zeile steht (`app_setting`-Schlüssel, Aktion `sync_adventures`, Audit-Aktionen, JSON-Schlüssel). Genau diese Trennung macht die Umbenennung sicher: der Deploy löscht nie (§10), die verwaisten alten Endpunkte antworten weiter richtig, und ein `RENAME TABLE` bräche das. Die übrigen Fallen (Kopfzähler, `PRODUCT_TYPE_GROUPS`, Spotlight-Rollen, `.avesmaps-adv*`) im Entwurf §§4–8.
- **Die Übernahme-Vorschau** — jeder Wiki-Abgleich zeigt erst, was er tun würde; geschrieben wird nur, was angehäkelt ist. Drei Kategorien (**Neu · Geändert · Gelöscht**), Neu und Geändert vorangehäkelt, Gelöscht nie und mit zweiter Bestätigung. Entwurf **`docs/superpowers/specs/2026-08-06-sync-uebernahme-design.md`**, Baupläne `docs/superpowers/plans/2026-08-06-sync-uebernahme-sitzung-{1,2}.md`, Mockups `docs/sync-uebernahme-mockup.html` + `-fallliste-mockup.html`. Live seit 2026-08-06 für Stadtkarten, Literatur, Publikationsquellen und Vorkommen; Territorien (Sitzung 4) ziehen nach. Bauteil `js/review/sync-plan-sheet.js`, Endpunkt `api/edit/wiki/sync-plan.php`, Fundament `api/_internal/wiki/sync-plan.php`, Tabellen `sync_plan_run`/`sync_plan_item` + 🔴 `sync_decision` (die EINZIGE dauerhafte Entscheidung, nie automatisch geleert). 💣 Sieben Fallen, alle im Entwurf begründet: sie kommt **immer**, auch bei null Unterschieden · jeder Abgleich ist in Rechen- und Ausführ-Hälfte geschnitten, und die Rechen-Hälfte schreibt in KEINE Nutztabelle (`sync-plan-purity-test.php`) · die dritte Kategorie gehört dem Verschwinden einer ganzen EINHEIT, nie einzelner Kindzeilen · bei den Vorkommen ist die „Löschung" ein Grabstein (`status='retired'`, das Wort heißt „stilllegen") · „Löschung abgelehnt" ist NICHT `origin='manual'` · eine Übernahme schreibt EINE Protokollzeile je Lauf · der Löschriegel steht serverseitig in `apply`, nicht nur am ausgegrauten Knopf. ⚠️ Sitzung 3 (Orte, Wege, Regionen) bekam bewusst nur die **Formensprache**, keine `sync_plan_item`-Zeilen: von 16 Falltypen passen 2 auf ein Häkchen.
- **Die Wiki-Zuweisung — EIN Bauteil, das seine Felder aus einer Erklärung liest.** „Welcher Wiki-Artikel gehört zu diesem Objekt" war in **sechs** Fassungen über ~1.400 Zeilen verteilt; daraus wurde ein Bauteil (`js/ui/wiki-assign.js`) plus ein **Feldregister** (`js/ui/wiki-assign-registry.js`) und je Objektart ein reiner Datenweg (`js/ui/wiki-assign-{weg,ort,landschaft,territorium,literatur,karte}.js`). Entwurf **`docs/superpowers/specs/2026-08-15-wiki-zuweisung-vereinheitlichung-design.md`**, Bauplan `docs/superpowers/plans/2026-08-15-wiki-zuweisung-vereinheitlichung.md`, Mockup `docs/wiki-zuweisung-mockup.html`; live 16.08.2026 in **einem** Zug (Owner-Entscheid gegen die Einzeln-live-Regel aus §9, deshalb auf eigenem Zweig gebaut). **Acht Objektarten in elf Oberflächen:** kraftlinie · weg · ort · landschaft · landschaftslabel · territorium · literatur · karte. Eine neue Objektart kostet eine **Erklärung**, ein neues Feld **eine Zeile**. 💣 **DER VERTRAG, und er ist der wichtigste Satz hier: ein `laden`/`zuweisen`/`loesen`/`syncUebernehmen` darf im Fehlerfall NIEMALS mit `null`/`undefined`/Zahl/Liste AUFLÖSEN — es muss ABLEHNEN.** Sonst hält das Bauteil den leeren Zustand für „nichts zugewiesen", und das nächste Speichern löscht die echte Zuweisung. Drei solche Pfade wurden beim Bau gefunden und geschlossen: eine abgelehnte Zusage, eine Zusage die mit *nichts* auflöst (der schlimmere — dabei zeigt der Kasten ruhig „keine Zuweisung" statt einer Fehlermeldung), und ein zweites `neuLaden`, das nichts liefert. Der Riegel steht im Bauteil: `avesmapsWikiAssignRufen` macht aus einem synchronen Wurf eine Ablehnung, `bereit === true` heißt ausnahmslos „`lies()` ist ein gültiger Schreibwert". ⚠️ Ein `.catch(() => null)` in einem Datenweg hebelt das aus — der Hausstil `.catch(() => [])` steht in derselben Datei und liest sich harmlos. 🔴 **Zwei Hüllen, und das ist die Obergrenze** (`.dt-*` im Editorfenster, `.label-wiki-*` im Kartendialog) — dieselbe Regel wie bei den Listenzeilen. 🔴 **Das Bauteil kennt KEINE Objektart** (kein `if (subject === …)`); ein Datenweg kennt kein DOM, kein `fetch`, keinen Modulzustand. Wer eine Objektart anschließt, schreibt eine Erklärung und einen Datenweg — nichts im Bauteil. 🔴 **Vorangehakt ist nur das Füllen einer LÜCKE** (Owner 16.08.2026): ein bereits gefüllter Kartenwert startet ungehakt mit Grund. ⚠️ Damit ist für ein gepflegtes Objekt praktisch nichts vorangehakt und „Alle anhaken" der Normalweg — deshalb trägt der die Füllung, solange nichts angehakt ist. Das Mockup zeigt noch den alten Stand. 🔴 **Der dritte Zustand** („Kein Wiki-Artikel vorhanden", `properties.wiki_no_article`) ist gebaut für ort · weg · landschaft · landschaftslabel · karte und **begründet abwesend** bei territorium (ein eigener Knoten fällt schon durch den `INNER JOIN political_territory_wiki` aus der Konfliktliste) und literatur (`adventure` hat kein `properties_json`, nur `field_origins_json`). Die Begründungen stehen im Register — **kein TODO, kein Schweigen.** 💣 Er reist **nur mit, wenn er seit dem Laden verändert wurde**, sonst nimmt ein alter offener Dialog die Entscheidung des Konfliktzentrums zurück. 💣 **Beim Weg gilt er für den ganzen Namensverbund**, nicht das bearbeitete Segment (`avesmapsConflictRepairSpansNameGroup`) — zwei Knöpfe am selben Fall mit verschiedener Reichweite sind schlimmer als zwei Fehler. 💣 **Die Art-Regel der Landschaft ist eine Ordnung, keine Wahl:** erst das eigene Vokabular der Ebene (exakt), dann die Server-Synonyme (`avesmapsWikiRegionArtToSubtype`), Ergebnis muss ein `type_key` DIESER Ebene sein. Blind gespiegelt würde aus jeder Wiki-**Schlucht** ein Tal (die Servertabelle ist einen Tag älter als die Flächenart `schlucht`). 💣 **Bei den Karten heißt schon zweierlei „wiki"**: `citymap.wiki_key` ist ein **Bauschlüssel** (`index:stadt:quelle:variante`, keine Seitenidentität) und `citymap.map_url` zeigt auf die **Publikation** — der eigene Artikel heißt deshalb `article_url`/`article_key`/`article_title`. 🪤 **Der Entwurf ist an drei Stellen an der Wirklichkeit gemessen falsch** und wurde bewusst nicht rückwirkend geschönt: §9 verortet die Territoriums-Zuweisung falsch (sie sitzt in `review-region-wiki-picker.js`, nicht im Sync-Monitor), §8 nennt eine Spalte `citymap.wiki_url`, die es nicht gibt, und §5 behauptet, die Label-Liste könne „hängt schon woanders" anzeigen — **kein** Suchendpunkt kann das. Jede Korrektur steht an ihrer Codestelle. 🔧 **Offen:** der Ablauf mit angemeldeter Sitzung (kein Handgriff lief je gegen die echte Datenbank oder im Browser) · das Bauteil emittiert kein `data-i18n`, die Landschaft verliert damit unter `?lang=en` Abdeckung (gehört in M8) · sieben neue Skripte (212 KB) laden bei **jedem** anonymen Besucher mit, obwohl sie nur Editordialoge bedienen · für Karten-Artikelseiten gibt es keinen Wiki-Katalog, gesucht wird ersatzweise in `wiki_sync_pages`.
- **Einen eigenen Knoten nachträglich an einen Wiki-Artikel binden** — ein Herrschaftsgebiet, das als `eigener-knoten:knotenNNN` von Hand angelegt wurde, bekommt seinen Wiki-Artikel; der Wiki-Knoten überlebt, die eigene Zeile wandert in den Papierkorb. Gebaut 02.09.2026; Entwurf **`docs/superpowers/specs/2026-09-02-eigene-knoten-wiki-zuweisung-design.md`**, Bauplan `docs/superpowers/plans/2026-09-02-eigene-knoten-wiki-zuweisung.md`. Kasten im Detailpanel des Monitors, dazu ein Sammellauf über die Namensgleichen. 🔴 **Der `wiki_key` IST die Identität eines Monitor-Knotens** — deshalb war der Monitor bis zum 16.08.2026 ausdrücklich aus der Wiki-Zuweisung ausgenommen („dort wird nichts zugewiesen", `js/ui/wiki-assign-territorium.js`); diese Aktion kehrt das um und ist deshalb **kein Setzen einer Referenz, sondern ein Identitätswechsel**. 💣 **Die Wanderung geht durch EINE Funktion** (`avesmapsEigenerKnotenBindungAnwenden`), und in ihrem Kommentar steht **keine Zahl** — eine Zahl liest sich wie eine vollständige Liste. Das Inventar hält `eigener-knoten-wiki-bindung-ziele-test.php` gegen den Code. 💣 **Der Kraftlinien-Präzedenzfall darf NICHT abgeschrieben werden:** `features.php:3927` wandert eine `entity_public_id` mit einem glatten `UPDATE` ohne Kollisionsbehandlung — dort korrekt, weil bauartbedingt nur das Ankersegment Quellen trägt. Hier bricht dasselbe `UPDATE` an `uq_feature_source (entity_type, entity_public_id, source_id)`, sobald beide Gebiete dieselbe Quelle zitieren. 💣 **Kein `UPDATE IGNORE` und kein Upsert:** die Syntax ist in MySQL und SQLite verschieden (`UPDATE IGNORE`/`UPDATE OR IGNORE`, `ON DUPLICATE KEY`/`ON CONFLICT`), ein SQLite-Test sähe die MySQL-Regression also nicht — Dubletten werden per `DELETE` weggeräumt, dann glatt umgehängt (`avesmapsEigenerKnotenBindungSetzen` ist das portable Upsert-Ersatzstück). 💣 **Der Slug-UNIQUE kennt `is_active` nicht** (`avesmapsPoliticalSlugExists` fragt ohne die Spalte): ohne Freigabe der Papierkorb-Zeile bekäme der Überlebende `t-y-rret-2` und der weggeworfene Platzhalter den sauberen Namen. 💣 **Der stille ist `properties.territory_wiki_key`** — daran hängen die Literatur-Aggregation und die Kartennutzlast, und ein veralteter Schlüssel wirft keinen Fehler, sondern lässt die Zuordnung wegfallen. 🔴 **`parent_locked` erbt** — sonst zöge der nächste `sync_parent_cache` die von Hand entschiedene Hierarchie um. 🔴 **Ein Hauptraum-Artikel bekommt KEINE Aussage, nicht „offiziell":** `avesmapsWikiTitleNamespace` liest den Raum aus dem PRÄFIX, ein Titel ohne Präfix liefert `null`, und die Rangfolge des Kanon-Etiketts begründet aus dem Namensraum ausschließlich „inoffiziell". Markiert wird also, was Fanmaterial IST. 🪤 **Und die Falle, die beim Bau zuschlug:** der Verdrahtungstest liest Quelltext mit PHPs **Tokenizer**, nicht mit zwei `preg_replace`. `sync-monitor.php:39` trägt `_internal/wiki/*-Libs` in einem ZEILENkommentar; ein Blockkommentar-Entferner, der zuerst läuft, sieht dort `/*` und frisst **380 Zeilen echten Code** bis zum nächsten `*/`. Hier fiel es als roter Test auf — dieselbe Verstümmelung kann eine Zusicherung auch LEER laufen lassen. ⚠️ **Nicht per Knopf umkehrbar**; Kasten und Bestätigung nennen Papierkorb und Unumkehrbarkeit beim Namen. 🔴 **DER KASTEN IST DER ORTSPICKER, mit SEINEN Bauteilen** (Owner 02.09.2026: „so designen wie hier — wo man die vorauswahl sehen konnte"): Hülle aus `avesmapsWikiAssignSkin("dt")`, Zeile aus `avesmapsWikiAssignTrefferMarkup`, Liste als `.dt-picker-list` aus `css/components/editor-page.css`, Trefferdeckel aus `AVESMAPS_WIKI_ASSIGN_TREFFER_LIMIT` (40). Nachgebaut wäre das die **siebte** Fassung derselben Liste. ⚠️ `avesmapsWikiAssignMount` wird dabei NICHT gerufen — sein Vertrag ist „Objekt behält seine Identität und bekommt eine Referenz", und diese Bindung ERSETZT die Identität; benutzt werden nur seine reinen Bauer. 🔴 **Die Vorauswahl ist der Knotenname**: der Kasten öffnet mit ihm im Suchfeld und den Treffern dazu, Rückfall auf den alphabetischen Anfang, damit die Liste nie leer dasteht — dafür beantwortet der Leser auch eine LEERE Suche. 🪤 **Der Hinweistext ist bewusst NICHT der geteilte:** jener verspricht „Enter zuweisen", hier zeigt Enter die VORSCHAU, weil die Übernahme nicht per Knopf umkehrbar ist. Ein geteilter Satz, der das Falsche zusagt, ist schlimmer als ein eigener, der stimmt. 💣 **EIN Pfad für Klick und Enter** (`bindungVorschauOeffnen`), und der Klick liest denselben Index, den der geteilte Zeilenbauer als `data-wa-treffer` hinausgibt. 💣 **DIE SUCHE LIEST BEIDE TABELLEN — Spiegel UND Staging.** `avesmapsWikiDumpPersistTerritoryRecords` schreibt AUSSCHLIESSLICH `political_territory_wiki_test`; in `political_territory_wiki` kommt eine Seite erst mit „3 · Übernehmen". Wer nur den Spiegel liest, bietet einen frisch gesyncten Artikel nie an — live gemessen am 02.09.2026 an `Inoffiziell:Táyârret`, eine Stunde nach dem Sync. ⚠️ Und die Zielwerte brauchen **denselben** Rückfall: anbieten ohne lesen zu können ist die schlimmere Hälfte, die Übernahme legte sonst eine Zielzeile mit nichts als dem Namen an. Der Spiegel gewinnt bei Dopplung, und dieselbe Seite in beiden Tabellen zählt EINMAL — sonst wäre jeder übernommene Artikel fälschlich „mehrdeutig" und nie vorangehakt. 🪤 Gefunden hat das der Blick im Browser, kein Test: die Fixture hatte die Staging-Tabelle gar nicht. 🔧 **Offen:** die Übernahme selbst lief nie gegen die echte Datenbank — Suche und Vorschau sind im Browser abgenommen (an `Herzogtum Garetien`: Abweichungen ungehakt und durchgestrichen, Lücken angehakt, Folgenliste „1 Geometrie · 1 Siedlungszuordnung"), der Knopf „Übernehmen" wurde bewusst nicht gedrückt. Und die harte Voraussetzung: die Bindung findet erst Kandidaten, wenn nach `42ecc35f2` **„Dump holen" bis ans Dumpende** durchgelaufen ist. 💣 **UND DAS IST NICHT „1 · Syncen".** Der Sync im Monitor rescannt den Dump NICHT: er liest den Sandkasten `wiki_dump_hybrid_state`, gefiltert nach `run_id` + `entity_kind`, den ein früherer **„Dump holen"**-Lauf gefüllt hat (`dump-sync-kind.php`). Wer „Syncen" drückt, weil ein Artikel fehlt, drückt den Knopf, der das Problem nicht lösen KANN. 🚩 Live belegt am 02.09.2026 über `GET api/edit/wiki/dump.php?action=status`: `last_read_run_at` = **2026-09-01 19:42**, während der Sync um **02:18** lief und der Fix des stehengebliebenen Scanners (`42ecc35f2`) um **01:43** — der Sandkasten war Stunden älter als der Fix, kein ns-222-Gebiet konnte darin sein. `Káhet Ni Kemi` (Hauptraum) war da, `Inoffiziell:Táyârret` und `Inoffiziell:Ordoreum` nicht. ⚠️ Die Leermeldung des Kastens nennt deshalb ausdrücklich „Dump holen" — sie sagte zuerst „Dump-Sync", und genau das hat eine Runde gekostet.
- **`docs/konfliktmanagement-design.md` — the conflict centre** (editor surface: WikiSync → „⚖️ Konflikte"). Conflicts are **computed, never stored** — only the editor's decision is durable (`conflict_decision`, keyed by `(rule_id, fingerprint)`), so a repaired case disappears by itself and one whose facts changed reopens by itself. **Shipped:** rule registry + decision store + endpoint (`api/edit/map/conflicts.php`, libs in `api/_internal/conflicts/`), the two global wiki-namespace rules, per-party evidence, the repair verbs, and the 12 legacy `wiki_sync_cases` types merged into the same list (surface only — they keep their own storage and resolve flows). 🔴 **Seit 20.08.2026 eine dritte Regel: `label.duplicate` — „Dieselbe Beschriftung zweimal“** (Discord #83). Gleicher Wiki-Schlüssel + gleicher Name + gleiche Art, und nicht alle an einer Fläche. Sie hat als einzige ein **löschendes** Verb, und das muss sie haben: eine Beschriftung, die die Label-Kollision verliert, wird nicht gezeichnet — und was nicht gezeichnet ist, lässt sich auf der Karte nicht anklicken (die Owner-Regel von den verwaisten Außenhüllen, zweite Auflage). 💣 **ZWEI Kaskaden hängen an so einem Löschen, und beide sind still.** (a) Die Landschafts-Kaskade: das letzte Label einer Region nimmt Region UND Flächen mit. Verhindert wird sie nicht durch eine eigene Rechnung, sondern durch `refuse_ecosystem_cascade` im Rumpf von `avesmapsDeleteMapFeature` — die Regel steht damit INNERHALB der Transaktion, hinter dem `FOR UPDATE`, und jeder künftige Erzeuger erbt sie. ⚠️ Ein nur beratender Riegel hätte nicht gereicht: `ecosystem_region.label_public_id` wird allein in `ecosystem_region` geschrieben, die Label-Zeile bleibt unberührt, das `FOR UPDATE` deckt diese Richtung also NICHT. (b) Das Höhenfeld: ein `berggipfel`/`vulkan`-Label IST ein Stützpunkt (`terrain-store.php` liest `is_active = 1` + `height_schritt`). Owner-Entscheid: **keine Verweigerung, aber eine zweite Rückfrage, die die Folge beim Namen nennt** — das Löschen ist weich und über den Änderungs-Log umkehrbar. ⚠️ Die Parteien eines Dubletten-Falls sehen einander gleich; unterschieden werden sie an der **Lage**, nicht am Zeitstempel (`updated_at` ist `ON UPDATE`, und `avesmapsWikiRegionAssign` schreibt beide Zeilen im selben Lauf). 💣 **Die Rauschfilter sind tragend und getestet — und hier steht seit 20.08.2026 bewusst KEINE ZAHL mehr.** Sie stand zweimal da („zwei“, dann „DREI“) und war jedes Mal beim nächsten Detektor falsch; eine Zahl liest sich wie eine vollständige Liste, und niemand zählt nach (dieselbe Lehre wie bei der Verkehrsmittel-Sperre). Wer einen Filter ergänzt, ergänzt ihn in DIESER Aufzählung: (1) `path|path` is the ONLY legitimate shared-article *pairing* in `avesmapsConflictSharedWikiVerdict` (215 groups / 1547 objects are one road's own segments) — 🔴 **still true and unchanged**; (2) auto-named ways `<Subtype>-<n>` never reach the watchlist (2448 of 3721); (3) 🔴 **a citymap whose `article_origin = 'wiki_publication'` never ENTERS the detector** (`avesmapsConflictLoadCitymapRows`). Its `article_url` names the PUBLICATION the map was printed in, not an article the map claims for itself — 363 maps on 140 publication pages. Without it the mass assignment of 17.08.2026 would have added **136 groups / 482 objects**, 123 of them mixed with the Literaturwerk holding the same article, i.e. the heaviest category (measured live). ⚠️ Note where filter (3) sits: it is a **loader** filter, not a verdict exception — the owner rejected whitelisting the pairing `citymap|game_literature` because that would also hide genuine mis-assignments („weil ich sehen will, was gesynct und was von uns editiert ist"). A **hand-set** map assignment (`article_origin = 'manual'`) stays fully in view. (4) 🔴 **Mehrere Beschriftungen EINER Landschaftsfläche sind keine Dublette** (`avesmapsConflictFindDuplicateLabels`, core.php): Fläche→Label ist 1:N; das Ingvaltal und das Yaquirtal tragen ihren Namen dreimal. 🚩 Hier stand „der Finsterkamm ist 57 Einheiten lang und trägt seinen Namen zweimal" — live gemessen am 25.08.2026 trägt er **kein einziges** Label (und liegt in EINER Fläche). Die Regel ist unberührt, nur ihr Beispiel war überholt: **13 von 1026** Flächen tragen zwei oder drei Beschriftungen, 334 gar keine. Live gemessen 20.08.2026: ohne ihn 10 Gruppen / 22 Beschriftungen, davon **8 Gruppen / 19 Beschriftungen** genau diese Lage — 8 Fehltreffer gegen 1 echten, und er böte an, genau die Labels zu löschen, an denen gezeichnete Geometrie hängt. (5) **Die Verdrängung** (`avesmapsConflictMergeDuplicateIntoShared`, rules.php): eine Artikelgruppe aus GENAU denselben Beschriftungen wird vom Dubletten-Fall ersetzt — sonst stünde derselbe Sachverhalt zweimal da, das zweite Mal mit Verben, die den doppelten Namen gar nicht loswerden. 🪤 Und sie **erbt dessen Entscheidung**: ohne das wäre ein „Genehmigt“ des Owners (Maraskansund, 21.07.2026) stillschweigend zurückgenommen und der Fall als „Erledigt — Daten repariert“ verbucht, obwohl niemand etwas repariert hat. Removing any of them makes the tool unusable, not merely noisy. Tests: `api/_internal/conflicts/__tests__/conflict-citymap-test.php` (card 5 is filter 3), `api/_internal/wiki/__tests__/citymap-article-assign-test.php`, `…/conflict-label-duplicate-test.php` (4 und 5). **Open:** the enrichment still guesses for parenthetical names (P3). 🔴 **Korrigiert 16.08.2026:** Territorien und Literatur haben längst einen Detektor (`avesmapsConflictLoadTerritoryRows`/`…GameLiteratureRows`), Karten seit der Wiki-Zuweisung (`avesmapsConflictLoadCitymapRows`) — ohne Detektor ist nur noch **`sources`**. Die alte Aufzählung nannte vier und war für drei davon falsch.
- **Der Social-Media-Hub** — Editoren veröffentlichen aus Avesmaps heraus (Text, Hashtags, Bild) auf mehreren Netzen; die automatischen Feature-Updates landen als **Vorschlag** in derselben Liste und warten auf Freigabe. Entwurf **`docs/superpowers/specs/2026-08-10-social-media-hub-design.md`**, Bauplan `docs/superpowers/plans/2026-08-10-social-media-hub-stufe-1.md`, Mockup `docs/social-hub-mockup.html`. **Stufe 1 live 10.08.2026** (Reiter Community → „Social Media", Hub, Register, Endpunkte, Bild-Pipeline, Status je Kanal, Rechteriegel, Probe-Kanal, Freigabe); **Facebook scharf 10.08.2026** (`adapters/facebook.php`, Entwurf §12.3), **Instagram scharf 11.08.2026** (`adapters/instagram.php`, Entwurf §12.4), **Mastodon scharf 11.08.2026** (`adapters/mastodon.php`, `@Avesmaps@rollenspiel.social`, Entwurf §12.5), Video Stufe 3. 🔴 **MASTODON SENDET SEIT 30.08.2026 NICHT MEHR VON DIESEM SERVER** (Entwurf `docs/superpowers/specs/2026-08-30-mastodon-relais-design.md`, live und am echten Beitrag abgenommen). `rollenspiel.social` verwirft die Pakete unserer Ausgangsadresse — gemessen mit `api/edit/admin/ausgang-check.php`: Phase `tcp`, **Port 80 UND 443**, während dieselbe Adresse jedes andere Ziel in 17 ms erreicht, die Namensauflösung stimmt und die Instanz von anderen Anschlüssen in 20 ms antwortet. Die Adresse ist STRATO-Shared-Hosting (`81.169.144.135`) mit fremder Reputation (DroneBL); die Instanz-Administration hat eine Ausnahme geprüft, **abgelehnt** und um eine Alternative gebeten. Beiträge wandern deshalb in eine Warteschlange (`status='queued'`), die der Workflow `.github/workflows/mastodon-relais.yml` alle 30 Minuten abholt und von GitHubs Adresse aus sendet (`tools/social/mastodon-relais.mjs`, Endpunkte `api/social/relay-{next,result}.php`, Bibliothek `api/_internal/social/relay.php`). 💣 **Die Weiche ist das Registerfeld `relay`, kein `$key === 'mastodon'`** — sie steht in `avesmapsSocialDispatch` **nach** Not-Aus, Zeichenlimit und Bildprüfung, sonst scheiterte ein zu langer Beitrag erst eine halbe Stunde später an etwas, das beim Klick schon feststand. 💣 **`queued` und `sending` sind ZWEI Zustände** — ohne die Trennung ist ein abgestürzter Lauf von einem wartenden Beitrag nicht zu unterscheiden; ein `sending` **verfällt nach 60 Minuten**, sonst liegt ein Beitrag nach einem abgebrochenen Lauf für immer still. 💣 **Der übernommene Beitrag bekommt IMMER eine Rückmeldung**, auch wenn der Versand scheitert, und die Rückmeldung **überschreibt kein `sent`**. 🔴 **Eigener Schlüssel `social.relay_token`**; der Mastodon-Token liegt in den **GitHub Secrets** (`AVESMAPS_MASTODON_TOKEN`), dieser Server kennt ihn für den Versand nicht mehr. ⚠️ **Die Sendelogik steht damit zweimal im Haus** (PHP für den Direktweg, JS im Läufer) — gewollt: fällt die Sperre je, nimmt man `relay` aus dem Register und alles läuft wieder direkt. Nicht doppelt sind Beitragstext und Idempotency-Key, die gibt der Server vor. 🔧 **Der Bild-Weg lief live noch nie** — Beitrag 46 hatte keins; im Test ist er abgedeckt. 💣 **Mastodon meldet Fehler als STRING, Facebook als OBJEKT** — wer den Facebook-Leser abschreibt, fällt in den „keine id"-Zweig und ist damit zufällig richtig, aber ohne Mastodons Diagnose; beweiskräftig ist der TEXT, nicht `ok === false`. 💣 **`social.mastodon.base_url` ist die INSTANZ, nicht das Profil** (die Adresszeile zeigt `…/@Avesmaps`, mit Pfad gäbe es einen 404, den man dem Token anlastet). 💣 **`202` beim Bild heißt „noch nicht verarbeitet"**, und `media_ids` muss eine LISTE sein (deshalb JSON statt `http_build_query`, das `media_ids[0]=` schreibt und von Rack als Hash gelesen wird). 💣 **Die 500 Zeichen sind instanzabhängig** — gemessen an `api/v2/instance`, nicht geglaubt; dieselbe Antwort nennt `characters_reserved_per_url = 23` (🔧 offen, unser Zähler zählt Verknüpfungen echt). ⭐ **Einziger Kanal mit `Idempotency-Key`** (aus der `post_id`): nach einer Zeitüberschreitung ist Wiederholen hier gefahrlos, bei Facebook nicht. 🔴 **Der Mastodon-Schlüssel gehört dem Konto, das ihn anlegt** — als Privatperson erstellt, veröffentlicht er als Privatperson; das ist Facebooks `/me`-Falle ohne Vorwarnung, und es gibt keinen technischen Riegel, nur die Sichtprüfung am Profil. 🔴 **Ein Kanal muss nicht nach draußen führen: „Neuigkeiten"** (Schlüssel `changelog`, Entwurf §3.0) schreibt in `changelog_entry` und erscheint im Fenster Hinweise → Neuigkeiten — ohne fremdes Konto, ohne Token, also immer verfügbar. 💣 Sein Adapter ruft **`avesmapsChangelogSeedIfEmpty()` vor jedem Schreibvorgang**: eine leere Tabelle fällt im Lesepfad auf die Saat zurück, und ein einzelner Insert würde die 42 Meilensteine verdrängen, ohne dass es jemandem auffällt. 💣 **Die erste Zeile wird die Überschrift** (`title` ist VARCHAR(190)) — zu lang wird **abgelehnt, nicht gekürzt**, weil eine stumm abgeschnittene Überschrift öffentlich steht. 💣 Eine Facebook-**Seite hat ZWEI Kennungen** — die `6159…`-Profil-ID, die man im Browser sieht, und die klassische Graph-ID (`delegate_page`); **nur die zweite adressiert ein Seiten-Token**, maßgeblich ist die `id` aus `GET /me/accounts`. 🔴 Ein **SEITEN**-Token, und **nie `/me`**: mit einem Nutzer-Token veröffentlichte `/me/feed` den Projektbeitrag öffentlich auf dem privaten Profil des Owners — deshalb gibt es keinen Rückfall darauf. 💣 Der Token gehört in den **Rumpf**, nie in die Adresse (Server-Protokolle); `caption` bei `/photos`, `message` bei `/feed` (vertauscht wird der Text lautlos verworfen); **`post_id` vor `id`** (`id` ist das Foto). 🔑 **Zugänge richtet der SERVER ein** (`connect.php`, Knopf im Hub, Entwurf §3.2): ein kurzlebiger Token hinein, Tausch + `/me/accounts` + `debug_token` + Ablage heraus. 💣 Gespeichert wird nur, was **`expires_at === 0`** UND **`type === 'PAGE'`** UND die konfigurierte **Kennung** (nie den Namen — es gibt mehrere „Avesmaps") UND die **je Kanal geforderten Rechte** vorweist (`connect_scopes` im Register: Facebook `pages_manage_posts`, Instagram `instagram_basic` + `instagram_content_publish`); fehlendes `expires_at` ist **kein** Freibrief. Von Hand ging genau das am 10.08.2026 dreimal daneben, zweimal unbemerkt. 💣 **Instagram läuft über die FACEBOOK-SEITE** (`instagram_business_account`, gemessen 11.08.2026), also derselbe Wirt und derselbe nie ablaufende Seiten-Token — der in §12.1 beschlossene Instagram-Login-Weg mit 60-Tage-Token und Erneuerungs-Läufer ist damit **verworfen**. Trotzdem eine **eigene Token-Zeile**: läse Instagram Facebooks Zeile mit, meldete der Hub „eingerichtet", sobald Facebook es ist — auch ohne die Instagram-Rechte, was genau der Zustand vom 10.08. war. ⚠️ Die Messung `?fields=instagram_business_account` **lügt ohne `instagram_basic`**: das Feld fehlt dann kommentarlos und liest sich wie „nicht verknüpft". 💣 **Instagram veröffentlicht in ZWEI Schritten** (`/media` → `creation_id`, dann `/media_publish`) — ein Behälter ist **kein Beitrag**, und ein Abbruch dazwischen hat nichts veröffentlicht. **Fehler 9007 ist der EINZIGE wiederholbare**, weil er beweist, dass nichts draußen steht (Instagram rechnet noch); gewartet wird per gedeckelter Wiederholung, **nicht** per Statusabfrage — die wäre ein GET und trüge den Token durch die Adresse. ⚠️ `image_url` (nicht Facebooks `url`), und das Bild wird seit 11.08.2026 auf **1440 px Breite gedeckelt** — die Pipeline schnitt vorher nur zu und verkleinerte nie. Bauteile: `api/_internal/social/{channels,compose,media,store,publish}.php` + `adapters/`, Endpunkte `api/edit/social/{list,media,publish,retry}.php` und `api/social/routine-post.php`, Client `js/review/review-social.js`, Stil `css/components/social-hub.css`. 💣 Acht Fallen, alle im Code begründet und getestet: **der Status gehört dem KANAL, nicht dem Beitrag** (ein gemeinsames „gesendet" verschluckt das eine Netz, das abgelehnt hat) · ein **unbekannter Zustand fällt auf „wartet", nie auf „gesendet"** — Grün heißt „es steht draußen" · ein **fehlender Adapter ist `null`**, nie ein Leerlauf, der Erfolg meldet · **Instagram nimmt kein PNG**, also wird alles zu JPEG — und **JPEG kennt keine Transparenz**, ohne ausdrücklich weißen Untergrund kommt ein schwarzes Quadrat heraus, das niemand vor der Veröffentlichung sieht · **zugeschnitten, nicht gestaucht** (4:5 … 1,91:1) · **Hashtags zählen zum Zeichenlimit** und werden je Kanal gekappt · **erst live, dann posten** (das Bild muss HTTP 200 liefern, sonst cacht das Netz den Fehlschlag) · die Spalte heißt **`body`, nicht `text`** (reserviertes Wort) und **`source_ref` ist nullable** unter seinem UNIQUE-Schlüssel, weil MySQL viele NULL erlaubt, aber nur ein `''`. 🔴 Der **rotierende Zugangs-Token steht in der Datenbank** (`social_token`), nicht in `config.local.php`: eine Datei, die der Server im Zeitplan umschreibt, ist beim ersten Fehlschlag eine kaputte Konfiguration. 🔴 Eigene Fähigkeit **`social`** — seit 11.08.2026 **admin UND editor** (vorher nur `admin`; die Öffnung kostete EINE Zeile in `avesmapsUserCan` und **keinen** Aufrufer, weil alle schon durch `avesmapsUserCan(…, 'social')` gehen — genau dafür hatte sie von Anfang an einen eigenen Namen). ⚠️ **Reviewer bleiben draußen**, und dass `social` und `edit` jetzt dieselben zwei Rollen nennen, ist Zufall, keine Verschmelzung: jemanden **namentlich** freizuschalten bräuchte weiterhin eine Spalte `users.can_social`. ⚠️ Editoren sehen damit auch „Zugang einrichten" — umleiten können sie nichts, die Seite wird über die konfigurierte Kennung gesucht. ⚠️ `#social-hub-overlay` steht in **drei** Selektorlisten in `dialog-overlays.css` — ein Overlay-`<div>` erbt nichts. 🔴 **KI-Kennzeichnung live 16.08.2026** (Entwurf `docs/superpowers/specs/2026-08-16-ki-kennzeichnung-design.md`): Häkchen „Mit KI erstellt" im Hub, **vorgehakt** (Owner-Entscheid), Spalte `social_post.ai_declared`. 💣 **Facebook kann NUR Beiträge mit Bild kennzeichnen** — `provenance_info` steht an `/photos`, `/feed` kennt das Feld überhaupt nicht (Metas Parameterlisten beider Endpunkte, gemessen 16.08.2026). Ein unbebilderter Facebook-Beitrag geht damit unweigerlich ohne Kennzeichnung raus, und das trifft die **Routine vollständig** (sie setzt nie ein `media_url`). Deshalb ist die eigentliche Arbeit dieses Schalters der **Warnsatz** im Hub — angehakt + Facebook + kein Bild —, nicht der Adapter: ohne ihn hakt jemand an, drückt, und hält den Beitrag für gekennzeichnet. 🔴 Der Typ ist `EXPLICIT` („selbst erklärt"), **nicht** `EXPLICIT_AI_EDIT`/`_EXPANDER`/`_RESTYLE` — die benennen konkrete Meta-Werkzeuge, die wir nicht benutzt haben, und wären eine Falschangabe in die andere Richtung. Kein `provenance_metadata` (das trägt C2PA/IPTC aus der **Bilddatei**, die unsere Pipeline nicht schreibt), und ohne Häkchen wird **gar nichts** geschickt — kein `is_gen_ai:false`, die Abwesenheit ist die Aussage. 💣 **Instagram schreibt es anders**: `is_ai_generated` (Bool) am **Behälter**, nie an `/media_publish` — die dritte Asymmetrie nach `url`/`image_url` und `caption`/`message`. 🔴 **Mastodon hat kein solches Feld** (`POST /api/v1/statuses`); bewusste Lücke, keine Baustelle. ⭐ **Zwei Registerfelder statt einer Herleitung**: `ai_label` („nimmt eine Erklärung an") und `ai_label_needs_media` („braucht dafür ein Bild" — nur Facebook). „nimmt an, verlangt aber kein Bild, ALSO braucht die Erklärung eins" wäre für Facebook zufällig richtig und beim nächsten Kanal geraten. 🔧 **Ungemessen: ob Meta `provenance_info` wirklich annimmt** — dafür braucht es einen echten `/photos`-Beitrag mit Bild. Lehnt Meta es ab, scheitert jeder angehakte Facebook-Beitrag mit Bild. 🔴 **Der Kartenausschnitt ist seit 26.08.2026 scharf** (Owner: „aufziehbarer rahmen, mit beschriftungen“) — der Hub blendet sich weg, der Editor zieht einen Rahmen auf der Karte, das Bild hängt am Beitrag. Bauteil `js/ui/karten-abzug.js` + `css/components/karten-abzug.css`, Test `js/ui/__tests__/karten-abzug.test.js`. 🔴 **Der Browser kann eine Leaflet-Karte nicht abfotografieren** — Kacheln sind `<img>`, Orte und Beschriftungen liegen auf `<canvas>`, Grenzen auf `<svg>`, jedes in einer eigenen Pane mit eigener z-Ordnung; gemalt wird Schicht für Schicht von Hand. Genau das ist der Grund, warum der Knopf in Stufe 1 grau blieb — der Bauplan nannte es „ein eigenes Stück Arbeit, kein Nebeneffekt der Bild-Pipeline“. 💣 **DENYLIST, nicht Allowlist.** `tools/layer-tiles/capture.js` macht dieselbe Aufnahme für die sechs Ansichts-Icons und führt eine ALLOWLIST, weil seine Bilder anonym sein müssen — was nicht drinsteht, kann keinen Namen ins Bild tragen. Hier gilt das Gegenteil, und wer es umdreht, verliert bei jeder neuen Ebene stillschweigend deren Inhalt. Draußen bleibt nur, was Bedienung ist und nicht Karte: Popup, Tooltip, die Markierung aus „Was ist hier?“, das Messwerkzeug — plus `leaflet-map-pane`, der Behälter aller übrigen (ohne ihn wird jede Ebene ein zweites Mal gemalt, was bei halbdurchsichtigen Flächen sofort auffällt und bei deckenden nie). 💣 **Ein `<img>` fremder Herkunft vergiftet die Leinwand**, und `toBlob` wirft erst am Ende, wenn alles gemalt ist — ein durchgelassenes fremdes Bild kostet nicht das Bild, sondern die ganze Aufnahme. 💣 **Die Karte wird während des Ziehens stillgestellt** (`dragging.disable()`): die Schicht liegt zwar darüber, aber Ereignisse blubbern, und Leaflet begänne beim selben `pointerdown` zu schieben. Zurückgegeben wird nur, was vorher an war. 💣 **Ein gezogener Rahmen ist ein BILDSCHIRM-Rechteck** und fällt beim Zoomen weg — sonst liegt bei der Aufnahme etwas anderes darunter als beim Ziehen, und es fällt niemandem auf. 💣 **Der Hub blendet sich WEG, statt zu schließen**: `closeHub()` setzt `editingId` und `pendingChannels` zurück, ein halb geschriebener Entwurf käme als neuer, leerer Beitrag zurück. ⚠️ Die Ortsnamen liegen als `data:`-PNG in DIVs der `labels`-Pane — sie landen im Bild, weil der Sammler `img, canvas, svg` liest, nicht weil jemand an Text gedacht hätte. 🪤 **Ein Test über erfundene Pane-Namen ist grün und prüft NICHTS**: „gehört ins Bild“ ist für einen Namen, den die Denylist nicht kennt, immer wahr. Der Test hält seine Namen deshalb gegen die echten `createPane`-Aufrufe des Projekts UND der Leaflet-Bibliothek (die drei Standard-Panes stehen nur dort). 🔧 **Offen: der Ablauf mit angemeldeter Sitzung** — Rahmen, Aufnahme und Hub-Rückkehr sind im Browser gegen echte Kartendaten abgenommen, der Upload selbst lief nie gegen den echten Endpunkt.
- **Die Karte als SVG — und seit 23.08.2026 auch für Maschinen.** Erzeugt wird ein Abzug wie bisher auf `/edit/svg-export.php` (seit 23.08.2026 **Editor**, nicht mehr Admin; alle Regler); neu ist, dass er **hinterlegt** werden kann und dann über **`GET /api/svg-export.php`** mit `Authorization: Bearer <token>` abrufbar ist — ohne Browser-Login. Entwurf `docs/superpowers/specs/2026-08-14-svg-export-design.md`, Vertrag der `avm:*`-Semantik `docs/svg-export-semantik-uebergabe.md`, Steckbrief beider Endpunkte `api/README.md`. 🔴 **DER BAUER IST JAVASCRIPT UND BLEIBT ES.** 1.356 Zeilen Kartenbild in `js/pages/svg-export-build.js`; eine PHP-Fassung wäre eine zweite Wahrheit über das Aussehen der Karte (§5) und müsste je Abruf ~21 MB JSON dekodieren — die Last, vor der CLAUDE.md warnt. PHP **baut nie**, es lagert und liefert nur. 🔴 **ZWEI ERZEUGER, EIN WEG HINEIN:** der Owner im Browser (Knopf „Abzug hinterlegen", Herkunft `manuell`, seine Regler) und die nächtliche Routine um 03:17 UTC (`.github/workflows/svg-export-abzug.yml` → `tools/svg-export/abzug-bauen.js` unter Node, Herkunft `routine`, feste Einstellungen). Beide gehen durch **denselben** Endpunkt `POST /api/svg-export-deposit.php`. 💣 Die erste Fassung schob die Routine-Datei per **SFTP** hoch und räumte per `lftp` auf — ein zweiter Schreibweg mit einer zweiten Fassung derselben Regeln; „zwei von drei Löschwegen gebunden ist keine Regel". Jetzt kennt nur PHP die Ablage. 💣 **`X-Avesmaps-Quelle` ist tragend:** die Fassungsstempel nennen den **Datenstand**, nicht den Erzeuger, und ein Handabzug darf geglättet und umgefärbt sein. 🔴 Die Herkunft bestimmt der **Riegel**, nie der Anfragerumpf — sonst gäbe sich ein Handabzug als Routine aus. 🔴 **ZWEI TOKEN, beide in `api/config.local.php`** (dort sammeln sich alle Token des Projekts): `svg_export.token` liest, `svg_export.deposit_token` schreibt. Ein gemeinsamer machte aus jedem Leserecht ein Schreibrecht, und der Lesetoken geht an fremde Werkzeuge. ⚠️ Die Umgebungsvariable `AVESMAPS_SVG_EXPORT_TOKEN` bleibt als Rückfall für einen Aufbau ohne `config.local.php`; 💣 der Aufruf der Tokenfunktion darf deshalb **nicht im `try` um `avesmapsLoadApiConfig`** stehen — die wirft ohne Konfiguration, und der Rückfall wäre genau im gedachten Fall unerreichbar. 💣 **Hinterlegt wird GESTÜCKELT**, wie der Datenbank-Dump: ein Abzug ist ~8,6 MB, ein einzelner POST läuft auf STRATO in `post_max_size` — deren Fehlerbild ein **leerer Rumpf ohne Ausnahme** ist, also nicht von „nichts geschickt" zu unterscheiden. 💣 **Kein `mb_*` im Schreibweg:** die erste Fassung kappte mit `mb_substr`, und ohne mbstring ist das ein Fatal → leerer Rumpf → „Unexpected end of JSON input" beim Aufrufer; gekappt wird mit PCRE (`/su`, damit nicht mitten im Umlaut geschnitten wird). 💣 **Der ZEIGER ist die Wahrheit, nicht das Verzeichnis** (`aktuell.json`, zuletzt geschrieben, auf einen Namen, den vorher niemand kannte). 💣 **Der aktuelle Abzug wird NIE weggeräumt**, auch als ältester nicht — ein toter Zeiger meldet „kein Abzug vorhanden", direkt nachdem einer hinterlegt wurde (`AVESMAPS_SVG_EXPORT_KEEP_FILES = 3`, wie beim Backup). 🔴 **UND SEIT 31.08.2026 GIBT ES ZWEI FASSUNGEN: `?smooth=1` liefert die GEGLÄTTETE** — dieselbe Karte mit den Bézierkurven, die der Browser zeichnet (`M`/`L`/`C`/`Z`, absolut, sonst nichts), damit ein maschineller Renderer nicht Ecken malt, wo die Karte Kurven hat. Ohne Parameter unverändert die rohe; alles Übrige ist identisch — dieselben `avm:`-Attribute, dieselbe viewBox, dieselben Kopfzeilen, dasselbe bedingte Abrufen, dazu `X-Avesmaps-Variante`. 💣 **Ein Anfrageparameter kann sie nicht ERZEUGEN** (PHP baut nie): es ist eine ZWEITE hinterlegte Datei mit eigenem Zeiger (`aktuell-glatt.json`) und eigenem Namensraum (`abzug-glatt-…`) — und die getrennten Namen sind der einzige Riegel dagegen, dass die Aufräumung der einen Fassung die andere wegwirft, denn das `glob` trifft beide. 🔴 **In welche Schublade eine hochgeladene Datei gehört, liest der Server aus IHREM Wurzelelement** (`avm:geglaettet`), nie aus dem Anfragerumpf — dieselbe Regel wie bei `X-Avesmaps-Quelle`, und aus demselben Grund: der Owner hängt im Browser seine eigenen Regler an den Abzug. 💣 **Der Kopf ist eine Behauptung, die Geometrie ist der Beleg** — die Abnahmeliste prüft, ob wirklich `C`-Kommandos drinstehen; ein Kopf mit „ja" über M/L-Ketten wäre der schlimmste Fehlschlag dieses Umbaus, weil der Abrufer den Fehler dann bei sich sucht. ⚠️ **Herrschaftsgebiete bleiben auch dort eckig** (ihr `smooth: false` ist ausgeschrieben, Owner 15.08.2026) — `avm:flaechen_geglaettet="ja"` gilt den Landschaftsflächen, nicht den politischen Grenzen. ⚠️ Aufbewahrt werden **3 rohe, 2 glatte** (Owner 31.08.2026: „2 fassungen reichen") — die Bézier-Geometrie misst gemessene 2,53-mal so viele Zeichen, und ein volles Webspace entzieht auf STRATO der Datenbank die Schreibrechte. ⚠️ Die Routine baut **beide aus EINEM Datenabruf**: die drei Endpunkte sind bekannte Perf-Brennpunkte, und die Daten sind dieselben. 💣 **Stimmt die Größe im Zeiger nicht mit der Datei, wird neu gehasht statt geglaubt**: ein ETag auf fremdem Inhalt beschert dem Client später ein 304 für eine halbe Datei. 💣 **Ein leerer erwarteter Token passt NIE**, und ein fehlender Schlüssel ist **503 `export_not_configured`**, kein 401 — ein 401 schickte den Aufrufer auf die Suche nach einem Fehler, den er nicht hat. 🔴 **Die Ablage `uploads/svg-export/` hat KEINE `.htaccess` im Repo**, anders als `uploads/db-backups`: `uploads/` steht nicht in der Deploy-Allowlist, eine Repo-Kopie käme nie auf den Server und wäre nur eine zweite, veraltende Fassung — gemessen 23.08.2026 ist genau das beim Backup der Fall (Repo CRLF, PHP-Konstante LF, also Neuschreiben bei jedem Lauf). PHP schreibt und repariert die Sperre zur Laufzeit (`avesmapsSvgExportEnsureAblage`), **nach** dem Riegel, damit keine anonyme Anfrage einen Schreibvorgang auslöst. ⚠️ Die Vorgabefarben liegen in `js/pages/svg-export-farben.js`, geteilt von Browser und Läufer (der eine reicht `getComputedStyle` herein, der andere einen Leser von `css/base/tokens.css`). Tests: `api/_internal/app/__tests__/svg-export-ablage-test.php`, `…/svg-export-glatt-test.php`, `js/pages/__tests__/svg-export-farben.test.js`, `tools/svg-export/__tests__/{tokens-tafel,abzug-bauen}.test.js`; die echten HTTP-Abläufe (14 Schritte lesend, 10 Schritte schreibend mit echtem Mehrstück-Upload beider Fassungen) in `tools/svg-export/__tests__/{endpunkt-ablauf,ablage-ablauf}.js` — bewusst **kein** `.test.js`, damit ein Portproblem nicht das Deploy-Tor schließt.
- `docs/political-territory-editor.md` — editor architecture.
- `docs/stylized-map-tiles.md` — tile pipeline.
- `docs/political-territory-global-display-and-derived-boundaries-{plan,progress}.md`,
  `docs/derived-territory-geometry-plan.md` — derived-boundary system.
- **Verwaiste Außenhüllen** — was die Karte zeichnet, muss im Editor erreichbar sein. Owner,
  wörtlich: „es darf doch auf der map keine elemente geben über die ich keine kontrolle mehr habe."
  Live 16.08.2026; Entwurf **`docs/superpowers/specs/2026-08-16-verwaiste-aussenhuellen-design.md`**,
  Bauplan `docs/superpowers/plans/2026-08-16-verwaiste-aussenhuellen.md`.
  🔴 **Abgeleitete Hüllen sind im Bearbeiten-Modus absichtlich inert** (der `interactive`-Ausdruck in
  `map-features-region-rendering.js`), damit Klicks an die Quellgeometrien gehen — sonst landet
  `update_geometry` auf einer Derived-ID. Die Regel setzt voraus, dass unter der Hülle eine Quelle
  LIEGT; liegt keine da, ist das Gebiet **vollständig unerreichbar**. Gemessen an „Neues
  Herrschaftsgebiet (1008)": alle 116 Prüfpunkte innerhalb der Hülle trafen den nackten
  `leaflet-container`. Seither ist eine Hülle **ohne Quelle** im Editor interaktiv, und ihr
  Rechtsklick zeigt drei statt elf Einträgen, unterster „Außenhülle löschen".
  💣 **„Hat Quellen?" ist EINE Rechnung, und sie muss DIESELBE sein wie im Layer.**
  `avesmapsPoliticalDerivedHullIsSourceless` (`api/_internal/political/derived-orphans.php`) ruft
  denselben Sammler wie `territories-derived-layer.php`, und der kennt **drei** Zweige
  (`parent_id`-Nachfahren, **Wiki-Zweig**, Selbst-Rückfall). Eine eigene, strengere Rechnung weist
  gesunde Hüllen als Waisen aus — und die Strafe dafür ist unumkehrbares Löschen. ⚠️ Im Zweifel gilt
  die sichere Richtung: lieber eine Waise übersehen als eine gesunde Hülle löschen (eine leere
  Antwort des Sammlers zählt deshalb als „nicht verwaist").
  💣 **Das Frontend-Signal `derived_source_geometry_public_ids` kommt vom SERVER und wird nie im
  Browser gezählt** — „im Layer liegt nur die Hülle" trifft bei Zoom 4 auf **114** Gebiete zu, und
  **111 davon sind kerngesund**; ihre Quellflächen sind bei diesem Zoom nur nicht ausgeliefert.
  💣 **„Kein Territorium" heisst: die Zeile in `political_territory` existiert nicht mehr** — nicht
  „fehlt im Rechenschnappschuss", denn der filtert `is_active` UND Kontinent; ein Gebiet im
  Papierkorb ist keine Waise.
  🔴 **Hart gelöscht wird nur, was niemand mehr erzeugen kann** (Owner-Entscheid); alles andere
  bleibt beim umkehrbaren `is_active = 0`, weil „Grenzen berechnen" es zurückholt. Die Weiche sitzt
  in `avesmapsPoliticalDeleteDerivedGeometryForTerritory` und **nur dort** — Scanner, Sammelknopf
  und der Rechtsklick auf der Karte gehen alle durch sie. ⚠️ Wer eine unumkehrbare Handlung anbietet,
  muss sie auch so beschriften: Bestätigung und Erfolgsmeldung nennen hart und weich getrennt.
  💣 Entstanden ist der Zustand am Sammelknopf: `purge_unassigned_geometries` löschte die
  Quellflächen der Platzhalter roh und ging an der vorhandenen Ketten-Deaktivierung **vorbei** —
  zwei von drei Löschwegen gebunden ist keine Regel.
  ⚠️ `geometry_inventory` kappte bei 500 nach Fläche **vor** dem Waisen-Filter; weil Waisen klein
  sind, fielen sie systematisch heraus (25 gezählt, 6 gezeigt). Das Fenster fordert jetzt
  `limit=2000`. Tests: `api/_internal/political/__tests__/verwaiste-aussenhuellen-test.php`,
  `js/map-features/__tests__/verwaiste-aussenhuelle-interaktiv.test.js`.
- `docs/feature-umstrittene-gebiete.md` — active contested-territory feature.
- **„Hierher reisen“ + der Querfeldein-A\*** (Instruction `docs/superpowers/plans/2026-07-30-hierher-reisen-und-astar.md`, Entwurf `docs/superpowers/specs/2026-07-30-landschaften-v14-astar-design.md`) — **live 2026-08-01**. Rechtsklick auf einen beliebigen Kartenpunkt → Wanderschuh → Route bis genau dorthin. `POST /api/route/` nimmt `from_point`/`to_point` = `{x, y}`; der Punkt wird ein Graphknoten mit EINER Kante (dem A\*-Weg zum nächsten Graphknoten), danach laufen Dijkstra, Segmentbauer und Renderer unverändert. Kern: `api/_internal/routing/{land-areas,offroad-grid,offroad-data,offroad-leg}.php`, Client `js/routing/route-travel-here.js`. 🔴 **STAND 15.08.2026 ABEND — DREI REGELN, UND SIE ÜBERSCHREIBEN TEILE DESSEN, WAS WEITER UNTEN STEHT.** (1) **Der Ausstieg ist der nächste ERREICHBARE Punkt des Netzes** — eine Ordnung, keine Auswahl. Owner wörtlich: „er soll auf dem strassensystem bleiben bis zu dem punkt gehen wo er am nächsten zur freien zielmarkierung ist und von dort durch wieder durch die landschaft.“ Gerechnet werden die `AVESMAPS_ROUTE_OFFROAD_EXIT_NEAREST_TRIES` (6) nächsten Kandidaten, die Kante bekommt **genau der erste, der querfeldein erreichbar ist** — das `break` in `avesmapsAttachOffroadPointToGraph` IST die Regel. (2) **Eine Querfeldein-Etappe wird mit ihrer eigenen Länge langsamer** (Längenaufschlag, eigener Eintrag unten). (3) **Ein Fluss ist im Gelände eine Wand** (eigener Eintrag unten). 💣 **Und die Regel gilt für ALLE Erzeuger, nicht nur die Anbindung** — sie wurde zuerst nur dort gebaut und war damit an zwei Stellen wirkungslos: die Umweg-Sehnen legten weiter Kanten an den Kartenpunkt (gemessen: Ausstieg bei 5,04 Einheiten angeboten, gefahren `offroad-detour-12-21`, Jurios → Ziel, 34,4 Meilen quer), und die direkte Kante zwischen zwei Kartenpunkten gewann unter „Kürzeste“ IMMER, weil dort das Gewicht die Strecke ist und eine Gerade per Definition kürzer als jede Straße (gemessen: EINE Etappe über 148,5 Meilen bei 140,4 Meilen Luftlinie — die Kürzeste war zum Drachenflug geworden). **Dieselbe Lehre wie am 14.08. bei der Verkehrsmittel-Sperre, zum zweiten Mal: eine Regel, die einen von vier Erzeugern bindet, ist keine Regel.** Sehnen an einen Kartenpunkt gibt es seither nicht mehr (`avesmapsRouteChordCandidates`), und die direkte Kante entsteht nur, wenn der andere Punkt für mindestens EINEN der beiden näher liegt als dessen nächster Netzpunkt (`avesmapsOffroadDirectEdgeAllowed`; ⚠️ `max`, nicht `min` — liegt A dicht an der Straße und B weit ab, ist A für B trotzdem das Nächstliegende). Test: `ausstiegsregel-alle-erzeuger-test.php`. 💣 Die Landprüfung gilt **nur dem angeklickten Punkt** — Orte im Wasser werden nie geprüft (571 von 4.653 liegen geometrisch im Meer). 💣 Das Gitter ist **nie ein PHP-Array** (33,2 gegen 1 Byte je Zelle); drei Gelände-Byte-Ebenen, per **Maximum** verknüpft. **Drei Aufrufer, ein Rechner** (Instruction C `docs/superpowers/plans/2026-08-02-ausloeser-anker-und-x25-aufschlag.md`, **live 2026-08-02**): (1) „Hierher reisen"; (2) der **automatische Umweg-Auslöser** — jede Route vergleicht nach dem Dijkstra gefahrene Strecke gegen Luftlinie, über `AVESMAPS_ROUTE_OFFROAD_DETOUR_THRESHOLD` (3,0) rechnet der A\* quer und BIETET die Kante an (`detour.php`). 🔴 **STILLGELEGT AM 16.08.2026** (`AVESMAPS_ROUTE_OFFROAD_DETOUR_ENABLED = false`, Sperre am einzigen Aufrufer in `response.php`): seit der Ausstiegsregel war die Sehne die EINZIGE Stelle, an der der Router noch eigenmächtig die Straße verließ, und sie hat einen sichtbaren Widerspruch erzeugt — „Kürzeste“ 305,3 Meilen gegen „Schnellste“ 242,6 auf derselben Route, weil Sehnen aus der ZUERST gefundenen Kette gerechnet werden und die je nach Modus verschieden ist. Verbinden konnte sie nie (sie kommt NACH einer gefundenen Route); abgetrennte Orte hängen an der Komponentenbrücke, netzferne Kartenpunkte an der Ausstiegsregel. ⚠️ Gemessen und abgenommen: dieselbe Route wurde bei „Schnellste“ 310,7 statt 242,6 Meilen lang, Owner: „die fühlen sich absolut richtig an“. Modul und Tests bleiben stehen und tragen den Vermerk; 🔧 vermisst sie in ein, zwei Wochen niemand, fliegen sie raus; (3) die **Sehnen der gefundenen Route** — Komponentenbrücke und Wegpunkt-Anker werden nachträglich gebogen (`synthetic-refine.php`). 💣 Der A\* läuft für (3) **nie beim Graphbau**: 876 synthetische Kanten je Graph, aber 0–1 je Route. 💣 `AVESMAPS_ROUTE_CLIENT_SYNTHETIC_DISTANCE_COST_FACTOR` (×25) ist ein **Dijkstra-Gewicht, keine Strecke und keine Zeit** — er stand bis 2026-08-02 in `distance_units` des stabilen Vertrags (484,65 für eine Linie von 19,39). Er bleibt im Gewicht, wird aber aus jeder gemeldeten oder verglichenen Zahl per `cost_factor` herausgerechnet. 🔴 **Auslöser (2) bedient seit 2026-08-14 nur noch Orte OHNE Anschluss ans Wegenetz** (Owner: „querfeldein sollen nur orte angefahren werden, die nicht mit dem straßennetz verbunden sind"). Hängen **beide** Enden einer Sehne an einer gezeichneten Landwegart, wird sie gar nicht erst gerechnet — Absagegrund `both_ends_on_network`, `avesmapsRouteKeepChordsWithOffNetworkEnd` in `detour.php`. Anlass war Luring → Salmingen: quer für 3,38 statt 9,52 über Ferdok, obwohl beide Orte an Straßen liegen. ⚠️ Die Schwelle entscheidet erst danach, und an Land entscheidet sie allein: seit dem Querfeldein-Quellenwert 2,30 liegt das Tempoverhältnis bei 1,335, die Zeitprobe kann dort also nichts mehr auffangen (auf dem Wasser bleibt sie scharf). 🔴 **Der Ausstieg einer Querfeldein-Etappe ist ein Punkt AUF einem Weg, nicht die nächste Ortschaft** (live 14.08.2026; Entwurf `docs/superpowers/specs/2026-08-14-querfeldein-ausstiegspunkt-design.md`, Plan `docs/superpowers/plans/2026-08-14-querfeldein-ausstiegspunkt.md`). Beide Erzeuger netzferner Anbindungen — der Kartenpunkt und der Wegpunkt ohne Weganbindung — bekommen bis zu `AVESMAPS_ROUTE_CLIENT_ANCHOR_LIMIT` (6) Fußpunkte als **Angebot**, je einen pro Weg (`avesmapsCollectNearestClientLandPathAnchors` entdoppelt über die Kanten-`id`). 🪤 Hier stand „welcher gewinnt, entscheidet der Dijkstra“ — seit dem 15.08.2026 abends entscheidet er das NICHT mehr (Stand-Block oben). Er entscheidet weiterhin, WIE man zum Abgangspunkt kommt, nur nicht mehr, WANN man den Weg verlässt. Gemessen: ein Kartenpunkt zwischen Luring und Spinnried verlässt die Reichsstraße 0,702 statt 2,652 Einheiten vor dem Ziel (−74 % Querfeldein). 💣 `avesmapsSplitClientPathAtAnchor` **entfernt die Ursprungskante**, sobald beide Hälften stehen — sonst teilt der nächste Endpunkt denselben Weg ein zweites Mal und die Fußpunkte hängen unverbunden nebeneinander. 💣 **Fußpunkte und Ortschaften stehen in EINEM Topf, und der Maßstab der Reichweitenschranke (× 2,5) ist der nächste ORTSKNOTEN.** Drei Fassungen an einem Tag, jede von der nächsten Messung widerlegt: (a) Staffelung „Ortschaften nur als Rückfall" — ließe eine Stadt 4,6 Einheiten neben dem Punkt hinter einem Fußpunkt bei 40 verschwinden; (b) Maßstab „nächster Kandidat überhaupt" — warf live alle vier Ortschaften aus dem Angebot, Reise 2,8 % teurer; (c) Maßstab „je Familie" — schnitt einen Fußpunkt bei 7,61 weg, während eine Ortschaft bei 8,30 blieb, weil deren Familie ihren eigenen weiteren Maßstab hatte (Reise rund 15 % länger). ⚠️ **Die Lehre: eine relative Schranke braucht einen Maßstab, der nicht mitwandert.** 🪤 **Und am 15.08.2026 abends ist die Schranke ganz weggefallen** — es gibt keinen Faktor mehr, sondern eine Ordnung (Stand-Block oben). Der Grund ist genau diese Lehre: der Owner hat am 14.08. einen Knoten bei **1,77×** der nächsten Luftlinie ERWARTET und am 15.08. einen bei **1,72×** beanstandet — keine Schranke der Form „höchstens F mal der nächste“ erfüllt beides, weil sich der Maßstab zwischen den Fällen geändert hat (damals Ortsknoten, heute jeder gezeichnete Stützpunkt). Die drei Fassungen (a)/(b)/(c) bleiben als Messungen wahr; (b) ist heute der **gewollte** Preis. 🔴 **Und ein Weg bietet JEDEN seiner gezeichneten Punkte als Ausstieg an, nicht einen** (live 15.08.2026; Entwurf `docs/superpowers/specs/2026-08-15-querfeldein-abgangspunkt-design.md`, Plan `docs/superpowers/plans/2026-08-15-querfeldein-abgangspunkt.md`, Vorgeschichte `docs/superpowers/plans/2026-08-15-querfeldein-ausstieg-optimum-uebergabe.md`). 💣 Der Befund davor war KEINE Fehlplatzierung, sondern ein fehlendes Angebot: ein Wegstück lieferte genau einen Kandidaten — die Projektion des Ziels —, und sobald der gegen den Direktweg verlor, existierte die Strasse für diese Reise nicht mehr. Gemessen an Salmingen → Kartenpunkt (504.530, 501.076): EINE Etappe über 42,06 Meilen querfeldein, neben dem Talloner Hügelsteig her, den die Reise nie betrat. Owner, wörtlich: „es gibt kein ausstieg heute". Jetzt liefert `avesmapsCollectClientLandPathExitCandidates` je Wegstück alle inneren Vertices PLUS den Fußpunkt (Deckel `AVESMAPS_ROUTE_OFFROAD_EXIT_VERTEX_LIMIT` = 24, am Livebestand gemessen: p99 = 23, max = 53; eine Kappung steht als `exit_vertices_capped` in der Antwort). Die Reise stieg daraufhin am Knick (505.470, 511.933) aus: 7,80 Meilen Straße, dann 36,65 Meilen Gelände, Kosten 6,21 statt 6,54. 🪤 **Genau dieser Ausstieg ist am selben Abend zurückgenommen worden**: der Knick war der BILLIG ERREICHBARE, nicht der zielnächste. Die Fixture in `abgangspunkt-test.php` unterscheidet beide bis heute (Knick 12,0 nach 8 Wegeinheiten gegen zielnächst 11,66 nach 20,7) und prüft seither die neue Regel — samt der Zusicherung, dass die erzwungene Reise TEURER ist (13,42 gegen einen abgeschafften Direktweg von rund 11,83). Das ist der Preis der Regel und steht dort, damit ihn niemand „wegoptimiert“. ⚠️ Sie ist damit SCHNELLER, aber LÄNGER (44,5 statt 42,1 Meilen) — für „Schnellste" richtig, für „Kürzeste" nicht geprüft. ⭐ Bezahlbar wird das durch `avesmapsOffroadFindPathsFromPoint`: EIN Dijkstra vom Kartenpunkt nach außen bedient alle Kandidaten, statt eines A\*-Laufs je Kandidat (gemessen: vorher 15 Läufe je Anfrage). 💣 **Er bepreist jeden Schritt in GEGENRICHTUNG** — der Reisende geht vom Ausstieg zum Kartenpunkt, der Lauf geht andersherum, und Steigung kostet mehr als Gefälle; ohne den Tausch stünde die Zeit der Rückreise in der Antwort (die Fehlerklasse aus V11 §6.3). 💣 **Die Bremse ist der Abbruch beim letzten erreichten Ziel**, nicht die volle Kiste — ohne ihn läuft ein Dijkstra ohne Ziel kreisförmig über bis zu 150.000 Zellen. ⚠️ Gemessen ist er trotzdem TEURER als vorher: 5,9 ms gegen 3,9 ms für den doppelten Kandidatenvorrat (der naive Weg, 38 einzelne A\*, wäre 8,6 ms) — bei einer Anfrage von 1,99 s sind das 0,3 %, Owner-Entscheid „2ms ist egal". 💣 **Geteilt wird in EINEM Durchgang je Wegstück** (`avesmapsSplitClientPathAtPoints`); der Einzelteiler entfernt die Ursprungskante, sobald beide Hälften stehen, und k Aufrufe hintereinander hängen die späteren Punkte ins Leere. Er selbst bleibt unangetastet — er trägt den Wegpunkt-Anker. ⚠️ **Gefiltert wird VOR dem Teilen**, sonst wird eine Straße für Kandidaten zerschnitten, die die Reichweitenschranke ohnehin verwirft. 🪤 Der Satz „Die Schranke und ihr Maßstab (nächster ORTSKNOTEN, × 2,5) sind unberührt“ galt bis zum 15.08.2026 abends; `AVESMAPS_ROUTE_OFFROAD_EXIT_DISTANCE_FACTOR` gibt es nicht mehr. 🔴 Der Übergang heißt in der Etappenliste **„Abgangspunkt"** (Weg → Gelände) bzw. **„Anschlusspunkt"** (Gelände → Weg), richtungsabhängig in `nameRoutePlanTransferPoints` (`route-plan.js`) — NACH `cleanRoutePlanNoiseEntries`, weil jene an `isRoutePlanMarkerName` entscheidet, ob eine Etappe schließt. Davor stand dort „Markierung" (zwei Umbenennungen hintereinander: `normalizeNodeName` → „Kreuzung", `formatRoutePlanNodeName` → „Markierung"); beide bleiben als Rückfall stehen. Tests: `abgangspunkt-test.php` (der Abnahmefall), `exit-vertices-test.php`, `path-multisplit-test.php`, `offroad-multi-goal-test.php`, `abgangspunkt-label.test.js`. ✅ Der damals offene Punkt („dieselbe Kandidatenauswahl ist bei den zwei übrigen Erzeugern noch eng“) hat sich am 15.08.2026 abends anders erledigt als gedacht: die Sehnen dürfen einen Kartenpunkt gar nicht mehr berühren. ⚠️ **Ein Freiheitsgrad bleibt** — der Umweg-Auslöser darf weiterhin eine Sehne zwischen zwei STRASSENKNOTEN legen, wenn das Netz dort einen dreifachen Bogen macht. Das ist die Fassung, die der Owner „richtig“ genannt hat; soll die Wegbindung je absolut werden, ist das die Stelle. 🔧 Und der Blick auf die Naht im Browser steht weiter aus. 🪤 **Und eine zweite, teurer bezahlte Falle:** (c) wurde an einem Kartenpunkt gemessen, den der Prüfende aus der Wegpunkt-Beschriftung abgetippt hatte — die zeigt `lat, lng`, `to_point` will aber `{x: lng, y: lat}` (`applyMapPointRouteEndpoints`, `route-travel-here.js`). Eine Stunde Fehlersuche galt damit einer ganz anderen Stelle der Karte als der gemeldeten. Der Befund (c) blieb trotzdem echt und der Test dazu ist synthetisch — aber **wer einen Kartenpunkt aus der Oberfläche in eine API-Probe übernimmt, muss ihn drehen.** Die Ortschaften spannen die Suchkiste seit jeher auf; die Fußpunkte liegen darin und kosten nichts extra, und einer, der weiter liegt, würde die Kiste aufziehen und fällt zu Recht heraus. 💣 **Querfeldein-Kanten haben VIER Erzeuger, und die Verkehrsmittel-Sperre muss in jedem einzeln stehen** (14.08.2026): `avesmapsConnectClientCompatibleDetachedGraphComponents` + `avesmapsConnectClientRouteWaypointsToNearestLandPath` (`client-graph.php`) sowie `avesmapsAttachOffroadPointToGraph` + `avesmapsConnectOffroadPoints` (`offroad-leg.php`, letzterer trägt auch die Umweg-Sehnen aus `detour.php`). Die beiden ersten fragten `avesmapsIsClientTransportAllowedForPath` von Anfang an, die beiden letzten — die vom **Nutzer** ausgelösten — nicht: sie prüften nur, ob es ein **Tempo** gibt, und für die Kutsche steht querfeldein 3,84 in der Tabelle. Die Kutsche fuhr deshalb bei „Hierher reisen" quer über die Wiese (live gemessen: Luring → Kartenpunkt, HTTP 200). ⚠️ **Die eigentliche Falle war die ZAHL im Kommentar** — dort stand „ERZEUGER 1 VON 2" / „2 VON 2", und eine Zahl liest sich wie eine vollständige Liste, also suchte niemand weiter. `avesmapsFindOffroadPathBetween` (`synthetic-refine.php`) ist **kein fünfter**: es biegt nur die Geometrie einer bereits geprüften Kante. 🔴 Absagegrund ist `offroad_transport_not_allowed`, **nicht** `no_offroad_route` — hier liegt es an der ANFRAGE, nicht am Punkt, und kein anderer Punkt der Karte hilft; der Satz dazu steht in `js/routing/route-travel-here.js`. Gewacht von `api/_internal/routing/__tests__/carriage-offroad-test.php` (fährt den Kartenpunkt wirklich an, mit Gegenprobe zu Fuß).
- **Der Längenaufschlag für Querfeldein** (live 15.08.2026, Entwurf `docs/superpowers/specs/2026-08-15-querfeldein-laengenaufschlag-design.md`) — eine Etappe wird mit ihrer eigenen Länge langsamer: `zeit_final = zeit_gemessen × min(deckel, 1 + steigung × meilen)`, Vorgabe **0,6 % je Meile**, Deckel **2,0** (erreicht bei 167 Meilen Luftlinie). Anlass: eine Reise lief 103,28 Meilen querfeldein statt über die Straße, weil das Querfeldein-Tempo am 14.08. um 18:38 von 0,96 auf den GA-Wert 2,30 gezogen wurde. 🔴 **Er misst die LUFTLINIE der Etappe, nicht die gelaufene Strecke** — die Suche ordnet OHNE ihn; hinge er an der gelaufenen Länge, bestrafte er nachträglich genau den Bogen, den der A\* zum Zeitsparen geschlagen hat (gemessen an der Fixture von `offroad-shortest-test.php`: Zeitmodus 14,01 gegen Streckenmodus 12,40 — eine „schnellste“ Etappe, die messbar langsamer war als eine verworfene). An der Luftlinie ist er für ein festes Endpunktpaar eine **Konstante**, und eine Konstante verschiebt kein Minimum. 💣 Er sitzt in `avesmapsOffroadFinishPath`, dem gemeinsamen Abschluss ALLER gerasterten Erzeuger — die Vier-Erzeuger-Falle galt der Sperre VOR dem Suchlauf, der Preis kommt danach und deshalb einmal. 💣 **Nur die Zeit**, `distance` bleibt unangetastet. ⚠️ Der Bezug ist die EINZELNE Etappe: ein kurzes Stück Weg setzt den Aufschlag zurück — Absicht (wer einen Ort berührt, rastet dort) und Owner-Entscheid vom 15.08. Einstellbar im Fenster „Tempowerte“ (Abschnitt „Querfeldein-Aufschlag“, Speicherabschnitt `offroad_ramp`, Rücksetzer `data-section="offroad"`); ⚠️ die GA-Spalte zeigt dort **„—“**, weil die Quelle überhaupt keine längenabhängige Regel kennt. Tests: `offroad-ramp-test.php`.
- **Flüsse sperren das Gelände** (live 15.08.2026, Entwurf `docs/superpowers/specs/2026-08-15-fluesse-sperren-gelaende-design.md`) — 🔴 `AVESMAPS_ROUTE_WATER_REGION_TYPES` ist `['meer','see']`; ein Fluss ist bei uns keine Fläche, sondern ein **`Flussweg`-WEG**, also eine Linie, und `avesmapsOffroadRasteriseBlocked` rasterte nur Polygone. Der Fluss war nicht schwer zu queren, er war **nicht da** (gemessen: 61,8 Meilen in EINER Etappe quer über die Rakula). Owner-Entscheid: **unpassierbar, Absage statt Furt** — gequert wird nur, wo ein gezeichneter Weg quert, und das wirkt von selbst, weil Wege Graph-Kanten sind und das Gitter nie sehen. 💣 **Jede berührte Zelle UND die Eckzellen**: der Suchlauf geht über acht Nachbarn, und zwischen zwei diagonal benachbarten gesperrten Zellen schlüpft er hindurch, solange die Ecken frei bleiben. 💣 Die **gerade Linie** unter „Kürzeste“ geht am Raster VORBEI (das ist ihr Sinn) und braucht einen eigenen Schnitt-Test gegen die Flusslinien. ⭐ Die Geometrien sind bereits geladen (`$routeNetworkData['paths']`) — keine zweite Abfrage je Route. 🔴 `avesmapsRouteChordCrossesWater` bleibt UNBERÜHRT: sie trägt auch die Reparaturkanten zwischen losen Komponenten, und Flüsse dort zu sperren könnte eine abgetrennte Komponente ohne jede trockene Verbindung zurücklassen. ✅ **Und der Bach ist seit 30.08.2026 da — als FURT, nicht als Wand** (Owner: „ja ein bach wird überquert werden können, aber nur mit etwas erschwernis, ich wollte aber nicht, dass du ihn komplett aus der hinternis erkennung rausnimmst“). Er fällt aus der Sperrebene heraus und in die **Faktor-Ebene** hinein: `AVESMAPS_ROUTE_OFFROAD_BACH_FACTOR` = **3,0**, eingefroren, **kein Regler** (die „Tempowerte“ beschreiben Reisemittel und Untergründe, ein Bach ist weder das eine noch das andere). Eine Zelle ist 1,5 Meilen breit, das Queren kostet also wie 4,5 Meilen offenes Gelände — gemessen an der Test-Fixture: eine 90-Meilen-Geländeetappe mit EINEM Bach wird 3,3 % teurer. ⭐ Die Vorhersage von damals hat gehalten: die Entscheidung „Wand oder nicht“ stand an **genau einer** Stelle, `avesmapsCollectRouteRiverBarrierLines`, und dort ist sie geblieben — sie liefert seither **zwei Fächer** (`wand` / `furt`) statt einer flachen Liste. 💣 **EIN Rückgabewert, kein zweiter Parameter:** ein `$bachLines` neben `$riverLines` durch dieselben sechs Signaturen zu reichen wäre die Falle, die dieses Haus zweimal bezahlt hat — der nächste Erzeuger reicht das eine weiter und das andere nicht, und der Fehler ist STILL (eine Route wird ohne Aufschlag einfach etwas billiger). 💣 **Die drei Zusammenbau-Stellen der Gitterebenen gehen jetzt durch EINEN Erzeuger** (`avesmapsOffroadBuildPlanes`, `offroad-data.php`); dieselben vier Zeilen standen vorher dreimal da, und der Bach-Aufschlag wäre die nächste Regel gewesen, die man an jeder einzeln hätte nachziehen müssen. Er gibt die **Wand-Linien mit zurück**, weil `avesmapsOffroadStraightPathIfDry` am Raster vorbeigeht und die Flüsse eigens gefragt bekommen muss — so wird der Bund an genau einer Stelle ausgepackt. 💣 **Die alte flache Linienliste wird als WAND gelesen** (`avesmapsOffroadBarrierLines`), nicht als nichts: der schlimmste Fall ist damit „ein Bach sperrt wieder“ und nicht „kein Fluss sperrt mehr“. ⚠️ **Unter „Kürzeste“ wirkt der Aufschlag NICHT** — dort ist das Gewicht die Strecke, und Boden wie Steigung sind seit jeher neutralisiert (Wald, Sumpf und Gebirge bremsen dort ebenso wenig). Die gemeldete ZEIT trägt ihn trotzdem. Das ist die Hausregel, kein Versehen. ⚠️ Gelesen wird strikt `properties.is_bach === true`, genau wie im Browser — eine großzügigere Lesart hieße: die Karte zeichnet einen Fluss und der Router nimmt eine Furt an. Tests: `fluss-sperre-test.php`, `bach-furt-test.php` (13 Mutationen gefahren, alle gefangen).
- **„Schnellste“ heißt Zeit, „Kürzeste“ heißt Meilen — auf DEMSELBEN Weltmodell** (Owner-Entscheid 15.08.2026). Die Wegbindung gilt für beide; die Modi unterscheiden sich nur im Gewicht und in der Form der Geländeetappe (gerade gegen zeitoptimalen Bogen). ⚠️ **Damit heißt „Schnellste“ die schnellste WEGGEBUNDENE Reise, nicht die schnellste überhaupt** — auf der Owner-Route kostet sie 21,02, der aufgegebene frühe Ausstieg kostete 10,10. Gesehen und gewollt. ⭐ Gemessen an vier Routen, ob die zwei Knöpfe noch etwas taugen: Ferdok→Gerrun **12,4 %** Unterschied, zwei andere 0 %, das Kartenpunkt-Paar 0,7 %. Die Modi sind **genau dann gleich, wenn das Netz keine Alternative bietet** — das ist richtig, nicht Rauschen.
- **Die Tempowerte gelten auch fuer die KARTE, nicht nur fuer den Router** (live 26.08.2026,
  Owner: „die tempowerte sollen auch für die karte gelten“). Der Router liest sein Raster über
  `avesmapsTravelValuesRead()` — die Konstante, darüber die im Fenster „Tempowerte“ gespeicherten
  Werte. Der Browser trug `SPEED_TABLE` (js/config.js) als feste Zahl. 💣 **Zwei Ansprüche auf
  dieselbe Zahl, und sie waren live auseinander:** 5,07 gegen 5,18 für die Reisegruppe zu Fuß auf
  der Reichsstraße, 5,95 gegen 6,00 für den Flusssegler — der Reiseplan zeigte rund 2 % kürzere
  Zeiten, als der Router gerechnet hatte, und aufgefallen ist es erst, als ein Melder die stabile
  API gegen die Karte hielt (#101). ⭐ **Die Leitung gab es schon:** die drei REISETAGE reisen seit
  dem 16.08.2026 in der Kartennutzlast mit (`travel_hours` → `applyServerTravelHours`); die
  Tempotabelle fährt seither auf derselben (`travel_speeds` → `applyServerTravelSpeeds`,
  js/routing/routing.js). Kein zweiter Endpunkt, kein Wettrennen — der Aufruf steht in
  `prepareLocationData`, und darunter entsteht erst `locationData`, ohne die niemand eine Route
  rechnen kann. 🔴 **Die Konstante im Browser BLEIBT** — als Rückfall auf „der Server sagt nichts“,
  nicht als zweiter Anspruch. 💣 **Übernommen wird ZELLE FÜR ZELLE**, nie das ganze Raster: ein
  ungenanntes Reisemittel behält seins, ein unbekannter Schlüssel wird NICHT angelegt —
  `SPEED_TABLE[t]?.[type] || 1` macht aus einer fehlenden Zelle klaglos Tempo 1.
  💣 **UND DER STEMPEL IST TRAGEND.** Das ETag der Kartennutzlast hängt an `map_revision`, und die
  Tempowerte ändern kein Kartenobjekt — ohne Stempel bekäme jeder warme Browser sein 304 und
  rechnete unbegrenzt lange mit den alten Werten weiter. Genau diese Falle hat die Klimaebene schon
  bezahlt und der Wappen-Notaus vier Monate lang getragen. Der Kommentar an der Nutzlast behauptete
  bis dahin „wer im Fenster speichert, sieht ihn beim nächsten vollen Laden“ — mit
  `no-cache, must-revalidate` und unverändertem ETag IST das nächste volle Laden ein 304.
  ⚠️ Ein LEERER Stempel (Lesevorgang ausgefallen) hält den Keim zeichengleich, damit nicht die
  halbe Welt 21 MB neu lädt, weil einmal eine Einstellung nicht lesbar war.
  🪤 **Und eine Zusicherung, die es schon gab, war VAKUUM:**
  `routingSource.includes("applyServerTravelHours(data)")` traf auch die DEFINITIONSZEILE — der
  Aufruf hätte gelöscht werden können, ohne dass ein Test rot wird. Gefunden von einer
  Mutationsprobe; jetzt mit Semikolon. Bauteile: `api/_internal/app/travel-values.php` (ausgelagert,
  weil `api/app/map-features.php` ein Endpunkt ist und sich nicht einbinden lässt),
  `applyServerTravelSpeeds` in `js/routing/routing.js`. Tests:
  `api/_internal/app/__tests__/tempowerte-nutzlast-test.php` (Leser, Stempel, ETag) und
  `js/routing/__tests__/tempowerte-vom-server.test.js` (Übernahme — und dass die Etappenzeit ihr
  wirklich folgt). 🔧 **Offen bleiben die übrigen Abschnitte des Fensters:** die Untergrund-Abzüge
  (`ground_penalties`) liest im Routing **niemand** — weder Server noch Browser; der Browser hat in
  `js/routing/season-ground.js` eine unabhängige Kopie derselben Zahlen. Der Querfeldein-Aufschlag
  (`offroad_ramp`) wirkt nur serverseitig; der Reiseplan rechnet ihn nicht nach.
- **Die Etappe der Anzeige trennt das REISEMITTEL, nicht die Wegart** (live 26.08.2026, Meldung #102).
  `cleanRoutePlanNoiseEntries` (`js/routing/route-plan.js`) sammelt Roh-Etappen bis zum nächsten echten
  Ort — und behielt dabei `type`, `transport` und `flowState` der **ersten**. 💣 Damit erbte eine
  Flussfahrt hinter einem kurzen Stück Weg „Weg" und „zu Fuß", und **drei Rechnungen lesen genau diese
  zwei Felder**: die Flusspassage (`entry.type === "Flussweg"`, `route-costs.js`), das Dach über Nacht
  (`TRAVEL_COST_SHELTER_BY_SUBTYPE`) und der **Reisetag** der Etappe (`avesmapsRouteLegTravelHours`,
  Land 8 gegen Wasser 12). Live gemessen Gareth → Perricum: 3,042 Meilen Weg vor dem Anleger machten
  aus 187,667 Meilen Flussfahrt eine Landetappe — „Weg über Darpat (190,71 Meilen)", 8×
  Gemeinschaftszimmer, keine Flusspassage, und 199 statt 171 Kalenderstunden. 🔴 **Die Wegart trennt
  bewusst NICHT** — Reichsstraße, Straße und Weg hintereinander sind EIN Marsch bis zur nächsten Stadt,
  und das ist der Sinn des Grenz-Laufs. 💣 **Die Regel steht an ZWEI Stellen** (Mitte der Liste und
  Schluss-Absorption); für das Querfeldein wurde die zweite am 01.08.2026 einzeln nachgezogen, nachdem
  der Fall live aufgetreten war. ⚠️ Und die Landetappe trägt seither ihr `transport` — vorher fiel
  `resolveRouteStepTransport` auf die WEGART zurück, was den Reisetag *zufällig* richtig traf und
  deshalb nie auffiel. 🪤 Genau diese Zufallsgleichheit hielt eine Lücke in
  `speed-table-and-rest-rule.test.js` verborgen: dem Test fehlte `VALID_TRANSPORT_OPTIONS`, er
  rechnete also einen Zweig, den die App nie nimmt. 🔴 Der Übergang heißt **„Anlegestelle"** (beide
  Richtungen, auch Fluss→See); die ältere Gelände-Benennung „Abgangs-/Anschlusspunkt" geht vor. Tests:
  `js/routing/__tests__/land-wasser-etappe-trennen.test.js` (Trennung, Benennung, Reisetag) und der
  Abschnitt 5 in `…/travel-costs.test.js` (warum es eine Rechnung ist und keine Beschriftung).
- **Klimazonen** — die **vierte** Landschaften-Ebene (`kind='klima'`): acht Zonen von Polar bis Tropisch (die Zahl ist **Daten**, nicht Code — `AVESMAPS_ECOSYSTEM_REGION_TYPE_SEED`), dazu die Zeile „Klimazone" in der Infobox bei Ort, Region, Weg und — seit 2026-08-12 — **Herrschaftsgebiet** und **Routen-Etappe** (die Etappe liest denselben `path_ecosystem`-Verschnitt wie der Weg, ist also eine fünfte Fläche, aber keine fünfte Quelle). Entwürfe **`docs/superpowers/specs/2026-08-03-klimazonen-design.md`** (§14 ist der Nachtrag von nach dem Bau) und `…-klimazone-infobox-design.md`, Bauplan `docs/superpowers/plans/2026-08-03-klimazonen.md`. 💣 Sechs Fallen, alle dort begründet: die Trennlinien (`ecosystem_climate_divider`) sind die Wahrheit, die Bänder sind ABGELEITET · ein Band darf nie als Polygon bearbeitet werden (`avesmapsClimateAssertNotDerived`) · eine Trennlinie darf zurücklaufen (Überhang), sich aber nicht selbst schneiden · eine Zone einzuschieben darf keine vorhandene Grenze bewegen (`south_type_key`) · VIER Formen, VIER Quellen (Ort = genau eine Zone aus dem Payload, Region = anteilig aus `ecosystem_region_overlap`, Weg = abschnittsweise aus `path_ecosystem`, Herrschaftsgebiet = anteilig aus `ecosystem_region_territory` über `territory-detail.php`) — ein Zeilenbauer für alle vier (`avesmapsClimateRowMarkup`), und die Zeile steht überall direkt unter Flora · der Punkttest fragt die **Bänder**, nicht die Trennlinien. ⚠️ `map_revision` deckt das im ETag NICHT ab — `avesmapsClimateReadStamp()` steht mit im Seed von `map-features.php`. Kein Reise-Effekt in dieser Fassung.
- **Der Prüfhaken „Keine Wiki-Zuweisung“** — was auf der Karte liegt und keinen Wiki-Artikel zugewiesen hat, wird **rot markiert**: Ring am Ort, Saum am Weg, Kontur an der Landschaftsfläche, Halo an der Beschriftung. Owner 01.09.2026: „das objekt ohne wiki-eintrag auffällig rot markieren“, egal ob Ort, Fläche oder Weg. Live 01.09.2026. Im Browser gemessen: **982** offene Orte (6 nachgesehen, 1923 zugewiesen, 2079 Kreuzungen ausserhalb) · **268** Wege (3798 ohne Menschennamen ausserhalb) · **889** Landschaftsflächen (8 Klimabänder ausserhalb) · **361** Beschriftungen. 🔴 **ER BLENDET ALS EINZIGER DER GRUPPE NICHTS EIN**, und das nimmt den Owner-Entscheid vom 14.08.2026 („ein Prüfhaken ZEIGT seine Funde“, `resolveLocationCheckFinding`) für diesen einen Haken ausdrücklich zurück: jener gilt für eine Handvoll Anbindungslücken, die man sonst wegzoomt — 982 von 2911 Orten sind ein DRITTEL des Bestands, eingeblendet wäre das keine Fundstelle mehr, sondern eine zweite Ortsebene an den Zoombändern vorbei. Der Auftrag lautete „markieren“, nicht „einblenden“; bei den Landschaftsflächen ginge „einblenden“ ohnehin nicht (die gibt es nur in ihrer Ansicht). Deshalb ruft **nur der Icon-Bau** `resolveLocationWikiMark`, **nie** `shouldShowLocationMarker` — ein Test schneidet die Funktion heraus und hält das fest. 💣 **Der Weg bekommt einen SAUM, keine rote Mitte.** „Offene Wegenden“ färbt `style.center`; zwei Mittenfärbungen hiessen, dass stumm die untere Zeile gewinnt und ein Haken wirkungslos aussieht. So sprechen beide auf demselben Weg — rote Mitte plus dunkler Saum, in ZWEI Tönen (`--color-path-open-end` gegen `--color-check-no-wiki`, bewusst nicht derselbe: sie treffen sich auf demselben Objekt). 💣 **DIE NAMENSFRAGE BEANTWORTET DER PRÜFER NICHT SELBST.** „Trägt dieser Weg einen von Menschen gegebenen Namen?“ war zunächst als **vierte** Abschrift derselben Regel nachgebaut (PHP `avesmapsWikiPathNameIsGeneric` · `shouldShowRoutePathDisplayName` · `getPathTitleName`) und las `properties.name` — im Browser trägt das den MASCHINENnamen, `normalizeRoutePathFeature` schreibt ihn beim Laden auf `<Art>-<n>` um und legt den echten nach `original_name` (`map-features-path-prepare.js`). Der Haken erklärte damit **ALLE 6041 Wege** für „nicht gemeint“ und färbte keinen einzigen — **während beide Tests grün waren**, weil sie die ROHE Nutzlast nachbauten, und dort stimmte die Zeile noch. ⭐ Behoben nicht durch Reparatur, sondern durch Streichung: `avesmapsWikiZuweisungWeg(properties, hatEchtenNamen)` nimmt die Antwort als Parameter, im Browser von `getPathTitleName`. **Wer die Antwort hereinreicht, kann kein Feld verwechseln.** 💣 **`hatEchtenNamen !== true`, strikt** — der Aufrufer reicht ein Vergleichsergebnis herein; wahrheitswertig geprüft wäre ein durchgereichter NAME still ein Ja. 💣 **Gemessen wird das ZUWEISUNGSNEST, nie `wiki_url`** (der Lesepfad rät das bei Leere per Namen nach: 99 Phantome bei den Orten, 12 bei den Wegen) — dieselben vier Familien wie beim Statuskreis. 🔴 **Der Label-Halo braucht die scharfe KONTUR**, nicht nur den weichen Schein: am Bild entschieden (fünf Labelarten × fünf Stärken) las sich das Rot bei `region` (#ffffff) und `gebirge` (#e2ddd2) deutlich, bei `wald` (#bfeec8) und `see` (#b9e7ff) kaum — `strokeRatio` 0.06 → 0.13. ⚠️ **Dieselben drei Zahlen im Canvas-Maler der Kurvenlabels** (`kurvenlabelHalo`), sonst lesen sich gebogener und gerader Name derselben Landschaft als zwei verschiedene Befunde; und der Canvas kennt kein `glowPasses`, dort trägt die Kontur allein. ⚠️ **16 der 361 offenen Beschriftungen sind Kurvenlabels** — ein Prüfhaken mit einer stillen Ausnahme ist schlimmer als keiner, man hielte die Lücke für geprüft. 💣 **Das Nachziehen fasst alle vier Flächen in EINEM Aufruf** (`avesmapsSyncWikiZuweisungCheck`); getrennt stünde nach dem Umlegen die halbe Karte im alten Zustand — genau das hat „Offene Wegenden“ am 22.08.2026 gekostet. Die Kurvenlabel-Ablage ist nur nach Zoom und Ausschnitt gestempelt und braucht deshalb ihren eigenen Verwerfer. ⚠️ Kosten gemessen: `getPathTitleName` über alle 6041 Wege **4 ms**, ein voller Durchgang **111 ms** in der Landschaftsansicht (der Nachbarhaken zahlt allein 465 ms für seinen Index). Bauteile: `js/map-features/wiki-zuweisung.js` (rein, kein DOM, kein Modulzustand) und `…/map-features-wiki-zuweisung-check.js` (Schalter, Farbtoken, Nachziehen) — dieselbe Zweiteilung wie bei `map-features-open-path-check.js`. Tests: `js/map-features/__tests__/wiki-zuweisung{,-verdrahtung}.test.js`. 🪤 **Und die Commit-Falle, die ihn erwischt hat:** seine 16 Dateien lagen sauber einzeln gestaged im GETEILTEN Index, als eine andere Sitzung committete — sie sind deshalb in `97e24d5ad docs(quellen): Mockup auf das Korpus-Modell …` gelandet, unter einem Betreff, der von diesem Haken kein Wort sagt; `git log --grep` findet ihn dort nie, und die nächtliche Handbuch-Routine liest genau diese Betreffzeilen. §9 sagt „never `git add -A`“ und meint den Schaden, den man ANDEREN zufügt. **Die Kehrseite fehlte: ein Index gehört auch dem, der ihn nicht gefüllt hat.** ⭐ Also: `git add` und `git commit` in EINEM Zug, nie mit einer Prüfung dazwischen — und wer vor dem Commit noch etwas messen will, misst es vorher.
- **Die Beschriftung erbt die Wiki-Landschaft ihrer Fläche** (Owner 01.09.2026, nach dem ersten Befund des Prüfhakens darüber). 🔴 **EINE Landschaft trägt ihre Zuweisung an ZWEI Stellen** — an der Fläche (`ecosystem_region.wiki_region_key`) und an jeder ihrer Beschriftungen (`properties.wiki_region`); der Statuskreis führt sie seit jeher als zwei unabhängige Bits. 🪤 **Und genau das war die Verwechslung, die den Fall erzeugt hat:** der Kasten „Wiki-Landschaft" im Landschaftsdialog gehört der **Fläche** (`effectiveWikiRegion`, `map-features-ecosystem-properties.js`) — er zeigte „zugewiesen", während der Prüfhaken die **Beschriftung** rot markierte, und beide hatten recht. Live gemessen: 745 Flächen-Beschriftungs-Paare, davon **12** mit zugewiesener Fläche und leerer Beschriftung; bei **7** lag die Zuweisung auf einem zweiten, gleichnamigen Label **ohne** Fläche (Nordwalser Höhen, Grüne Ebene, Dalvrettawald, Altnordener Forst, Baliiriwald, Dünthark, Fech'Adajant) — fünf davon in den zwölf Stunden davor entstanden, weil ein neu gezeichnetes Flächenlabel leer auf die Welt kommt und das alte den Artikel behält. 🔴 **Die Regel wandert NUR ABWÄRTS und löscht NIE:** hat die Fläche keinen Schlüssel, bleibt die Beschriftung unangetastet — andersherum nähme jedes Speichern einer wiki-losen Region still die Zuweisung zurück, die „Label zuweisen" (V6c) von Hand gesetzt hat. 🔴 **Seit 03.09.2026 mit der EINEN Ausnahme, die der Owner am Bild bestellt hat („Lawaralîr" sollte vom Wiki gelöst und „Cronwald" heissen — je nachdem, ob man Fläche oder Beschriftung anklickte, war die Landschaft gelöst oder nicht, und die Infobox zeigte weiter den alten Artikel): ein AUSDRÜCKLICHES Entfernen wandert mit, in beide Richtungen.** Nennt der Rumpf `wiki_url` und meint es leer (`avesmapsEcosystemWikiAusdruecklichEntfernt`), nimmt `avesmapsEcosystemClearWikiRegionFromLabels` ALLEN Beschriftungen der Fläche ihre Kopie — ein Rumpf ohne `wiki_url` (blosses Umbenennen) nimmt weiterhin nichts zurück; die zwei Fälle auseinanderzuhalten ist der ganze Inhalt der Weiche. 💣 Aufwärts wandert die Zuweisung nur noch, wenn sie im Beschriftungsdialog ANGEFASST wurde (`wikiGeaendert` aus dem Rumpf, `ecosystemRegionWriteBackPayload`), dann aber auch als Rücknahme (leere Adresse). Das stille Hochheilen bei jedem Label-Save ist gefallen, und zwar gemessen: das vereinigte Fenster schickt BEIDE Formulare ab, der Beschriftungs-Rumpf brachte sein GELADENES Nest mit (der Kasten war verborgen, „es gewinnt die Fläche") und der Rückweg trug es an die Fläche zurück, die es gerade entfernt hatte. `buildLabelEditPayload` schickt `wiki_region` seither nur, wenn es seit dem Laden angefasst wurde (`getLabelWikiRegionGeaendert`, Inhalt statt Schlüssel, weil „Sync übernehmen" dasselbe Nest mit neuen Werten baut) oder beim Anlegen. 🔴 Und die Antwort von `update_region`/`assign_wiki_region` trägt die nachgezogenen Beschriftungen (`labels`, in der Form von `update_label`, gebaut über `avesmapsBuildFeatureResponseFromStoredFeature` — kein zweiter Bauer); der Browser wendet sie sofort an (`applyLabelFeaturesLocally`, über `applyLabelFeatureResponse`, damit eine Kurve überlebt), sonst zeigte die Infobox bis zum nächsten Live-Abgleich den Artikel, den die Fläche gerade verloren hat. 🪤 Nebenbefund: der Panel-Weg „Zuweisung entfernen" (`assign_wiki_region` mit leerer Adresse) lief mit `$wikiKey = null` in den Durchtrag und brach dort unter `strict_types` mit einem TypeError ab — 500 nach geschriebener Fläche; die Weiche behebt das mit. 🪤 **Zwei Nebenbefunde aus der Browser-Abnahme am selben Tag, beide von keinem Test gesehen:** die Antwort eines Schreibwegs trägt weder Kurve noch `ecosystem_region_kind` (beides entsteht nur im LESEPFAD, `ecosystem-label-link.php`) — nach dem Speichern stand der „Cronwald" als einziges Label mit Fläche und ohne Ebene da und war aus der Vegetations-Ebene VERSCHWUNDEN (`isLabelOfActiveEcosystemLayer` zeigt nur die eigene Ebene); `avesmapsLabelEbeneErgaenzen` hält sie seither in beiden Anwendern der Antwort, wie der Kurven-Erhalt daneben. Und das offene Infopanel zog nach einem Label-Save nie mit, obwohl der Label-Klick ihm einen Bauer gibt — `applyLabelFeatureResponse` ruft jetzt `avesmapsRefreshInfopanel`. Tests: `ecosystem-label-wiki-durchtrag-test.php` (§14–16), `js/review/__tests__/label-wiki-entfernen-durchtrag.test.js`, `js/map-features/__tests__/wiki-entfernen-sichtbar-verdrahtung.test.js`. 💣 **Sie steht SERVERSEITIG** (`avesmapsEcosystemPushWikiRegionToLabels`, `api/_internal/app/ecosystem.php`) und hängt an BEIDEN Schreibwegen — `assign_wiki_region` und `update_region` —, weil sie vorher nur der clientseitige Durchtrag im Flächendialog kannte (`renameLinkedEcosystemLabel`) und der Panel-Knopf „Fläche zuweisen" an ihr vorbeilief: dieselbe Ein-von-mehreren-Erzeugern-Falle wie bei der Verkehrsmittel-Sperre (§11). 💣 **Geschrieben wird nur, was sich wirklich ÄNDERT**, und daran hängt die Kartennutzlast: ein Label-Save bumpt `map_revision` und macht die ~21 MB für JEDEN Besucher ungültig — die eine Regel, für die `ecosystem.php` existiert. Beim echten Schreiben ist der Bump trotzdem Pflicht (Labels reisen in genau dieser Nutzlast; ohne ihn behielte jeder warme Client den roten Namen über sein 304) — dieselbe Ausnahme wie bei `avesmapsEcosystemDeleteLabels`. ⭐ **Gelesen wird der GESPEICHERTE Stand** (`$after`), nicht der Rumpf: damit heilt jedes Speichern einer Region eine bestehende Lücke nebenbei mit. Für den Bestand gibt es zusätzlich den Lauf **„Wiki-Zuweisung"** im „Rechnen ▾"-Menü des Landschaften-Editors (`push_wiki_regions_to_labels`) — Trockenlauf als Vorgabe, scharf über dieselben zwei Signale wie `assign_wiki_region`, und er entscheidet **nichts selbst**: er wählt nur aus und reicht jede Region durch denselben Durchtrag. ⚠️ Seine Auswahl ist EIN Durchgang über alle Beschriftungen, kein LIKE-Scan je Region (427 Regionen mit Schlüssel, ~1000 Beschriftungen). ⚠️ Die zwei `require_once` auf `wiki/{sync,regions}.php` stehen **im Funktionsrumpf**, nicht am Dateikopf: `ecosystem.php` hängt am öffentlichen Lesepfad, und `api/edit/wiki/dump.php` lädt `regions.php` mit einem BLANKEN `require` — am Kopf wäre das beim ersten Umsortieren dort ein Fatal mit leerem Rumpf. Tests: `api/_internal/app/__tests__/ecosystem-label-wiki-durchtrag-test.php` (gegen 15 Mutationen gefahren, alle gefangen; beide Nähte per Reflection bzw. Quelltext festgenagelt). ⭐ **Und seit 02.09.2026 wandert auf demselben Weg die ART mit** (Owner: „zieh die art der fläche an die beschriftung nach"): live trugen **31** Beschriftungen `region`, während ihre Fläche eine andere Art hatte — 27 Wälder. Zwei Folgen: der Name wird weiss statt im Grünton gezeichnet, und die Dubletten-Regel des Konfliktzentrums vergleicht die ART und sah doppelte Waldlabels deshalb nicht. 🔴 **Der Erzeuger war der LANDSCHAFTEN-EDITOR** — er ändert die Art über `update_region` und zieht das Label nicht nach, anders als der Flächendialog auf der Karte. 💣 **Geschrieben wird nur `feature_subtype`, nie Grösse und Ab-Zoom** — gemessen: 30 der 31 tragen den Stil ihrer Art bereits korrekt (er wird beim ANLEGEN gesetzt), der eine Ausreisser sieht nach Handarbeit aus, und die Stiltabelle `ECOSYSTEM_LABEL_STYLE_BY_TYPE` lebt im BROWSER — eine Serverkopie wäre die zweite Wahrheit aus §5. ⚠️ **Der Preis ist benannt:** wer die Art im Landschaften-Editor wechselt, bekommt die neue Farbe, aber Grösse und Ab-Zoom der alten; im Flächendialog kommt beides mit. (`avesmapsEcosystemPushRegionTypeToLabels`; der Menüknopf heisst seither **„Wiki & Art"** und fährt beide Läufe über `avesmapsEcosystemPushRegionDataToLabelsAll` — 🔴 die AKTION heisst weiter `push_wiki_regions_to_labels`, weil eine gecachte Editorseite genau diese Zeichenkette ruft.) 🔧 **Offen:** die sieben gleichnamigen Zwillingslabels ohne Fläche sind damit NICHT beseitigt — sie tragen denselben Artikel wie die Beschriftung ihrer Fläche und gehören ins Konfliktzentrum (`label.duplicate` verlangt bislang einen gleichen Wiki-Schlüssel auf BEIDEN Seiten und sieht sie deshalb nicht).
- **„Freie Labels markieren" — ein SCHEINWERFER, kein Prüfhaken.** Das Auswahlfeld unter „Mapstil" im Anzeige-Menü zieht einen roten Kasten um jede freie Beschriftung der gewählten Art. Owner 02.09.2026: „alle freien labels vom typ z.B. ‚Fluss‘ markieren (roter rahmen) […] mein ziel ist eigentlich die zu entfernen, die wir doppelt auf der karte haben (z.B. Inoscha)". Live 02.09.2026. 🔴 **ES STEHT BEWUSST NICHT IN DER GRUPPE „Prüfen", und das ist die Aussage:** die acht Haken dort kennen jeder einen BEFUND und rechnen ihn aus — hier gibt es keinen. Welche zwei Namen dieselbe Sache meinen, entscheidet ein Mensch: von den **19** Fluss-Beschriftungen tragen **6** auch einen gleichnamigen Flussweg (Escarra, Horne, Inoscha, Rôn, Selke, Weidenbach), die übrigen **13** (die Mhanadi-Arme, Andrafälle, Donnerfall …) haben keinen und sind berechtigt — eine Maschine daraus meldete 13 Fehltreffer. Es ist eine Einstellung der Anzeige wie der Mapstil. 🪤 **Und „Inoscha" ist gar keine Label-Dublette:** der Name steht **einmal** als freie Beschriftung und **einmal** als Name seines Flusswegs, beide praktisch auf demselben Punkt (459,7/458,0 gegen die Wegmitte 459,7/457,4). Wer nur `labelData` gegen sich selbst hält, findet den gemeldeten Fall nie — gesucht werden muss gegen die WEGE. Die andere Sorte gibt es auch: **34** Namen stehen zweimal als freies Label da (Yaquirtal viermal, Hexenwald und Ingvaltal dreimal), 17 davon mit gleicher Art. 💣 **EIN KASTEN, KEIN SCHEIN — die tragende Entscheidung.** Der Prüfhaken „Keine Wiki-Zuweisung" färbt seit dem 01.09.2026 den HALO derselben Beschriftungen rot. Beide sind gleichzeitig einschaltbar; unterschieden wird an der FORM, nicht am Ton (Schein = „fehlt etwas", Kasten = „das ist die Art, die du suchst"). Wer sie je zusammenlegt, nimmt dem Editor die Antwort auf „warum ist der rot?". Eigener Token `--color-check-free-label` (#d1005d). 💣 **Der Ton ist gegen ALLE DREI Rots der Karte gemessen, nicht nur gegen den einen Nachbarn im Diff:** der erste Entwurf stand auf #e0342f und war von `--color-marker-waypoint` (#e33b35) **nicht zu unterscheiden** — RGB-Abstand **9,7** bei unter 1° Farbtondifferenz, also genau die Kollision, die `--color-path-open-end` wenige Zeilen darüber ausdrücklich vermeidet („das Rot gehört den Reise-Wegpunkten“). Jetzt Wegpunkt 73,5 · offenes Wegende 64,8 · Wiki-Zuweisung 73,2; die einzige freie Nische im Rot der Karte ist der KALTE Rand (Karmin), weil die drei anderen den neutralen und den warmen belegen. ⚠️ Gefunden hat das der `usability-design`-Agent, nicht der eigene Test — der prüfte nur den einen Nachbarn, den der Autor im Kopf hatte. 💣 **`outline`, NICHT `border`:** das Label ist ein 0×0-divIcon, sein Bild trägt ein `transform: translate(-50%, -50%)` — ein Rahmen im Kasten-Modell schöbe es um seine eigene Breite vom Fleck, den der Kollisionsdurchgang gerechnet hat. 💣 **ZWEI Zeichenflächen, und die zweite fällt sonst still aus.** Der waagerechte Name ist ein divIcon (CSS-Klasse `map-label--markiert`), ein GEBOGENER wird auf Canvas gemalt und hat gar keinen Marker — dort zieht `zeichneMarkierungsrahmen` ein Rechteck um die Buchstabenlagen (`glyphsHullBox`, dasselbe Polster wie die Klickfläche daneben, **keine dritte Zahl**). Live sind das **85 von 1017** Beschriftungen, darunter **44 der 75** Gebirgsnamen — dieselbe Lücke, die schon der rote Schein einzeln nachziehen musste. Nachgezogen wird beides in EINEM Aufruf (`avesmapsSyncFreieLabelMarkierung`): getrennt trüge der gerade Name derselben Landschaft den Kasten und der gebogene nicht, und das liest sich wie ein Befund. ⚠️ **Die Kurvenlabel-Ablage ist nur nach Zoomstufe und Ausschnitt gestempelt** — am Feld gedreht ändert sich keins von beiden, sie wird deshalb ausdrücklich verworfen. 🪤 **ES BLENDETE ZUERST NICHTS EIN — UND DAS IST NOCH AM SELBEN TAG UMGEDREHT WORDEN.** Der Auftrag lautete „markieren", nicht „einblenden", also galt das Zoomband weiter; der Preis war gemessen und benannt (die Fluss-Labels tragen `minZoom: 5`, wer bei Zoom 4 „Fluss" wählte, sah **nichts**). Genau daran ist der Owner hängengeblieben: „mach dass die anzeigen zoomstufen unabhängig sind (sprich, dass ich auf allen zoomstufen die markierungen sehe)". Seither hebt **jede** Markierung das Zoomband auf — siehe den eigenen Eintrag „Eine Markierung hebt das Zoomband auf" weiter unten. ⭐ Die Lehre ist älter als dieser Tag und stand schon da: **ein Prüfhaken ZEIGT seine Funde** (Owner 14.08.2026). 💣 **Die Zeilen des Felds sind DATEN, keine Auszeichnung** — sie entstehen aus `labelData` samt Anzahl (`Fluss (19)`, `Wald (175)`), im Markup stehen nur „Keine" und „Alle". Eine Liste von Hand wäre beim nächsten neuen Subtyp still unvollständig; und **eine Art, die `avesmapsLabelArtName` nicht kennt, bekommt trotzdem ihre Zeile** unter dem rohen Schlüssel — eine fehlende Zeile in einer Auswahlliste meldet nichts, sie ist einfach nicht da. Gebaut wird in `prepareLabelData`, nicht im Startlauf: die Funktion läuft auch nach einem Live-Abgleich, und die gewählte Art überlebt das (fällt aber auf „Keine", wenn es sie nicht mehr gibt). 🔴 **KEIN URL-PARAMETER, anders als die acht Prüfhaken.** Die reisen im geteilten Link mit, weil ein Editor einem anderen einen Befund zeigen will; hier gibt es keinen zu zeigen. Der `IS_EDIT_MODE`-Riegel steht trotzdem im Modul und nicht nur am verborgenen Menüeintrag. Bauteile: `js/map-features/freie-label-markierung.js` (rein, kein DOM, kein Modulzustand) und `…/map-features-freie-label-markierung-check.js` (Feld, Farbton, Nachziehen) — dieselbe Zweiteilung wie bei `wiki-zuweisung.js`. Tests: `js/map-features/__tests__/freie-label-markierung{,-verdrahtung}.test.js` (zusammen gegen 19 Mutationen gefahren, alle gefangen; der Verdrahtungstest FÄHRT das Schalter-Modul mit gefälschtem DOM, statt seinen Quelltext zu lesen). 🔧 **Offen:** das Feld findet die 6 Fluss-Dubletten, aber es benennt sie nicht — „welcher Name steht auch an einem Weg?" wäre eine eigene Regel, und die 34 gleichnamigen Doppellabels gehören ohnehin ins Konfliktzentrum (`label.duplicate` verlangt bislang auf beiden Seiten denselben Wiki-Schlüssel und sieht sie nicht).
- **Der Prüfhaken „Doppelte Beschriftungen" — und die Markierung, die das Zoomband aufhebt.** Owner 02.09.2026, unmittelbar nach dem Scheinwerfer darüber: „ja mach den haken für doppelte beschriftungen und mach dass die anzeigen zoomstufen unabhängig sind". Live 02.09.2026. 🔴 **ZWEI SORTEN DUBLETTE, UND DIE ZWEITE IST DIE GEMELDETE.** (a) derselbe Name mehrfach als freie Beschriftung — **34 Namen / 72 Beschriftungen** (Yaquirtal viermal, Hexenwald und Ingvaltal dreimal); (b) eine freie Beschriftung UND ein gleichnamiger **WEG** — **9 Beschriftungen**, darunter „Inoscha", der Fall, der den Haken ausgelöst hat. Wer nur (a) baut, findet genau diesen nicht; wer `labelData` nur gegen sich selbst hält, sieht (b) nie. ⚠️ Ein Name, den es nur an WEGEN mehrfach gibt, ist **keine** Dublette — „Reichsstraße 2" liegt in 57 Abschnitten. 💣 **DER ECHTE WEGNAME KOMMT VON `getPathTitleName` UND WIRD NICHT NACHGEBAUT.** Im Browser trägt `properties.name` eines Weges den MASCHINENnamen (`normalizeRoutePathFeature` schreibt beim Laden `<Art>-<n>` hinein) — dieselbe Falle, die den Prüfhaken „Keine Wiki-Zuweisung" am 01.09.2026 gekostet hat, dort mit zwei grünen Tests daneben. Die Namen werden in die reine Regel **hereingereicht**: wer die Antwort übergibt, kann kein Feld verwechseln. 🔴 **ER STEHT IN DER GRUPPE „Prüfen", der Scheinwerfer nicht** — dieser rechnet einen Befund aus, jener leuchtet eine gewählte Art an. Deshalb reist er auch im geteilten Link mit (`?toggleDuplicateLabels=1`, wie seine sieben Nachbarn), während der Scheinwerfer das bewusst nicht tut. 💣 **DER TRICHTER IST DIE TRAGENDE STELLE.** Zwei Werkzeuge ziehen einen Kasten, und es gibt DREI Leser — der waagerechte Name (CSS-Klasse), der gebogene (gemalter Rahmen auf Canvas) und die Sichtbarkeit. Das sind sechs Stellen mit derselben Frage, und die nächste Markierung wäre die neunte; genau daran ist in diesem Haus schon die Verkehrsmittel-Sperre gescheitert. Alle drei fragen deshalb **nur** `avesmapsLabelMarke` (`js/map-features/label-markierungen.js`), und ein drittes Werkzeug kostet eine Zeile in `AVESMAPS_LABEL_MARKIERER`. 🔴 **Der BEFUND schlägt den Scheinwerfer** — die Reihenfolge in dieser Liste IST die Regel; wer eine Art anleuchtet und zugleich Dubletten sucht, kennt die Art schon (er hat sie gewählt). Live gegengemessen: 33 violett, 86 karmin, **0 mit beiden Klassen**. 💣 **VIOLETT, NICHT EIN FÜNFTES ROT.** Das Rot der Karte ist voll (Wegpunkt, offenes Wegende, Wiki-Zuweisung, Scheinwerfer); `--color-check-duplicate-label` (#6a1b9a) ist gegen **sieben** gleichzeitig sichtbare Töne gemessen, Minimum 76. ⭐ Der Test führt sie als benannte Liste und nicht als Einzelvergleich — die Lehre aus dem Scheinwerfer, dessen erster Ton vom Wegpunkt-Rot 9,7 entfernt war, während sein Test nur den einen Nachbarn prüfte, den der Autor im Kopf hatte.
- **Eine Markierung hebt das Zoomband auf** (Owner 02.09.2026: „dass ich auf allen zoomstufen die markierungen sehe"). Ein Name mit einer Marke wird gezeichnet, auch wenn seine eigene Erscheinungsstufe ihn ausschließt — live gegengemessen an „Rakulahöhen" (Band 7–7) bei Zoom 3: `avesmapsLabelImBand` sagt `false`, der Marker steht trotzdem. 💣 **NUR DAS BAND, und das ist der ganze Umfang.** Die vier `return false` darüber bleiben unberührt (Labels-Schalter, „Regionname anzeigen", Regionenfilter, Ebenenwahl) und das Culling am Bildrand darunter ebenso: ein Werkzeug, das einen AUSGESCHALTETEN Schalter überstimmt, ist keine Hilfe mehr, sondern eine Überraschung — und ohne Culling würden 1017 Marker für die ganze Karte gebaut statt für den Ausschnitt. 🪤 Genau diese zweite Hälfte hat die Mutationsprobe gefunden: der Test kannte die Regel „nur das Band" nur als Kommentar, und ein `markiert || isLatLngInRenderBounds(…)` blieb grün. 💣 **`(markiert || avesmapsLabelImBand(…))`, nie ersetzt** — sonst verschwände jede UNmarkierte Beschriftung. 💣 **Und beide Schalter müssen `syncLabelVisibility` mitrufen**, nicht nur die Icons neu bauen: sie ändern seither die SICHTBARKEIT, und ohne den Aufruf kämen die Funde außerhalb ihres Bandes erst beim nächsten Zoomschritt — was wie ein verschluckter Klick aussieht, nicht wie eine fehlende Zeile. Bauteile: `js/map-features/doppelte-beschriftung.js` (rein), `…/label-markierungen.js` (der Trichter), `…/map-features-doppelte-beschriftung-check.js` (Haken, Index, Nachziehen). Tests: `doppelte-beschriftung{,-verdrahtung}.test.js` und `label-markierungen.test.js` — zusammen mit den zwei Scheinwerfer-Tests gegen **25 Mutationen** gefahren, alle gefangen. 🔧 **Offen:** die Zeile im Menü nennt keine Zahl (die sieben Nachbarn auch nicht), und der Haken markiert nur — er sagt nicht, WELCHE zwei zusammengehören.
- **Die Strömungsdreiecke liegen UNTER jeder Beschriftung** (Owner 02.09.2026: „Dreieck zur Anzeige von Strömungen auf Flüssen sind über den Labels, sollten aber darunter einsortiert sein"). Die Pfeil-Pane (`map-features-river-flow-arrows.js`, nur im Bearbeiten-Modus) stand auf **639** — unter `labelsPane` (650) und damit scheinbar richtig einsortiert, aber **über dem Wegenamen-Canvas** (470), und der trägt ausgerechnet die FLUSSNAMEN: ein Dreieck fiel mitten ins Wort. Jetzt **469**. 💣 **Die niedrigste Beschriftungsfläche entscheidet, nicht `labelsPane`** — genau daran ist der Zustand entstanden, und wer nur gegen die eine prüft, baut ihn wieder ein. ⚠️ Nicht weiter herunter: über `roadsPane` (400) muss es bleiben, sonst säße das Dreieck unter seinem eigenen Fluss. Der Test (`js/map-features/__tests__/stroemungspfeile-unter-labels.test.js`) liest **beide Zahlen aus dem Code**, statt eine erwartete festzuschreiben — wer eine Beschriftungsfläche verschiebt, nimmt die Pfeile mit.
- **Der Prüfhaken „Kreuzungen mit 2 Wegen"** — der türkise Ring sucht den **auflösbaren Durchgangsknoten**, den ein Editor zu `----------` zusammenzieht. Live 15.08.2026; Entwurf **`docs/superpowers/specs/2026-08-15-kreuzungen-pruefhaken-design.md`**, Bauplan `docs/superpowers/plans/2026-08-15-kreuzungen-pruefhaken.md`. Drei Bedingungen, alle in `computeLocationConnectivityIndex` (`js/routing/route-graph-routing.js`): **genau** `SPARSE_CROSSING_WAY_COUNT` Arme · **kein fremder Weg läuft über den Punkt hinweg** · **beide Arme dieselbe Wegart**. Gemessen 921 → rund 95 Ringe; 0/1-Arm-Fälle gehören seither dem pinken „Unverbunden"-Ring, nicht diesem. 💣 **Der Haken zählte im CLIENT-Graphen, gefahren wird auf dem SERVER-Graphen.** `avesmapsAddClientCompatiblePathConnection` (`api/_internal/routing/client-graph.php`) splittet einen Weg an jedem inneren Stützpunkt, der round-5 auf einem Ort liegt — genau die Bauform, die der Editor-Knopf „Ort verbinden und Straße weiterführen" erzeugt. Der Client tat es nicht: 182 von 921 Markierungen galten Kreuzungen, die einwandfrei funktionieren. 🔴 **Der Split gehört NUR in den `transports:"all"`-Zweig** von `addRegularPathToGraph`; im Routing-Zweig wurde er am 20.06.2026 wegen nicht-deterministischer Routen zurückgerollt (Revert `1f9e0b9e`). 💣 **Der Zwilling hat ZWEI Wachen gegen doppelte Stützpunkte** (`client-graph.php:204-207` beim Sammeln, `:223` beim Kantenbau); ohne sie zieht ein doppelt gezeichneter Vertex eine Selbstkante, und die gibt dem Knoten **zwei Phantom-Arme** — die Zahl, auf der alle drei Regeln entscheiden. **24 Kreuzungen im Livebestand** sind davon betroffen. Der Bauplan ließ beide Wachen zunächst aus; wer den Block abschreibt, prüft sie. 💣 **`Kreuzung-N` ist keine Adresse**: der Name entsteht erst im Browser als laufender Zähler über die Payload-Reihenfolge (`prepareLocationData`, `js/routing/routing.js`) und rutscht, sobald jemand eine Kreuzung anlegt, die früher einsortiert. Die Kachel „Kreuzung melden" (⚑, `crossingActionsMarkup` → `reportCrossingWithFeedback` in `map-features-share-pin.js`) kopiert deshalb `Kreuzung (<Wegart>) · <Pin-Link>` und **baut den Index nicht** — ein Popup-Klick zahlt keinen Graphbau über ~5900 Wege (per Spion-Test gewacht). ⚠️ Das Segment-Gitter für Regel 2 läuft die **Strecke** ab, nicht die Hüllbox: gemessen 482.542 → 157.252 Einträge, schlimmstes Einzelsegment 38.760 → 393. `SPARSE_CROSSING_SEGMENT_CELL` muss größer bleiben als `SPARSE_CROSSING_OVERLAY_DISTANCE`, weil die Sonde nur 3×3 Zellen liest — als Zusicherung verankert. 🔴 Beschriftung gewandert, **Kennungen nicht**: `toggleSparseCrossings`, `toggleSparseCrossingsControl`, `.location-visual-marker__shape--sparse-crossing` und `--color-marker-sparse-crossing-ring` heißen weiter wie vorher (dieselbe Trennung wie „Neuigkeiten"/`changelog`). Tests: `js/routing/__tests__/location-connectivity-index.test.js`, `js/ui/__tests__/popup-crossing-report.test.js`.
- **Die WEG-EBENE des Wege-Editors** — ein Weg liegt auf der Karte in Abschnitten (der Schattenbachpass in acht, „Reichsstraße 2" in 57); seit 19.08.2026 ist die Weg-Zeile auswählbar und ihre Eigenschaften gelten für alle Abschnitte. Owner: „für die editoren war/ist es mühselig alle abschnitte zu konfigurieren." Entwurf **`docs/superpowers/specs/2026-08-19-wege-editor-weg-ebene-design.md`**, Mockup `docs/wege-editor-weg-ebene-mockup.html`. 💣 **DIE EINE REGEL: geschrieben wird NUR, was jemand angefasst hat** (`fields` im Rumpf von `update_path_group_details`). Ein uneiniges Feld trägt KEINEN Wert — der Wegtyp steht auf „— gemischt lassen —", ein Fahrtyp auf einem halben Haken samt Zähler („2 von 8"), und ein halber Haken ist ein **eigener Wert, kein „aus"**. Ohne das macht ein Sammel-Speichern jede gewollte Ausnahme lautlos platt — derselbe Fehler wie am 17.08. in `avesmapsUpsertGameLiterature`. 💣 **Der Client schickt die `public_ids`, der Server bildet die Gruppe NICHT nach** (`wpGroupWays` ist die eine Regel; eine zweite Fassung liefe beim ersten geänderten Namen auseinander). 🔴 Ein **einteiliger** Weg behält die Abschnittsmaske. ⚠️ `transport_seasons` behält seinen alten Propagationsweg über den `wiki_key` — zwei Regeln mit verschiedener Reichweite auf demselben Feld wären eine Divergenz. 💣 **Das Gesamt-Höhenprofil braucht eine KETTE, und die Listenreihenfolge ist keine**: die Liste sortiert nach bbox-Ecke. Gebaut wird ein Graph aus den Endpunkten; ein Knoten mit einer Kante ist ein Ende, mit zweien ein Durchgang, mit dreien eine Verzweigung. 💣 **Ein gedrehter Abschnitt tauscht seine vier Profilzahlen PAARWEISE** — sie sind `[Anstieg, Abstieg, steiler Anstieg, steiler Abstieg]` in Speicherrichtung (`avesmapsTerrainProfileForLine`); wer nur die Liste umkehrt, zeichnet eine Kurve, die bergauf läuft, wo der Weg bergab geht — **und die Summen darunter stimmen trotzdem**, es fällt also nicht auf. 🪤 **Eine Rundung ist keine Toleranz.** Der erste Bau rundete die Endpunkte auf 5 Stellen (die Zahl aus `client-graph.php`) und verglich die Ergebnisse — das ist ein Raster: `10,0` und `10,0005` fallen in verschiedene Zellen, `10,0004` und `10,0006` nicht. Jetzt eine echte Distanz (`WP_CHAIN_TOLERANZ` = 0,001 Einheiten = 3 m) über ein Gitter mit 3×3-Sonde. ⚠️ **Die mehrteilige Kurve ist der NORMALFALL** — live gemessen 19.08.2026: 4157 Namensgruppen, davon **416 mehrteilig**, und von denen gehen nur **143 (34 %)** durch; die übrigen zerfallen, fast immer an einer **Lücke** (236), nicht an einer Verzweigung. Eine gebrochene Kette wird deshalb **gezeigt, nicht überbrückt**. Bauteile: `js/pages/wege-editor{,-model}.js`, `avesmapsUpdatePathGroupDetails` (`api/_internal/map/features.php`), `group_detail` (`api/edit/map/paths-editor.php`). Tests: `wege-gruppe-{felder,ablauf,kette}.test.js`, `wege-gruppe-schreiben-test.php`. ⚠️ **Der LESEweg ist am 19.08.2026 vom Owner im Browser bestätigt** — nach einem Absturz beim ersten Klick: `AVESMAPS_PATH_GROUP_DETAIL_MAX` stand hinter dem `try`-Block, und **PHP hoistet Funktionen, aber keine `const` auf Dateiebene**; ein Fatal Error antwortet mit einem LEEREN Rumpf („Unexpected end of JSON input" im Browser — sieht aus wie ein Netzfehler). Gewacht von `const-vor-benutzung-test.php` (repoweit) und `wege-gruppe-detail-test.php` (der Leser wird jetzt ausgeführt — er ging live, ohne dass ihn je etwas angefasst hatte). 🔧 **Offen bleibt das SPEICHERN gegen die echte Datenbank.**
- **Eingeschränkt befahrbare Wege** — ein Weg, den man nicht das ganze Jahr oder nicht mit jedem Reisemittel benutzen kann, trägt seinen Namen auf der Karte **kursiv** und in der Infobox die Zeile **„Einschränkungen"**. Live 01.09.2026; Owner: „viele straßen, insbesondere pässe sind nur eingeschränkt befahrbar […] kursiver name und eine zusammenfassung der einschränkung in deren infobox." Mockup `docs/wege-einschraenkung-mockup.html`. Live gemessen **83 Wege in 150 Abschnitten**. 🔴 **Das ist der ERSTE Leser der Zeitfenster.** `transport_seasons` liegt seit dem 03.08.2026 in der Datenbank und hatte ausserhalb seiner Tests keinen einzigen Aufrufer — die Routenwahl kennt die Jahreszeit bis heute nicht (`route-request.js` schickt weder Monat noch Tag), ein im Firun gesperrter Pass wird also weiterhin verplant. 🔴 **ZWEI Arten von Einschränkung, LANDwege only** (Owner-Entscheid am Mockup): das Zeitfenster („Saljethweg: Peraine 15 – Efferd 30") und die Reisemittel-Sperre („Schattenbachpass: nur zu Fuß"). Fluss- und Seewege bleiben aussen vor, weil dort eine Sperre der Normalfall ist — **564 Flussabschnitte** tragen eine, kursiv gesetzt wäre das die halbe Karte. 💣 **EINE Regel, ZWEI Anzeigen** (`js/map-features/path-einschraenkung.js`): die Kartenschrift fragt „betroffen?", die Infobox zusätzlich nach dem Satz — beide gehen durch dieselbe Funktion. 💣 **Die Schriftzeile des Canvas hatte ZWEI Erzeuger.** Wegenamen und Flussketten bauten ihr `ctx.font` je selbst aus Größe, Gewicht und Familie zusammen; ein `fontStyle` im Stilobjekt wäre dort STILL verschluckt worden — der SVG-Rückfall (den kaum jemand sieht) wäre kursiv geworden, die echte Karte nicht. Jetzt `avesmapsPathLabelFontString` als einziger Bauer, per Laufzeit-Test festgenagelt. 💣 **Eine ERWEITERUNG ist keine Sperre.** Die erste Fassung fragte „weicht die Liste von der Vorgabe der Wegart ab?" und meldete damit auch die **zwei** Wege, auf denen jemand ZUSÄTZLICH etwas erlaubt hat (eine von Hand angehakte Kutsche auf einem Pfad) — kursiv, mit einer Zeile, die nichts zu sagen hat. Gefragt wird nur nach dem, was FEHLT (`Vorgabe \ tatsächlich erlaubt`). 💣 **Die Regel gilt dem WEG, gezeichnet werden ABSCHNITTE.** Beim Saljethweg tragen **2 von 7** Abschnitten kein Fenster, beim Schattenbachpass **3 von 7** die Sperre; ohne den Gruppen-Index (Schlüssel wie `wpGroupWays`: `wiki:<key>`, Rückfall `name:<Wegart>:<Name>`) stünde derselbe Weg streckenweise kursiv und streckenweise aufrecht. ⚠️ **Und der Rückfall trennt sichtbar:** zwei Schattenbachpass-Abschnitte tragen keine Wiki-Zuweisung und bilden damit eine eigene Gruppe — sie bleiben aufrecht. Das ist eine **Datenlücke, kein Anzeigefehler**, und wird gezeigt statt überbrückt (dieselbe Haltung wie bei der gebrochenen Höhenprofil-Kette). 🪤 **Die Satzformen der Reisemittel sind eine eigene Liste, und das ist Absicht:** die Namen im Routenplaner sind Substantive für eine Auswahlliste („Reiter zu Pferd mit leichtem Gepäck") und ergäben „Nicht mit Reiter zu Pferd mit leichtem Gepäck". Die Liste ist dieselbe Menge in Satzform, gegen `TRANSPORT_DOMAIN_OPTIONS.land` per Test gehalten, damit ein neues Landreisemittel nicht still herausfällt. 🪤 **„als", nicht „mit", bei den Reisegruppen** — an den echten Daten gefunden: die sechs Mittel sind zweierlei Art (Fahrzeuge „mit Kutsche", Fortbewegungsarten „zu Fuß"), und mit je eigenem „mit" kam zweimal live „Nicht mit Kutsche und mit einer Reisegruppe zu Pferd." heraus. Im Testfeld sah es niemand, weil kein Test zwei Mittel VERSCHIEDENER Art zusammen sperrte. ⭐ Genannt wird die **kürzere** Liste, bei Gleichstand das MÖGLICHE („Nur zu Fuß und zu Pferd." statt „Nicht mit Karawane und Kutsche.") — ein Reisender will wissen, womit er durchkommt. ⚠️ Die Monatsnamen kommen über `routePlanMonthLabel` aus dem `<select>` des Routenplaners; es gibt nur EINE Liste der zwölf. ⚠️ Kosten gemessen: Index-Aufbau **6,7 ms** einmalig, Nachschlagen für alle 6041 Wege **1,4 ms** — gegen 215–836 ms Zoom-Blockade unter einem Prozent. Tests: `js/map-features/__tests__/wege-einschraenkung{,-anzeige}.test.js` (zusammen gegen 9 Mutationen gefahren, alle gefangen). 🔧 **Offen:** die Routenwahl ignoriert die Fenster weiterhin; der SVG-Abzug setzt den Namen nicht kursiv.
- **Zoombänder** — wann eine Ortsklasse auf der Karte erscheint, wie groß ihr Punkt dort ist und wie groß ihr **Label**, ist seit 16.08.2026 keine Konstante mehr, sondern eine **Tafel, die ein Admin einstellt**: sechs Ortsklassen × neun Zoomstufen (z0–z8), zwei Bänder (`marker` in px Außendurchmesser, `label` in pt). Entwurf **`docs/superpowers/specs/2026-08-16-zoombaender-design.md`** (🔴 dessen §0 überschreibt Teile des Restes), Bauplan `docs/superpowers/plans/2026-08-16-zoombaender.md`, Prototyp `docs/zoombaender-mockup.html`. Bedient wird sie über **zwei zieh-bedienbare Plots** im Ortseditor (Kachel „Zoombänder / Zoomlevel aller Orte"), Speichern nur mit Fähigkeit `admin`, Ansehen mit `edit`. 🔴 **Die erste gefüllte Zelle IST die Erscheinungsstufe** — es gibt keine zweite Zahl mehr, die mit ihr auseinanderlaufen kann. Bis dahin stand `0/0/0/1/2/3` zweimal im Code (`minZoomByType` als if-Kette UND `LOCATION_MARKER_RADIUS_SPEC[*].from`). Entfernt sind damit `LOCATION_NAME_LABEL_CONFIG`, `LOCATION_NAME_LABEL_SIZE_BY_ZOOM`, `LOCATION_MARKER_RADIUS_SPEC`, `locationZoomScale`, `getVillageMarkerStyle`, `getBuildingMarkerStyle`, `isVillageMarkerStyleLocation`. 💣 **Die Vorgabewerte reproduzieren das Bild vom 16.08.2026 Ziffer für Ziffer** — beim Ausliefern ändert sich nichts. Gewacht von `js/map-features/__tests__/zoombaender-vorgabe.test.js`, das die abgeschaffte geometrische Kurve als **Zeugen** mitführt; wer diese Kopie „aufräumt", nimmt dem Umbau seinen einzigen Beleg. 💣 **Die Dorf-Zeile war heimlich auch die Straßen-Zeile.** `getLocationNameLabelSize("dorf")` war die Grundgröße der Wege-/Flussnamen (+1 bzw. +3) und der Kraftlinien (+7), und `dorf.minZoom` die Schwelle, ab der Wegenamen erscheinen — an **drei** weiteren Stellen. Beides ist entkoppelt (`PATH_LABEL_BASE_SIZE_BY_ZOOM`, `PATH_LABEL_MIN_ZOOM` in `map-features-path-labels.js`), mit denselben Zahlen. ⚠️ Wer Dörfer verstellt, verstellt seither Dörfer — soll die Straßenschrift je wieder folgen, sind das die zwei Stellen. 🪤 **Warum die drei Stellen fast durchrutschten:** das Inventar-Grep lautete `LOCATION_NAME_LABEL_CONFIG\[` — MIT KLAMMER, und sie greifen per `.dorf.minZoom` zu. **Ein Suchmuster, das eine Zugriffssyntax voraussetzt, findet die andere nie.** Wer etwas restlos entfernt, sucht den blanken Bezeichner. 🔴 **Ablage:** EIN `app_setting`-Schlüssel `location_zoom_bands` (plus `_stamp`), darin `marker`, `label` und `abstaende`. **`null` heißt „auf dieser Stufe gibt es das nicht"**, ein fehlender Wert heißt „nimm die Vorgabe" — die beiden zu verwechseln macht entweder das Ausblenden unmöglich oder löscht die halbe Karte. **Zurücksetzen LÖSCHT die Zeile**, statt eine Kopie der Vorgabe zu hinterlassen (die veraltete sonst). 🔴 **Der Server kennt die Vorgabewerte NICHT** und führt **keine Klassenliste** — er prüft Form und Schranken (Marker 0,5–100 px, Label 4–30 pt), über die Klassen entscheidet der Browser. 💣 Der Schreibvorgang **liest zurück** (`avesmapsZoomBandsWrite`) — dieselbe stille MySQL-Kürzung, an der der Speichern-Knopf der Tempowerte wochenlang nichts tat. ⭐ **Drei globale Abstände** im selben Fenster: **Spalt** (4 px, Marker-Rand → sein Label), **Repel** (2 px, Luft um jedes Label vor der Kollisionsprüfung), **Versatz** (8 px, Weite des kleinen Ausweichschritts — er steckt in **zehn** der zwölf Ausweichstellen, nicht in vier). 🔴 Die **Reihenfolge** der zwölf Ausweichstellen bleibt fest und ist bewusst kein Regler: „links" abzuschalten verschiebt nichts, es **löscht** Ortsnamen, weil der Löser früher aufgibt. ⚠️ **z8 gibt es auf der Karte noch nicht** (`maxZoom: 7` in `js/app/bootstrap.js`); die Werte liegen bereit und erben z7, das Fenster sagt es sichtbar. 💣 Eine Änderung muss die Zeichner-Zwischenspeicher anstoßen — `bumpLocationNameLabelStyleRevision` UND `bumpLocationMarkerStyleRevision`: beide Icon-Schlüssel kennen Zoomstufe und Warnring, aber **nicht die Größe**. Die zweite Hälfte dieser Falle wurde beim Bau zuerst übersehen. ⚠️ Der öffentliche Leser `GET /api/app/zoom-bands.php` fällt **offen** aus (jeder Fehler ⇒ `bands: null` ⇒ Vorgabe) und macht **kein DDL**. Bauteile: `js/map-features/location-zoom-bands.js` (die einzige Quelle der Vorgabe), `api/_internal/app/zoom-bands.php`, `api/edit/map/zoom-bands.php`, das Fenster in `html/wiki-sync-settlement-editor.html`. Tests: `zoombaender-vorgabe`, `-zusammenfuehrung`, `-erscheinungsstufe`, `-abstaende`, `wegenamen-grundgroesse`, `zoombaender-dialog`, `api/_internal/app/__tests__/zoom-bands-test.php`.
- **Landschaftsnamen weichen einander aus** — ein freies Kartenlabel (Landschaft, Meer, Gipfel) bekam bis zum 31.08.2026 **neun feste Ausweichstellen** mit höchstens ±12 px waagerecht und ±8 px senkrecht; bei 187–304 px Namensbreite bewegte das nichts, und drei Namen auf einem Punkt blieben vollständig gestapelt (Owner-Meldung mit Bild, „Grüne Zwillinge“). Seither weicht er auf einem **Ring** aus — Schrittweite **Versatz**, gedeckelt durch **Drift**, dazu **Repel** als Luft um jeden Namen; alle drei einstellbar im Fenster **Darstellung** des Landschaften-Editors, Abschnitt `abstaende` derselben Tafel wie Farbe, Größe und Zoomband. Entwurf **`docs/superpowers/specs/2026-08-31-landschaften-label-kollision-design.md`**, Mockup `docs/landschaften-label-kollision-mockup.html`. 🔴 **DIE OWNER-REGEL VOM 28.07.2026 IST UMGEDREHT: ein Flächen-Label wird jetzt AUSGEBLENDET, wenn es keinen Platz findet.** Sie stand da, weil das Label „der einzige garantierte Anfasser seiner Fläche“ war — seit dem 23.08.2026 trägt es im Bearbeiten-Modus ohnehin keinen Marker mehr, und der Weg zu ihm führt über das Kontextmenü der FLÄCHE („Beschriftung bearbeiten“). Der Grund ist damit entfallen, die Owner-Regel von den verwaisten Außenhüllen bleibt gewahrt. 💣 **`gruppe` TRUG ZWEI BEDEUTUNGEN, und nur eine ist geblieben**: „darf mit Namen DERSELBEN Fläche überlappen“ (Finsterkamm) bleibt, „wird nie ausgeblendet“ fällt. Beide hingen an derselben Zeile (`const gesetzt = gruppe !== ""`) — wer sie wieder zusammenzieht, nimmt einem Gebirge seinen zweiten Namen, und das fällt niemandem auf. 💣 **Der Deckel gilt JE EINTRAG, nicht je Aufruf**: Orts- und Landschaftsnamen liegen in EINEM Durchgang (`resolveLabelCollisions`) und haben zwei verschiedene Regler; `eintrag.maxDrift` schlägt die Aufrufoption. 💣 **Eine Stelle OHNE `drift` bleibt ungedeckelt** — gefallen ist nur das `relativ &&` vor der Typprüfung. Der schlimmste Fall ist damit „ein Name weicht weiter aus als gedacht“, nicht „alle Namen verschwinden“. 🪤 **Der Landschaftsname las bis dahin den Repel-Regler der ORTSCHAFTEN** — `measureLabelCollisionRect` fiel für ALLE Elemente des Durchgangs auf `avesmapsLocationLabelSpacing("repel")` zurück. Wer im Fenster „Orte → Darstellung“ daran drehte, verschob längst auch Landschaftsnamen, und das stand nirgends. 🪤 **UND DIE MESSFALLE, die den Vorgabewert um 16 px danebenlegte:** `createLabelIcon` rendert das Namensbild **MIT Halo** (`getLabelHaloParams`); ein blanker `renderMapLabelToImage`-Aufruf, wie ihn eine Messung naheliegenderweise macht, liefert ein Bild, das **6 px schmaler und 6 px höher** ist — bei einem 40 px hohen Namen 15 %. Die Simulation meldete daraufhin „alle drei Namen stehen bei 56“, der Browser verlor einen; beide Zahlen sahen wie eine Messung aus. ⭐ Die Gegenprobe kostet nichts: das gerechnete Bildmaß gegen die `width`/`height` des `<img>` im DOM halten, bevor irgendetwas darauf aufgebaut wird — ein systematischer Versatz über ALLE Namen ist sofort als Pfadfehler erkennbar. 🔴 **Vorgabe Drift 72**, im laufenden Browser gemessen: bei 40 steht einer der drei Namen, bei 56 zwei, ab 64 alle drei — die 72 gibt dem Fall eine Stufe Luft, weil 64 ihn auf den Pixel genau löst. Über den Bestand (~900 Namen) verschwinden an z4 acht statt heute 37, und die heute 29 überlappenden gibt es nicht mehr. 🔴 **Kein dritter Löser** — es bleibt bei den zwei Durchgängen, die es gibt. ⚠️ Global über alle 28 Landschaftsarten, nicht je Art: einen Unterschied hat nie jemand entschieden, und eine geratene Vorgabe sähe aus wie eine getroffene. Bauteile: `avesmapsFreeLabelCandidatePlacements` (`js/map-features/label-placement.js`, rein), `avesmapsEcosystemDisplayAbstand` (`js/map-features/ecosystem-display.js`), der Abschnitt `abstaende` in `api/_internal/app/ecosystem-display.php`. Tests: `js/map-features/__tests__/ecosystem-display-abstaende.test.js`, `…/label-placement.test.js` (Abschnitte D, F, K, L), `…/label-kollision-zoomband.test.js`, `api/_internal/app/__tests__/ecosystem-display-test.php`.
- **Namen — Kollision: zwei Häkchen je Namensart** — was auf der Karte einem anderen Namen ausweicht und was auf seinem Punkt stehen bleibt, ist seit dem 02.09.2026 eine **Tafel im Fenster „Darstellung“** des Landschaften-Editors, nicht mehr eine Eigenschaft aller freien Kartenlabels. Anlass war der Owner-Befund „berggipfel werden derzeit in die kollisionserkennung aufgenommen und verschoben“ — ein Gipfel IST sein Punkt und darf nicht danebenrutschen. Mockup `docs/label-kollision-matrix-mockup.html` (er faehrt den ECHTEN Loeser, nicht einen nachgebauten). 🔴 **ZWEI Haekchen je Art, und sie haengen voneinander ab:** `teil` („Bei Kollisionserkennung berücksichtigen“) und `fest` („Verschiebung bei Kollision unterdrücken“). Daraus DREI Rollen — `avesmapsEcosystemDisplayKollisionsRolle`, eine Ableitung mit drei Lesern (Durchgang, Kurvennamen, Fenster): **beweglich** (heute) · **fest** (steht, die anderen weichen ihm aus) · **aus** (steht, belegt aber NICHTS). ⚠️ `fest` ohne `teil` hat keinen Gegenstand; das Haekchen wird STUMM, nicht weggenommen, damit sein Wert unveraendert zurueckkommt. 🔴 **Der Grundwert ist das bisherige Bild** (`teil` ja, `fest` nein); gespeichert wird nur die Abweichung, und wer auf die Vorgabe zurueckstellt, dessen Zeile fliegt ganz raus. 🔴 **MIT AUSNAHMEN, und sie sind ein Owner-Entscheid vom 02.09.2026** (erst „berggipfel und vulkan jetzt auf fest stellen“, dann „Berg-/Huegelkette Felsformation Huegel fest“): `AVESMAPS_ECOSYSTEM_DISPLAY_KOLLISION_VORGABE_JE_ART` stellt **Berggipfel, Vulkan, Berg-/Huegelkette, Felsformation und Huegel** ab Werk auf `fest` — die einzige Stelle, an der die Vorgabe das Kartenbild bewusst AENDERT. Dieselbe Bauform wie bei `AVESMAPS_ECOSYSTEM_DISPLAY_VORGABE_JE_ART` („berggipfel und vulkane sollen ab Z4 erscheinen“, 27.08.2026): ein Owner-Entscheid gehoert in die Tafel, nicht in eine gespeicherte Einstellung — er soll ein „Auf Vorgabe zuruecksetzen“ ueberleben und ohne Datenbankzeile fuer jeden gelten. 🔴 **Sichtbar wirken heute nur zwei davon** (Berggipfel 56, Vulkan 20 — live gemessen 02.09.2026); die anderen drei haben **NULL** Beschriftungen und stehen genau deshalb da: sie sind im Beschriftungsdialog waehlbar, die Tafel im Fenster listet aber nur Arten, die es wirklich GIBT — die erste Berg-/Huegelkette waere also Monate nach dem Entscheid stillschweigend als bewegliches Label auf die Karte gekommen, und niemand haette daran gedacht. ⚠️ **`fluss` und `ebene` stehen bewusst NICHT dort:** der Fluss war vorgeschlagen und wurde vom Owner nicht uebernommen (19 sichtbare Namen), die Ebene ist ein Flaechenname ohne Flaeche und darf ausweichen wie ein Waldname. Der Test nennt beide Listen beim Namen, damit ein spaeterer „Vollstaendigkeitsschub" sie nicht mitnimmt. 🔧 Die drei neuen Arten haben **keinen eigenen Namenston** in `css/features/map-labels.css` — beim ersten Objekt faellt die Farbe auf den Grundton zurueck. 💣 **Jeder Leser muss die Vorgabe DIESER ART nehmen** (`avesmapsEcosystemDisplayKollisionVorgabe`), nie den Grundwert: das Fenster zeigte sonst ein leeres Haekchen, waehrend die Karte festnagelt, ein nicht genanntes Feld fiele beim Gipfel faelschlich auf `fest: false` zurueck, und der Ruecksetz-Vergleich liesse eine Zeile stehen, die dasselbe sagt wie die Vorgabe. ⚠️ Die Vorgabe ist ein STARTWERT, kein Riegel — wer das Haekchen abnimmt, bekommt seinen beweglichen Gipfel. 💣 **`fest` HEISST AUCH: NIE AUSGEBLENDET.** Umgesetzt ist es als **Vorbelegung** (`seedRects`) statt als Teilnehmer — derselbe Weg, den die Namen auf gerechneten Kurven laengst gehen. Der reine Loeser (`label-placement.js`) bleibt damit **unangetastet**; die Gegenrichtung („steht still ODER verschwindet“) machte aus „verschieb meine Gipfel nicht“ ein „loesch meine Gipfel“. 💣 **Die Gruppe reist am vorgelegten Rechteck mit** — ohne sie blockierte ein festgenagelter Name die uebrigen Namen SEINER EIGENEN Flaeche, und der Finsterkamm verloere seinen zweiten Namen (Owner 2026-07-28), lautlos. 💣 **Der Versatz wird ueber ALLE zurueckgesetzt, auch ueber die Ausgenommenen** — sonst bliebe ein aus dem Durchgang genommener Name fuer immer dort stehen, wohin ihn ein Nachbar geschoben hat, den es nicht mehr gibt. ⚠️ **„aus“ schuetzt den Namen auch nicht mehr:** er belegt nichts, also darf ein Strassen- oder Flussname quer durch ihn gezeichnet werden (die Wegnamen lesen dieselbe Belegungskarte). Fuer einen Gipfel ist deshalb `fest` das Richtige, nicht `aus`. 💣 **DIE KURVENNAMEN LAUFEN SONST AN BEIDEN HAEKCHEN VORBEI** (Owner: „kurven auch nachziehen“): sie werden VOR dem Durchgang gesetzt und nur als Hindernis gemeldet. `avesmapsKurvenlabelPlatzierungen` fragt deshalb dieselbe Regel — live gemessen 02.09.2026 waeren es **85 von 1018** Beschriftungen gewesen, darunter **44 der 75** Gebirgsnamen. 🔴 Dort wirkt nur `teil`: ein Kurvenname rueckt von Bauart ohnehin nie aus. ⚠️ Und der Riegel steht NUR in der Platzierungsmeldung, nie im Zeichner — „nicht beruecksichtigen“ ist eine Aussage ueber die Kollision, nicht ueber die Sichtbarkeit. 🔴 **Die vier Namensarten OHNE gleichnamige Flaechenart stehen bei DEROGRAPHIE** (Owner-Entscheid; `berggipfel` 56, `vulkan` 20, `fluss` 19, `ebene` 2 — gemessen zugleich die einzigen vier, von deren Beschriftungen KEINE an einer Flaeche haengt). Das beantwortet einen offenen Punkt, der seit dem 24.08.2026 in `ecoDisplayZeilen` stand. 💣 **Die Liste wird GERECHNET** („Namensart ohne gleichnamige Flaechenart“), nie aufgeschrieben — eine feste Liste der heute vier waere ein zweites Vokabular (§5) und liefe beim naechsten Subtyp still auseinander. ⚠️ Sie stehen **nur** in dieser Matrix; Haupttabelle, Zoomband und Groessenplot bleiben unveraendert (dort ist der offene Punkt weiterhin offen). 🪤 **Eine Zeile gehoert der ART, nicht dem einzelnen Namen** — `region` steht EINMAL, bei Derographie, obwohl 34 seiner 158 Namen an Vegetationsflaechen haengen und 37 an gar keiner. Genau so verteilt das Fenster schon Farbe und Groesse. 💣 **Das Fenster reicht seinen Satz ins Modul HEREIN** (`avesmapsEcosystemKollisionAus`), statt seine Arbeitstafel per `avesmapsEcosystemDisplayInstall` dort abzulegen — das machte den Vorgabengeber zum Spiegel des Fensters und gab beim Zuruecksetzen den gerade getippten Wert als „Vorgabe“ zurueck (am 24.08.2026 im Browser gemessen, von keinem Test). 🪤 **Und ein Befund aus dem ABLAUF, nicht aus einem Test:** der Median (Zahl der Namen je Art und wie viele auf einer Kurve) traegt sich nach, und nachgezeichnet wurde zunaechst nur das Zoomband — wer schneller war als die Anfrage, sah die Spalte „Namen“ dauerhaft auf „—“. Klimazonen tragen den Abschnitt nicht (keine Namensart, 0 von 1018), wie schon Band und Plot. Bauteile: `avesmapsEcosystemDisplayKollision*` (`js/map-features/ecosystem-display.js`), die Rollen-Weiche in `resolveLabelCollisions` (`…/map-features-label-collisions.js`), `kurvenlabelIstHindernis` (`…/map-features-path-label-canvas-overlay.js`), Abschnitt `kollision` in `api/_internal/app/ecosystem-display.php`. Tests: `js/map-features/__tests__/kollisionsmatrix.test.js` und der Abschnitt H2 in `api/_internal/app/__tests__/ecosystem-display-test.php` (zusammen gegen 18 Mutationen gefahren, alle gefangen). 🔧 **Offen:** die Routenfindung und der SVG-Abzug kennen die Tafel nicht; und ein einzelner Name laesst sich weiterhin nicht ausnehmen, nur seine ganze Art.
- **Das Zoomband einer Beschriftung gehört dem OBJEKT — die Tafel „Darstellung" schlägt es NIE, auch nicht bei Gipfeln.** Owner 04.09.2026, wörtlich und zum dritten Mal: **„wie ich 1000x gesagt habe: die vorschläge aus der topographie-einstellung sind nur vorschläge (die dreieckchen unter den slidern), aber gelten muss immer die einstellung der editoren also der objekte"**. 💣 **DIESE ZEILE IST IN EINER WOCHE DREIMAL FALSCH GEBAUT WORDEN, jedes Mal mit einer plausiblen Begründung — wer sie anfasst, liest zuerst die drei Anläufe:** (1) 27.08.–02.09.: die Tafel (`berggipfel`/`vulkan: { ab: 4 }`) schlug das eigene Band ÜBERALL, weil „berggipfel und vulkane sollen ab Z4 erscheinen" als **Riegel** gelesen wurde. Preis: „Sichtbar ab Zoom" war für alle 76 Gipfel **still** wirkungslos — der Wert wurde weiter gespeichert und nur beim Zeichnen ignoriert. (2) 02.09. (`6a0390e32`): der Riegel fiel, das eigene Band galt überall. Richtig — und der Owner nahm die Folge ausdrücklich an („passt so perfekt", 33 Gipfel erscheinen wieder ab z2/z3). (3) 03.09. (`df5493a04`): der Wunsch „dass in den Landschaftsansichten 'Alle' und 'Topographie' wieder alle Berge, Vulkane, etc. angezeigt werden" wurde als „ab z4 sichtbar" gelesen und als `avesmapsLabelImBandDerAnsicht` gebaut — derselbe Riegel, nur auf zwei Ansichten verengt. Gemeint war **„überhaupt sichtbar"**. Zurückgebaut am 04.09.2026. 🔴 **Die Regel, die bleibt:** `avesmapsLabelImBand` ist die EINE Instanz — eigener Wert gewinnt, die Tafel füllt nur die Lücke —, und `shouldShowLabelMarker` fragt sie **direkt**. Eine Weiche davor, die „hier gilt dein Wert nicht" sagt, ist der Riegel in neuer Verkleidung, egal wie eng ihr Geltungsbereich ist. `ecosystem-display-groesse.test.js` hält das mit einer Zusicherung fest, die den blossen NAMEN einer solchen Weiche verbietet. ⭐ **Der dritte Weg, der zweimal übersehen wurde:** soll eine Owner-Zahl wirklich Gipfel bewegen, gehört sie **in die Daten** — „Alle übernehmen" im Fenster „Darstellung" schreibt sie in die Beschriftungen. Dann gilt sie, ohne dass ein Regler lügt. ⚠️ Und wer einen Wert doch irgendwo übersteuert, **beschriftet das Bedienelement** — ein Regler, dessen Wert stillschweigend nirgends gilt, ist von einem kaputten Formular nicht zu unterscheiden.
- **Verborgene Orte** — ein Ort trägt neben *Nodix* und *Ruine* ein drittes Merkmal: **verborgen**. Die Karte zeichnet weder Markierung noch Namen, die Routenfindung wählt ihn nicht als Kandidat, und gefunden wird er über seinen Namen. Owner, wörtlich: „Wer den Namen kennt, findet ihn; wer nur über die Karte scrollt, nicht." Live 15.08.2026; Entwurf **`docs/superpowers/specs/2026-08-15-versteckte-orte-design.md`**, Bauplan `docs/superpowers/plans/2026-08-15-versteckte-orte.md`. 🔴 **Das Verstecken hängt NICHT an der Weganbindung** — es ist ein von Hand gesetztes Merkmal; ein Ort an der Reichsstraße kann versteckt sein, einer ohne jeden Weg muss es nicht. Owner-Entscheide: das Feld startet überall auf 0 (**kein** Einmal-Lauf, der irgendetwas setzt) · ein gefundener Ort bleibt für **diesen Besuch** sichtbar, nicht dauerhaft · ein versteckter Ort an einer Straße wird **still durchfahren**. ⚠️ **Es ist keine Spalte**: wie `is_nodix`/`is_ruined` liegt `is_hidden` im `properties_json`, es gibt keine DDL und keine Migration. 🪤 **Der Name ist im Projekt zweimal vergeben** — `map_reviews.is_hidden` heißt „von der Moderation verborgen" und gehört den Rezensionen; keine gemeinsame Tabelle, keine gemeinsame Datei, aber `git grep is_hidden` findet beide (der Unterschied steht als Kommentar an der Schreibstelle in `features.php`). 💣 **DIE TRAGENDE ENTDECKUNG: einen versteckten Ort aus der Ortsliste des Graphbaus zu streichen, ZERSTÖRT STRASSEN.** `avesmapsAddClientCompatiblePathConnection` verwirft jeden Weg, dessen Endpunkt auf keinem bekannten Ort liegt — der Ort bliebe also nicht nur unangefahren, jede an ihm endende Straße verschwände, und bei zwei Straßen zerfiele der Graph in Inseln, über die eine ×25-Notkante gelegt wird. Der Ort bleibt deshalb **vollständig im Graphen**; an den Verbindungen ändert sich nichts. 🔴 **Gefiltert wird eine ZWEITE Liste** (`candidate_locations`), aus der synthetische Kanten ihre Ziele wählen — einmal im Graphbau gerechnet (versteckte raus, außer die Anfrage nennt sie als `from`/`to`/`via`) und von dort **herausgegeben**; die rohe Liste verlässt `avesmapsBuildClientCompatibleRouteGraph` nicht mehr. `response.php` holte sie sich bisher direkt und wäre am Riegel vorbeigelaufen. Das ist die Falle vom 14.08. (vier Erzeuger, Sperre in zweien) samt ihrer Lehre: **die ZAHL im Kommentar war das Problem**, denn sie liest sich wie eine vollständige Liste — hier steht keine. 🔴 **Zwei Stellen bleiben bewusst ungefiltert:** die Umweg-Sehnen verbinden Knoten der bereits gefundenen Route (dort fährt der Reisende ohnehin durch), und `avesmapsRouteResolveEndpointPoint` schlägt die Koordinaten der **eigenen** Endpunkte nach, statt einen Kandidaten zu wählen. ⚠️ **Der JS-Graph bleibt unangetastet** — er routet seit `?clientrouting=1` nur noch als Hintertür, bedient aber den Prüfhaken-Index, und ein versteckter Ort ohne Weganbindung ist weiterhin ein Editorbefund. 💣 **Der Zeichen-Riegel steht NACH den Prüfhaken und VOR der Kreuzungsweiche** (`shouldShowLocationMarker` + `shouldShowLocationNameLabel`, ein gemeinsames Prädikat `isHiddenLocation`): darüber wäre „verstecken" ein Weg, den Prüfhaken stillzulegen; darunter würde ein versteckter Nodix im Kraftlinien-Modus doch leuchten. 💣 **Die Aufdeckung hängt an ZWEI Trichtern, nicht an einzelnen Aufrufern** — `openLocationPopupForMarkerEntry` (dort münden beide Popup-Öffner, der Spotlight-Treffer und der Ortslink im Reiseplan) und `collectAndValidateSelectedLocations` (Vorschlagsliste, getippter Name, geteilter Link). Die Semantik stimmt dort von selbst: einen Marker, der nicht gezeichnet ist, kann niemand anklicken. ⚠️ `?pin=` ist **kein** dritter Fall — das ist eine Koordinate, kein Ortsbezug. Die Menge ist Laufzeit und **additiv** (kein `localStorage`, kein URL-Parameter, kein Serverzustand); ein entfernter Wegpunkt nimmt sie nicht zurück. 💣 **Im Spotlight hängt die Verbreiterung der Trefferspalte jetzt an `--two-line`, nicht mehr an `--not-on-map`** — die richtige Frage ist „hat diese Zeile zwei Zeilen", nicht „springt der Treffer woanders hin": ein versteckter Ort IST auf der Karte, hätte die 240px nicht bekommen, und die Ellipse bei 150px frisst dann genau das Wort „Verborgen". Der Hinweis steht seither auf **11px** (war 10, unter der Grenze aus §12). Beide Merkmale teilen eine Zeile: „Ruine · Verborgen" — im Spotlight wie in der Typangabe der Infobox, mit demselben Trenner. 💣 **Die Wegpunktsuche konnte gar nicht kennzeichnen**: `getWaypointAutocompleteEntries` warf das `location`-Objekt schon im ersten `.map()` weg — ohne dass `isHidden` mitreist, prüft die Kennzeichnung für immer `undefined` (grüner Test, wirkungslose Liste). 🔴 **Ein Innerorts-Objekt erbt die Kennzeichnung von seinem ZIEL** (Owner 15.08.2026, nach dem ersten Bau: drei unbeschriftete Vorschläge führten an dasselbe versteckte Warunk) — aber in **einer** Klammer: „Warunker Höhenburg (Warunk · verborgen)". Geöffnet wird nur eine Klammer ganz am ENDE; eine mitten im Namen gehört dem Namen. `waypointInSettlementLabel` bleibt unangetastet, es trägt seine eigene Regel gegen die doppelte Stadtklammer. ⚠️ Der Zustand wird **einmal je Cache-Aufbau** abgeleitet; der Cache misst die ANZAHL der Orte, ein frisch verstecktes Ziel wird also erst nach einem Neuladen gekennzeichnet. Beschriftung „Verborgene Orte" im Auge-Menü (`toggleHidden`, dieselbe Lupe wie „Nodices" und **kein** eigener Ring — er erzeugt keinen Marker). In der Etappenliste fällt ein versteckter, nicht aufgedeckter Durchgangsort über `isRoutePlanMarkerName` heraus; ⚠️ ein Name OHNE Marker fällt **nicht** heraus (das ist kein versteckter Ort, sondern ein unbekannter Knoten, und ihn zu schlucken verlöre eine Etappe). Tests: `api/_internal/routing/__tests__/versteckte-orte-test.php` (erste Zusicherung ist die zerstörte Straße), `js/map-features/__tests__/versteckter-ort-sichtbarkeit.test.js`, `…/wegpunkt-versteckt-label.test.js`, `js/ui/__tests__/spotlight-versteckt-zeile.test.js`, `js/routing/__tests__/versteckte-etappe.test.js`. 🔴 **Die Typangabe der Infobox liest seit 15.08.2026 das EIGENE Feld** (Owner: „die infobox soll auch das eigene feld lesen") und schlägt das Wiki — dieselbe Vorrangregel wie bei Wappen und Covern. Vorher las sie nur `wikiSettlement.is_ruined`: am Livebestand trugen 70 Orte das eigene Feld, 44 das aus dem Wiki, **31 nur das eigene** — die sagten im Spotlight „Ruine" und in der Infobox nichts, darunter ausgerechnet „Ruine Khell Dairon". ⚠️ Die andere Hälfte der alten Regel **bleibt**: „(Ruine)" hängt an einer ART, nie an der blossen Ortsgrösse — „Dorf (Ruine)" stand nie da und ist im Test festgenagelt; die 24 Ruinen ohne Art sagen es über die Statuszeile und das Spotlight. „(Verborgen)" hängt dagegen an **keiner** Art: es beschreibt nicht, was der Ort ist, sondern wie die Karte mit ihm umgeht. Beide Zusätze laufen seither über `tr()` (`popup.typeRuined`/`popup.typeHidden`) — vorher stand dort ein hartes deutsches „(Ruine)", das unter `?lang=en` durchschlug. 🔴 **Das Wort heisst überall „Verborgen"** (Owner 15.08.2026, nach einem Tag mit „Versteckt"): Editorhaken, Auge-Menü, Spotlight-Zeile, Wegpunkt-Klammer, Infobox. ⚠️ Es war eine Umbenennung von **Beschriftungen, nicht von Kennungen** — `is_hidden`, `isHidden`, `toggleHidden`, `spotlight.hidden`, `waypoint.hidden` und sämtliche Datei- und Testnamen (`versteckte-orte-*`) heissen weiter wie vorher; dieselbe Trennung wie bei „Neuigkeiten"/`changelog`. 🪤 Der Grund für „verborgen" statt „versteckt" ist **nicht** frei: `verborgen` bezeichnet bei den Rezensionen „von der Moderation ausgeblendet" — Besucher sehen das nie, Editoren schon, und das sind dieselben Leute. Die Kollision ist bekannt und in Kauf genommen.
- **Die Suche sagt, wenn sie nichts gefunden hat** — „Nicht auf Avesmaps gefunden." unter dem Suchfeld, live 02.09.2026 (Owner: „wenn die spotlight suche nichts gefunden hat - kannst du das kommunizieren"). Davor wurde bei null Treffern nur die Liste ausgeblendet und das Statusfeld geleert: das Fenster stand leer da, und „nicht gefunden" war von „ich habe noch nicht geantwortet" nicht zu unterscheiden. ⭐ Das Feld gab es schon — `#spotlight-search-status` (`role="status"`, `aria-live="polite"`, Stil in `css/components/spotlight-search.css`) war seit jeher da und wurde **nie gefüllt**. 🔴 **DIE TRAGENDE REGEL IST DAS SCHWEIGEN, NICHT DER SATZ.** Die Suche ist **zweistufig**: erst lokal aus den geladenen Kartendaten, dann schiebt der Server Literatur, Kartensammlung, Vorkommen und die Off-Map-Treffer nach — und genau die kennt der Browser gar nicht. Wer schon im lokalen Durchgang meldet, lässt den Satz bei JEDEM Tastendruck aufblinken und eine Sekunde später von echten Treffern ersetzen. Live gemessen an „Borbarad": lokal **0** Treffer, **1,8 s** später **17** vom Server — das wäre 1,8 s sichtbare Falschaussage gewesen. 💣 **Ein ausgefallener Abruf ist kein leeres Ergebnis:** „Nicht auf Avesmaps gefunden" hieße dort „das gibt es nicht", obwohl niemand nachgesehen hat — deshalb ein eigener Satz („Die Suche ist gerade nicht erreichbar.", `spotlight.searchFailed`). ⚠️ Ein **Abbruch** (`AbortError`) ist KEIN Fehlschlag — er heißt, dass schon die nächste Suche läuft, und die setzt ihren eigenen Stand; ein Fehlersatz stünde sonst bei jedem Tastendruck da. ⚠️ Und ein Fehlschlag **mit** lokalen Treffern schweigt: der Benutzer sieht eine Liste und könnte ohnehin nichts tun. 💣 **Ohne Servertreffer wurde früher GAR NICHT gezeichnet** (`if (resolvedEntries.length)`) — der Status wäre damit für immer im wartenden, also stummen Zustand hängengeblieben; der Endzustand braucht diesen Durchgang, und er kostet nichts, weil `resolveBackendSpotlightEntries` bei leerem Ergebnis dieselben `localEntries` zurückgibt. 🔴 Entschieden wird in **einer reinen Funktion** (`spotlightSearchStatusText`, kein DOM, kein Modulzustand), geschrieben wird durch **einen Trichter** (`renderSpotlightSearchResults` — die war schon vorher der einzige Erzeuger des Statuszustands, sie leerte ihn). ⚠️ Gemessen wird das NORMALISIERTE Suchwort: „???" ist kein Suchwort, sondern nichts. ⭐ Das Suchfenster ist in **beiden** Themen dunkel, der Satz misst dort 8,12:1 — keine Hell/Dunkel-Falle. Test: `js/ui/__tests__/spotlight-leere-suche.test.js` (Regel und Verdrahtung wirklich ausgeführt, gegen 12 Mutationen gefahren, alle gefangen).
- **Der Wiki-Override — was gesynct ist und was von uns.** Der Territoriumseditor konnte es seit Monaten: die Abweichung zum Wiki DAUERHAFT zeigen (Wiki-Wert durchgestrichen), sagen wer den Wert gesetzt hat, und ihn feldweise mit ↺ zurückholen. Seit 17.08.2026 können es auch die übrigen Objektarten. Entwurf **`docs/superpowers/specs/2026-08-17-wiki-override-fuer-alle-design.md`**, Bauplan `docs/superpowers/plans/2026-08-17-wiki-override-ort.md`, Mockup `docs/wiki-override-mockup.html`. **Live: Ort** (Ortseditor + Kartendialog) **und Literatur**; 🔧 Region, Landschaftslabel und Weg fehlen noch (Fundament auf Zweig `wiki-override-stufe3`). 🔴 **Nur Felder, die aus dem Wiki kommen** (Owner) — das sind genau die Registerzeilen mit einem Kartenziel: Ort 5, Literatur 10, Landschaft 2, Label 2, Weg 1(+1). **Kraftlinie und Karte bekommen NICHTS**, gemessen: dort landet kein Wiki-Wert auf einem Kartenfeld. 🔴 **DIE EINE REGEL:** beim Speichern bekommt jedes Feld, dessen Wert sich WIRKLICH ändert, eine Herkunft — `wiki`, wenn die Anfrage es als Übernahme nennt (`wiki_uebernommen`), sonst `manual`; Unverändertes bleibt unangetastet. Rein in `avesmapsFieldOriginsStempeln` (`api/_internal/map/field-origins.php`), Ablage `properties.field_origins` — keine Tabelle, keine Migration, kein Einmal-Lauf. 💣 **„Nur was sich ändert“ ist die Hälfte, an der es schon einmal gescheitert ist:** `avesmapsUpsertGameLiterature` stempelte jedes MITGESCHICKTE Feld auf `manual`, und das Formular schickt alle mit — nach EINEM Speichern trug dort jedes Feld „von Hand“, und die Spalte sah gepflegt aus, ohne etwas auszusagen. Repariert; ⚠️ aber **nicht rückwirkend**: der Stempler setzt nur, er löscht nie, Altbestand bleibt gesperrt bis jemand ein Feld mit ↺ zurückholt (als Zusicherung festgenagelt). 💣 **Der Client muss sagen, was aus dem Wiki kam** — drei Felder sind ABGEBILDETE Werte (`feature_subtype` bei Ort und Weg, `region_type` bei der Landschaft), und die Abbildungen leben nur im Browser; sie serverseitig nachzubauen wäre die zweite Wahrheit aus §5. 🔴 **Im Zweifel `manual`** — eine falsche `wiki`-Angabe liesse einen späteren Abgleich Handarbeit überschreiben, eine falsche `manual`-Angabe schützt nur zu viel. ⚠️ Damit darf kein Schreibweg live gehen, bevor SEINE Oberflächen `wiki_uebernommen` schicken — sonst stempelt der Sync-Knopf seine eigenen Übernahmen als „von uns“ (die Falle vom 14.08.: eine Regel, die einen von mehreren Erzeugern bindet, ist keine). ⭐ Das **↺ ist die Sync-Übernahme EINER Zeile** — kein neuer Schreibweg, kein zweiter Endpunkt, dieselbe Speicherleiste. 🔴 **Zwei sichtbare Zustände, nicht vier** (Owner: „bau doch einfach territorien nach“): braune Beschriftung = von uns, Durchstreichung ohne Farbe = weicht ab, Herkunft unbekannt. `wiki` wird mitgeschrieben, aber nicht angezeigt — es wirkt beim Vorhäkeln (`avesmapsWikiAssignDiff`, Fall 4; ohne gespeicherte Herkunft ändert sich nichts). 🔴 **50 | 50** (Owner 17.08.2026): links Beschriftung SAMT durchgestrichenem Wiki-Stand und ↺, rechts das Eingabefeld. 🪤 Davor zwei verworfene Fassungen — eine dritte Rasterspalte mit gerechnetem Mindestplatz (`clamp(90px, 50%, 100% - 165px)`), dann eine zweite Bauform für den Literatur-Editor, die „ISBN (für DNB)“ auf drei Zeilen brach. **Das Markup sagt nicht, welche Form gilt — das CSS sagt es** (`.ae-field` sieht aus wie der Kartendialog, ist aber ein Flex-ROW wie `.dt-grid`). Und: wer einen Mindestwert ausrechnet, sollte zuerst fragen, ob eine Hälfte reicht. 💣 **Die Auslassungspunkte gehören an den TEXT, nicht an die Zelle** — `ellipsis` wirkt nur auf das Element, dessen eigener Text überläuft; an der Zelle wird hart abgeschnitten. 💣 **`--color-accent-brown-text` ist im hellen Thema `#fff`** — weiße Beschriftung auf weißem Panel; die braune Hervorhebung nimmt `--color-accent-brown`, wie der Territoriumseditor. 🪤 **Und der teuerste Befund: der Parser RÄT eine Ortsklasse.** `avesmapsWikiSettlementParseInfobox` setzt `settlement_class` auf `'dorf'`, wenn die Wiki-KATEGORIE keine hergibt, und schreibt die Vermutung als Datum ins Nest — das ↺ bot daraufhin an, die **Metropole Gareth zum Dorf** zu machen, und der Owner hat es geklickt. Die Sync-Vorschau bot dieselbe Herabstufung schon seit dem 16.08. an, nur zwei Klicks tiefer. Seither sagt der Parser, dass er geraten hat (`settlement_class_guessed`), und ein Nest OHNE die Angabe gilt für `'dorf'` als geraten — bewusst zu streng, die sichere Richtung, löst sich mit jedem Abgleich von selbst. Bauteile: `js/ui/wiki-feld-herkunft.js` (rein), `api/_internal/map/field-origins.php` (rein), `.dt-grid--wiki`/`.wiki-alt` in `css/components/editor-page.css` + `css/components/location-report-dialog.css`. Tests: `wiki-feld-herkunft`, `field-origins-test.php` (zählt die Schreibwege zur LAUFZEIT — sie fand den zweiten, den der Autor übersehen hatte), `ort-wiki-override-form`, `wiki-assign-ort` (Fall Gareth), `wiki-assign-literatur`.
- **„Was ist hier?"** — Rechtsklick auf die Karte, und das Infopanel sagt, was an der Stelle liegt: Herrschaftskette mit Wappen, die vier Landschaftsebenen, Klimazone, Natur & Waren, und „In der Nähe" (Orte und Wege mit Entfernung und drehendem Peil-Pfeilchen). Entwurf **`docs/superpowers/specs/2026-08-15-was-ist-hier-design.md`**, Bauplan `docs/superpowers/plans/2026-08-15-was-ist-hier.md`, Mockup `docs/was-ist-hier-mockup.html`, live 15.08.2026. Bauteile: Endpunkt `api/app/what-is-here.php` + `api/_internal/app/what-is-here.php`, Panel `js/map-features/map-features-what-is-here.js`, Nachbarschaft `js/map-features/map-features-what-is-here-nearby.js`. 🔴 **Es gibt genau EINE Markierung.** „Stelle markieren und teilen" ist ersatzlos gefallen (Owner 15.08.2026), das schwebende Zwei-Kachel-Menü am Marker ebenso — ein Klick auf die Markierung öffnet seither das Panel, und `?pin=` bringt die Auskunft mit statt nur den Punkt. 💣 **Doppelt verwendete PDO-Platzhalter sind hier ein FEHLER, kein Stilfrage:** `avesmapsCreatePdo` setzt `ATTR_EMULATE_PREPARES => false`, und MySQL lehnt `:x` zweimal im selben Statement mit `HY093` ab. Kein funktionierender Endpunkt im Haus tut es; unserer tat es, und weil die Ausnahme im `catch (Throwable) { return []; }` verschwand, antwortete er **`ok:true` mit leerem Inhalt** — für den Betrachter nicht von „hier liegt nichts" zu unterscheiden. Gefunden hat es die erste Anfrage gegen die Live-Datenbank, nach sechs grünen Prüfläufen. ⚠️ Der inerte `catch` war für „Tabelle fehlt" gedacht und verschluckt seither jeden SQL-Fehler. 💣 **bbox ist ein Vorfilter, kein Treffer** — am Seepunkt lagen 9 Gebiete im bbox und 0 bestanden den Punkttest; danach läuft `avesmapsClimateGeometryContains` (der EINZIGE Punkt-in-Polygon im Haus, kein zweiter). 💣 **Die Vorfahren einer Herrschaftskette haben meist GAR KEINE gezeichnete Fläche** — sie sind abgeleitete Außengrenzen in `political_territory_derived_geometry`. Der Punkttest allein findet deshalb nur das Blatt; die Kette entsteht aus dem `parent_id`-Lauf wie bei der Siedlung (`map-features.php`), gedeckelt auf 12 Stufen. Der Entwurf entschied das zuerst anders und wurde von der Datenlage widerlegt (§3, korrigiert). 💣 **Die Kette steht BLATT → WURZEL** — `buildSettlementHierarchyMarkup` erwartet genau das und dreht selbst um. 💣 **Wappen laufen NUR über `avesmapsResolveGatedCoatUrl`** (`api/_internal/coat-url.php`, seit 15.08.2026 samt `avesmapsLoadSettlementCoatGateInputs`/`avesmapsSettlementTerritoryCoatUrl` dort, vier Leser teilen eine Implementierung). Die Spalte `political_territory.coat_of_arms_url` allein ist **leer** und liefert nichts; der Riegel lässt zudem nur `public_domain` durch (NOTICE.md). ⚠️ Das Wappen muss aufgelöst sein, BEVOR `avesmapsWhatIsHereTerritoryPayload` den `wiki_key` aus der Antwort wirft — dieselbe Reihenfolgefalle wie bei `lore.place`, das den Territoriums-Schlüssel ebenfalls aus der ungekürzten Kette liest. 💣 **Die Peilung des Pfeilchens ist `atan2(dx, dy)`**, Argumente vertauscht, damit 0° Norden ist und im Uhrzeigersinn gezählt wird wie bei `rotate()`. Mit der Schulform zeigt jeder Pfeil an der Diagonale gespiegelt, und das fällt bei genau N/O/S/W **nicht** auf. Es gilt nur, weil y hier nach NORDEN wächst (Riva y=790, Al'Anfa y=152) — die Kachelnamen `map_x_-y` tragen ein negatives y. 💣 **Die Markierung liegt in `sharePinPane` (z=700)**, über `labelsPane` (650) und unter `tooltipPane` (875); in Leaflets Standard-`markerPane` (600) verschwand sie unter den Kartenlabels. Ihr Zeiger ist `pointer` wie bei allem Anklickbaren — `grab` war vom Kartenhintergrund (ebenfalls `grab`) nicht zu unterscheiden. ⚠️ „In der Nähe" nennt die drei nächsten Ortschaften und je Wegart höchstens einen Weg, höchstens vier, **keiner weiter als das Anderthalbfache der weitesten gezeigten Ortschaft** — der Maßstab ist die ORTSLISTE, nicht die Wegeliste (sonst wandert die Schranke mit dem mit, was sie begrenzen soll; auf Maraskan stand ohne sie eine Reichsstraße 534 Meilen weit weg drin). Ein Weg ohne echten Namen wird nur mit seiner Art genannt — `getPathTitleName`/`getPathTypeLabel` (`map-features-path-domain.js`), **nicht** nachbauen: die Hausfassung liest zuerst `wiki_path.name` und liefert „Straße" statt des Join-Keys „Strasse".
- **Performance-Fixes Editor und Takt-Endpunkte** (live 03.09.2026, Bauplan `docs/superpowers/plans/2026-09-03-performance-editor-fixes.md`) — neun Dauerlasten abgebaut, ohne eine sichtbare Funktion zu ändern; jede Zahl vorher/nachher am selben Tag live gemessen. **(1) Der Editor baute 62.607 DOM-Knoten in ein `hidden`-Overlay, beim Start**: 4.220 WikiSync-Fälle mit 13.642 Knöpfen, 82 % des gesamten Editor-DOMs, und jeder Selektorlauf der Karte ging seither über sie hinweg. Gebaut wird jetzt erst beim Öffnen des Konfliktfensters (`renderWikiSyncCasesWennAusstehend`, gerufen aus `setWikiSyncConflictsDialogOpen` NACH dem Einblenden) — **71.322 → 10.091 Knoten**, Öffnen 179 ms. 🔴 Die DATEN (`wikiSyncCases`) bleiben sofort da, das Konfliktzentrum braucht sie; nur das BAUEN wartet. 💣 Der zuletzt gemeldete Lauf wird gemerkt, sonst trifft ein nachgeholtes Rendern eine andere Kopfzeile als ein sofortiges. **(2) Ein Delta-Abruf trug die ganze Welt mit**: `?since_revision=<aktuell>&edit_mode=1` lieferte 0 Features, aber **6,47 MB in 1,13 s** — Quellenkatalog, ~13.000 Verweise, Kanon über 11.500 Objekte, Innerorts-Objekte; alle 15 s nach jeder fremden Speicherung. Der Live-Abgleich liest davon NICHTS (`pollLiveMapUpdates`, `js/routing/routing.js`: nur `features` und `revision`, und der Kommentar an der Nutzlast sagt das seit jeher). Fünf Blöcke hängen jetzt an `avesmapsMapFeaturesIstDeltaAbruf` — **1.626 Bytes in 0,19 s**, ein Faktor 3.980. 🔴 **Die Schlüssel bleiben stehen, leer statt fehlend**, damit ein alter Client ein leeres Objekt sieht und nicht `undefined`; ein Delta MIT Änderungen liefert weiterhin vollständige Features (371 geprüft), der Vollabruf ist unverändert (12.216 Features, 1.438 Katalogzeilen, 3.561 Stätten). ⚠️ Nur `since_revision` entscheidet — eine `bbox`-Anfrage ist ein gekürzter VOLLabruf und behält alles, was ihre Popups brauchen. **(3) Ein Pan in der politischen Ansicht kostete 8 Anfragen und ~6,5 MB**, in der Standardansicht 0 — der Unterschied war EINE Bedingung (`mapLayerMode !== "political"`) im Pan-Guard. Der Guard gilt jetzt in allen Ansichten und vergleicht den vollen Schlüssel (Zoom, Jahr, Bearbeiten-Modus, `avesmapsPoliticalLayerAktuellerSchluessel`) — **Pan: 0 Anfragen**, Zoomwechsel weiterhin genau eine, ein Jahr aus der Zeitleiste eine plus Fan-out-Bündel. 🔴 Das darf er, weil die Ebene an genau diesen dreien hängt und **an keiner bbox** (`fetchPoliticalTerritories({action, year_bf, zoom, edit_mode})`, nachgezählt: alle drei `bbox`-Vorkommen der Datei sind Kommentare). 💣 **Jeder Speicherweg ruft mit `immediate` und geht am Guard vorbei** — 18 Aufrufer; die 6 ohne `immediate` sind zoomend/moveend, Sichtbarkeit, Zeitleiste, der Selbstheil-Reload und der Fehlerpfad der Gebietsanlage. 🪤 **Genau dieser Fehlerpfad musste nachgezogen werden**: sein Toast verspricht „Die Karte wird neu geladen", und ohne `immediate` täte er bei unverändertem Schlüssel nichts. Fan-out 60 s → 300 s, Stilliste (ein Fünf-Tabellen-Join, `action=list`, 297 KB, lief mit **1 s** bei JEDEM Pan mit) → 30 s. ⚠️ Der Kommentar am Selbstheil-Reload sagte „der pan-sichere Guard greift für political NICHT" — er ist mitgewandert; wer ihn wieder liest, findet die neue Lage. **(4) Ein Sperr-Timer lief für immer**: `acquireFeatureSoftLock` trug den Eintrag erst NACH dem `await` ein, ein `releaseFeatureSoftLock` während der Anfrage fand nichts, gab auf, und der 45-s-Wecker schlug danach endlos — jeder Tick ein POST mit zwei `CREATE TABLE IF NOT EXISTS`. 🔴 **Der Platzhalter (`null`) steht jetzt VOR dem `await`**, und wurde inzwischen freigegeben, löst `acquire` die Serversperre selbst wieder. Dazu stoppt der Wecker bei verlorener Sperre (`avesmapsFeatureLockIstVerloren`: „nicht gefunden" = gelöscht, „wird gerade von" = fremd) — ⚠️ **ein Netzfehler ist KEIN Verlust**, der nächste Tick darf es wieder versuchen. Live: nach acquire+sofort release **60 s lang null** weitere Anfragen. **(5) DDL auf dem heissen Pfad** — siehe §10, `avesmapsSchemaEnsureOnce`. **(6)** `avesmapsApiMetricsAufraeumen` schrieb bei JEDEM Request einen Upsert auf EINE Zeile, die alle Besucher teilen (serialisiert an einer Zeilensperre); es würfelt jetzt und fragt nur bei jedem hundertsten nach — das tägliche DELETE bleibt, am Tag kommen Tausende Würfe. **(7)** Der 45-s-Poll der Meldungsliste lief in versteckten Tabs weiter (11 Abrufe in 10 min ohne Betrachter, jeder mit `CREATE TABLE` + 5× `SHOW COLUMNS`); er bekommt den `document.hidden`-Riegel des Live-Abgleichs — live 100 s im versteckten Tab: **0 Abrufe**. ⚠️ **Der Anwesenheits-Takt bekommt ihn NICHT**: an ihm hängt der Territorien-Anspruch, und ein Editor im Nachbartab hat ihn nicht aufgegeben. **(8)** `POLITICAL_LAYER_CACHE` (api-client) löschte nie einen Eintrag, nur nach einer Speicherung alles — jede Zusage hält die geparste Ebene (im Editor 4–5 MB JSON je Zoom × Jahr) und die Zeitleiste kennt 1050 Jahre; abgelaufene fliegen jetzt beim Setzen hinaus (live nach drei Zoomstufen: **1 statt 3**). **(9)** Der Signaturpoll des Grenz-Canvas baute **fünfmal je Sekunde** einen String über alle `regionData` — für jeden Besucher, in jeder Ansicht, auch versteckt; jetzt 1 s und still im versteckten Tab (live: Änderung zieht binnen 1,6 s nach, Ruhe zeichnet nicht, versteckt gar nicht). 🪤 **Und die Messfalle des Tages**: Aufgabe 1 wurde nach einem einmaligen Einfrieren des Tabs zurückgerollt und nach vier Gegenmessungen (Render 123/130/132 ms, Öffnen 28 ms auf der alten Fassung) wieder eingespielt — der Hänger war nicht reproduzierbar. **Ein einzelnes Einfrieren ist noch kein Befund; die Zahl daneben ist einer.** Tests: `wikisync-faelle-erst-beim-oeffnen`, `map-features-delta-schlank-test.php`, `ebenen-pan-guard`, `sperr-timer-race`, `schema-ensure-once-test.php`, `api-metrics-aufraeumen-takt-test.php`, `meldungen-poll-versteckt`, `ebenen-zusagen-verfall`, `grenz-canvas-signaturpoll`.
- **Verweildauer** — wie lange ein Besuch dauert, als Histogramm bis 12 h mit Median und Durchschnitt (Status → Besucher, Karte „Verweildauer"). Live 26.08.2026; Entwurf **`docs/superpowers/specs/2026-08-25-verweildauer-design.md`**, Mockup `docs/besucherstatistik-verweildauer-mockup.html`. 🔴 **Gemessen wird die Zeit mit der Karte IM VORDERGRUND**, nicht „der Tab war offen" — das ist keine Wahl, sondern die einzige messbare Grösse: der Anwesenheits-Ping schweigt bei unsichtbarem Tab, und ein seit gestern im Hintergrund liegender Tab ist von einem geschlossenen nicht zu unterscheiden. Ein über Nacht offener Tab wäre sonst ein Neun-Stunden-Besuch, und der Median kippt als Erstes. 💣 **DREI Ausgänge, EIN Buchhalter** (`avesmapsVisitorFinishLiveRun`): der `gone`-Beacon, der Neustart (ein Ping auf eine Zeile, die länger als das 150-s-Fenster kalt ist) und die Aufräumung. Die Aufräumung ist der Ausgang, den die MEISTEN Besuche nehmen — ein `pagehide` geht auf Mobilgeräten oft nicht mehr ab —, und sie **löschte bis dahin nur**. Wer einen vierten Erzeuger anbaut und ihn nicht anschliesst, bekommt ein Histogramm, das je nach Ausgang zählt: das sieht aus wie ein Datenmangel, nicht wie ein Fehler (`verweildauer-test.php` zählt die Aufrufer). 💣 **Der Neustart misst am ANWESENHEITSFENSTER (150 s), nicht an der Aufräumfrist (15 min)** — ohne ihn verschmelzen Morgen- und Abendbesuch desselben Anschlusses zu EINEM von zwölf Stunden, der Tages-Hash ist derselbe. ⚠️ Der Preis in die Gegenrichtung: eine Pause über 150 s zählt als zwei Besuche; das ist die kürzende und damit die sichere Richtung. 💣 **`first_seen` darf der Upsert nicht mitnehmen** — sonst wäre jeder Besuch genau einen Ping lang und das Histogramm hätte genau einen Balken, gefüllt und plausibel. 💣 **Gebucht wird auf den UTC-Tag des ANFANGS**, aus dem Versatz `NOW()` ↔ `UTC_TIMESTAMP()` gerechnet: ein Besuch, der um 23:58 endet und um 00:14 aufgeräumt wird, gehört in den Vortag, und `DATE(first_seen)` allein ist in einer nicht-UTC-Sitzung ein bis zwei Stunden je Tag falsch. ⭐ **Fein gespeichert (10 s / 60 s / 300 s je nach Länge), grob gezeigt (elf Klassen)** — die Anzeige darf später anders schneiden, und der Median wird im feinen Korb interpoliert. Der Durchschnitt kommt dagegen aus zwei eigenen Zählern und ist exakt. 💣 **Die Striche sitzen anteilig in IHRER Klasse**, nicht linear in Sekunden — linear klebten beide am linken Rand. 💣 **Unter jedem Strich liegt eine Fassung in der Kartenfarbe**: Tinte gegen goldenen Balken misst im dunklen Thema 1,48:1, mit Fassung 11,9:1. 💣 **EINE Balkenfarbe, keine sequenzielle Leiter** — `--color-accent-brown` ist hell dunkelbraun und dunkel ein helles Tan, die Hausleiter kehrt sich im Dunkelmodus um. 🔴 **Was die Zahl NICHT sagt** (alles in die kürzende Richtung): sie fängt bei null an · reines Zuschauen wird nach 15 min abgeschnitten (Owner-Entscheid 26.08.2026: „15 minuten sind ok") · ein verlorener `gone`-Beacon rundet auf den letzten Ping ab · unter 10 s ist die Genauigkeit gemischt. ⚠️ Seit 26.08.2026 zählt **Zoomen mit dem Mausrad** als Bedienung — davor tat es das nicht, und damit galt genau der stille Kartenleser nach 15 Minuten als weg. Tests: `api/_internal/analytics/__tests__/verweildauer-test.php`, `js/review/__tests__/verweildauer-render.test.js` (zusammen gegen 11 Mutationen geprüft).
- **Drei Ringe statt einem: Ansicht · Untergrund · Landschaften-Ebene** (Status → Besucher, live 29.08.2026). Der Kartenfächer kreuzt seit dem 26.08.2026 fünf Ansichten mit drei Untergründen (§11), die Statistik kannte aber nur die Ansicht — und `original` war bis dahin eine Ansicht gewesen, die Kurve hat den Untergrund mit diesem Tag also schlicht **verloren**. 💣 **Weder der Untergrund noch die Landschaften-Ebene wurde je gezählt**: es fehlte nicht der Ring, es fehlten die Daten. Neue Merkmale `map_style` (`#mapStyleSelect` → `change`) und `eco_kind` (`#ecosystem-layer-switch` → `click`/`keydown`), beide in `js/app/visitor-tracking.js`, in der Whitelist von `api/app/track.php` und in `avesmapsVisitorMetrics`. Kein DDL — `visitor_metric.metric` ist `VARCHAR(40)`. 🔴 **Beide Ringe starten LEER und füllen sich erst ab dem Deploy**; rückwirkend gibt es nichts. Deshalb sagt ein leerer Ring seit heute „noch keine Daten“ — davor zeichnete `vaDonut` einen unsichtbaren Kreis ohne ein Wort dazu, und ein weißer Kasten ist von einem kaputten Bauteil nicht zu unterscheiden. 💣 **Gezählt werden UMSCHALTUNGEN, keine Besucher** — wie beim bestehenden Ring; wer nie umschaltet, taucht nirgends auf. 💣 **Die Ebene zählt nur bei ÄNDERUNG.** Der Reiterbund kennt kein `change`, gelesen wird der resultierende Zustand im nächsten Tick — und ein zweiter Klick auf die schon aktive Kachel feuert wirklich noch einmal (live gemessen 29.08.2026), blähte also genau die Scheibe auf, die ohnehin vorne liegt. 💣 **„Alle“ trägt bewusst kein `data-ecosystem-kind`** (es ist ein Anzeige-Flag neben der Ebene) und bekommt den Schlüssel `alle` — ließe man es weg, wären die Prozente der übrigen vier falsch. 💣 **`vaPrettyMapStyle` schaut NICHT in `VA_ZURUECKGEZOGENE_MODI`.** Die Tabelle gehört den ANSICHTEN: sie macht aus `original` „Original (Ansicht bis 26.08.2026)“, damit die alte Ansicht vom gleichnamigen Untergrund zu unterscheiden ist. Am Untergrund wäre derselbe Griff genau verkehrt herum — dieselbe Verwechslung, nur andersherum. ⭐ **EIN Beschriftungsleser für alle drei** (`vaDomLabels`), gespeist aus dem DOM statt aus einer Tabelle: die Ansichts-Tabelle war 2026-08-13 genau daran auseinandergelaufen, und der dritte Ring wäre die dritte Kopie gewesen. ⚠️ Die Ringfarben sind die Palette DIESER Datei (hartkodierte Hex wie alle Ringe dort); §12 erlaubt Blau, wo es DATEN kodiert und nicht Rahmenwerk. Tests: `js/app/__tests__/besucher-tracking-haken.test.js` (führt `installVisitorTrackingHooks` mit gefälschtem jQuery wirklich aus) und `js/review/__tests__/visitor-analytics-render.test.js` (Beschriftungen gegen das echte `index.html`); zusammen gegen 9 Mutationen gefahren, alle gefangen.
- `docs/routing-featurestand.md`, `docs/stabilization-smoke-test.md`,
  `docs/routing-transport-smoke-checklist.md` — routing state & smoke checklists.
- `api/README.md` — canonical API contract.
- **`docs/refactoring-masterplan.md` — the active refactoring program (M0–M8).**

> Spent per-refactor process logs were deleted in M0; do **not** recreate
> per-split "boundary-check / stable" logs.

## 12. Design language (read before any CSS / UI work)

**One warm, *aventurian* visual language for every surface** — route planner
(`#search`), infobox (`.avesmaps-infopanel`), dialogs, popups, editor. Warm
browns + parchment + coat-of-arms gold. **No blue in the CHROME** — panels,
controls, links, menus (it reads as a foreign UI kit and is what made the panels
diverge). 🔴 Blue is allowed, and sometimes required, where colour encodes DATA
rather than chrome: the Landschaften water tones, the analytics chart palette,
the edit-in-progress handles, and the path-subtype colours of the Wege-Editor
list (`--color-path-*`). `docs/design-language.md` has always said this
("**No blue = UI chrome only**", with its own list of exceptions) — §12 stated
the ban flatly and was therefore read as absolute. Wording aligned 14.08.2026
after exactly that misreading; the rule itself is unchanged.

**Hard rule: never hardcode a colour / radius / divider — always use a token
from `css/base/tokens.css`.** Need a value with no token? Add the token first,
then use it. A literal colour written twice is divergence waiting to happen —
this is exactly how the infobox and route planner drifted apart (1000+ hardcoded
hex values across 38 CSS files).

- **Buttons** have a hierarchy: main action *filled* (`--color-button`), the rest
  *soft/outline* (`--color-button-soft`); radius `--radius-md`; no pill shapes.
- **Group by divider** (`--color-divider` line + heading), **not** by framed
  boxes; popup/infobox dividers run full-bleed (negative side-margin = padding).
- **Eine Kontur gehört dem BEARBEITEN, nicht dem Ansehen** (Owner 2026-08-03, gilt für alle
  Landschaften-Ebenen). Die Fläche füllt immer, ihre Kante zeichnet sie nur, wenn **eine** Ebene im
  Editiermodus offen ist — nicht in „Alle" und nicht für den, der die Karte bloß ansieht. Getragen von
  `ecosystem-pane--editable` an der Pane (`--eco-contour` in `css/features/ecosystem-layer.css`).
  ⚠️ „Im Frontend fehlen die Ränder" ist damit **kein Fehler**, sondern die Regel.
- **Eine Zeilenhandlung ist nie die Haupthandlung der Seite** (2026-08-07, gemessen an den
  WikiSync-Listen). Ein *gefüllter* Knopf in einer Listenzeile multipliziert sich mit der Zeilenzahl:
  „Fläche zuweisen" + „Label zuweisen" standen als Akzentknöpfe in **jeder** Regionenzeile — live
  2×1.577 = **3.154** davon in einer 246 px schmalen Spalte —, und der Wege-Reiter trug 462 weitere,
  während die Ortsliste daneben über 3.414 Zeilen **gar keinen** Knopf hat. Die Haupthandlung steht im
  Menüband und heißt „Syncen"; alles in einer Zeile ist weich/outline (`--color-button-soft*`,
  `--radius-md`). ⚠️ Die Klassen `region-sync__*` gehören **Regionen UND Wegen** (`review-path-sync.js`
  schreibt `.region-sync__cand` an 15 Stellen) — wer nur eine der beiden Listen anpasst, poliert die
  kleinere Hälfte und baut die Divergenz in die größere ein.
- **Links** use `--color-link` (gold-brown), never blue; **external links**
  (off-site) always get a trailing `↗`.
- 💣 **Ein Fenster ist entweder ein WERKZEUGFENSTER oder ein BLATT, und die Regel entscheidet,
  nicht der Geschmack** (Owner 04.09.2026, Mockup `docs/fensterformen-mockup.html`, Regelwerk
  `docs/design-language.md` §Fenster). *Werkzeugfenster* = bleibt offen, während man daneben
  arbeitet, trägt Menüband oder Fußleiste, ist einklappbar; *Blatt* = kurzes Formular, ausfüllen,
  speichern, weg. Vermessen vor dem Entscheid: **13** Rezepturen für den Schließknopf in sechs
  Formen, **9** Polsterwerte für die Kopfzeile, **4** Titelgrößen — während das Verschieben, das
  **einen** Erzeuger hat (`js/ui/dialog-drag.js`), nirgends auseinandergelaufen ist.
  🔴 **Fast keine neuen Tokens** — die Maße existieren als `--avm-*` und tragen die vier
  Editoren: `--avm-ribbon-pad` (10/14) fürs Menüband, `--avm-col-pad` für den Rumpf,
  `--avm-ribbon-gap` (6), `--avm-control-h` (32). **Der Seiteneinzug ist überall 14**; genau EIN
  Wert soll nachziehen (`--avm-col-pad` horizontal 12 → 14) — 💣 **aber NICHT global, solange
  nicht alle Fenster dort sind: neun lesen ihn gleichzeitig** (`.avm-col` in sechs Editoren,
  `.wp-fn`, `.wp-tempo`, der Vorkommen-Editor, fünf iframe-Seiten inline), und **kein Test hält
  den Zahlenwert fest**. Wer ein Fenster einzeln auf die Kante zieht, überschreibt den Token **an
  seiner Hülle** (so macht es `.gi-win` seit 04.09.2026); erst wenn alle dort sind, wandert der
  globale Wert und die lokalen Zeilen fallen. Und EIN Name kommt dazu:
  `--avm-kopf-pad` (**6**/14) für Kopf- und Fußleiste.
  💣 **Das senkrechte Polster folgt der BEDIENHÖHE des Bandes, das waagerechte niemals:**
  Kopf/Fuß tragen 32px-Elemente → 6, das Menüband 48px-Kacheln → 10; waagerecht bleibt überall
  14, daran hängt die eine Kante.
  💣 **`--space-N` ist NICHT N Pixel** — die Skala trägt global +2 (`--space-12` = 14px); wer
  sich für ein Mockup wörtliche Werte definiert, liegt auf jeder Kante 2px daneben.
  🔴 **Die Hülle hat `padding: 0`**, jede Zone trägt ihr Polster selbst — dadurch läuft jeder
  Trenner ohne negative Außenränder von Kante zu Kante.
  🔴 **Der Trenner unter der Kopfleiste ist DURCHGEHEND und steht immer — der TITEL bekommt
  keinen.** 💣 Die Falle ist eine generische Elementregel, die ins Bauteil hineinreicht: der
  Titel ist ein `<h2>`, also legt ein `h2 { border-bottom }` aus dem Dokument eine zweite,
  KURZE Linie unter das Wort (gemessen 350 von 469px). Element-Selektoren aus dem Inneren eines
  Bauteils heraushalten — im Mockup per `body > h2`, im Code per Klassenregel.
  🔴 **Scrollen: Kopf und Fuß stehen, nur der Rumpf läuft** (Owner: „…den titel aus den augen
  verliere"). Hülle `overflow: hidden`, Rumpf `flex: 1 1 auto; min-height: 0; overflow: auto`.
  💣 `min-height: 0` ist tragend — ein Flex-Kind schrumpft sonst nicht unter seinen Inhalt, und
  es scrollt wieder die Hülle. ⚠️ Nur DREI Regeln machen es heute falsch:
  `.location-report-dialog` (≈14 Fenster, „Ort bearbeiten"), `.avm-modal`,
  `.ecosystem-transfer-dialog`.
  🔴 **Listen: EIN Zeilenmaß, `.avm-row`** (`editor-row.css`) — gemessen 26 Rezepturen mit
  eigenem Polster. Die Funktion darf sich unterscheiden (Statuskreis, Bild, Pfeil, Zähler), das
  Skelett nicht. ⚠️ Kein neuer Vorschlag: §11 sagt seit 14.08.2026 „es gibt ZWEI, und das ist
  die Obergrenze".
  🔧 **Reiter: Entscheidung offen** — 9 Rezepturen in zwei Grundformen (Unterstrich `.avm-tab`
  gegen gefüllt `.ecosystem-layer-switch__tab`), dieselben fünf Beschriftungen in beiden.
  Vorschlag T3 im Mockup: Reiter (Ansicht im Fenster) = Unterstrich, Umschalter (Einstellung
  auf der Karte) = gefüllt.
  🔴 **Das Menüband ist `.avm-tile` / `.avm-ribbon`** aus `editor-body.css`, unverändert — nichts
  Neues bauen. 💣 `:where()` in `:where(.avm-editor-body) button` ist TRAGEND: ohne die Klammer
  (0,1,1 statt 0,0,1) kollabiert die Kachel gemessen von 48px auf 32.
  🔴 Titel **`--font-size-subhead`** in BEIDEN Bauarten · Kopflinie `--color-divider` ·
  Schließknopf 32×32, im Fenster gefasst, im Blatt nackt · Einklappen nur im Werkzeugfenster,
  und es bleibt stehen · **jedes** Fenster ist verschiebbar und trägt den Griff `⁝⁝`, gesetzt
  vom Zieh-Mechanismus (`is-draggable`), nie vom Markup — am Telefon zieht er nicht.
  ⚠️ Die **Bandhöhe** steht bewusst in keiner Tafel — sie ist Polster plus Inhalt (gemessen:
  Kopf 44,7, Menüband 68,7, Fuß 44,7); wer eine davon als `height` setzt, schneidet etwas ab.
  🔴 **Reiter = T3** (Owner 04.09.2026): im Fenster Unterstrich (`.avm-tab`), frei auf der Karte
  gefüllter Umschalter (`.ecosystem-layer-switch`) — der ORT entscheidet. 7 von 9 Rezepturen
  fallen weg, Pille `999px` und 11,5px inklusive.
  🔴 **Wappen/Thumbs in Listen: die Texte stehen bündig** (Owner) — der Bildplatz wird
  RESERVIERT, nicht gefüllt, und die LISTE entscheidet:
  `.avm-list:has(.avm-row__bild) .avm-row:not(:has(.avm-row__bild))::before`. 💣 Die Breite ist
  EIN Token (`--avm-row-bild-w`), den Bild und Platzhalter beide lesen. ⚠️ Die Höhe gehört der
  Liste (Wappen quadratisch, Cover 3:4, Kartenvorschau 4:3). ⭐ `:has`-Preis an 2000 Zeilen
  gemessen: 41,1 ms mit gegen 47,5 ms ohne — im Rauschen.
  🔴 **Die Bildlaufleiste gehört dem FENSTER, nicht dem Dokument** — heute 5 Rezepturen in 2
  Formen, und der Unterschied ist ein Versehen: `base.css` (7px) gilt in `index.html`,
  `editor-page.css` (10px/Pille/2px) in den iframes, und `lore.css` + die zwei
  `political-territory-editor*` tragen DREI ID-gescopte Abschriften der zweiten, nur weil ihre
  Fenster in `index.html` leben. Eine Regel an `.fen` löst alle drei auf.
  🔧 Offen: ob die SEITE selbst (Karte, Infopanel) von 7px mitzieht — für Besucher sichtbar.
- **New components:** reuse the nearest existing one as a template plus the
  tokens; match the warmth. Full guide: **`docs/design-language.md`**.
