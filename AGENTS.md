# AGENTS.md — Avesmaps project brief for AI agents

> Authoritative, tool-agnostic onboarding for any AI coding agent (Claude Code,
> Cursor, Copilot, Codex, …). Keep this file current; it is the single source of
> truth for "how this project is built". `CLAUDE.md` is a thin stub that imports
> this file.

## 1. Project purpose & status

Avesmaps is a **non-commercial fan project** for *Aventurien*, the world of the
German pen-and-paper RPG *Das Schwarze Auge* (DSA / "The Dark Eye"). It is an
interactive tile map **and an in-browser route planner**.

- **Live:** https://avesmaps.de (full app, with PHP+MySQL backend).
- **Frontend:** vanilla JavaScript, **no build step**, no bundler. `index.html`
  hand-includes ~117 `<script>`/`<link>` tags. Leaflet **1.9.4** with
  `L.CRS.Simple`, image bounds `0..1024`, zoom `0..5` (a marker tier exists up to 6).
- **Backend:** PHP 8 (strict types) + MySQL via PDO, hosted on **STRATO shared
  hosting**. Optional in the sense that the static map renders without it, but
  the live site relies on it for features, search, territories, routing, reviews
  and the editor.
- **Routing:** Dijkstra over a weighted graph built from GeoJSON paths
  (min-heap priority queue); runs both client-side and via `POST /api/route/`.

> **Note:** `llms.txt`, `site-summary.md` and `README.md` once claimed "no backend /
> no database / no external API". That was **false** and was **corrected on
> 2026-06-29** — all three now describe the PHP+MySQL backend (see §4).

## 2. Domain glossary (English ↔ Aventurien/DSA)

These German terms are **content, not translatable** — they are data, join keys,
or in-world vocabulary. Keep them as-is in code.

| German / in-world | Meaning |
|---|---|
| *Herrschaftsgebiet* | political / dominion territory (the core entity) |
| *Reich*, *Grafschaft*, *Baronie* | realm / county / barony (territory ranks) |
| *Umstrittene Gebiete* | disputed/contested territories (rendered as diagonal hatching) |
| *Weg / Pfad / Straße / Reichsstraße / Gebirgspass / Wüstenpfad / Flussweg / Seeweg* | path subtypes (stable keys in `PATH_SUBTYPE_KEYS`) |
| *Querfeldein* | synthetic cross-country route type |
| *Kreuzung* | crossing node (route graph), name prefix `Kreuzung-` |
| *Kraftlinien (powerlines), Regionen, Aggregat* | map display modes |
| *Wappen* | coat of arms |
| *Literatur* | the published DSA works tied to a place (code: `game-literature` / `GameLiterature`). *Abenteuer* is **one kind** of it, not the category. 💣 Not to be confused with *Quellen* — see the next row |
| *Quellen* | the **citations** an entry carries (`sources` + `feature_sources`, `publication-*.php`, `avesmapsPublication*`, `publisher`). The opposite direction from *Literatur*: a work vs. the reference to a work. The two words must never share an autocomplete prefix |
| *Gültigkeit / valid_to_bf*, **BF** | in-world validity; **BF = "Bosparans Fall"** calendar year (e.g. `1049 BF`) |
| *Derived / abgeleitete Außengrenze* | computed outer boundary of a territory aggregate |
| *WikiSync* | importer that crawls *Wiki Aventurica* into staging tables |
| *Schraffur* | hatching (contested-territory rendering) |
| *Albenhus / Zwerch* | known data-anomaly territories (display-inheritance bug, see §10) |

Settlement type slugs (stable keys): `metropole`, `grossstadt`, `stadt`,
`kleinstadt`, `dorf`, `gebaeude`. Their **visible labels** are German UI strings
(see §8).

## 3. Architecture map

Build-free, multi-`<script>` app. Load order in `index.html` is a **contract**
(later files can shadow earlier globals — verify order before assuming which
definition wins). Frontend clusters under `js/`:

| Dir | Responsibility |
|---|---|
| `js/app/` | bootstrap, `api-client.js`, runtime state, shared utils, share-link |
| `js/map-features/` | ~50 modules: layers, markers, labels, regions, political layer, canvas overlays |
| `js/territory/` | political-territory editor (embedded host), wiki-tree engine, derived geometry |
| `js/routing/` | route engine, graph, plan/render, waypoints (+ inline glue in `index.html`) |
| `js/review/` | review panels, WikiSync UI |
| `js/ui/` | spotlight search, popups, controls |
| `js/community/` | location reviews/ratings |
| `js/pages/` | scripts for standalone `html/*.html` pages |
| `js/third-party/` | Leaflet 1.9.4, jQuery 3.6.0, jQuery-UI, leaflet.textpath, polygon-clipping, polylabel |

Rendering: Leaflet SVG paths for vectors; **Canvas overlays** for markers,
labels, boundaries, contested hatching (performance). SQL is the source of truth;
the map hydrates from `GET /api/app/map-features.php`.

## 4. API contract

Backend lives under `api/`, tiered by audience:

| Zone | Purpose | Auth |
|---|---|---|
| `api/route/`, `api/locations/` | **stable** public developer API | public |
| `api/app/` | app-facing read endpoints (map-features, search, coat, territory-detail, …) | mostly public |
| `api/edit/{map,political,reports,wiki}/` | authenticated editor/review writes | capability-gated |
| `api/import/location-reports/` | server-to-server import | import token |
| `api/diagnostics/` | read-only diagnostics | mixed |
| `api/_internal/{routing,wiki,political}/` | private PHP libraries (no direct public surface) | — |

**Stable contract = `POST /api/route/` and `GET /api/locations/`.** They define
the canonical envelope every endpoint is being unified toward:

```
success: { "ok": true,  ... }
error:   { "ok": false, "error": { "code": "<machine_code>", "message": "<human>" } }
```

Auth/CORS/JSON live in `api/_internal/bootstrap.php`
(`avesmapsLoadApiConfig`, `avesmapsApplyCorsPolicy`, `avesmapsJsonResponse`,
`avesmapsReadJsonRequest`, `avesmapsCreatePdo`) and `api/_internal/auth.php`
(`avesmapsRequireUserWithCapability`). Protected dirs (`_internal`, `_schema`,
`diagnostics`) must be `.htaccess`-denied in deployment. **Canonical reference:
`api/README.md`.**

> The contract is **not yet uniform** — most endpoints still return a flat
> `error: "string"` and several leak exception text. This is the subject of the
> refactoring milestone M3 (see `docs/refactoring-masterplan.md`).

## 5. Data model

Schema of record currently lives **mostly as inline `CREATE TABLE IF NOT EXISTS`
DDL in PHP** (self-healing pattern), partially mirrored in `sql/`. Key tables:

- `map_features` — locations, crossings, paths, rivers, regions, labels (+ `map_revision`, `map_audit_log`, `map_feature_locks`, `editor_presence`).
- `political_territory` + `political_territory_wiki` + `political_territory_geometry` (+ `_geometry_audit_log`, `_derived_geometry`, `_claim`) — territory hierarchy via `parent_id`, GeoJSON Polygon/MultiPolygon, BF-year timeline (`valid_from_bf`/`valid_to_bf`, `9999` = open/never-dissolved sentinel).
- `location_reports` / `map_reports`, `map_reviews`, `map_share_links`.
- `changelog_entry` — the milestones behind „Änderungsverlauf" in the notices dialog (see §11).
- `sources` + `feature_sources` — multi-source system (shipped): shared source catalog (`url`/`url_hash`=SHA256, `label`, `source_type`, `is_official`) linked to elements (`entity_type` ∈ settlement|region|path|territory|citymap|lore, `entity_public_id` — VARCHAR(190), widened 2026-07-22 because a lore key is a wiki-article slug, not a short public id, `status`). Public read `GET /api/app/feature-sources.php`; editor write `POST /api/edit/map/feature-sources.php` (capability `edit`, dedup by `url_hash`, atomic `other_source` takeover). Wiki-publication bulk lookup + provenance **shipped**: `feature_sources` gains `origin` (wiki_publication|manual|community), `reference_kind`, `pages`, `note` (+ `status='suppressed'` tombstone); a resumable `publication_sources` dump-sync phase parses `{{Infobox Produkt}}` + `==Publikationen==` into `wiki_publication_catalog`/`wiki_entity_publication` staging, and an **owner-triggered `sync_publications` action** reconciles them override-safely into the wiki layer (writes/deletes ONLY `origin='wiki_publication'`, manual/suppressed untouched, idempotent by `wiki_key`). Sources travel in the `map-features` payload (shared `source_catalog` + per-entity refs, rendered synchronously — no lazy per-popup fetch). See §11.
- WikiSync staging: `wiki_sync_runs/pages/cases`, `wiki_*_staging/queue`, `political_territory_wiki_test`, `wiki_territory_model`, `wiki_redirect_alias`.

> 💣 **Sources live in ONE place. Never build a second source system.**
> A new entity that needs sources joins `sources` + `feature_sources` by adding its
> name to the `entity_type` whitelist — today `settlement | region | path | territory
> | citymap | lore`, in `api/edit/map/feature-sources.php` and
> `api/app/feature-sources.php`. That is a two-line change, and it is how `citymap`
> and `lore` joined.
>
> This is written down because it was ignored once: the Lore feature (Natur & Waren,
> 2026-07-21) shipped its own `lore_source` table. The cost was not theoretical — a
> publication title is stored once in the shared catalogue but was duplicated into
> every one of ~35 000 lore rows, the editor had no add/remove/autocomplete and no
> `note`/`status` provenance, and the same wiki publication data flowed through two
> unrelated reconcilers. **Undone on 2026-07-22** (spec:
> `docs/superpowers/specs/2026-07-22-lore-quellen-vereinheitlichung-design.md`): it
> cost a schema widening, a data migration and a re-test of the whole lore sync —
> against the two lines it would have cost up front.
>
> The tell: if you are about to write `CREATE TABLE <feature>_source`, stop — the
> answer is one more `entity_type`.

**`wiki_key` derivation is a fixed table, never a locale.** `avesmapsPoliticalSlug()`
and the WikiSync match key fold non-ASCII through `avesmapsFoldToAscii()`
(`api/_internal/text/ascii-fold.php`) — **not** `iconv//TRANSLIT`, which is
libc-dependent and keyed the same name differently on the dev machine and on
STRATO. The table reproduces the **server's** form: umlauts fold to `'?'` and
lose their base letter (`Fürstentum Kosch` → `wiki:f-rstentum-kosch`), verified
against 1384/1384 live rows. Making it "nicer" rewrites every umlaut-bearing key
and silently breaks every join using one — that is a data migration across ~10
tables, not an edit to the table. Guarded by `tools/wikidump/test-ascii-fold.php`
(self-test panel) and `tools/wikidump/verify-live-key-parity.php` (against prod).

**Coordinate convention:** GeoJSON stores `[x, y]`; Leaflet `L.CRS.Simple` uses
`[lat, lng] = [y, x]`. Always swap consciously.

## 6. Build / deploy flow

- **No build.** Source files are served directly.
- GitHub Action `.github/workflows/deploy-avesmaps-strato.yml` mirrors an
  **allowlist** of items to STRATO over SFTP. **It does not delete** — files
  removed from the repo persist on the server (see §10 server↔repo drift).
- An asset-version stamping step rewrites `?v=` hashes for every asset reachable
  from `index.html` / `html/*.html`, following the CSS `@import` chain. **The
  sources themselves stay unstamped** (§7).

## 7. Asset-versioning gotcha (read before debugging "my change didn't show up")

Two independent cache-busting mechanisms:

1. **Automatic** content-hash `?v=` for everything reachable from `index.html` or
   an `html/*.html` page — linked directly *or* through the CSS `@import` chain
   (`styles.css` → `base/tokens.css` → …, at any depth). Handled by the deploy
   stamping step; do nothing. **Never write a `?v=` by hand anywhere** — the deploy
   overwrites it and a hand-written tag can only go stale. The deploy verifies the
   chain and refuses to upload if a hash disagrees.
2. **Manual** `const ASSET_VERSION` in
   `js/territory/territory-editor-inline-host.js` — governs the **dynamically
   loaded editor HTML/CSS/JS**. **Bump it on every change to editor assets**, or
   the browser serves stale editor code. `inline-host.js` itself is loaded by
   `index.html` *without* `?v=`, so after editing it you need one hard reload.
3. **Manual, and the one exception to "never by hand":** `edit/index.php` links
   `css/pages/edit.css` with a hand-written `?v=`. The stamper walks `index.html`
   and `html/*.html` only, so it never reaches a `.php` page and cannot overwrite
   or verify this tag. **Bump it by hand whenever `edit.css` changes**, otherwise
   editors keep the cached stylesheet. Rule 1's ban applies to everything the
   stamper *does* reach — which is everything else.

Diagnosis when a deployed change doesn't appear: compare `fetch(url+'?cb='+Date.now())`
(server-fresh) vs `fetch(url)` (as the app loads). See
`docs/asset-caching-and-versioning.md`.

## 8. Language policy

Primary UI language is **German** and stays German. An i18n layer
(`?lang=en` overlay backed by a string table) is planned (milestone M8) — German
is the default, English is opt-in. Therefore:

- **Do not** translate user-facing German UI strings inline (that would change the
  default UX). Extract them into the i18n table instead.
- 💣 **Code comments, docs and commit messages are written in GERMAN** — like the
  code you are standing in. The `error.code` machine values stay English and are
  never translated; internal API error *messages* may be either.
  🔴 This line said the opposite until 2026-08-15 ("write them in English going
  forward"). It was dead law and had been for months: every spec under
  `docs/superpowers/`, every recent commit subject and nearly every new source file
  is German, while a handful of older libraries are English. A reviewer following
  the old rule flagged brand-new, correctly-written German comments as a deviation
  — which is what a rule nobody follows costs. Owner's ruling on that review:
  German, and fix the rule. **Match the file you are editing**; do not translate
  an existing English file into German just because of this line.
- Never translate domain content (§2), `<option value>` slugs, `queryParam`
  toggle names, `PATH_SUBTYPE_KEYS`, or the `BF` calendar suffix.

## 9. Dev conventions

- **OS:** Windows + PowerShell. Watch the CRLF edit trap (prefer single-line
  edits on CRLF files). `.gitattributes` now sets `text=auto` + binary markers.
- **Git:** small, verified commits **directly to `master`**; push triggers a
  ~1–2 min auto-deploy. Verify the remote SHA after pushing. Conventional-commit
  prefixes (`feat/fix/chore/docs/perf/refactor`, plus the repo's custom `ui:`). 💣 **A scope word names ONE feature.** `verlauf:` belongs to the paths' wiki-course sync (`path-verlauf.php`, `verlauf_cases`) and **never** to the „Neuigkeiten" window — that one is `neuigkeiten:` or `changelog:`. It was miscommitted twice on 2026-08-08; `git log --grep` and `git grep` cannot tell the two apart afterwards.
- **Shared working tree — never `git add -A`:** multiple agent sessions share
  this one checkout and `.git`, and other sessions may have **live uncommitted
  work** in the tree right now. Never `git add -A`, `git add .`, or `git commit
  -a` — that sweeps another session's half-finished files into your commit under
  your message and mangles their history. Always `git status` first, identify
  changes that aren't yours, and stage **only the files you yourself touched**,
  by explicit path. Leave foreign modified/untracked files alone (that session
  will commit them). If a push is rejected, `fetch` + `rebase origin/master`
  (autostash) + retry — never force-push.
- **STRATO caution:** never loop expensive endpoints (e.g. the political layer) —
  it saturated PHP workers once and looked like a DB outage. Probe with a single
  request.
- **Editor-visible change → name it in the commit subject; do NOT edit the
  handbook in passing.** `html/editor-handbuch.html` is owned by a nightly routine
  (`avesmaps-handbuch-pflege`, 00:00 local — see §11), not by whoever happens to
  touch an editor surface. Your only obligation: a commit subject that states the
  user-visible effect ("rename the X button", "new Y tab", "Z now runs globally").
  The routine reads `git log`, judges whether an editor following the handbook
  would now find something different, and rewrites the affected section. Editing
  the handbook yourself competes with that routine over the same file and its
  `Stand:` date — leave it alone unless the owner asks you directly. This replaced
  the old "same commit" rule on 2026-07-22: it depended on every one of many
  parallel sessions remembering, and a wrong handbook is worse than none (it went
  from written to materially wrong in **13 days**, 2026-07-07 → 07-20, back when
  it was nobody's job).
- 💣 **Sichtbare Änderungen an der Oberfläche gehen EINZELN live, und der Owner sieht jede.**
  Ein Commit, ein Push, sein Blick — dann der nächste. Kein Bündel.
  Am 10.08.2026 gingen neun Mobil-Commits am Stück auf die Live-Seite; vier Regressionen waren
  darin, gemeldet vom Telefon des Owners („irgendwie is mobil jetzt alles kaputt"), Rückbau war
  die einzige schnelle Antwort. Der Auftrag hatte „Aufgabe für Aufgabe, Halt nach jeder"
  gelautet; ein späteres „zieh alles durch" meint das Tempo, **nicht** den Wegfall des Blicks.
  ⚠️ Gilt für alles, was ein Besucher sieht — Layout, Bedienelemente, Kartenbedienung. Nicht
  für Tests, Doku und reine Innenumbauten.
- 💣 **Abnahme heißt ABLAUF, nicht Maß.** Eine Prüfseite, die Rechtecke misst, ist kein Beleg,
  dass etwas funktioniert — sie belegt nur, dass eine Zahl stimmt. Vor „fertig" werden die
  echten Handgriffe ausgeführt und benannt: das Ding anklicken, das Panel aufziehen, den
  Suchtreffer wählen, die Infobox öffnen. Genau diese drei Handgriffe waren am 10.08. kaputt,
  während sämtliche Maßtabellen grün waren. ⚠️ Was ein Emulator nicht beantworten kann
  (Bildschirmtastatur, echtes Touch-Verhalten), wird als offene Frage gemeldet, nicht als
  bestanden.
- 💣 **Der eigene Entwurf ist die Abnahmeliste.** Jede Zeile mit 💣 / ⚠️ / 🔴 in Spec und Plan
  wird vor „fertig" einzeln abgehakt — erfüllt, oder ausdrücklich verworfen mit Begründung.
  Zwei der vier Regressionen vom 10.08. standen wörtlich als Warnung im eigenen Entwurf
  (`--avesmaps-corner-stack` an die Steuerhöhe binden; die versprochene Animation) und wurden
  nicht gebaut. Es fehlte kein Wissen, sondern das Abhaken.
  ⭐ Dafür gibt es zwei Sub-Agenten: **`usability-konsistenz`** (vor jedem Commit: Entwurf gegen
  Diff, gekoppelte Werte, gewinnt die Regel überhaupt?) und **`usability-design`** (vor dem
  Push: Mockup gegen gebauten Zustand, Designsprache, Rangfolge in hell UND dunkel). Sie
  ersetzen das Abhaken nicht, sie fangen das, was man selbst überliest.
- 💣 **Vor dem Push läuft das GANZE Testfeld, nicht die eigenen Tests.** Der Deploy ist
  ein Tor: ein einziger roter Test lädt **nichts** hoch. Am 12.08.2026 brach eine
  erweiterte Selektorliste in `legal-dialog.css` einen Test in
  `js/app/__tests__/touch-scale.test.js`, der wörtlich einen Block nur für die Suchkachel
  erwartete — an der Regel war nichts falsch, und fünf Deploys hintereinander fielen aus.
  Wer nur seine eigenen Tests laufen lässt, sieht so etwas nie: die Datei, die bricht,
  gehört jemand anderem. Der Lauf ist der aus dem Workflow:
  `for t in $(find js tools -path '*__tests__*' -name '*.test.js'); do node "$t"; done`
  PHP analog -- 💣 **aber mit den Erweiterungen, sonst luegt der Lauf**:
  `for t in $(find api tools -path '*__tests__*' -name '*-test.php'); do php -d zend.assertions=1 -d assert.exception=1 -d extension=php_mbstring.dll -d extension=php_pdo_sqlite.dll -d extension=php_gd.dll "$t"; done`
  ⚠️ Ohne `mbstring`/`pdo_sqlite`/`gd` melden **45 Tests** rot, die alle nur die Erweiterung
  vermissen und es in ihrer eigenen Fehlermeldung auch sagen -- am 15.08.2026 gemessen. Wer das
  nicht weiss, haelt das Testfeld fuer kaputt oder sich selbst fuer den Verursacher. Vorbestehend
  rot bleibt genau einer: `linkcheck/link-url-test.php` (echter DNS-Abruf), kein Regressionssignal.
  💣 **Und dieser Lauf ist NICHT das ganze Feld -- der Workflow faehrt mehr, als dieses Muster
  findet.** Unter `tools/wikidump/` liegen 21 weitere PHP-Tests, die weder in einem `__tests__`-
  Verzeichnis stehen noch auf `-test.php` enden (sie heissen `test-*.php`). Am 15.08.2026 kostete
  genau diese Luecke **zwei** Deploys: ein Commit gab der geteilten Fixture `mini-dump.xml` eine
  24. Seite und zog nur den eigenen Test nach; `test-dump-reader.php` schreibt dieselbe Zahl an
  sieben Stellen fest und fiel um. Beide Sitzungen fuhren vorher ein gruenes Feld -- das Muster
  oben sah die Datei nie. Also zusaetzlich:
  `for t in tools/wikidump/test-*.php; do php -d extension=php_mbstring.dll "$t" >/dev/null || echo "ROT: $t"; done`
  ⚠️ **Und danach: der Fehlschlag vergiftet den `?v=`-Stempel.** Der nächste grüne Lauf
  hält die nie hochgeladenen Dateien für aktuell — live standen zwei davon auf HTTP 404
  und sechs in alter Fassung, während `index.html` schon die neue anforderte. Nur eine
  **Inhaltsänderung** heilt das. Diagnose: `fetch(url+'?cb='+Date.now())` gegen
  `fetch(url)` (siehe `docs/asset-caching-and-versioning.md`).
- 💣 **Ein SQLite-Test kann eine MySQL-Regression ERZWINGEN.** Am 16.08.2026 baute eine Sitzung
  `DELETE g FROM political_territory_geometry g LEFT JOIN …` in
  `DELETE … WHERE id IN (SELECT g.id FROM political_territory_geometry g …)` um, damit die
  SQLite-Fixture den Sammelknopf fahren konnte. MySQL lehnt genau das mit **Error 1093** ab
  („You can't specify target table … for update in FROM clause"): der Knopf wäre live beim ersten
  Klick abgebrochen, und der Test wäre **grün geblieben**, weil SQLite die Einschränkung nicht
  kennt — der Test hatte die Regression nicht verhindert, sondern erzwungen. ⭐ Das Haus-Idiom
  dagegen steht längst da: `avesmapsPoliticalPruneGeometryAuditLog` (`territories-audit.php`)
  wickelt die Subquery **doppelt** in eine Ableitungstabelle
  (`… IN (SELECT id FROM (SELECT id FROM …) x)`) und erzwingt so die Materialisierung; das läuft
  auf beiden. ⚠️ Die Lehre ist grösser als 1093: wer die Produktionsform verbiegt, damit ein Test
  läuft, hat den Test gegen die Produktion gedreht. Geht beides nicht, gilt **MySQL** — und ein
  Kommentar an der Stelle sagt warum, sonst „vereinfacht" der nächste Leser sie zurück.
- **Secrets:** `api/config.local.php` is gitignored and must never be committed.
  No production dumps, reports, audit logs, tokens or credentials in the repo.
- **Legal:** DSA assets follow the Ulisses fan guidelines (see `NOTICE.md`).

## 10. Known fragilities

- **Server↔repo drift:** the prod webroot has PHP files not in the repo (deploy
  never deletes). "Not in the repo" ≠ "safe to delete". A 161-orphan surgical
  cleanup ran 2026-06-14 (deleted the pre-reorg flat `js/`, old icons/css/images,
  M1/M4 leftovers, and stale dirs `/map /politics /test /js/pages /css/legacy`)
  via the deploy's "Retire orphaned remote files" step — **never `mirror --delete`**
  (its dry-run would also delete live files). **Still load-bearing on the server &
  protected:** `tiles/` (base map; tile files use NEGATIVE y, `map_x_-y`),
  `uploads/`, `admin/phpMyAdmin`, `api/wiki-sync.php` (frontend fallback),
  `api/app/.user.ini`, `config.local.php`. Root shims `api/{auth,bootstrap}.php`
  are kept (clean-deploy safety; the `political-territory-lib`/`wiki-sync-lib` lib
  shims were removed in M3 step 4). Directory URLs return 404 on this server
  (listing off) — not a sign of deletion.
- **`territories-endpoint.php` runs DDL + metadata probes before its cache read**
  on every political-layer request; the derived layer has an N+1 over the full
  territory table. Both are perf hotspots (milestone M6).
- **Albenhus/Zwerch** display-inheritance anomaly: a save writes resolved displays
  globally onto all ancestors.
- **Schema is in code, not `sql/`:** ~14 tables exist only as inline PHP DDL;
  `sql/` is a partial, partly-stale snapshot. `map_feature_relations` and
  `map_proposals` are defined but unused (dead schema).
- 💣 **A silent MySQL truncation is indistinguishable from "nothing was ever
  saved".** `app_setting.setting_value` was `VARCHAR(255)` — sized for kill-switch
  flags (`"0"`/`"1"`) and fine for three months. The first key holding a real value,
  `travel_values`, is **1414 characters**: outside strict mode MySQL cut it, the
  reader's `json_decode` returned `NULL`, and every reader fell back to its constant.
  No error, no exception, no warning — and the Tempowerte window's „Speichern"
  therefore did nothing at all from 2026-08-14 until it was measured the same day.
  Widened to `MEDIUMTEXT`; existing installs are pulled up by
  `avesmapsAppSettingEnsureWideValue()` (⚠️ **call it only from writers of large
  values** — its `information_schema` probe on the kill-switch path is exactly the
  load §10 already lists). **The rule this leaves behind: a writer whose value
  matters must READ IT BACK before treating the write as done** — a marker may only
  attest that something *is there*, never that a write *was issued*
  (`avesmapsTravelValuesStoredMatches`).
- 💣 **`css/pages/political-territory-editor-inline.css` is a BUILD PRODUCT** —
  `tools/scope_editor_css.js` generates it from `political-territory-editor.css`,
  `-layout.css` and `political-territory-wiki-tree.css`. A rule written into it by
  hand works immediately and dies, silently, at the next `node tools/scope_editor_css.js`.
  That happened three times (`e5cb8f1b`, `127a7c78`, `5fa1f323`); the regeneration in
  `0bc22ffc` took all **14** rules with it — the Hierarchie-Level table, the
  transparency row and the WikiSync button — and nobody noticed for two weeks, until
  the colour picker started sticking out of its panel. **Edit the source, then
  regenerate, then bump `ASSET_VERSION`** (§7). Guarded by
  `tools/__tests__/scope-editor-css.test.js` (generated == generator output; no markup
  class without a rule). ⚠️ `css/components/political-territory-editor-columns.css`
  loads *after* the generated sheet and blanket-sets `.manual-data-section table {
  table-layout: auto }` — an equal-specificity `table-layout: fixed` in the editor
  sheet is inert, so address such a table through its section.
- Several edit endpoints leak `getMessage()` to clients (info disclosure,
  milestone M1).

## 11. Documentation index

Authoritative docs (being translated to English in M8):

- **`docs/design-language.md` — the warm/aventurian design language + token rules (see §12). Read before any CSS/UI work.**
- **Die Listenzeile — es gibt ZWEI, und das ist die Obergrenze.** `.wikisync-itemlist .tree-item` (`css/components/region-sync.css`) traegt alle acht Listen des WikiSync-Panels, `.avm-row` (`css/components/editor-page.css`) alle sechs Listen der Editorfenster. Entwurf **`docs/superpowers/specs/2026-08-14-wikisync-listen-vereinheitlichung-design.md`**, Mockup `docs/wikisync-listen-mockup.html`, live 14.08.2026. Davor waren es **sieben** Rezepturen: zweimal dieselbe Panel-Zeile in zwei Dateien, eine dritte als ID-Regel, die beide ueberstimmte, plus vier inline abgeschriebene in den Editoren — eine davon mit dem Kommentar „Referenz .se-row“ ueber sich. 💣 Beim Abschreiben faellt die Skala: `.se-row-type`/`.se-row-l2` standen auf `font-size:10px`, unter der 11px-Untergrenze aus §12, und der Fehler wanderte von einer Abschrift in die naechste. 💣 Eine ID-Regel auf einer Listenzeile ueberstimmt die geteilte lautlos (1,1,0 gegen 0,2,0) — die Aenderung wirkt dann bei zwei von acht Listen einfach nicht. 🔴 Der Statuskreis haengt an `.has-map-status` und gehoert NUR den fuenf Karten-Subjekten; sein Fehlen bei Literatur, Karten und Vorkommen ist Information. ⚠️ Ausdruecklich gesetzt statt aus den Daten abgeleitet, weil Kraftlinien gar keinen `.tree-map-status`-Marker setzt und trotzdem einen (immer leeren) Kreis traegt. Die Bilanzzeile unter der Suche hat EINEN Erzeuger (`js/review/review-list-balance.js`) und traegt nur, was der Filter bewegt. Gewacht von `js/review/__tests__/wikisync-list-form.test.js`, `…/wikisync-balance-line.test.js` und `js/pages/__tests__/editor-row-single-source.test.js`.
- **`html/editor-handbuch.html` — the handbook human editors actually read.** Not a doc *about* the code; it is product surface, reachable from the edit shell's top bar and the editor panel's status line. Five layers: Erste Schritte / Karten-Features / Aufgaben / Verstehen / Nachschlagen. **Maintained by a nightly scheduled task, not by feature sessions (see §9)**: `avesmaps-handbuch-pflege` runs at 00:00 local and does two things — (A) read every commit since its last run and rewrite the sections an editor would now find wrong, (B) re-verify 3–4 of the 33 anchored `<h3 id=…>` sections against the live code on rotation, so the whole book is checked roughly every 8–10 days. It commits only when something actually changed, and only `html/editor-handbuch.html`. Plan and gap inventory in `docs/superpowers/plans/2026-07-20-editor-handbuch-aufwertung.md`.
- `docs/asset-caching-and-versioning.md` — **the deploy/cache gotcha** (see §7).
- `docs/future-map-architecture.md` — north-star architecture & full data model.
- `docs/territories.md` — political-territory data model + WikiSync.
- `docs/refactoring-map.md` — frontend responsibility-cluster map & load order.
- `docs/repository-data-policy.md` — what may/may not enter the repo.
- `docs/database-backup.md` — **the full-database backup** (edit shell top bar → „💾 Datenbank-Backup", `edit/backup.php`, **admin only**). Chunked resumable dump → one gzip-packed `.sql` that restores with `gunzip -c … | mysql` or a phpMyAdmin import; lib `api/_internal/backup/db-dump.php`, endpoint `api/edit/admin/database-backup.php`, state `db_backup_run`, files in HTTP-denied gitignored `uploads/db-backups/` (3 newest kept). 💣 **The `.gz` is built the pigz way — one gzip MEMBER, not one member per chunk.** A deflate stream cannot be resumed across requests, so the naive writer appends a member per flush; `gunzip`/zlib read that whole, but PHP's `gzdecode()` and 7-Zip's GUI read only the FIRST member and hand back a silently truncated dump. Hence: fixed 10-byte header once, then per flush a FRESH raw-deflate context with `ZLIB_SYNC_FLUSH`, then a final empty block + CRC/ISIZE, with the CRC carried across steps via `crc32_combine`. The unit test's `gzdecode()`-returns-everything assert is what guards it. ⚠️ Hot backup (no transaction spans the run) and the file carries password hashes — never in the repo.
- **Das Fenster „Neuigkeiten"** — die Projekt-Meilensteine, erzählt für Leser, die weder Commits noch Code kennen. Live 2026-08-03; **`docs/neuigkeiten-fenster.md`**, Mockup `docs/changelog-mockup.html`. 🔴 Die **Beschriftung** heißt seit 09.08.2026 „Neuigkeiten", der **Code** weiter `changelog` (Tabelle `changelog_entry`, i18n-Schlüssel `changelog.*`, Endpunkt, Dateinamen) — der Deploy löscht nie (§10), eine umgetaufte Adresse liesse eine gecachte `index.html` ins Leere greifen. Aus demselben Grund ist **`verlauf:` kein Commit-Scope dafür**: das Wort gehört dem Wiki-Kurs-Sync der Wege (`path-verlauf.php`, `verlauf_cases`). 💣 Vier Fallen, alle im Dokument begründet: die Ecke ist EIN Bund (`#map-corner-actions`, `--avesmaps-corner-stack` an `:root`) · eine leere Tabelle fällt auf `avesmapsChangelogSeed()` zurück, und ein Seed sieht genau wie ein gepflegter Verlauf aus (so blieb er fünf Tage unbemerkt stehen) · `overflow-anchor: none` am Scroll-Kasten ist tragend · die pflegende Routine hat keine Session, nur ein Token mit **eigenem** Schlüssel (`changelog.app_token`, nie der von Discord) und nur `list`/`save`, nie `delete`.
- **Tastaturbefehle** (`js/app/keyboard-shortcuts.js`, live 2026-08-05) — die Karte lässt sich mit der Tastatur bedienen: Suche, Reiseziel, Verschieben/Zoomen, die sechs Ansichten, die sechs Ortsklassen, schnellste/kürzeste Route, Etappe vor/zurück. 💣 **Die Liste `SHORTCUTS` in dieser Datei ist die EINZIGE Quelle** — sie ist zugleich die Belegung *und* der Bauplan der Tabelle unter **Hinweise → „Bedienung" → „Bedienhilfen"** (`#legal-shortcuts`, Stil in `css/components/legal-dialog.css`). Wer eine Taste woanders verdrahtet oder die Erklärung von Hand in `index.html` schreibt, baut die Divergenz ein, die das verhindern soll. 💣 **Kein Strg, kein Alt, kein Meta** (Owner-Entscheid 2026-08-05): der erste Entwurf legte sieben Befehle auf Strg, und die gehören dem Browser (Strg+R lädt neu, Strg+P druckt, Strg+F sucht im Text, Strg+L/K sind die Adresszeile und lassen sich nicht überall abfangen). Dieselben Buchstaben ohne Strg — `matchShortcut` gibt bei jedem dieser Modifier sofort auf. 💣 **W A S D schieben und liegen VOR den Ansichten — diese vier Buchstaben sind für eine Ansicht unerreichbar.** `matchShortcut` nimmt die erste Zeile mit der Taste, und das Schieben steht oben. Daran ist „Standard" auf `S` gescheitert (Owner 2026-08-05, danach zurückgenommen: „behalte WASD"); die Ansicht sitzt seither auf `N`. Wer einen neuen Buchstaben vergibt, prüft ihn zuerst gegen W A S D. Die Ansichten heißen `O` Original, `P` Politisch, `K` Kraftlinien, `N` Standard, `L` Landschaften, `I` Nur Karte — „Landschaften" ist seit 2026-08-05 dabei (ansehen darf die Ebene seit 2026-08-04 jeder, nur das Bearbeiten hängt an einer Fähigkeit). ⚠️ **Leaflets eigene Tastatursteuerung ist abgeschaltet** (`map.keyboard.disable()`): sie wirkte nur nach einem Klick auf die Karte, und ihre Pfeiltasten hätten sich mit den hiesigen zu doppelten Sprüngen addiert. ⚠️ Der Riegel ist EINE Regel für alle: nichts wirkt beim Tippen, bei offenem Fenster (`[role=dialog]` sichtbar) oder bei laufendem Werkzeug — erkannt an den Klassen, die die Werkzeuge ohnehin an den Kartencontainer hängen (`TOOL_CLASSES`), also ohne zweiten Zustand. Test: `js/app/__tests__/keyboard-shortcuts.test.js`.
- **Die Ansichts-Kachel in der Kartenecke** (Entwurf `docs/superpowers/specs/2026-08-11-ansichts-kacheln-design.md`, Mockup `docs/layer-kacheln-mockup.html`, live 2026-08-12) — die sechs Derographie-Ansichten stehen als Kachel unter dem Suchknopf im Bund `#map-corner-actions`; zugeklappt zeigt sie die eingestellte Ansicht samt Namen, aufgeklappt faltet sie sich zu allen sechs auf (am Zeiger eine Reihe, am Telefon 2×3). 🔴 **Damit ist die Zeile „Derographie" aus dem Routenplaner verschwunden** — wer sie sucht: sie ist nur AUSGEBLENDET, das `<select>` steht weiter im DOM, denn es IST der Zustand (`getSelectedMapLayerMode` liest es, der geteilte Link `?mapLayerMode=…` kommt dort an). `?layerPanelActive=0` ist der Notausgang und holt die Auswahlbox zurück. 💣 **Vier Kopplungen, jede einmal danebengegangen und vom Owner gesehen, nicht vom Werkzeug:** die aktive Ansicht ist immer die LETZTE Rasterzelle und Kachel wie Raster tragen dieselbe Polsterung an derselben Ecke — nur so bleibt sie beim Aufklappen auf ihrem Fleck, ohne dass ein Versatz gerechnet wird · das Raster steht IM FLUSS, nicht schwebend (schwebend verdeckte es die Zoom-Knöpfe; im Fluss wächst der Bund und der Zoom liest dessen gemessene Höhe) · **an der Hülle werden `opacity` und `transform` NICHT animiert**, Blende und Bewegung sitzen an den Zellen und die aktive ist von beidem ausgenommen (sonst wackelt bzw. flackert der Knopf) · die sechs Ansichten stehen nur in den `<option>`, nie ein zweites Mal im Picker. 🔴 **Seit 15.08.2026 rollt die Hülle beim Aufklappen auf** (Owner: „C aber auch mit mouse over"; Entwurf `docs/layer-kacheln-animation-mockup.html`) — 150 ms auf, 110 ms zu, dazu die Zellen gestaffelt von der Kachel nach außen (20 ms Abstand; die erste Fassung lief mit 260/160/40 und war dem Owner am selben Tag zu langsam). Die Zeile davor stand bis dahin als „an der Hülle wird NICHTS animiert" da und war damit die **einzige** nicht animierte Fläche im Bild: das Aufklappen las sich trotz Blende als Schnitt. 💣 Erlaubt ist genau `clip-path`, weil es weder verschiebt noch blendet und die aktive Zelle im nie beschnittenen rechten Ende liegt; `opacity`/`transform` bleiben verboten. 💣 Der Startwert ist `--map-layer-tile` (= `--map-layer-cell` + 12), nie eine abgeschriebene 78 · gestaffelt wird mit `:nth-last-child`, weil die aktive Ansicht die letzte Zelle ist und ihre Stelle mit jeder Auswahl wechselt · am Telefon (2×3) rollt **nichts** auf, dort trägt die Staffelung allein · `BLENDE_ZU_MS` im JS und die Zuklapp-Dauer im CSS sind ein gekoppelter Wert in zwei Dateien. 💣 **Der Zustand ist eine Variable, nicht `is-open` und nicht `hidden`** — an der Klasse gelesen kam das Zuklappen beim Verlassen nicht zustande (sie wird erst im nächsten Bild gesetzt); derselbe Fehler wie beim Anzeige-Menü nebenan. 🔴 **Ein Klick HÄLT das Menü offen, bis woanders hingeklickt wird** (Owner 15.08.2026: „mouse over wie bisher, aber wenn ich draufklicke bleibts offen"); das Überfahren bleibt flüchtig. 💣 Der Riegel (`festgehalten`) sitzt an der **aktiven Zelle**, nicht an der Kachel: sobald das Überfahren geöffnet hat, ist die Kachel `hidden`, und der Klick, den der Benutzer für einen Kachelklick hält, trifft die aktive Zelle auf deren Fleck — die führte vorher direkt ins Zuklappen, es sah also aus, als ginge das Menü vom Anklicken zu. Zurückgesetzt wird nur in `schliesse()`, damit Auswahl, `Esc` und Kartenklick alle drei greifen. Bauteile: `js/ui/map-layer-picker.js`, `css/components/map-layer-picker.css`, Bilder `icons/layer-tiles/` samt Aufnahme-Werkzeug `tools/layer-tiles/`. Test: `js/ui/__tests__/map-layer-picker.test.js`. 🪤 **Und der Befund, der wie ein Code-Fehler aussah:** das Überfahren war vom 12.–15.08.2026 im Repo, aber nicht auf dem Server — ein Deploy-Fehlschlag hatte den `?v=`-Stempel vergiftet (§9). Wer meldet „Funktion X tut nichts", vergleicht **zuerst** das Ausgelieferte mit dem Repo, bevor er den Code liest.
- **Das Inhaltsverzeichnis der Hinweise** (Entwurf `docs/superpowers/specs/2026-08-05-hinweise-inhaltsverzeichnis-design.md`, live 2026-08-05) — die acht Abschnitte des Fensters „Hinweise" (`#legal-dialog` in `index.html`) sind `<details>`: **zugeklappt sind sie sein Inhaltsverzeichnis** (Überschrift plus eine Zeile, was drinsteckt), aufgeklappt sein Inhalt. Aus 4.937 px Scrollstrecke wurden 803. 💣 **Nativ, und nichts anderes:** nur `<details>/<summary>` lässt Strg+F Text in einem ZUgeklappten Abschnitt finden und klappt ihn selbst auf — ein selbstgebautes Auf- und Zuklappen mit `display:none`/`hidden` nimmt der Seitensuche den Text weg, und dieses Fenster trägt Impressum und Datenschutzerklärung. Fokus, Enter/Leertaste und `aria-expanded` kommen ebenfalls vom Element; hier gehört kein JS hin. ⚠️ Die Trennlinie sitzt an `.legal-section` (dem Abschnitt), nicht mehr an der Überschrift: im `<details>` ist das `<summary>` immer erstes Kind, die alte Ausnahme `.legal-dialog__group:first-child` hätte alle acht Linien gelöscht. ⚠️ Kein vollbreiter Hover-Streifen mit negativen Seitenrändern — der Scroll-Kasten hat `overflow-y: auto` und hängt sich sonst eine waagerechte Bildlaufleiste an. Der Absatz „Betreiber/Impressum" steht seit 2026-08-05 in **„Kontakt und Impressum"**, nicht mehr in „Projekt und rechtlicher Status".
- `docs/map-features-rest-architecture.md` — structure of the map-features layer.
- `docs/quellen-system-design.md`, `docs/quellen-system-2-editor-design.md` — multi-source system (infobox display + editor management).
- `docs/wiki-publikations-quellen-design.md` — Wiki publication-source bulk lookup (**shipped**; sources travel in the map payload; wiki/manual/community provenance; manual/suppressed overrides preserved). Implementation instruction: `docs/wiki-publikations-quellen-instruction.md`.
- **Literatur** — DSA-Werke ↔ ihre geordneten Orte (Siedlung/Territorium/Region/Weg), als Infopanel-Abschnitte plus spoilergesteuerte Questroute. Entwurf **`docs/superpowers/specs/2026-08-06-literatur-design.md`**; Phasen in `docs/abenteuer-feature-design.md`, `-instruction.md`, `-editor-ui-spec.md`, `-editor-p3-autocomplete.md`. P1–P4 live (Katalog, Aggregation, Editor, Wiki-Sync + Ribbon + Cover). 💣 **Drei** Rollen, nicht zwei: `start` („beginnt hier") und `covers` („beschreibt") sind spoilerfrei, **`play` IST der Spoiler** — jede Stelle, an der „alles, was nicht start ist, ist Spoiler" stand, war damit lautlos falsch. Normalisiert an EINER Stelle je Seite (`avesmapsGameLiteratureNormalizeRole`), Rückfall immer Richtung `play`. 🔴 **Umbenannt 2026-08-07 (Abenteuer → Literatur) — die TABELLEN aber nicht** (`adventure`, `adventure_place`, `wiki_adventure_*`), ebenso wenig irgendein String, der als WERT in einer Zeile steht (`app_setting`-Schlüssel, Aktion `sync_adventures`, Audit-Aktionen, JSON-Schlüssel). Genau diese Trennung macht die Umbenennung sicher: der Deploy löscht nie (§10), die verwaisten alten Endpunkte antworten weiter richtig, und ein `RENAME TABLE` bräche das. Die übrigen Fallen (Kopfzähler, `PRODUCT_TYPE_GROUPS`, Spotlight-Rollen, `.avesmaps-adv*`) im Entwurf §§4–8.
- **Die Übernahme-Vorschau** — jeder Wiki-Abgleich zeigt erst, was er tun würde; geschrieben wird nur, was angehäkelt ist. Drei Kategorien (**Neu · Geändert · Gelöscht**), Neu und Geändert vorangehäkelt, Gelöscht nie und mit zweiter Bestätigung. Entwurf **`docs/superpowers/specs/2026-08-06-sync-uebernahme-design.md`**, Baupläne `docs/superpowers/plans/2026-08-06-sync-uebernahme-sitzung-{1,2}.md`, Mockups `docs/sync-uebernahme-mockup.html` + `-fallliste-mockup.html`. Live seit 2026-08-06 für Stadtkarten, Literatur, Publikationsquellen und Vorkommen; Territorien (Sitzung 4) ziehen nach. Bauteil `js/review/sync-plan-sheet.js`, Endpunkt `api/edit/wiki/sync-plan.php`, Fundament `api/_internal/wiki/sync-plan.php`, Tabellen `sync_plan_run`/`sync_plan_item` + 🔴 `sync_decision` (die EINZIGE dauerhafte Entscheidung, nie automatisch geleert). 💣 Sieben Fallen, alle im Entwurf begründet: sie kommt **immer**, auch bei null Unterschieden · jeder Abgleich ist in Rechen- und Ausführ-Hälfte geschnitten, und die Rechen-Hälfte schreibt in KEINE Nutztabelle (`sync-plan-purity-test.php`) · die dritte Kategorie gehört dem Verschwinden einer ganzen EINHEIT, nie einzelner Kindzeilen · bei den Vorkommen ist die „Löschung" ein Grabstein (`status='retired'`, das Wort heißt „stilllegen") · „Löschung abgelehnt" ist NICHT `origin='manual'` · eine Übernahme schreibt EINE Protokollzeile je Lauf · der Löschriegel steht serverseitig in `apply`, nicht nur am ausgegrauten Knopf. ⚠️ Sitzung 3 (Orte, Wege, Regionen) bekam bewusst nur die **Formensprache**, keine `sync_plan_item`-Zeilen: von 16 Falltypen passen 2 auf ein Häkchen.
- **Die Wiki-Zuweisung — EIN Bauteil, das seine Felder aus einer Erklärung liest.** „Welcher Wiki-Artikel gehört zu diesem Objekt" war in **sechs** Fassungen über ~1.400 Zeilen verteilt; daraus wurde ein Bauteil (`js/ui/wiki-assign.js`) plus ein **Feldregister** (`js/ui/wiki-assign-registry.js`) und je Objektart ein reiner Datenweg (`js/ui/wiki-assign-{weg,ort,landschaft,territorium,literatur,karte}.js`). Entwurf **`docs/superpowers/specs/2026-08-15-wiki-zuweisung-vereinheitlichung-design.md`**, Bauplan `docs/superpowers/plans/2026-08-15-wiki-zuweisung-vereinheitlichung.md`, Mockup `docs/wiki-zuweisung-mockup.html`; live 16.08.2026 in **einem** Zug (Owner-Entscheid gegen die Einzeln-live-Regel aus §9, deshalb auf eigenem Zweig gebaut). **Acht Objektarten in elf Oberflächen:** kraftlinie · weg · ort · landschaft · landschaftslabel · territorium · literatur · karte. Eine neue Objektart kostet eine **Erklärung**, ein neues Feld **eine Zeile**. 💣 **DER VERTRAG, und er ist der wichtigste Satz hier: ein `laden`/`zuweisen`/`loesen`/`syncUebernehmen` darf im Fehlerfall NIEMALS mit `null`/`undefined`/Zahl/Liste AUFLÖSEN — es muss ABLEHNEN.** Sonst hält das Bauteil den leeren Zustand für „nichts zugewiesen", und das nächste Speichern löscht die echte Zuweisung. Drei solche Pfade wurden beim Bau gefunden und geschlossen: eine abgelehnte Zusage, eine Zusage die mit *nichts* auflöst (der schlimmere — dabei zeigt der Kasten ruhig „keine Zuweisung" statt einer Fehlermeldung), und ein zweites `neuLaden`, das nichts liefert. Der Riegel steht im Bauteil: `avesmapsWikiAssignRufen` macht aus einem synchronen Wurf eine Ablehnung, `bereit === true` heißt ausnahmslos „`lies()` ist ein gültiger Schreibwert". ⚠️ Ein `.catch(() => null)` in einem Datenweg hebelt das aus — der Hausstil `.catch(() => [])` steht in derselben Datei und liest sich harmlos. 🔴 **Zwei Hüllen, und das ist die Obergrenze** (`.dt-*` im Editorfenster, `.label-wiki-*` im Kartendialog) — dieselbe Regel wie bei den Listenzeilen. 🔴 **Das Bauteil kennt KEINE Objektart** (kein `if (subject === …)`); ein Datenweg kennt kein DOM, kein `fetch`, keinen Modulzustand. Wer eine Objektart anschließt, schreibt eine Erklärung und einen Datenweg — nichts im Bauteil. 🔴 **Vorangehakt ist nur das Füllen einer LÜCKE** (Owner 16.08.2026): ein bereits gefüllter Kartenwert startet ungehakt mit Grund. ⚠️ Damit ist für ein gepflegtes Objekt praktisch nichts vorangehakt und „Alle anhaken" der Normalweg — deshalb trägt der die Füllung, solange nichts angehakt ist. Das Mockup zeigt noch den alten Stand. 🔴 **Der dritte Zustand** („Kein Wiki-Artikel vorhanden", `properties.wiki_no_article`) ist gebaut für ort · weg · landschaft · landschaftslabel · karte und **begründet abwesend** bei territorium (ein eigener Knoten fällt schon durch den `INNER JOIN political_territory_wiki` aus der Konfliktliste) und literatur (`adventure` hat kein `properties_json`, nur `field_origins_json`). Die Begründungen stehen im Register — **kein TODO, kein Schweigen.** 💣 Er reist **nur mit, wenn er seit dem Laden verändert wurde**, sonst nimmt ein alter offener Dialog die Entscheidung des Konfliktzentrums zurück. 💣 **Beim Weg gilt er für den ganzen Namensverbund**, nicht das bearbeitete Segment (`avesmapsConflictRepairSpansNameGroup`) — zwei Knöpfe am selben Fall mit verschiedener Reichweite sind schlimmer als zwei Fehler. 💣 **Die Art-Regel der Landschaft ist eine Ordnung, keine Wahl:** erst das eigene Vokabular der Ebene (exakt), dann die Server-Synonyme (`avesmapsWikiRegionArtToSubtype`), Ergebnis muss ein `type_key` DIESER Ebene sein. Blind gespiegelt würde aus jeder Wiki-**Schlucht** ein Tal (die Servertabelle ist einen Tag älter als die Flächenart `schlucht`). 💣 **Bei den Karten heißt schon zweierlei „wiki"**: `citymap.wiki_key` ist ein **Bauschlüssel** (`index:stadt:quelle:variante`, keine Seitenidentität) und `citymap.map_url` zeigt auf die **Publikation** — der eigene Artikel heißt deshalb `article_url`/`article_key`/`article_title`. 🪤 **Der Entwurf ist an drei Stellen an der Wirklichkeit gemessen falsch** und wurde bewusst nicht rückwirkend geschönt: §9 verortet die Territoriums-Zuweisung falsch (sie sitzt in `review-region-wiki-picker.js`, nicht im Sync-Monitor), §8 nennt eine Spalte `citymap.wiki_url`, die es nicht gibt, und §5 behauptet, die Label-Liste könne „hängt schon woanders" anzeigen — **kein** Suchendpunkt kann das. Jede Korrektur steht an ihrer Codestelle. 🔧 **Offen:** der Ablauf mit angemeldeter Sitzung (kein Handgriff lief je gegen die echte Datenbank oder im Browser) · das Bauteil emittiert kein `data-i18n`, die Landschaft verliert damit unter `?lang=en` Abdeckung (gehört in M8) · sieben neue Skripte (212 KB) laden bei **jedem** anonymen Besucher mit, obwohl sie nur Editordialoge bedienen · für Karten-Artikelseiten gibt es keinen Wiki-Katalog, gesucht wird ersatzweise in `wiki_sync_pages`.
- **`docs/konfliktmanagement-design.md` — the conflict centre** (editor surface: WikiSync → „⚖️ Konflikte"). Conflicts are **computed, never stored** — only the editor's decision is durable (`conflict_decision`, keyed by `(rule_id, fingerprint)`), so a repaired case disappears by itself and one whose facts changed reopens by itself. **Shipped:** rule registry + decision store + endpoint (`api/edit/map/conflicts.php`, libs in `api/_internal/conflicts/`), the two global wiki-namespace rules, per-party evidence, the repair verbs, and the 12 legacy `wiki_sync_cases` types merged into the same list (surface only — they keep their own storage and resolve flows). 💣 **DREI** noise filters are load-bearing and unit-tested — the count said **two** until 17.08.2026, and the third is the newest and the largest: (1) `path|path` is the ONLY legitimate shared-article *pairing* in `avesmapsConflictSharedWikiVerdict` (215 groups / 1547 objects are one road's own segments) — 🔴 **still true and unchanged**; (2) auto-named ways `<Subtype>-<n>` never reach the watchlist (2448 of 3721); (3) 🔴 **a citymap whose `article_origin = 'wiki_publication'` never ENTERS the detector** (`avesmapsConflictLoadCitymapRows`). Its `article_url` names the PUBLICATION the map was printed in, not an article the map claims for itself — 363 maps on 140 publication pages. Without it the mass assignment of 17.08.2026 would have added **136 groups / 482 objects**, 123 of them mixed with the Literaturwerk holding the same article, i.e. the heaviest category (measured live). ⚠️ Note where filter (3) sits: it is a **loader** filter, not a verdict exception — the owner rejected whitelisting the pairing `citymap|game_literature` because that would also hide genuine mis-assignments („weil ich sehen will, was gesynct und was von uns editiert ist"). A **hand-set** map assignment (`article_origin = 'manual'`) stays fully in view. Removing any of the three makes the tool unusable, not merely noisy. Tests: `api/_internal/conflicts/__tests__/conflict-citymap-test.php` (card 5 is filter 3), `api/_internal/wiki/__tests__/citymap-article-assign-test.php`. **Open:** the enrichment still guesses for parenthetical names (P3). 🔴 **Korrigiert 16.08.2026:** Territorien und Literatur haben längst einen Detektor (`avesmapsConflictLoadTerritoryRows`/`…GameLiteratureRows`), Karten seit der Wiki-Zuweisung (`avesmapsConflictLoadCitymapRows`) — ohne Detektor ist nur noch **`sources`**. Die alte Aufzählung nannte vier und war für drei davon falsch.
- **Der Social-Media-Hub** — Editoren veröffentlichen aus Avesmaps heraus (Text, Hashtags, Bild) auf mehreren Netzen; die automatischen Feature-Updates landen als **Vorschlag** in derselben Liste und warten auf Freigabe. Entwurf **`docs/superpowers/specs/2026-08-10-social-media-hub-design.md`**, Bauplan `docs/superpowers/plans/2026-08-10-social-media-hub-stufe-1.md`, Mockup `docs/social-hub-mockup.html`. **Stufe 1 live 10.08.2026** (Reiter Community → „Social Media", Hub, Register, Endpunkte, Bild-Pipeline, Status je Kanal, Rechteriegel, Probe-Kanal, Freigabe); **Facebook scharf 10.08.2026** (`adapters/facebook.php`, Entwurf §12.3), **Instagram scharf 11.08.2026** (`adapters/instagram.php`, Entwurf §12.4), **Mastodon scharf 11.08.2026** (`adapters/mastodon.php`, `@Avesmaps@rollenspiel.social`, Entwurf §12.5), Video Stufe 3. 💣 **Mastodon meldet Fehler als STRING, Facebook als OBJEKT** — wer den Facebook-Leser abschreibt, fällt in den „keine id"-Zweig und ist damit zufällig richtig, aber ohne Mastodons Diagnose; beweiskräftig ist der TEXT, nicht `ok === false`. 💣 **`social.mastodon.base_url` ist die INSTANZ, nicht das Profil** (die Adresszeile zeigt `…/@Avesmaps`, mit Pfad gäbe es einen 404, den man dem Token anlastet). 💣 **`202` beim Bild heißt „noch nicht verarbeitet"**, und `media_ids` muss eine LISTE sein (deshalb JSON statt `http_build_query`, das `media_ids[0]=` schreibt und von Rack als Hash gelesen wird). 💣 **Die 500 Zeichen sind instanzabhängig** — gemessen an `api/v2/instance`, nicht geglaubt; dieselbe Antwort nennt `characters_reserved_per_url = 23` (🔧 offen, unser Zähler zählt Verknüpfungen echt). ⭐ **Einziger Kanal mit `Idempotency-Key`** (aus der `post_id`): nach einer Zeitüberschreitung ist Wiederholen hier gefahrlos, bei Facebook nicht. 🔴 **Der Mastodon-Schlüssel gehört dem Konto, das ihn anlegt** — als Privatperson erstellt, veröffentlicht er als Privatperson; das ist Facebooks `/me`-Falle ohne Vorwarnung, und es gibt keinen technischen Riegel, nur die Sichtprüfung am Profil. 🔴 **Ein Kanal muss nicht nach draußen führen: „Neuigkeiten"** (Schlüssel `changelog`, Entwurf §3.0) schreibt in `changelog_entry` und erscheint im Fenster Hinweise → Neuigkeiten — ohne fremdes Konto, ohne Token, also immer verfügbar. 💣 Sein Adapter ruft **`avesmapsChangelogSeedIfEmpty()` vor jedem Schreibvorgang**: eine leere Tabelle fällt im Lesepfad auf die Saat zurück, und ein einzelner Insert würde die 42 Meilensteine verdrängen, ohne dass es jemandem auffällt. 💣 **Die erste Zeile wird die Überschrift** (`title` ist VARCHAR(190)) — zu lang wird **abgelehnt, nicht gekürzt**, weil eine stumm abgeschnittene Überschrift öffentlich steht. 💣 Eine Facebook-**Seite hat ZWEI Kennungen** — die `6159…`-Profil-ID, die man im Browser sieht, und die klassische Graph-ID (`delegate_page`); **nur die zweite adressiert ein Seiten-Token**, maßgeblich ist die `id` aus `GET /me/accounts`. 🔴 Ein **SEITEN**-Token, und **nie `/me`**: mit einem Nutzer-Token veröffentlichte `/me/feed` den Projektbeitrag öffentlich auf dem privaten Profil des Owners — deshalb gibt es keinen Rückfall darauf. 💣 Der Token gehört in den **Rumpf**, nie in die Adresse (Server-Protokolle); `caption` bei `/photos`, `message` bei `/feed` (vertauscht wird der Text lautlos verworfen); **`post_id` vor `id`** (`id` ist das Foto). 🔑 **Zugänge richtet der SERVER ein** (`connect.php`, Knopf im Hub, Entwurf §3.2): ein kurzlebiger Token hinein, Tausch + `/me/accounts` + `debug_token` + Ablage heraus. 💣 Gespeichert wird nur, was **`expires_at === 0`** UND **`type === 'PAGE'`** UND die konfigurierte **Kennung** (nie den Namen — es gibt mehrere „Avesmaps") UND die **je Kanal geforderten Rechte** vorweist (`connect_scopes` im Register: Facebook `pages_manage_posts`, Instagram `instagram_basic` + `instagram_content_publish`); fehlendes `expires_at` ist **kein** Freibrief. Von Hand ging genau das am 10.08.2026 dreimal daneben, zweimal unbemerkt. 💣 **Instagram läuft über die FACEBOOK-SEITE** (`instagram_business_account`, gemessen 11.08.2026), also derselbe Wirt und derselbe nie ablaufende Seiten-Token — der in §12.1 beschlossene Instagram-Login-Weg mit 60-Tage-Token und Erneuerungs-Läufer ist damit **verworfen**. Trotzdem eine **eigene Token-Zeile**: läse Instagram Facebooks Zeile mit, meldete der Hub „eingerichtet", sobald Facebook es ist — auch ohne die Instagram-Rechte, was genau der Zustand vom 10.08. war. ⚠️ Die Messung `?fields=instagram_business_account` **lügt ohne `instagram_basic`**: das Feld fehlt dann kommentarlos und liest sich wie „nicht verknüpft". 💣 **Instagram veröffentlicht in ZWEI Schritten** (`/media` → `creation_id`, dann `/media_publish`) — ein Behälter ist **kein Beitrag**, und ein Abbruch dazwischen hat nichts veröffentlicht. **Fehler 9007 ist der EINZIGE wiederholbare**, weil er beweist, dass nichts draußen steht (Instagram rechnet noch); gewartet wird per gedeckelter Wiederholung, **nicht** per Statusabfrage — die wäre ein GET und trüge den Token durch die Adresse. ⚠️ `image_url` (nicht Facebooks `url`), und das Bild wird seit 11.08.2026 auf **1440 px Breite gedeckelt** — die Pipeline schnitt vorher nur zu und verkleinerte nie. Bauteile: `api/_internal/social/{channels,compose,media,store,publish}.php` + `adapters/`, Endpunkte `api/edit/social/{list,media,publish,retry}.php` und `api/social/routine-post.php`, Client `js/review/review-social.js`, Stil `css/components/social-hub.css`. 💣 Acht Fallen, alle im Code begründet und getestet: **der Status gehört dem KANAL, nicht dem Beitrag** (ein gemeinsames „gesendet" verschluckt das eine Netz, das abgelehnt hat) · ein **unbekannter Zustand fällt auf „wartet", nie auf „gesendet"** — Grün heißt „es steht draußen" · ein **fehlender Adapter ist `null`**, nie ein Leerlauf, der Erfolg meldet · **Instagram nimmt kein PNG**, also wird alles zu JPEG — und **JPEG kennt keine Transparenz**, ohne ausdrücklich weißen Untergrund kommt ein schwarzes Quadrat heraus, das niemand vor der Veröffentlichung sieht · **zugeschnitten, nicht gestaucht** (4:5 … 1,91:1) · **Hashtags zählen zum Zeichenlimit** und werden je Kanal gekappt · **erst live, dann posten** (das Bild muss HTTP 200 liefern, sonst cacht das Netz den Fehlschlag) · die Spalte heißt **`body`, nicht `text`** (reserviertes Wort) und **`source_ref` ist nullable** unter seinem UNIQUE-Schlüssel, weil MySQL viele NULL erlaubt, aber nur ein `''`. 🔴 Der **rotierende Zugangs-Token steht in der Datenbank** (`social_token`), nicht in `config.local.php`: eine Datei, die der Server im Zeitplan umschreibt, ist beim ersten Fehlschlag eine kaputte Konfiguration. 🔴 Eigene Fähigkeit **`social`** — seit 11.08.2026 **admin UND editor** (vorher nur `admin`; die Öffnung kostete EINE Zeile in `avesmapsUserCan` und **keinen** Aufrufer, weil alle schon durch `avesmapsUserCan(…, 'social')` gehen — genau dafür hatte sie von Anfang an einen eigenen Namen). ⚠️ **Reviewer bleiben draußen**, und dass `social` und `edit` jetzt dieselben zwei Rollen nennen, ist Zufall, keine Verschmelzung: jemanden **namentlich** freizuschalten bräuchte weiterhin eine Spalte `users.can_social`. ⚠️ Editoren sehen damit auch „Zugang einrichten" — umleiten können sie nichts, die Seite wird über die konfigurierte Kennung gesucht. ⚠️ `#social-hub-overlay` steht in **drei** Selektorlisten in `dialog-overlays.css` — ein Overlay-`<div>` erbt nichts. 🔴 **KI-Kennzeichnung live 16.08.2026** (Entwurf `docs/superpowers/specs/2026-08-16-ki-kennzeichnung-design.md`): Häkchen „Mit KI erstellt" im Hub, **vorgehakt** (Owner-Entscheid), Spalte `social_post.ai_declared`. 💣 **Facebook kann NUR Beiträge mit Bild kennzeichnen** — `provenance_info` steht an `/photos`, `/feed` kennt das Feld überhaupt nicht (Metas Parameterlisten beider Endpunkte, gemessen 16.08.2026). Ein unbebilderter Facebook-Beitrag geht damit unweigerlich ohne Kennzeichnung raus, und das trifft die **Routine vollständig** (sie setzt nie ein `media_url`). Deshalb ist die eigentliche Arbeit dieses Schalters der **Warnsatz** im Hub — angehakt + Facebook + kein Bild —, nicht der Adapter: ohne ihn hakt jemand an, drückt, und hält den Beitrag für gekennzeichnet. 🔴 Der Typ ist `EXPLICIT` („selbst erklärt"), **nicht** `EXPLICIT_AI_EDIT`/`_EXPANDER`/`_RESTYLE` — die benennen konkrete Meta-Werkzeuge, die wir nicht benutzt haben, und wären eine Falschangabe in die andere Richtung. Kein `provenance_metadata` (das trägt C2PA/IPTC aus der **Bilddatei**, die unsere Pipeline nicht schreibt), und ohne Häkchen wird **gar nichts** geschickt — kein `is_gen_ai:false`, die Abwesenheit ist die Aussage. 💣 **Instagram schreibt es anders**: `is_ai_generated` (Bool) am **Behälter**, nie an `/media_publish` — die dritte Asymmetrie nach `url`/`image_url` und `caption`/`message`. 🔴 **Mastodon hat kein solches Feld** (`POST /api/v1/statuses`); bewusste Lücke, keine Baustelle. ⭐ **Zwei Registerfelder statt einer Herleitung**: `ai_label` („nimmt eine Erklärung an") und `ai_label_needs_media` („braucht dafür ein Bild" — nur Facebook). „nimmt an, verlangt aber kein Bild, ALSO braucht die Erklärung eins" wäre für Facebook zufällig richtig und beim nächsten Kanal geraten. 🔧 **Ungemessen: ob Meta `provenance_info` wirklich annimmt** — dafür braucht es einen echten `/photos`-Beitrag mit Bild. Lehnt Meta es ab, scheitert jeder angehakte Facebook-Beitrag mit Bild.
- `docs/political-territory-editor.md` — editor architecture.
- `docs/stylized-map-tiles.md` — tile pipeline.
- `docs/political-territory-global-display-and-derived-boundaries-{plan,progress}.md`,
  `docs/derived-territory-geometry-plan.md` — derived-boundary system.
- **Verwaiste Außenhüllen** — was die Karte zeichnet, muss im Editor erreichbar sein. Owner,
  wörtlich: „es darf doch auf der map keine elemente geben über die ich keine kontrolle mehr habe."
  Live 16.08.2026; Entwurf **`docs/superpowers/specs/2026-08-16-verwaiste-aussenhuellen-design.md`**,
  Bauplan `docs/superpowers/plans/2026-08-16-verwaiste-aussenhuellen.md`.
  🔴 **Abgeleitete Hüllen sind im Bearbeiten-Modus absichtlich inert** (der `interactive`-Ausdruck in
  `map-features-region-rendering.js`), damit Klicks an die Quellgeometrien gehen — sonst landet
  `update_geometry` auf einer Derived-ID. Die Regel setzt voraus, dass unter der Hülle eine Quelle
  LIEGT; liegt keine da, ist das Gebiet **vollständig unerreichbar**. Gemessen an „Neues
  Herrschaftsgebiet (1008)": alle 116 Prüfpunkte innerhalb der Hülle trafen den nackten
  `leaflet-container`. Seither ist eine Hülle **ohne Quelle** im Editor interaktiv, und ihr
  Rechtsklick zeigt drei statt elf Einträgen, unterster „Außenhülle löschen".
  💣 **„Hat Quellen?" ist EINE Rechnung, und sie muss DIESELBE sein wie im Layer.**
  `avesmapsPoliticalDerivedHullIsSourceless` (`api/_internal/political/derived-orphans.php`) ruft
  denselben Sammler wie `territories-derived-layer.php`, und der kennt **drei** Zweige
  (`parent_id`-Nachfahren, **Wiki-Zweig**, Selbst-Rückfall). Eine eigene, strengere Rechnung weist
  gesunde Hüllen als Waisen aus — und die Strafe dafür ist unumkehrbares Löschen. ⚠️ Im Zweifel gilt
  die sichere Richtung: lieber eine Waise übersehen als eine gesunde Hülle löschen (eine leere
  Antwort des Sammlers zählt deshalb als „nicht verwaist").
  💣 **Das Frontend-Signal `derived_source_geometry_public_ids` kommt vom SERVER und wird nie im
  Browser gezählt** — „im Layer liegt nur die Hülle" trifft bei Zoom 4 auf **114** Gebiete zu, und
  **111 davon sind kerngesund**; ihre Quellflächen sind bei diesem Zoom nur nicht ausgeliefert.
  💣 **„Kein Territorium" heisst: die Zeile in `political_territory` existiert nicht mehr** — nicht
  „fehlt im Rechenschnappschuss", denn der filtert `is_active` UND Kontinent; ein Gebiet im
  Papierkorb ist keine Waise.
  🔴 **Hart gelöscht wird nur, was niemand mehr erzeugen kann** (Owner-Entscheid); alles andere
  bleibt beim umkehrbaren `is_active = 0`, weil „Grenzen berechnen" es zurückholt. Die Weiche sitzt
  in `avesmapsPoliticalDeleteDerivedGeometryForTerritory` und **nur dort** — Scanner, Sammelknopf
  und der Rechtsklick auf der Karte gehen alle durch sie. ⚠️ Wer eine unumkehrbare Handlung anbietet,
  muss sie auch so beschriften: Bestätigung und Erfolgsmeldung nennen hart und weich getrennt.
  💣 Entstanden ist der Zustand am Sammelknopf: `purge_unassigned_geometries` löschte die
  Quellflächen der Platzhalter roh und ging an der vorhandenen Ketten-Deaktivierung **vorbei** —
  zwei von drei Löschwegen gebunden ist keine Regel.
  ⚠️ `geometry_inventory` kappte bei 500 nach Fläche **vor** dem Waisen-Filter; weil Waisen klein
  sind, fielen sie systematisch heraus (25 gezählt, 6 gezeigt). Das Fenster fordert jetzt
  `limit=2000`. Tests: `api/_internal/political/__tests__/verwaiste-aussenhuellen-test.php`,
  `js/map-features/__tests__/verwaiste-aussenhuelle-interaktiv.test.js`.
- `docs/feature-umstrittene-gebiete.md` — active contested-territory feature.
- **„Hierher reisen“ + der Querfeldein-A\*** (Instruction `docs/superpowers/plans/2026-07-30-hierher-reisen-und-astar.md`, Entwurf `docs/superpowers/specs/2026-07-30-landschaften-v14-astar-design.md`) — **live 2026-08-01**. Rechtsklick auf einen beliebigen Kartenpunkt → Wanderschuh → Route bis genau dorthin. `POST /api/route/` nimmt `from_point`/`to_point` = `{x, y}`; der Punkt wird ein Graphknoten mit EINER Kante (dem A\*-Weg zum nächsten Graphknoten), danach laufen Dijkstra, Segmentbauer und Renderer unverändert. Kern: `api/_internal/routing/{land-areas,offroad-grid,offroad-data,offroad-leg}.php`, Client `js/routing/route-travel-here.js`. 🔴 **STAND 15.08.2026 ABEND — DREI REGELN, UND SIE ÜBERSCHREIBEN TEILE DESSEN, WAS WEITER UNTEN STEHT.** (1) **Der Ausstieg ist der nächste ERREICHBARE Punkt des Netzes** — eine Ordnung, keine Auswahl. Owner wörtlich: „er soll auf dem strassensystem bleiben bis zu dem punkt gehen wo er am nächsten zur freien zielmarkierung ist und von dort durch wieder durch die landschaft.“ Gerechnet werden die `AVESMAPS_ROUTE_OFFROAD_EXIT_NEAREST_TRIES` (6) nächsten Kandidaten, die Kante bekommt **genau der erste, der querfeldein erreichbar ist** — das `break` in `avesmapsAttachOffroadPointToGraph` IST die Regel. (2) **Eine Querfeldein-Etappe wird mit ihrer eigenen Länge langsamer** (Längenaufschlag, eigener Eintrag unten). (3) **Ein Fluss ist im Gelände eine Wand** (eigener Eintrag unten). 💣 **Und die Regel gilt für ALLE Erzeuger, nicht nur die Anbindung** — sie wurde zuerst nur dort gebaut und war damit an zwei Stellen wirkungslos: die Umweg-Sehnen legten weiter Kanten an den Kartenpunkt (gemessen: Ausstieg bei 5,04 Einheiten angeboten, gefahren `offroad-detour-12-21`, Jurios → Ziel, 34,4 Meilen quer), und die direkte Kante zwischen zwei Kartenpunkten gewann unter „Kürzeste“ IMMER, weil dort das Gewicht die Strecke ist und eine Gerade per Definition kürzer als jede Straße (gemessen: EINE Etappe über 148,5 Meilen bei 140,4 Meilen Luftlinie — die Kürzeste war zum Drachenflug geworden). **Dieselbe Lehre wie am 14.08. bei der Verkehrsmittel-Sperre, zum zweiten Mal: eine Regel, die einen von vier Erzeugern bindet, ist keine Regel.** Sehnen an einen Kartenpunkt gibt es seither nicht mehr (`avesmapsRouteChordCandidates`), und die direkte Kante entsteht nur, wenn der andere Punkt für mindestens EINEN der beiden näher liegt als dessen nächster Netzpunkt (`avesmapsOffroadDirectEdgeAllowed`; ⚠️ `max`, nicht `min` — liegt A dicht an der Straße und B weit ab, ist A für B trotzdem das Nächstliegende). Test: `ausstiegsregel-alle-erzeuger-test.php`. 💣 Die Landprüfung gilt **nur dem angeklickten Punkt** — Orte im Wasser werden nie geprüft (571 von 4.653 liegen geometrisch im Meer). 💣 Das Gitter ist **nie ein PHP-Array** (33,2 gegen 1 Byte je Zelle); drei Gelände-Byte-Ebenen, per **Maximum** verknüpft. **Drei Aufrufer, ein Rechner** (Instruction C `docs/superpowers/plans/2026-08-02-ausloeser-anker-und-x25-aufschlag.md`, **live 2026-08-02**): (1) „Hierher reisen"; (2) der **automatische Umweg-Auslöser** — jede Route vergleicht nach dem Dijkstra gefahrene Strecke gegen Luftlinie, über `AVESMAPS_ROUTE_OFFROAD_DETOUR_THRESHOLD` (3,0) rechnet der A\* quer und BIETET die Kante an (`detour.php`). 🔴 **STILLGELEGT AM 16.08.2026** (`AVESMAPS_ROUTE_OFFROAD_DETOUR_ENABLED = false`, Sperre am einzigen Aufrufer in `response.php`): seit der Ausstiegsregel war die Sehne die EINZIGE Stelle, an der der Router noch eigenmächtig die Straße verließ, und sie hat einen sichtbaren Widerspruch erzeugt — „Kürzeste“ 305,3 Meilen gegen „Schnellste“ 242,6 auf derselben Route, weil Sehnen aus der ZUERST gefundenen Kette gerechnet werden und die je nach Modus verschieden ist. Verbinden konnte sie nie (sie kommt NACH einer gefundenen Route); abgetrennte Orte hängen an der Komponentenbrücke, netzferne Kartenpunkte an der Ausstiegsregel. ⚠️ Gemessen und abgenommen: dieselbe Route wurde bei „Schnellste“ 310,7 statt 242,6 Meilen lang, Owner: „die fühlen sich absolut richtig an“. Modul und Tests bleiben stehen und tragen den Vermerk; 🔧 vermisst sie in ein, zwei Wochen niemand, fliegen sie raus; (3) die **Sehnen der gefundenen Route** — Komponentenbrücke und Wegpunkt-Anker werden nachträglich gebogen (`synthetic-refine.php`). 💣 Der A\* läuft für (3) **nie beim Graphbau**: 876 synthetische Kanten je Graph, aber 0–1 je Route. 💣 `AVESMAPS_ROUTE_CLIENT_SYNTHETIC_DISTANCE_COST_FACTOR` (×25) ist ein **Dijkstra-Gewicht, keine Strecke und keine Zeit** — er stand bis 2026-08-02 in `distance_units` des stabilen Vertrags (484,65 für eine Linie von 19,39). Er bleibt im Gewicht, wird aber aus jeder gemeldeten oder verglichenen Zahl per `cost_factor` herausgerechnet. 🔴 **Auslöser (2) bedient seit 2026-08-14 nur noch Orte OHNE Anschluss ans Wegenetz** (Owner: „querfeldein sollen nur orte angefahren werden, die nicht mit dem straßennetz verbunden sind"). Hängen **beide** Enden einer Sehne an einer gezeichneten Landwegart, wird sie gar nicht erst gerechnet — Absagegrund `both_ends_on_network`, `avesmapsRouteKeepChordsWithOffNetworkEnd` in `detour.php`. Anlass war Luring → Salmingen: quer für 3,38 statt 9,52 über Ferdok, obwohl beide Orte an Straßen liegen. ⚠️ Die Schwelle entscheidet erst danach, und an Land entscheidet sie allein: seit dem Querfeldein-Quellenwert 2,30 liegt das Tempoverhältnis bei 1,335, die Zeitprobe kann dort also nichts mehr auffangen (auf dem Wasser bleibt sie scharf). 🔴 **Der Ausstieg einer Querfeldein-Etappe ist ein Punkt AUF einem Weg, nicht die nächste Ortschaft** (live 14.08.2026; Entwurf `docs/superpowers/specs/2026-08-14-querfeldein-ausstiegspunkt-design.md`, Plan `docs/superpowers/plans/2026-08-14-querfeldein-ausstiegspunkt.md`). Beide Erzeuger netzferner Anbindungen — der Kartenpunkt und der Wegpunkt ohne Weganbindung — bekommen bis zu `AVESMAPS_ROUTE_CLIENT_ANCHOR_LIMIT` (6) Fußpunkte als **Angebot**, je einen pro Weg (`avesmapsCollectNearestClientLandPathAnchors` entdoppelt über die Kanten-`id`). 🪤 Hier stand „welcher gewinnt, entscheidet der Dijkstra“ — seit dem 15.08.2026 abends entscheidet er das NICHT mehr (Stand-Block oben). Er entscheidet weiterhin, WIE man zum Abgangspunkt kommt, nur nicht mehr, WANN man den Weg verlässt. Gemessen: ein Kartenpunkt zwischen Luring und Spinnried verlässt die Reichsstraße 0,702 statt 2,652 Einheiten vor dem Ziel (−74 % Querfeldein). 💣 `avesmapsSplitClientPathAtAnchor` **entfernt die Ursprungskante**, sobald beide Hälften stehen — sonst teilt der nächste Endpunkt denselben Weg ein zweites Mal und die Fußpunkte hängen unverbunden nebeneinander. 💣 **Fußpunkte und Ortschaften stehen in EINEM Topf, und der Maßstab der Reichweitenschranke (× 2,5) ist der nächste ORTSKNOTEN.** Drei Fassungen an einem Tag, jede von der nächsten Messung widerlegt: (a) Staffelung „Ortschaften nur als Rückfall" — ließe eine Stadt 4,6 Einheiten neben dem Punkt hinter einem Fußpunkt bei 40 verschwinden; (b) Maßstab „nächster Kandidat überhaupt" — warf live alle vier Ortschaften aus dem Angebot, Reise 2,8 % teurer; (c) Maßstab „je Familie" — schnitt einen Fußpunkt bei 7,61 weg, während eine Ortschaft bei 8,30 blieb, weil deren Familie ihren eigenen weiteren Maßstab hatte (Reise rund 15 % länger). ⚠️ **Die Lehre: eine relative Schranke braucht einen Maßstab, der nicht mitwandert.** 🪤 **Und am 15.08.2026 abends ist die Schranke ganz weggefallen** — es gibt keinen Faktor mehr, sondern eine Ordnung (Stand-Block oben). Der Grund ist genau diese Lehre: der Owner hat am 14.08. einen Knoten bei **1,77×** der nächsten Luftlinie ERWARTET und am 15.08. einen bei **1,72×** beanstandet — keine Schranke der Form „höchstens F mal der nächste“ erfüllt beides, weil sich der Maßstab zwischen den Fällen geändert hat (damals Ortsknoten, heute jeder gezeichnete Stützpunkt). Die drei Fassungen (a)/(b)/(c) bleiben als Messungen wahr; (b) ist heute der **gewollte** Preis. 🔴 **Und ein Weg bietet JEDEN seiner gezeichneten Punkte als Ausstieg an, nicht einen** (live 15.08.2026; Entwurf `docs/superpowers/specs/2026-08-15-querfeldein-abgangspunkt-design.md`, Plan `docs/superpowers/plans/2026-08-15-querfeldein-abgangspunkt.md`, Vorgeschichte `docs/superpowers/plans/2026-08-15-querfeldein-ausstieg-optimum-uebergabe.md`). 💣 Der Befund davor war KEINE Fehlplatzierung, sondern ein fehlendes Angebot: ein Wegstück lieferte genau einen Kandidaten — die Projektion des Ziels —, und sobald der gegen den Direktweg verlor, existierte die Strasse für diese Reise nicht mehr. Gemessen an Salmingen → Kartenpunkt (504.530, 501.076): EINE Etappe über 42,06 Meilen querfeldein, neben dem Talloner Hügelsteig her, den die Reise nie betrat. Owner, wörtlich: „es gibt kein ausstieg heute". Jetzt liefert `avesmapsCollectClientLandPathExitCandidates` je Wegstück alle inneren Vertices PLUS den Fußpunkt (Deckel `AVESMAPS_ROUTE_OFFROAD_EXIT_VERTEX_LIMIT` = 24, am Livebestand gemessen: p99 = 23, max = 53; eine Kappung steht als `exit_vertices_capped` in der Antwort). Die Reise stieg daraufhin am Knick (505.470, 511.933) aus: 7,80 Meilen Straße, dann 36,65 Meilen Gelände, Kosten 6,21 statt 6,54. 🪤 **Genau dieser Ausstieg ist am selben Abend zurückgenommen worden**: der Knick war der BILLIG ERREICHBARE, nicht der zielnächste. Die Fixture in `abgangspunkt-test.php` unterscheidet beide bis heute (Knick 12,0 nach 8 Wegeinheiten gegen zielnächst 11,66 nach 20,7) und prüft seither die neue Regel — samt der Zusicherung, dass die erzwungene Reise TEURER ist (13,42 gegen einen abgeschafften Direktweg von rund 11,83). Das ist der Preis der Regel und steht dort, damit ihn niemand „wegoptimiert“. ⚠️ Sie ist damit SCHNELLER, aber LÄNGER (44,5 statt 42,1 Meilen) — für „Schnellste" richtig, für „Kürzeste" nicht geprüft. ⭐ Bezahlbar wird das durch `avesmapsOffroadFindPathsFromPoint`: EIN Dijkstra vom Kartenpunkt nach außen bedient alle Kandidaten, statt eines A\*-Laufs je Kandidat (gemessen: vorher 15 Läufe je Anfrage). 💣 **Er bepreist jeden Schritt in GEGENRICHTUNG** — der Reisende geht vom Ausstieg zum Kartenpunkt, der Lauf geht andersherum, und Steigung kostet mehr als Gefälle; ohne den Tausch stünde die Zeit der Rückreise in der Antwort (die Fehlerklasse aus V11 §6.3). 💣 **Die Bremse ist der Abbruch beim letzten erreichten Ziel**, nicht die volle Kiste — ohne ihn läuft ein Dijkstra ohne Ziel kreisförmig über bis zu 150.000 Zellen. ⚠️ Gemessen ist er trotzdem TEURER als vorher: 5,9 ms gegen 3,9 ms für den doppelten Kandidatenvorrat (der naive Weg, 38 einzelne A\*, wäre 8,6 ms) — bei einer Anfrage von 1,99 s sind das 0,3 %, Owner-Entscheid „2ms ist egal". 💣 **Geteilt wird in EINEM Durchgang je Wegstück** (`avesmapsSplitClientPathAtPoints`); der Einzelteiler entfernt die Ursprungskante, sobald beide Hälften stehen, und k Aufrufe hintereinander hängen die späteren Punkte ins Leere. Er selbst bleibt unangetastet — er trägt den Wegpunkt-Anker. ⚠️ **Gefiltert wird VOR dem Teilen**, sonst wird eine Straße für Kandidaten zerschnitten, die die Reichweitenschranke ohnehin verwirft. 🪤 Der Satz „Die Schranke und ihr Maßstab (nächster ORTSKNOTEN, × 2,5) sind unberührt“ galt bis zum 15.08.2026 abends; `AVESMAPS_ROUTE_OFFROAD_EXIT_DISTANCE_FACTOR` gibt es nicht mehr. 🔴 Der Übergang heißt in der Etappenliste **„Abgangspunkt"** (Weg → Gelände) bzw. **„Anschlusspunkt"** (Gelände → Weg), richtungsabhängig in `nameRoutePlanTransferPoints` (`route-plan.js`) — NACH `cleanRoutePlanNoiseEntries`, weil jene an `isRoutePlanMarkerName` entscheidet, ob eine Etappe schließt. Davor stand dort „Markierung" (zwei Umbenennungen hintereinander: `normalizeNodeName` → „Kreuzung", `formatRoutePlanNodeName` → „Markierung"); beide bleiben als Rückfall stehen. Tests: `abgangspunkt-test.php` (der Abnahmefall), `exit-vertices-test.php`, `path-multisplit-test.php`, `offroad-multi-goal-test.php`, `abgangspunkt-label.test.js`. ✅ Der damals offene Punkt („dieselbe Kandidatenauswahl ist bei den zwei übrigen Erzeugern noch eng“) hat sich am 15.08.2026 abends anders erledigt als gedacht: die Sehnen dürfen einen Kartenpunkt gar nicht mehr berühren. ⚠️ **Ein Freiheitsgrad bleibt** — der Umweg-Auslöser darf weiterhin eine Sehne zwischen zwei STRASSENKNOTEN legen, wenn das Netz dort einen dreifachen Bogen macht. Das ist die Fassung, die der Owner „richtig“ genannt hat; soll die Wegbindung je absolut werden, ist das die Stelle. 🔧 Und der Blick auf die Naht im Browser steht weiter aus. 🪤 **Und eine zweite, teurer bezahlte Falle:** (c) wurde an einem Kartenpunkt gemessen, den der Prüfende aus der Wegpunkt-Beschriftung abgetippt hatte — die zeigt `lat, lng`, `to_point` will aber `{x: lng, y: lat}` (`applyMapPointRouteEndpoints`, `route-travel-here.js`). Eine Stunde Fehlersuche galt damit einer ganz anderen Stelle der Karte als der gemeldeten. Der Befund (c) blieb trotzdem echt und der Test dazu ist synthetisch — aber **wer einen Kartenpunkt aus der Oberfläche in eine API-Probe übernimmt, muss ihn drehen.** Die Ortschaften spannen die Suchkiste seit jeher auf; die Fußpunkte liegen darin und kosten nichts extra, und einer, der weiter liegt, würde die Kiste aufziehen und fällt zu Recht heraus. 💣 **Querfeldein-Kanten haben VIER Erzeuger, und die Verkehrsmittel-Sperre muss in jedem einzeln stehen** (14.08.2026): `avesmapsConnectClientCompatibleDetachedGraphComponents` + `avesmapsConnectClientRouteWaypointsToNearestLandPath` (`client-graph.php`) sowie `avesmapsAttachOffroadPointToGraph` + `avesmapsConnectOffroadPoints` (`offroad-leg.php`, letzterer trägt auch die Umweg-Sehnen aus `detour.php`). Die beiden ersten fragten `avesmapsIsClientTransportAllowedForPath` von Anfang an, die beiden letzten — die vom **Nutzer** ausgelösten — nicht: sie prüften nur, ob es ein **Tempo** gibt, und für die Kutsche steht querfeldein 3,84 in der Tabelle. Die Kutsche fuhr deshalb bei „Hierher reisen" quer über die Wiese (live gemessen: Luring → Kartenpunkt, HTTP 200). ⚠️ **Die eigentliche Falle war die ZAHL im Kommentar** — dort stand „ERZEUGER 1 VON 2" / „2 VON 2", und eine Zahl liest sich wie eine vollständige Liste, also suchte niemand weiter. `avesmapsFindOffroadPathBetween` (`synthetic-refine.php`) ist **kein fünfter**: es biegt nur die Geometrie einer bereits geprüften Kante. 🔴 Absagegrund ist `offroad_transport_not_allowed`, **nicht** `no_offroad_route` — hier liegt es an der ANFRAGE, nicht am Punkt, und kein anderer Punkt der Karte hilft; der Satz dazu steht in `js/routing/route-travel-here.js`. Gewacht von `api/_internal/routing/__tests__/carriage-offroad-test.php` (fährt den Kartenpunkt wirklich an, mit Gegenprobe zu Fuß).
- **Der Längenaufschlag für Querfeldein** (live 15.08.2026, Entwurf `docs/superpowers/specs/2026-08-15-querfeldein-laengenaufschlag-design.md`) — eine Etappe wird mit ihrer eigenen Länge langsamer: `zeit_final = zeit_gemessen × min(deckel, 1 + steigung × meilen)`, Vorgabe **0,6 % je Meile**, Deckel **2,0** (erreicht bei 167 Meilen Luftlinie). Anlass: eine Reise lief 103,28 Meilen querfeldein statt über die Straße, weil das Querfeldein-Tempo am 14.08. um 18:38 von 0,96 auf den GA-Wert 2,30 gezogen wurde. 🔴 **Er misst die LUFTLINIE der Etappe, nicht die gelaufene Strecke** — die Suche ordnet OHNE ihn; hinge er an der gelaufenen Länge, bestrafte er nachträglich genau den Bogen, den der A\* zum Zeitsparen geschlagen hat (gemessen an der Fixture von `offroad-shortest-test.php`: Zeitmodus 14,01 gegen Streckenmodus 12,40 — eine „schnellste“ Etappe, die messbar langsamer war als eine verworfene). An der Luftlinie ist er für ein festes Endpunktpaar eine **Konstante**, und eine Konstante verschiebt kein Minimum. 💣 Er sitzt in `avesmapsOffroadFinishPath`, dem gemeinsamen Abschluss ALLER gerasterten Erzeuger — die Vier-Erzeuger-Falle galt der Sperre VOR dem Suchlauf, der Preis kommt danach und deshalb einmal. 💣 **Nur die Zeit**, `distance` bleibt unangetastet. ⚠️ Der Bezug ist die EINZELNE Etappe: ein kurzes Stück Weg setzt den Aufschlag zurück — Absicht (wer einen Ort berührt, rastet dort) und Owner-Entscheid vom 15.08. Einstellbar im Fenster „Tempowerte“ (Abschnitt „Querfeldein-Aufschlag“, Speicherabschnitt `offroad_ramp`, Rücksetzer `data-section="offroad"`); ⚠️ die GA-Spalte zeigt dort **„—“**, weil die Quelle überhaupt keine längenabhängige Regel kennt. Tests: `offroad-ramp-test.php`.
- **Flüsse sperren das Gelände** (live 15.08.2026, Entwurf `docs/superpowers/specs/2026-08-15-fluesse-sperren-gelaende-design.md`) — 🔴 `AVESMAPS_ROUTE_WATER_REGION_TYPES` ist `['meer','see']`; ein Fluss ist bei uns keine Fläche, sondern ein **`Flussweg`-WEG**, also eine Linie, und `avesmapsOffroadRasteriseBlocked` rasterte nur Polygone. Der Fluss war nicht schwer zu queren, er war **nicht da** (gemessen: 61,8 Meilen in EINER Etappe quer über die Rakula). Owner-Entscheid: **unpassierbar, Absage statt Furt** — gequert wird nur, wo ein gezeichneter Weg quert, und das wirkt von selbst, weil Wege Graph-Kanten sind und das Gitter nie sehen. 💣 **Jede berührte Zelle UND die Eckzellen**: der Suchlauf geht über acht Nachbarn, und zwischen zwei diagonal benachbarten gesperrten Zellen schlüpft er hindurch, solange die Ecken frei bleiben. 💣 Die **gerade Linie** unter „Kürzeste“ geht am Raster VORBEI (das ist ihr Sinn) und braucht einen eigenen Schnitt-Test gegen die Flusslinien. ⭐ Die Geometrien sind bereits geladen (`$routeNetworkData['paths']`) — keine zweite Abfrage je Route. 🔴 `avesmapsRouteChordCrossesWater` bleibt UNBERÜHRT: sie trägt auch die Reparaturkanten zwischen losen Komponenten, und Flüsse dort zu sperren könnte eine abgetrennte Komponente ohne jede trockene Verbindung zurücklassen. 🔧 **Bäche kommen** (Owner 15.08.2026: „wir werden bald flüsse bauen die überquert werden können“) — die Entscheidung „Wand oder nicht“ steht an **genau einer** Stelle, `avesmapsCollectRouteRiverBarrierLines`; dort fällt der Bach durch eine zusätzliche Bedingung heraus und nirgends sonst. Test: `fluss-sperre-test.php`.
- **„Schnellste“ heißt Zeit, „Kürzeste“ heißt Meilen — auf DEMSELBEN Weltmodell** (Owner-Entscheid 15.08.2026). Die Wegbindung gilt für beide; die Modi unterscheiden sich nur im Gewicht und in der Form der Geländeetappe (gerade gegen zeitoptimalen Bogen). ⚠️ **Damit heißt „Schnellste“ die schnellste WEGGEBUNDENE Reise, nicht die schnellste überhaupt** — auf der Owner-Route kostet sie 21,02, der aufgegebene frühe Ausstieg kostete 10,10. Gesehen und gewollt. ⭐ Gemessen an vier Routen, ob die zwei Knöpfe noch etwas taugen: Ferdok→Gerrun **12,4 %** Unterschied, zwei andere 0 %, das Kartenpunkt-Paar 0,7 %. Die Modi sind **genau dann gleich, wenn das Netz keine Alternative bietet** — das ist richtig, nicht Rauschen.
- **Klimazonen** — die **vierte** Landschaften-Ebene (`kind='klima'`): acht Zonen von Polar bis Tropisch (die Zahl ist **Daten**, nicht Code — `AVESMAPS_ECOSYSTEM_REGION_TYPE_SEED`), dazu die Zeile „Klimazone" in der Infobox bei Ort, Region, Weg und — seit 2026-08-12 — **Herrschaftsgebiet** und **Routen-Etappe** (die Etappe liest denselben `path_ecosystem`-Verschnitt wie der Weg, ist also eine fünfte Fläche, aber keine fünfte Quelle). Entwürfe **`docs/superpowers/specs/2026-08-03-klimazonen-design.md`** (§14 ist der Nachtrag von nach dem Bau) und `…-klimazone-infobox-design.md`, Bauplan `docs/superpowers/plans/2026-08-03-klimazonen.md`. 💣 Sechs Fallen, alle dort begründet: die Trennlinien (`ecosystem_climate_divider`) sind die Wahrheit, die Bänder sind ABGELEITET · ein Band darf nie als Polygon bearbeitet werden (`avesmapsClimateAssertNotDerived`) · eine Trennlinie darf zurücklaufen (Überhang), sich aber nicht selbst schneiden · eine Zone einzuschieben darf keine vorhandene Grenze bewegen (`south_type_key`) · VIER Formen, VIER Quellen (Ort = genau eine Zone aus dem Payload, Region = anteilig aus `ecosystem_region_overlap`, Weg = abschnittsweise aus `path_ecosystem`, Herrschaftsgebiet = anteilig aus `ecosystem_region_territory` über `territory-detail.php`) — ein Zeilenbauer für alle vier (`avesmapsClimateRowMarkup`), und die Zeile steht überall direkt unter Flora · der Punkttest fragt die **Bänder**, nicht die Trennlinien. ⚠️ `map_revision` deckt das im ETag NICHT ab — `avesmapsClimateReadStamp()` steht mit im Seed von `map-features.php`. Kein Reise-Effekt in dieser Fassung.
- **Der Prüfhaken „Kreuzungen mit 2 Wegen"** — der türkise Ring sucht den **auflösbaren Durchgangsknoten**, den ein Editor zu `----------` zusammenzieht. Live 15.08.2026; Entwurf **`docs/superpowers/specs/2026-08-15-kreuzungen-pruefhaken-design.md`**, Bauplan `docs/superpowers/plans/2026-08-15-kreuzungen-pruefhaken.md`. Drei Bedingungen, alle in `computeLocationConnectivityIndex` (`js/routing/route-graph-routing.js`): **genau** `SPARSE_CROSSING_WAY_COUNT` Arme · **kein fremder Weg läuft über den Punkt hinweg** · **beide Arme dieselbe Wegart**. Gemessen 921 → rund 95 Ringe; 0/1-Arm-Fälle gehören seither dem pinken „Unverbunden"-Ring, nicht diesem. 💣 **Der Haken zählte im CLIENT-Graphen, gefahren wird auf dem SERVER-Graphen.** `avesmapsAddClientCompatiblePathConnection` (`api/_internal/routing/client-graph.php`) splittet einen Weg an jedem inneren Stützpunkt, der round-5 auf einem Ort liegt — genau die Bauform, die der Editor-Knopf „Ort verbinden und Straße weiterführen" erzeugt. Der Client tat es nicht: 182 von 921 Markierungen galten Kreuzungen, die einwandfrei funktionieren. 🔴 **Der Split gehört NUR in den `transports:"all"`-Zweig** von `addRegularPathToGraph`; im Routing-Zweig wurde er am 20.06.2026 wegen nicht-deterministischer Routen zurückgerollt (Revert `1f9e0b9e`). 💣 **Der Zwilling hat ZWEI Wachen gegen doppelte Stützpunkte** (`client-graph.php:204-207` beim Sammeln, `:223` beim Kantenbau); ohne sie zieht ein doppelt gezeichneter Vertex eine Selbstkante, und die gibt dem Knoten **zwei Phantom-Arme** — die Zahl, auf der alle drei Regeln entscheiden. **24 Kreuzungen im Livebestand** sind davon betroffen. Der Bauplan ließ beide Wachen zunächst aus; wer den Block abschreibt, prüft sie. 💣 **`Kreuzung-N` ist keine Adresse**: der Name entsteht erst im Browser als laufender Zähler über die Payload-Reihenfolge (`prepareLocationData`, `js/routing/routing.js`) und rutscht, sobald jemand eine Kreuzung anlegt, die früher einsortiert. Die Kachel „Kreuzung melden" (⚑, `crossingActionsMarkup` → `reportCrossingWithFeedback` in `map-features-share-pin.js`) kopiert deshalb `Kreuzung (<Wegart>) · <Pin-Link>` und **baut den Index nicht** — ein Popup-Klick zahlt keinen Graphbau über ~5900 Wege (per Spion-Test gewacht). ⚠️ Das Segment-Gitter für Regel 2 läuft die **Strecke** ab, nicht die Hüllbox: gemessen 482.542 → 157.252 Einträge, schlimmstes Einzelsegment 38.760 → 393. `SPARSE_CROSSING_SEGMENT_CELL` muss größer bleiben als `SPARSE_CROSSING_OVERLAY_DISTANCE`, weil die Sonde nur 3×3 Zellen liest — als Zusicherung verankert. 🔴 Beschriftung gewandert, **Kennungen nicht**: `toggleSparseCrossings`, `toggleSparseCrossingsControl`, `.location-visual-marker__shape--sparse-crossing` und `--color-marker-sparse-crossing-ring` heißen weiter wie vorher (dieselbe Trennung wie „Neuigkeiten"/`changelog`). Tests: `js/routing/__tests__/location-connectivity-index.test.js`, `js/ui/__tests__/popup-crossing-report.test.js`.
- **Zoombänder** — wann eine Ortsklasse auf der Karte erscheint, wie groß ihr Punkt dort ist und wie groß ihr **Label**, ist seit 16.08.2026 keine Konstante mehr, sondern eine **Tafel, die ein Admin einstellt**: sechs Ortsklassen × neun Zoomstufen (z0–z8), zwei Bänder (`marker` in px Außendurchmesser, `label` in pt). Entwurf **`docs/superpowers/specs/2026-08-16-zoombaender-design.md`** (🔴 dessen §0 überschreibt Teile des Restes), Bauplan `docs/superpowers/plans/2026-08-16-zoombaender.md`, Prototyp `docs/zoombaender-mockup.html`. Bedient wird sie über **zwei zieh-bedienbare Plots** im Ortseditor (Kachel „Zoombänder / Zoomlevel aller Orte"), Speichern nur mit Fähigkeit `admin`, Ansehen mit `edit`. 🔴 **Die erste gefüllte Zelle IST die Erscheinungsstufe** — es gibt keine zweite Zahl mehr, die mit ihr auseinanderlaufen kann. Bis dahin stand `0/0/0/1/2/3` zweimal im Code (`minZoomByType` als if-Kette UND `LOCATION_MARKER_RADIUS_SPEC[*].from`). Entfernt sind damit `LOCATION_NAME_LABEL_CONFIG`, `LOCATION_NAME_LABEL_SIZE_BY_ZOOM`, `LOCATION_MARKER_RADIUS_SPEC`, `locationZoomScale`, `getVillageMarkerStyle`, `getBuildingMarkerStyle`, `isVillageMarkerStyleLocation`. 💣 **Die Vorgabewerte reproduzieren das Bild vom 16.08.2026 Ziffer für Ziffer** — beim Ausliefern ändert sich nichts. Gewacht von `js/map-features/__tests__/zoombaender-vorgabe.test.js`, das die abgeschaffte geometrische Kurve als **Zeugen** mitführt; wer diese Kopie „aufräumt", nimmt dem Umbau seinen einzigen Beleg. 💣 **Die Dorf-Zeile war heimlich auch die Straßen-Zeile.** `getLocationNameLabelSize("dorf")` war die Grundgröße der Wege-/Flussnamen (+1 bzw. +3) und der Kraftlinien (+7), und `dorf.minZoom` die Schwelle, ab der Wegenamen erscheinen — an **drei** weiteren Stellen. Beides ist entkoppelt (`PATH_LABEL_BASE_SIZE_BY_ZOOM`, `PATH_LABEL_MIN_ZOOM` in `map-features-path-labels.js`), mit denselben Zahlen. ⚠️ Wer Dörfer verstellt, verstellt seither Dörfer — soll die Straßenschrift je wieder folgen, sind das die zwei Stellen. 🪤 **Warum die drei Stellen fast durchrutschten:** das Inventar-Grep lautete `LOCATION_NAME_LABEL_CONFIG\[` — MIT KLAMMER, und sie greifen per `.dorf.minZoom` zu. **Ein Suchmuster, das eine Zugriffssyntax voraussetzt, findet die andere nie.** Wer etwas restlos entfernt, sucht den blanken Bezeichner. 🔴 **Ablage:** EIN `app_setting`-Schlüssel `location_zoom_bands` (plus `_stamp`), darin `marker`, `label` und `abstaende`. **`null` heißt „auf dieser Stufe gibt es das nicht"**, ein fehlender Wert heißt „nimm die Vorgabe" — die beiden zu verwechseln macht entweder das Ausblenden unmöglich oder löscht die halbe Karte. **Zurücksetzen LÖSCHT die Zeile**, statt eine Kopie der Vorgabe zu hinterlassen (die veraltete sonst). 🔴 **Der Server kennt die Vorgabewerte NICHT** und führt **keine Klassenliste** — er prüft Form und Schranken (Marker 0,5–100 px, Label 4–30 pt), über die Klassen entscheidet der Browser. 💣 Der Schreibvorgang **liest zurück** (`avesmapsZoomBandsWrite`) — dieselbe stille MySQL-Kürzung, an der der Speichern-Knopf der Tempowerte wochenlang nichts tat. ⭐ **Drei globale Abstände** im selben Fenster: **Spalt** (4 px, Marker-Rand → sein Label), **Repel** (2 px, Luft um jedes Label vor der Kollisionsprüfung), **Versatz** (8 px, Weite des kleinen Ausweichschritts — er steckt in **zehn** der zwölf Ausweichstellen, nicht in vier). 🔴 Die **Reihenfolge** der zwölf Ausweichstellen bleibt fest und ist bewusst kein Regler: „links" abzuschalten verschiebt nichts, es **löscht** Ortsnamen, weil der Löser früher aufgibt. ⚠️ **z8 gibt es auf der Karte noch nicht** (`maxZoom: 7` in `js/app/bootstrap.js`); die Werte liegen bereit und erben z7, das Fenster sagt es sichtbar. 💣 Eine Änderung muss die Zeichner-Zwischenspeicher anstoßen — `bumpLocationNameLabelStyleRevision` UND `bumpLocationMarkerStyleRevision`: beide Icon-Schlüssel kennen Zoomstufe und Warnring, aber **nicht die Größe**. Die zweite Hälfte dieser Falle wurde beim Bau zuerst übersehen. ⚠️ Der öffentliche Leser `GET /api/app/zoom-bands.php` fällt **offen** aus (jeder Fehler ⇒ `bands: null` ⇒ Vorgabe) und macht **kein DDL**. Bauteile: `js/map-features/location-zoom-bands.js` (die einzige Quelle der Vorgabe), `api/_internal/app/zoom-bands.php`, `api/edit/map/zoom-bands.php`, das Fenster in `html/wiki-sync-settlement-editor.html`. Tests: `zoombaender-vorgabe`, `-zusammenfuehrung`, `-erscheinungsstufe`, `-abstaende`, `wegenamen-grundgroesse`, `zoombaender-dialog`, `api/_internal/app/__tests__/zoom-bands-test.php`.
- **Verborgene Orte** — ein Ort trägt neben *Nodix* und *Ruine* ein drittes Merkmal: **verborgen**. Die Karte zeichnet weder Markierung noch Namen, die Routenfindung wählt ihn nicht als Kandidat, und gefunden wird er über seinen Namen. Owner, wörtlich: „Wer den Namen kennt, findet ihn; wer nur über die Karte scrollt, nicht." Live 15.08.2026; Entwurf **`docs/superpowers/specs/2026-08-15-versteckte-orte-design.md`**, Bauplan `docs/superpowers/plans/2026-08-15-versteckte-orte.md`. 🔴 **Das Verstecken hängt NICHT an der Weganbindung** — es ist ein von Hand gesetztes Merkmal; ein Ort an der Reichsstraße kann versteckt sein, einer ohne jeden Weg muss es nicht. Owner-Entscheide: das Feld startet überall auf 0 (**kein** Einmal-Lauf, der irgendetwas setzt) · ein gefundener Ort bleibt für **diesen Besuch** sichtbar, nicht dauerhaft · ein versteckter Ort an einer Straße wird **still durchfahren**. ⚠️ **Es ist keine Spalte**: wie `is_nodix`/`is_ruined` liegt `is_hidden` im `properties_json`, es gibt keine DDL und keine Migration. 🪤 **Der Name ist im Projekt zweimal vergeben** — `map_reviews.is_hidden` heißt „von der Moderation verborgen" und gehört den Rezensionen; keine gemeinsame Tabelle, keine gemeinsame Datei, aber `git grep is_hidden` findet beide (der Unterschied steht als Kommentar an der Schreibstelle in `features.php`). 💣 **DIE TRAGENDE ENTDECKUNG: einen versteckten Ort aus der Ortsliste des Graphbaus zu streichen, ZERSTÖRT STRASSEN.** `avesmapsAddClientCompatiblePathConnection` verwirft jeden Weg, dessen Endpunkt auf keinem bekannten Ort liegt — der Ort bliebe also nicht nur unangefahren, jede an ihm endende Straße verschwände, und bei zwei Straßen zerfiele der Graph in Inseln, über die eine ×25-Notkante gelegt wird. Der Ort bleibt deshalb **vollständig im Graphen**; an den Verbindungen ändert sich nichts. 🔴 **Gefiltert wird eine ZWEITE Liste** (`candidate_locations`), aus der synthetische Kanten ihre Ziele wählen — einmal im Graphbau gerechnet (versteckte raus, außer die Anfrage nennt sie als `from`/`to`/`via`) und von dort **herausgegeben**; die rohe Liste verlässt `avesmapsBuildClientCompatibleRouteGraph` nicht mehr. `response.php` holte sie sich bisher direkt und wäre am Riegel vorbeigelaufen. Das ist die Falle vom 14.08. (vier Erzeuger, Sperre in zweien) samt ihrer Lehre: **die ZAHL im Kommentar war das Problem**, denn sie liest sich wie eine vollständige Liste — hier steht keine. 🔴 **Zwei Stellen bleiben bewusst ungefiltert:** die Umweg-Sehnen verbinden Knoten der bereits gefundenen Route (dort fährt der Reisende ohnehin durch), und `avesmapsRouteResolveEndpointPoint` schlägt die Koordinaten der **eigenen** Endpunkte nach, statt einen Kandidaten zu wählen. ⚠️ **Der JS-Graph bleibt unangetastet** — er routet seit `?clientrouting=1` nur noch als Hintertür, bedient aber den Prüfhaken-Index, und ein versteckter Ort ohne Weganbindung ist weiterhin ein Editorbefund. 💣 **Der Zeichen-Riegel steht NACH den Prüfhaken und VOR der Kreuzungsweiche** (`shouldShowLocationMarker` + `shouldShowLocationNameLabel`, ein gemeinsames Prädikat `isHiddenLocation`): darüber wäre „verstecken" ein Weg, den Prüfhaken stillzulegen; darunter würde ein versteckter Nodix im Kraftlinien-Modus doch leuchten. 💣 **Die Aufdeckung hängt an ZWEI Trichtern, nicht an einzelnen Aufrufern** — `openLocationPopupForMarkerEntry` (dort münden beide Popup-Öffner, der Spotlight-Treffer und der Ortslink im Reiseplan) und `collectAndValidateSelectedLocations` (Vorschlagsliste, getippter Name, geteilter Link). Die Semantik stimmt dort von selbst: einen Marker, der nicht gezeichnet ist, kann niemand anklicken. ⚠️ `?pin=` ist **kein** dritter Fall — das ist eine Koordinate, kein Ortsbezug. Die Menge ist Laufzeit und **additiv** (kein `localStorage`, kein URL-Parameter, kein Serverzustand); ein entfernter Wegpunkt nimmt sie nicht zurück. 💣 **Im Spotlight hängt die Verbreiterung der Trefferspalte jetzt an `--two-line`, nicht mehr an `--not-on-map`** — die richtige Frage ist „hat diese Zeile zwei Zeilen", nicht „springt der Treffer woanders hin": ein versteckter Ort IST auf der Karte, hätte die 240px nicht bekommen, und die Ellipse bei 150px frisst dann genau das Wort „Verborgen". Der Hinweis steht seither auf **11px** (war 10, unter der Grenze aus §12). Beide Merkmale teilen eine Zeile: „Ruine · Verborgen" — im Spotlight wie in der Typangabe der Infobox, mit demselben Trenner. 💣 **Die Wegpunktsuche konnte gar nicht kennzeichnen**: `getWaypointAutocompleteEntries` warf das `location`-Objekt schon im ersten `.map()` weg — ohne dass `isHidden` mitreist, prüft die Kennzeichnung für immer `undefined` (grüner Test, wirkungslose Liste). 🔴 **Ein Innerorts-Objekt erbt die Kennzeichnung von seinem ZIEL** (Owner 15.08.2026, nach dem ersten Bau: drei unbeschriftete Vorschläge führten an dasselbe versteckte Warunk) — aber in **einer** Klammer: „Warunker Höhenburg (Warunk · verborgen)". Geöffnet wird nur eine Klammer ganz am ENDE; eine mitten im Namen gehört dem Namen. `waypointInSettlementLabel` bleibt unangetastet, es trägt seine eigene Regel gegen die doppelte Stadtklammer. ⚠️ Der Zustand wird **einmal je Cache-Aufbau** abgeleitet; der Cache misst die ANZAHL der Orte, ein frisch verstecktes Ziel wird also erst nach einem Neuladen gekennzeichnet. Beschriftung „Verborgene Orte" im Auge-Menü (`toggleHidden`, dieselbe Lupe wie „Nodices" und **kein** eigener Ring — er erzeugt keinen Marker). In der Etappenliste fällt ein versteckter, nicht aufgedeckter Durchgangsort über `isRoutePlanMarkerName` heraus; ⚠️ ein Name OHNE Marker fällt **nicht** heraus (das ist kein versteckter Ort, sondern ein unbekannter Knoten, und ihn zu schlucken verlöre eine Etappe). Tests: `api/_internal/routing/__tests__/versteckte-orte-test.php` (erste Zusicherung ist die zerstörte Straße), `js/map-features/__tests__/versteckter-ort-sichtbarkeit.test.js`, `…/wegpunkt-versteckt-label.test.js`, `js/ui/__tests__/spotlight-versteckt-zeile.test.js`, `js/routing/__tests__/versteckte-etappe.test.js`. 🔴 **Die Typangabe der Infobox liest seit 15.08.2026 das EIGENE Feld** (Owner: „die infobox soll auch das eigene feld lesen") und schlägt das Wiki — dieselbe Vorrangregel wie bei Wappen und Covern. Vorher las sie nur `wikiSettlement.is_ruined`: am Livebestand trugen 70 Orte das eigene Feld, 44 das aus dem Wiki, **31 nur das eigene** — die sagten im Spotlight „Ruine" und in der Infobox nichts, darunter ausgerechnet „Ruine Khell Dairon". ⚠️ Die andere Hälfte der alten Regel **bleibt**: „(Ruine)" hängt an einer ART, nie an der blossen Ortsgrösse — „Dorf (Ruine)" stand nie da und ist im Test festgenagelt; die 24 Ruinen ohne Art sagen es über die Statuszeile und das Spotlight. „(Verborgen)" hängt dagegen an **keiner** Art: es beschreibt nicht, was der Ort ist, sondern wie die Karte mit ihm umgeht. Beide Zusätze laufen seither über `tr()` (`popup.typeRuined`/`popup.typeHidden`) — vorher stand dort ein hartes deutsches „(Ruine)", das unter `?lang=en` durchschlug. 🔴 **Das Wort heisst überall „Verborgen"** (Owner 15.08.2026, nach einem Tag mit „Versteckt"): Editorhaken, Auge-Menü, Spotlight-Zeile, Wegpunkt-Klammer, Infobox. ⚠️ Es war eine Umbenennung von **Beschriftungen, nicht von Kennungen** — `is_hidden`, `isHidden`, `toggleHidden`, `spotlight.hidden`, `waypoint.hidden` und sämtliche Datei- und Testnamen (`versteckte-orte-*`) heissen weiter wie vorher; dieselbe Trennung wie bei „Neuigkeiten"/`changelog`. 🪤 Der Grund für „verborgen" statt „versteckt" ist **nicht** frei: `verborgen` bezeichnet bei den Rezensionen „von der Moderation ausgeblendet" — Besucher sehen das nie, Editoren schon, und das sind dieselben Leute. Die Kollision ist bekannt und in Kauf genommen.
- **Der Wiki-Override — was gesynct ist und was von uns.** Der Territoriumseditor konnte es seit Monaten: die Abweichung zum Wiki DAUERHAFT zeigen (Wiki-Wert durchgestrichen), sagen wer den Wert gesetzt hat, und ihn feldweise mit ↺ zurückholen. Seit 17.08.2026 können es auch die übrigen Objektarten. Entwurf **`docs/superpowers/specs/2026-08-17-wiki-override-fuer-alle-design.md`**, Bauplan `docs/superpowers/plans/2026-08-17-wiki-override-ort.md`, Mockup `docs/wiki-override-mockup.html`. **Live: Ort** (Ortseditor + Kartendialog) **und Literatur**; 🔧 Region, Landschaftslabel und Weg fehlen noch (Fundament auf Zweig `wiki-override-stufe3`). 🔴 **Nur Felder, die aus dem Wiki kommen** (Owner) — das sind genau die Registerzeilen mit einem Kartenziel: Ort 5, Literatur 10, Landschaft 2, Label 2, Weg 1(+1). **Kraftlinie und Karte bekommen NICHTS**, gemessen: dort landet kein Wiki-Wert auf einem Kartenfeld. 🔴 **DIE EINE REGEL:** beim Speichern bekommt jedes Feld, dessen Wert sich WIRKLICH ändert, eine Herkunft — `wiki`, wenn die Anfrage es als Übernahme nennt (`wiki_uebernommen`), sonst `manual`; Unverändertes bleibt unangetastet. Rein in `avesmapsFieldOriginsStempeln` (`api/_internal/map/field-origins.php`), Ablage `properties.field_origins` — keine Tabelle, keine Migration, kein Einmal-Lauf. 💣 **„Nur was sich ändert“ ist die Hälfte, an der es schon einmal gescheitert ist:** `avesmapsUpsertGameLiterature` stempelte jedes MITGESCHICKTE Feld auf `manual`, und das Formular schickt alle mit — nach EINEM Speichern trug dort jedes Feld „von Hand“, und die Spalte sah gepflegt aus, ohne etwas auszusagen. Repariert; ⚠️ aber **nicht rückwirkend**: der Stempler setzt nur, er löscht nie, Altbestand bleibt gesperrt bis jemand ein Feld mit ↺ zurückholt (als Zusicherung festgenagelt). 💣 **Der Client muss sagen, was aus dem Wiki kam** — drei Felder sind ABGEBILDETE Werte (`feature_subtype` bei Ort und Weg, `region_type` bei der Landschaft), und die Abbildungen leben nur im Browser; sie serverseitig nachzubauen wäre die zweite Wahrheit aus §5. 🔴 **Im Zweifel `manual`** — eine falsche `wiki`-Angabe liesse einen späteren Abgleich Handarbeit überschreiben, eine falsche `manual`-Angabe schützt nur zu viel. ⚠️ Damit darf kein Schreibweg live gehen, bevor SEINE Oberflächen `wiki_uebernommen` schicken — sonst stempelt der Sync-Knopf seine eigenen Übernahmen als „von uns“ (die Falle vom 14.08.: eine Regel, die einen von mehreren Erzeugern bindet, ist keine). ⭐ Das **↺ ist die Sync-Übernahme EINER Zeile** — kein neuer Schreibweg, kein zweiter Endpunkt, dieselbe Speicherleiste. 🔴 **Zwei sichtbare Zustände, nicht vier** (Owner: „bau doch einfach territorien nach“): braune Beschriftung = von uns, Durchstreichung ohne Farbe = weicht ab, Herkunft unbekannt. `wiki` wird mitgeschrieben, aber nicht angezeigt — es wirkt beim Vorhäkeln (`avesmapsWikiAssignDiff`, Fall 4; ohne gespeicherte Herkunft ändert sich nichts). 🔴 **50 | 50** (Owner 17.08.2026): links Beschriftung SAMT durchgestrichenem Wiki-Stand und ↺, rechts das Eingabefeld. 🪤 Davor zwei verworfene Fassungen — eine dritte Rasterspalte mit gerechnetem Mindestplatz (`clamp(90px, 50%, 100% - 165px)`), dann eine zweite Bauform für den Literatur-Editor, die „ISBN (für DNB)“ auf drei Zeilen brach. **Das Markup sagt nicht, welche Form gilt — das CSS sagt es** (`.ae-field` sieht aus wie der Kartendialog, ist aber ein Flex-ROW wie `.dt-grid`). Und: wer einen Mindestwert ausrechnet, sollte zuerst fragen, ob eine Hälfte reicht. 💣 **Die Auslassungspunkte gehören an den TEXT, nicht an die Zelle** — `ellipsis` wirkt nur auf das Element, dessen eigener Text überläuft; an der Zelle wird hart abgeschnitten. 💣 **`--color-accent-brown-text` ist im hellen Thema `#fff`** — weiße Beschriftung auf weißem Panel; die braune Hervorhebung nimmt `--color-accent-brown`, wie der Territoriumseditor. 🪤 **Und der teuerste Befund: der Parser RÄT eine Ortsklasse.** `avesmapsWikiSettlementParseInfobox` setzt `settlement_class` auf `'dorf'`, wenn die Wiki-KATEGORIE keine hergibt, und schreibt die Vermutung als Datum ins Nest — das ↺ bot daraufhin an, die **Metropole Gareth zum Dorf** zu machen, und der Owner hat es geklickt. Die Sync-Vorschau bot dieselbe Herabstufung schon seit dem 16.08. an, nur zwei Klicks tiefer. Seither sagt der Parser, dass er geraten hat (`settlement_class_guessed`), und ein Nest OHNE die Angabe gilt für `'dorf'` als geraten — bewusst zu streng, die sichere Richtung, löst sich mit jedem Abgleich von selbst. Bauteile: `js/ui/wiki-feld-herkunft.js` (rein), `api/_internal/map/field-origins.php` (rein), `.dt-grid--wiki`/`.wiki-alt` in `css/components/editor-page.css` + `css/components/location-report-dialog.css`. Tests: `wiki-feld-herkunft`, `field-origins-test.php` (zählt die Schreibwege zur LAUFZEIT — sie fand den zweiten, den der Autor übersehen hatte), `ort-wiki-override-form`, `wiki-assign-ort` (Fall Gareth), `wiki-assign-literatur`.
- **„Was ist hier?"** — Rechtsklick auf die Karte, und das Infopanel sagt, was an der Stelle liegt: Herrschaftskette mit Wappen, die vier Landschaftsebenen, Klimazone, Natur & Waren, und „In der Nähe" (Orte und Wege mit Entfernung und drehendem Peil-Pfeilchen). Entwurf **`docs/superpowers/specs/2026-08-15-was-ist-hier-design.md`**, Bauplan `docs/superpowers/plans/2026-08-15-was-ist-hier.md`, Mockup `docs/was-ist-hier-mockup.html`, live 15.08.2026. Bauteile: Endpunkt `api/app/what-is-here.php` + `api/_internal/app/what-is-here.php`, Panel `js/map-features/map-features-what-is-here.js`, Nachbarschaft `js/map-features/map-features-what-is-here-nearby.js`. 🔴 **Es gibt genau EINE Markierung.** „Stelle markieren und teilen" ist ersatzlos gefallen (Owner 15.08.2026), das schwebende Zwei-Kachel-Menü am Marker ebenso — ein Klick auf die Markierung öffnet seither das Panel, und `?pin=` bringt die Auskunft mit statt nur den Punkt. 💣 **Doppelt verwendete PDO-Platzhalter sind hier ein FEHLER, kein Stilfrage:** `avesmapsCreatePdo` setzt `ATTR_EMULATE_PREPARES => false`, und MySQL lehnt `:x` zweimal im selben Statement mit `HY093` ab. Kein funktionierender Endpunkt im Haus tut es; unserer tat es, und weil die Ausnahme im `catch (Throwable) { return []; }` verschwand, antwortete er **`ok:true` mit leerem Inhalt** — für den Betrachter nicht von „hier liegt nichts" zu unterscheiden. Gefunden hat es die erste Anfrage gegen die Live-Datenbank, nach sechs grünen Prüfläufen. ⚠️ Der inerte `catch` war für „Tabelle fehlt" gedacht und verschluckt seither jeden SQL-Fehler. 💣 **bbox ist ein Vorfilter, kein Treffer** — am Seepunkt lagen 9 Gebiete im bbox und 0 bestanden den Punkttest; danach läuft `avesmapsClimateGeometryContains` (der EINZIGE Punkt-in-Polygon im Haus, kein zweiter). 💣 **Die Vorfahren einer Herrschaftskette haben meist GAR KEINE gezeichnete Fläche** — sie sind abgeleitete Außengrenzen in `political_territory_derived_geometry`. Der Punkttest allein findet deshalb nur das Blatt; die Kette entsteht aus dem `parent_id`-Lauf wie bei der Siedlung (`map-features.php`), gedeckelt auf 12 Stufen. Der Entwurf entschied das zuerst anders und wurde von der Datenlage widerlegt (§3, korrigiert). 💣 **Die Kette steht BLATT → WURZEL** — `buildSettlementHierarchyMarkup` erwartet genau das und dreht selbst um. 💣 **Wappen laufen NUR über `avesmapsResolveGatedCoatUrl`** (`api/_internal/coat-url.php`, seit 15.08.2026 samt `avesmapsLoadSettlementCoatGateInputs`/`avesmapsSettlementTerritoryCoatUrl` dort, vier Leser teilen eine Implementierung). Die Spalte `political_territory.coat_of_arms_url` allein ist **leer** und liefert nichts; der Riegel lässt zudem nur `public_domain` durch (NOTICE.md). ⚠️ Das Wappen muss aufgelöst sein, BEVOR `avesmapsWhatIsHereTerritoryPayload` den `wiki_key` aus der Antwort wirft — dieselbe Reihenfolgefalle wie bei `lore.place`, das den Territoriums-Schlüssel ebenfalls aus der ungekürzten Kette liest. 💣 **Die Peilung des Pfeilchens ist `atan2(dx, dy)`**, Argumente vertauscht, damit 0° Norden ist und im Uhrzeigersinn gezählt wird wie bei `rotate()`. Mit der Schulform zeigt jeder Pfeil an der Diagonale gespiegelt, und das fällt bei genau N/O/S/W **nicht** auf. Es gilt nur, weil y hier nach NORDEN wächst (Riva y=790, Al'Anfa y=152) — die Kachelnamen `map_x_-y` tragen ein negatives y. 💣 **Die Markierung liegt in `sharePinPane` (z=700)**, über `labelsPane` (650) und unter `tooltipPane` (875); in Leaflets Standard-`markerPane` (600) verschwand sie unter den Kartenlabels. Ihr Zeiger ist `pointer` wie bei allem Anklickbaren — `grab` war vom Kartenhintergrund (ebenfalls `grab`) nicht zu unterscheiden. ⚠️ „In der Nähe" nennt die drei nächsten Ortschaften und je Wegart höchstens einen Weg, höchstens vier, **keiner weiter als das Anderthalbfache der weitesten gezeigten Ortschaft** — der Maßstab ist die ORTSLISTE, nicht die Wegeliste (sonst wandert die Schranke mit dem mit, was sie begrenzen soll; auf Maraskan stand ohne sie eine Reichsstraße 534 Meilen weit weg drin). Ein Weg ohne echten Namen wird nur mit seiner Art genannt — `getPathTitleName`/`getPathTypeLabel` (`map-features-path-domain.js`), **nicht** nachbauen: die Hausfassung liest zuerst `wiki_path.name` und liefert „Straße" statt des Join-Keys „Strasse".
- `docs/routing-featurestand.md`, `docs/stabilization-smoke-test.md`,
  `docs/routing-transport-smoke-checklist.md` — routing state & smoke checklists.
- `api/README.md` — canonical API contract.
- **`docs/refactoring-masterplan.md` — the active refactoring program (M0–M8).**

> Spent per-refactor process logs were deleted in M0; do **not** recreate
> per-split "boundary-check / stable" logs.

## 12. Design language (read before any CSS / UI work)

**One warm, *aventurian* visual language for every surface** — route planner
(`#search`), infobox (`.avesmaps-infopanel`), dialogs, popups, editor. Warm
browns + parchment + coat-of-arms gold. **No blue in the CHROME** — panels,
controls, links, menus (it reads as a foreign UI kit and is what made the panels
diverge). 🔴 Blue is allowed, and sometimes required, where colour encodes DATA
rather than chrome: the Landschaften water tones, the analytics chart palette,
the edit-in-progress handles, and the path-subtype colours of the Wege-Editor
list (`--color-path-*`). `docs/design-language.md` has always said this
("**No blue = UI chrome only**", with its own list of exceptions) — §12 stated
the ban flatly and was therefore read as absolute. Wording aligned 14.08.2026
after exactly that misreading; the rule itself is unchanged.

**Hard rule: never hardcode a colour / radius / divider — always use a token
from `css/base/tokens.css`.** Need a value with no token? Add the token first,
then use it. A literal colour written twice is divergence waiting to happen —
this is exactly how the infobox and route planner drifted apart (1000+ hardcoded
hex values across 38 CSS files).

- **Buttons** have a hierarchy: main action *filled* (`--color-button`), the rest
  *soft/outline* (`--color-button-soft`); radius `--radius-md`; no pill shapes.
- **Group by divider** (`--color-divider` line + heading), **not** by framed
  boxes; popup/infobox dividers run full-bleed (negative side-margin = padding).
- **Eine Kontur gehört dem BEARBEITEN, nicht dem Ansehen** (Owner 2026-08-03, gilt für alle
  Landschaften-Ebenen). Die Fläche füllt immer, ihre Kante zeichnet sie nur, wenn **eine** Ebene im
  Editiermodus offen ist — nicht in „Alle" und nicht für den, der die Karte bloß ansieht. Getragen von
  `ecosystem-pane--editable` an der Pane (`--eco-contour` in `css/features/ecosystem-layer.css`).
  ⚠️ „Im Frontend fehlen die Ränder" ist damit **kein Fehler**, sondern die Regel.
- **Eine Zeilenhandlung ist nie die Haupthandlung der Seite** (2026-08-07, gemessen an den
  WikiSync-Listen). Ein *gefüllter* Knopf in einer Listenzeile multipliziert sich mit der Zeilenzahl:
  „Fläche zuweisen" + „Label zuweisen" standen als Akzentknöpfe in **jeder** Regionenzeile — live
  2×1.577 = **3.154** davon in einer 246 px schmalen Spalte —, und der Wege-Reiter trug 462 weitere,
  während die Ortsliste daneben über 3.414 Zeilen **gar keinen** Knopf hat. Die Haupthandlung steht im
  Menüband und heißt „Syncen"; alles in einer Zeile ist weich/outline (`--color-button-soft*`,
  `--radius-md`). ⚠️ Die Klassen `region-sync__*` gehören **Regionen UND Wegen** (`review-path-sync.js`
  schreibt `.region-sync__cand` an 15 Stellen) — wer nur eine der beiden Listen anpasst, poliert die
  kleinere Hälfte und baut die Divergenz in die größere ein.
- **Links** use `--color-link` (gold-brown), never blue; **external links**
  (off-site) always get a trailing `↗`.
- **New components:** reuse the nearest existing one as a template plus the
  tokens; match the warmth. Full guide: **`docs/design-language.md`**.
