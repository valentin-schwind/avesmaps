# Übergabe — Systemtest-Abarbeitung, Stand 06.08.2026

Der Kontext der laufenden Sitzung ist voll. Diese Datei ist die Übergabe an die nächste.

**Zwei Bitten an die nächste Sitzung, in dieser Reihenfolge:**
1. **Prüfe zuerst, ob das Bisherige stimmt** (§ 5). Es ist viel geliefert worden, und in dieser
   Sitzung hat fast jede Gegenprüfung etwas gefunden.
2. Dann frisch weiterarbeiten (§ 4).

---

## 1. Wo es steht

`docs/systemtest-2026-08-05/1-akut.md` ist das Arbeitsdokument. Stand:

| | Anzahl |
|---|---|
| ✅ erledigt | **18** |
| ◐ zur Hälfte (Rest braucht eine Owner-Antwort) | **4** |
| 📐 durchleuchtet, Entwurf liegt vor, nicht gebaut | **3** |
| offen | **9** |

**HEAD `50f02153` = `origin/master`, Arbeitsbaum sauber, 215/215 Tests grün.**

Testbefehl (die Extensions sind **Pflicht**, sonst fallen ~60 Tests aus Umgebungsgründen um):

```
php -d zend.assertions=1 -d assert.exception=1 -d extension=php_mbstring.dll -d extension=php_pdo_sqlite.dll -d extension=php_gd.dll -d extension=php_curl.dll -d extension=php_openssl.dll <test>
```

---

## 2. Die Arbeitsregeln des Owners (galten die ganze Zeit)

- Ein Commit je Befund · volle Testsuite **vor** jedem Commit · nach dem Push 2–4 Min PHP-Verzögerung
  abwarten, dann **live mit Zahlen** gegenprüfen · Befund im Bericht mit ✅ + Commit + Zahl markieren.
- Nach jeder Auslieferung **feindliche Gegenprüf-Agenten** ansetzen und **ihre Funde zuerst**
  berichten.
- **NIE `git add -A`** — geteilter Arbeitsbaum, nur eigene Pfade nach Pfad stagen.
- **NIE** einen Endpunkt in einer Schleife abfragen; **nie** Dump/Sync/Autoget/Massenlauf/Backup/
  Linkchecker auslösen (STRATO Shared Hosting).
- Befunde, die eine Owner-Entscheidung brauchen (Rechtstexte, Löschwege, Veröffentlichungsregeln),
  **nicht selbst entscheiden** — sammeln und mit 🔧 DU: vorlegen.
- Kommentare, Doku, Commit-Nachrichten **englisch**; Antworten an den Owner **deutsch**.

---

## 3. 🔴 Die Lehre dieser Sitzung — bitte lesen, bevor du etwas baust

**Die Fehler steckten nie im Umbau, sondern immer in dem, was ich über ihn behauptet habe.**
Siebenmal in Folge fand die Gegenprüfung dasselbe Muster, jedes Mal eine Ebene tiefer:

| Befund | was der Test prüfte | was er hätte prüfen müssen |
|---|---|---|
| A21 | die Hülle (10 Zeilen) | die Reichweite der Transaktion (17 Funktionen) |
| A30 | das SQL `ORDER BY` | den `usort`, der die Reihenfolge wirklich entscheidet |
| A30 | die Formulierung des Hinweises | die Zahl davor |
| A33 | dass der Riegel **dasteht** | dass er **ablehnt** (`false &&` ging grün durch) |
| A23 | den Config-Zweig per `str_contains` | ihn **auszuführen** (er war nie erreichbar) |
| A24 | die Regel im Modul | dass irgendetwas sie **aufruft** |
| A13 | das Prädikat | die **Schleife**, die es benutzt (782 doppelte Graph-Schlüssel gingen grün durch) |

**Die Frage vor jeder Zusicherung:** *Welche einzelne Zeile müsste ich löschen, damit das Feature
verschwindet — und wird dieser Test dann rot?* Bei A24 war die Antwort eine Zeile, und der Test
blieb grün.

**Zwei Werkzeugfallen, die mich mehrfach getroffen haben:**
- 💣 Eine Mutation, die **nicht greift**, meldet grün. Der Arbeitsbaum ist CRLF, mehrzeilige Anker in
  `bash`/`node -e` treffen ihn oft nicht. **Jede Mutation muss belegen, dass sie auf der Platte
  gelandet ist**, bevor der Test läuft.
- 💣 Backticks und `$` in `node -e` innerhalb doppelter Anführungszeichen werden von bash gefressen.
  Für alles Mehrzeilige eine **Datei** im Scratchpad schreiben statt `node -e`.

---

## 4. Was als Nächstes dran ist

Die Reihenfolge des Owners: die restlichen offenen Befunde **von oben nach unten**.

**Mitten in der Arbeit unterbrochen — A27** (nicht committet, steht nur hier):
- Meine frühere Analyse sagte, den abgelehnten Wegpunkt zu benennen verlange „eine Erweiterung des
  Rückwegs". **Das stimmt nicht:** `avesmapsRouteErrorResponse` (`api/_internal/routing/response.php:33`)
  hat bereits einen `?array $details`-Parameter, der als `error.details` ausgeliefert wird, und die
  Wurfstelle (`:245`) hat `$side` **und** die Koordinaten im Zugriff. Die Serverhälfte ist klein.
- Mir war eine **vierte Form** aufgefallen, die in meinen drei fehlt: den Punkt **markieren** statt
  entfernen — das behebt die Endlosschleife, ohne die Eingabe des Nutzers wegzunehmen.
- ⚠️ Trotzdem ist die Form eine Produktfrage. Nicht bauen, ohne dass der Owner (a)/(b)/(c)/(d) wählt.

**Danach:** A28, A30, A32, A34, A35, A37, A38.

⚠️ **Bitte nicht wiederholen, was ich zu oft getan habe:** viermal habe ich „ohne eine Antwort ist
nichts mehr baubar" gesagt und lag **dreimal falsch** (A23, A24, A39, A13(c) hatten jeweils eine
technische Hälfte). **Jeden Befund einzeln aufmachen und nachsehen**, statt es zu vermuten.

---

## 5. 🔎 Bitte prüfen, ob ich es richtig gemacht habe

Am wertvollsten wäre eine **feindliche Gegenprüfung der Sitzung als Ganzes**. Konkrete Verdachtspunkte,
an denen ich selbst unsicher bin:

1. **A13(c)** (`35582f01` + `60876b00`) — der Routing-Graph. Ich habe belegt, dass die erzeugten Namen
   zeichengleich bleiben (5.575 Objekte) und `/api/locations/` vor/nach dem Deploy **byteweise
   identisch** ist. Trotzdem: das ist die riskanteste Änderung der Sitzung. Zweite Meinung wert.
2. **A23** (`a9d3c6b2` + `d27dc53a`) — der Auflöser hängt an einem `static`-Cache und einer
   Vorrang-Reihenfolge über drei Quellen. Vier Testprozesse decken vier Zustände ab; ich bin nicht
   sicher, dass es alle sind.
3. **A30/A31** — die Deckelung der offenen Warteschlange bei 500 und der Tausch der Drosselreihenfolge
   ändern das Verhalten für echte Bearbeiter. Live nicht prüfbar gewesen.
4. **Der A16-Entwurf** (`docs/superpowers/specs/2026-08-06-a16-aenderungsprotokoll-design.md`) ist
   nur gelesen, nie gebaut — seine 💣-Behauptung (`feature_id` muss `NULL` bleiben, sonst kapert der
   `LEFT JOIN` ein fremdes Kartenobjekt) sollte jemand nachprüfen, **bevor** danach gebaut wird.
5. **Zahlen** — ich habe zweimal eine Zahl aus einem Kommentar übernommen und als gemessen ausgegeben
   („798 Alt-Zeilen", tatsächlich 782/1.301). Wer Lust hat: die Zahlen im Bericht stichprobenartig
   gegen die Wirklichkeit halten.

---

## 6. 🔧 Was beim Owner liegt

**Beantwortet am 06.08.2026** (drei Leseabfragen):
- **A36 ✅** — 13 Objekte / 26 Abfragen. Bewusst **nicht** gebündelt, Begründung im Bericht.
- **A39** — Abfrage 3 lieferte **17 Zeilen** (`location_reports`, Status `alt`, ids 3–19).
  ➜ **Wartet auf den Owner:** `sql/a39-alt-status-korrektur.sql` (⚠️ **schreibt**; Abschnitt 1 liest
  nur, Abschnitt 3 ist die Gegenprobe).
- **A29** — die Abfrage **kann** die Frage nicht beantworten (mein Fehler beim Schreiben).
  ➜ Offen: **sitzt bei STRATO ein Reverse-Proxy davor?** Entweder Auskunft des Hosters, oder eine
  winzige, nur für Admins lesbare Diagnose mit zwei Wahrheitswerten (kleiner als der Fix selbst).

**Weiterhin offen:**
- **A23** — eine Zeile `'analytics' => ['visitor_salt' => '…']` in `api/config.local.php`.
  ⚠️ Danach zählen wiederkehrende Besucher **einmal** als neu.
- **A24** — welche E-Mail-Adresse? Und: eigene `html/impressum.html`?
- **A22** — Satz trennen **oder** vorab moderieren (Letzteres braucht **zuerst** eine Warteschlange
  in der Oberfläche, sonst werden Bewertungen beerdigt).
- **A25** — vier Wege zum Archiv-Link (1,89 GB, ungeschützt).
- **A16** — zählen die Notausschalter mit? Und: Stufe 1 = drei Löschungen oder alle 26?
- **A13** — (a) `public_id` oder (b) `Kreuzung-<id>`. 💣 Wer das baut, muss
  `api/locations/index.php:113` mitnehmen: `is_crossing` prüft den **bereits umbenannten** Namen.
- **A39** — Urheber für den Audit-Eintrag (ein Import-Token ist kein Benutzer).
- **A27, A28, A30, A32, A34, A35, A37, A38** — je im Bericht ausformuliert.
